# R0.4.6 RF-006 Acceptance Residual Independent Review

Slice: RF-006 acceptance residual / proposed CLOSE (R0.4.6).
Workspace: `/Users/user/src/quantlineage` branch `r0-core-remediation`
(HEAD `740655c`; scored work is uncommitted working-tree vs that commit).
Implementer report: `reviews/r0.4.6-rf006-acceptance-report.md` (claims only).
Reviewer did not implement this work. Production code, FINDINGS, and milestone
were not edited by this review. Index, HEAD, and branch were not mutated. The
only write is this file.

Scored only:

- `backend/tests/test_market_snapshot.py` (O(1) freeze/copy lower+upper, K≥5)
- `backend/tests/test_stress.py` (P×S → S apply counts for `run` / `evaluate`)
- `reviews/r0.4.6-rf006-acceptance-report.md`
- `reviews/FINDINGS.md` RF-006 status / acceptance bullets (implementer set
  **CLOSED** — verify justification)
- `reviews/REMEDIATION_MILESTONE.md` (R0.4.6 COMPLETE + exit criteria)

Unrelated dirty tree (frontend BlockHelp, native, README, etc.) ignored.
RF-007 leftovers (wall-clock N×S pricing) are **not** scored as RF-006 fails.

## Verdict: APPROVE

Critical 0 · Important 0 · Minor 1

Structural acceptance evidence for RF-006 is real: multi-factor
`MarketSnapshot.apply` is O(1) freeze/copy with a non-vacuous lower+upper band
for K≥5; `StressEngine.run` / `evaluate` position P&L paths apply once per
scenario for P×S (not P×S); contribution sum↔portfolio P&L + single-factor
near-zero interaction are covered by the cited attribution suite (re-run here).
FINDINGS RF-006 **CLOSED** is justified. Lead may keep RF-006 **CLOSED**.
RF-004 correctly remains **IN PROGRESS**. Required suite: **46 passed**.

## Checks performed

- Read implementer report as claims only; read FINDINGS RF-006 acceptance
  evidence and required-direction text.
- Diffed scored tests + FINDINGS + milestone vs `740655c`. No production
  `.py` changes in this residual (R0.4.4 / R0.4.5 already landed).
- Confirmed `apply` still stages then one `model_copy` (HEAD); residual only
  tightens the structural test (R0.4.4 Minor #1 lower-bound gap closed).
- Confirmed new P×S counters for `run` (exact `len(calls) == S`) and
  `evaluate` (full-scenario id filter == S).
- Confirmed contribution cite targets exist:
  `_assert_reconciles`, `test_risk_factor_contributions_reconcile_with_interaction`,
  `test_single_factor_interaction_near_zero`,
  `test_stress_engine_evaluate_includes_contributions`.
- Confirmed RF-004 Status stays **IN PROGRESS** / “Do not close”; RF-007 keeps
  wall-clock N×S.
- Independent probes (below). Reran required pytest. Did not run CI. Did not
  mutate index, HEAD, or branch.

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_market_snapshot.py \
  tests/test_stress.py \
  tests/test_scenario_attribution.py
# 46 passed in 0.79s; no skips/xfails
```

Implementer “46 passed” is not treated as proof; the suite was rerun here.

Independent probe:

| Check | Result |
|---|---|
| Multi-factor `apply` `model_copy` / freeze (K=5,6,20) | **1 / 1** each |
| Sequential `bump` same K=6 | **6 / 6** (contrast) |
| `run` P=5 × S=3 → `stress_mod.apply_scenario` calls | **3** (≠ 15) |
| `evaluate` stress_mod patch only: full-scenario applies | **3** for S=3 |
| `evaluate` + attr binding (info): total applies | 6 = 3 full + 3 iso (`…__UNIT`); not P×S |
| Contribution sum ↔ portfolio P&L (multi-factor) | **match** (abs 1e-6) |
| Single-factor `interaction` | **0.0**; still reconciles |
| FINDINGS RF-006 | **CLOSED**; acceptance bullets marked **met** |
| FINDINGS RF-004 | **IN PROGRESS**; “Do not close” |

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | O(1) freeze/copy test still vacuous (0/0 passes) | **Pass.** `model_copy == 1`, freeze ∈ [1,2]; K≥5. Probe 1/1. |
| 2 | Upper-only “O(1)” while still O(K) | **Pass.** Absolute constants for K=6; probe flat through K=20; seq bump still O(K). |
| 3 | P×S still applies per position on `run` | **Pass.** Exact `len(calls) == S`; probe 3 for 5×3. |
| 4 | `evaluate` P×S full-scenario rebuilds hidden by id filter | **Pass.** With stress_mod patch, all calls are full-scenario ids; count == S. Iso uses different ids on attr binding (RF-007 cost class). |
| 5 | Contribution invariant only cited, not true | **Pass.** Suite re-run + independent reconcile / interaction probe. |
| 6 | Premature CLOSE / acceptance unmet | **Pass.** All five FINDINGS acceptance bullets met; structural bench framing explicit; N×S wall-clock left on RF-007. |
| 7 | RF-004 closed as collateral | **Pass.** RF-004 remains IN PROGRESS. |
| 8 | Required suite fails / skips | **Pass.** 46 passed. |

## Findings

### Critical (Must Fix)

None. VALID count: **0**.

### Important (Must Fix before APPROVE / blocks CLOSE)

None. VALID count: **0**.

CLOSE is justified: FINDINGS acceptance evidence matches the tree and independent
probes. Do **not** revert RF-006 to IN PROGRESS.

### Minor

#### 1. VALID — Minor — Evaluate P×S counter still patches `stress_mod` only

Same binding caveat as R0.4.5 Minor #1: `test_stress_evaluate_apply_count_scales_with_scenarios_not_positions`
instruments `stress_mod.apply_scenario` only, so factor-isolated applies on
`scenario_attribution.apply_scenario` are invisible. Full-scenario id filtering
is therefore mostly documentary under this patch. Independent dual-binding probe
still shows full applies == S and iso applies ≪ P×S. Coverage depth, not a
CLOSE blocker.

- Suggested fix (later): optionally also patch
  `app.risk.scenario_attribution.apply_scenario`, or assert an upper bound on
  total applies across both bindings (e.g. ≤ S + F·S).

## REJECTED implementer claims

None material. Structural O(1) band, P×S → S counters, contribution cite,
RF-006 CLOSE, RF-007 deferral, and RF-004 staying open match the tree.

## Must-verify matrix (FINDINGS RF-006 acceptance)

| # | Requirement | Result |
|---|---|---|
| 1 | One-pass apply (landed) + structural O(1) freeze/copy for K≥5 with meaningful lower+upper bounds | **Met** |
| 2 | Scenario-once price-many + structural apply count == S for P×S on `run` and `evaluate` | **Met** |
| 3 | Contribution invariant cited or tested (sum ↔ total P&L) | **Met** (tested + re-run; cite accurate) |
| 4 | Premature CLOSE? | **No** — CLOSE justified; APPROVE CLOSE |
| 5 | Required suite passes | **Met** (46 passed) |

## Counts

| Severity | VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 1 |

REJECTED implementer claims: 0.

**Verdict: APPROVE**

## Lead Architect adjudication (2026-09-09)

Accepted. Zero VALID Important. RF-006 **CLOSED** retained. Minor deferred. Next: RF-005 production default → `HistoricalFactorPanel`.

Zero VALID Important. Lead may keep RF-006 **CLOSED**. Do not reopen for this
residual. Wall-clock N×S full-revaluation remains RF-007. RF-004 stays
**IN PROGRESS**.
