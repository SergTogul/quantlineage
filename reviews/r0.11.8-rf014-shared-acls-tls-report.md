# RF-014 shared-profile ACLs + TLS + secrets (R0.11.8)

**Owner:** Backend / API Engineer  
**Date:** 2026-09-09  
**Branch:** `r0-core-remediation`  
**Finding:** RF-014  
**Recommendation:** **CLOSE** as **MET** for shared-profile object ACLs, TLS terminator, and secret placeholders. Local demo stays unauthenticated. Not OIDC.

## Shared cells

| Cell | Score | Evidence |
|---|---|---|
| Object ACLs / IDOR | **MET** | `QUANTLINEAGE_API_TOKENS` principal map; stored books/runs stamped with owner; other principals get 403. Seed/demo catalog owned by `demo` (readable; not writable by others). `backend/tests/test_shared_acls.py`. |
| TLS / reverse-proxy | **MET** | `docker-compose.shared.yml` + `deploy/Caddyfile`. Host publish is 443; backend `expose: 8000` only. Local `docker-compose.yml` unchanged. |
| Secret management | **MET** (shared profile) | `${POSTGRES_PASSWORD:?}` and token env; `.env.shared.example`. Local Compose may keep demo `quantlineage`. |
| Local demo unauthenticated | **MET** | Default Compose loopback; no shared env; `test_local_demo_stays_unauthenticated_without_acls`. |
| OIDC / SSO | **not provided** | Documented as not production-like. |

Named leftover (not blocking): HTTP enqueue queue-depth. Labeled-runner SLA is RF-016, **not MET**. Do not restore Milestone R0 COMPLETE.

## Commands

See `reviews/sdd-briefs/task-31-rf014-shared-acls-report.md`.
