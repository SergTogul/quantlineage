# RF-017 ABI close as MET (R0.12.9)

**Owner:** C++ Performance Engineer  
**Date:** 2026-09-09  
**Branch:** `r0-core-remediation`  
**BASE:** `90e40a2`  
**Finding:** RF-017  
**Recommendation:** **CLOSE** as **MET** for ABI

On-disk verification of the ABI/validation/contiguous-buffer required direction. Default Historical VaR remaining python/NumPy is the FINDINGS Decision, not an open ABI gap. No QuantLib or VaR kernels were added in C++. Milestone R0 leftover wave stays **IN PROGRESS**.

## Recommendation

**CLOSE** RF-017. ABI version, status/error return, length validation, contiguous `pnl_from_arrays`, and serial-below-4096 are **MET**. Named residual: product Historical VaR stays python/NumPy **by design**.

Would this gate defend CLOSE to a second reviewer? **Yes**, provided that residual is not relabeled as an ABI hole and C++ VaR/QuantLib stay out of scope.

## Required direction

| # | Required direction | Score | Evidence |
|---|---|---|---|
| 1 | ABI version | **MET** | `backend/native/include/risk_kernel_capi.h` `RISKFORGE_KERNEL_ABI`; `riskforge_kernel_abi_version()`; Python `KERNEL_ABI_VERSION`; `test_native_abi_version_readable` |
| 2 | Status/error return | **MET** | `KERNEL_OK` / `ERR_ABI` / `ERR_NULL` / `ERR_LENGTH`; `NativeKernelError` |
| 3 | Explicit shape/length validation | **MET** | `risk_kernel_capi.cpp` fail-closed before any buffer walk; wrap-sized and tight-buffer tests |
| 4 | QA-025 caller length mismatch | **MET** | `backend/native/tests/kernel_test.cpp`; Python wrapper `test_native_length_mismatch_fails_closed` (no new pin; already present) |
| 5 | Contiguous NumPy buffer path | **MET** | `NativeScenarioKernel.pnl_from_arrays`; pointer-spy tests; Historical native path uses this API |
| 6 | Avoid per-call threads for tiny work | **MET** | `KERNEL_PARALLEL_MIN_WORK == 4096`; C++ `kernel_use_serial`; `test_native_tiny_workload_matches_python_with_many_threads` |
| 7 | Do not move VaR/QuantLib into C++ | **MET** as Decision | Default `RISKFORGE_SCENARIO_KERNEL=python`; no new C++ kernels this slice |

## Why CLOSE (not KEEP OPEN)

Leftover wave required CLOSE when ABI/validation/contiguous buffers are scored MET, not “deferred because we refused C++ VaR.” That condition holds.

## Named residual (not an ABI hole)

Product Historical VaR stays python/NumPy **by design**. Expanding native VaR or QuantLib into C++ remains out of scope.

## Tests

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_native_kernel.py tests/test_rf020_r0_exit.py
# 28 passed in 23.10s
```

## Follow-up

Independent review APPROVE or KEEP OPEN. Do not restore Milestone R0 COMPLETE while RF-014 / RF-018 / RF-019 remain open.
