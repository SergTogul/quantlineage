# R0.12.3 Re-review (Important #1 and Minor #2 only)

Slice: Critical E2E journey (RF-016 / QA-014), after the review-fix pass.
Workspace: uncommitted `e2e/tests/r0-critical-journey.spec.ts` and `reviews/r0.12.3-report.md` vs `3a119b9`.
Prior review: `reviews/r0.12.3-independent-review.md` REQUEST CHANGES. Orchestrator: Important #1 VALID; Minor #2 VALID.
This review did not mutate the index, HEAD, or branch. The only write is this file. Playwright was not re-run.

## Verdict: APPROVE

Remaining on the prior two items: **0 Critical / 0 Important**.

Finding 1 is closed. The change-risk check is `base_var_99 !== hedged_var_99` from compare **JSON** after `ok()`, plus `var_improvement !== 0`. A 200 + table with equal VaRs fails. Finding 2 is closed: custom and compare are each paired with `waitForResponse` + `ok()`. Dollar amounts stay unpinned.

## Checks performed

- Diffed `git diff 3a119b9 -- e2e/tests/r0-critical-journey.spec.ts reviews/r0.12.3-report.md`. Read the working-tree spec in full. Did not treat the implementer report as proof.
- Re-read QA-014, the prior review, `HedgeComparisonReport` (`base_var_99` / `hedged_var_99` / `var_improvement` required floats), and `HedgeCompare` (`money(base_var_99 ?? 0) → money(hedged_var_99 ?? 0)`).
- Attacked the six questions below against the spec text. Did not assume Finding 1 closed.

## Attack on prior items

| # | Attack | Result |
|---|---|---|
| 1 | Is `hedged_var_99 !== base_var_99` taken from compare **JSON** after `ok()`, not from the UI? | **Yes.** `waitForResponse` on exact `POST /api/v1/risk/stress/compare` with `res.ok()`, then `hedgeRes.json()`, then `expect(compareJson.base_var_99).not.toBe(compareJson.hedged_var_99)`. UI labels / table run after that. |
| 2 | Would a 200 + table with equal VaRs still pass? | **No.** Same-number `base_var_99` / `hedged_var_99` fails `not.toBe`. That is the QA-014 escape. Closed. |
| 3 | `not.toBe` on numbers: NaN/undefined tricks? `var_improvement !== 0` as a second lock? | **Equal / both-empty fail.** Playwright `toBe` is `Object.is`: both `undefined` / `null` / `NaN`, or `0 === 0`, fail. One-sided missing/`null`/`NaN` vs a number would pass `not.toBe`; `var_improvement` omitted also passes `not.toBe(0)`. Not the Finding 1 escape. Live `HedgeComparisonReport` 200 always emits all three as floats. Residual nit; not Important. Second lock does close equal-VaR + `improvement === 0`. |
| 4 | Are custom and compare paired with `waitForResponse` + `ok()`? | **Yes.** Both waiters are armed before click, exact pathname, `res.ok()`. Optional `POST /api/v1/risk/var` `ok()` before Overview KPIs is present. Load ES also waits `/risk/es` `ok()`. |
| 5 | Dollar amounts still unpinned? | **Yes.** KPI `$` format only. No pinned VaR/ES dollars. Relational API fields only. |
| 6 | Playwright re-run needed? | **No.** Verdict is from the asserts. Report’s “1 passed / 9.0s” was not used as evidence. |

## Findings (prior two items only)

### 1. CLOSED — Important — Hedge rerun verifies changed risk from compare JSON

- Files: `e2e/tests/r0-critical-journey.spec.ts` L122–144
- What changed: Compare is `waitForRequest` + `waitForResponse` (`ok()`). After SPY equity qty `0`, JSON asserts `base_var_99 !== hedged_var_99` and `var_improvement !== 0`. No dollar pins. No client-side VaR.
- Why it is closed: QA-014 / Orchestrator required those two API fields to differ. The prior escape — broken hedge, 200, a table, `$0 → $0` or identical before/after still matching `/99% VaR/` — fails the JSON predicates. Visibility-only is no longer the terminal check.

### 2. CLOSED — Minor — Custom and compare responses are locked

- Files: `e2e/tests/r0-critical-journey.spec.ts` L32–36, L63–71, L101–105, L122–130
- What changed: Custom `evaluate/custom` and compare each have `waitForResponse` + `ok()` next to the existing request waiter. Dashboard waits `POST /api/v1/risk/var` `ok()` (optional in the first review). Load ES waits `/risk/es` `ok()`. Path match is exact `pathname`, not `includes`.
- Why it is closed: Orchestrator required response `ok()` on the two intercepted POSTs in this spec. Both are paired. The optional `/risk/var` wait is in.

## Residual (not Important; does not block)

`not.toBe` without `typeof === 'number'` / `Number.isFinite` still lets a 200 body with only one of `base_var_99` / `hedged_var_99` present (other `undefined`/`null`/`NaN`) and omitted `var_improvement` pass. Both-missing fails. Equal numbers fail. This API’s compare 200 cannot omit those floats. Hardening is optional; it does not reopen Finding 1.

## Finding counts (prior two items)

| Severity | Remaining VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 0 |

**Verdict: APPROVE**

---

## Orchestrator adjudication (2026-09-03)

Important #1 and Minor #2 closed. Residual `not.toBe` without finite-number checks: VALID, deferred (live compare 200 always emits the floats).
