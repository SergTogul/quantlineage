# R0.10.4 Heavy UI RiskRun Fallback Independent Review

**APPROVE** — Critical 0 / Important 0 / Minor 3. Lead may keep RF-015 **CLOSED**.

Slice: remaining interactive HEAVY UI POSTs fall back to typed RiskRun when
the inline gate refuses (`details.use=/risk/runs`). Do not treat the
implementer’s report as proof.
Workspace: uncommitted working tree vs HEAD on `/Users/user/src/riskforge-mvp`
(`r0-core-remediation` @ `bbe6164`).
Implementer report: `reviews/r0.10.4-heavy-ui-riskrun-fallback-report.md`
(claims, not proof).
Finding: `reviews/FINDINGS.md` RF-015 — Status **CLOSED** (working-tree edit
in this slice). This review does **not** flip FINDINGS; Lead may keep CLOSED.

Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. The only write
is this file. RF-004 / RF-008 / RF-009 remain **CLOSED** (not reopened).

## Verdict: APPROVE

## Checks performed

- Read the implementer report as claims only; read RF-015 acceptance
  (dashboard/heavy UI must not monopolize request-thread sync under the
  gate) and the attack checklist below.
- Diffed vs HEAD. Scored paths:
  - `backend/app/domain/models.py` — 10 typed RiskRun request schemas +
    `RISK_RUN_REQUEST_SCHEMAS` entries; base `extra="forbid"` retained
  - `backend/app/services/risk_run_worker.py` — `SUPPORTED_RUN_TYPES` +
    `execute_run_type` dispatch
  - `backend/tests/test_risk_run_api.py` — per-type dispatch fixtures;
    membership smoke
  - `frontend/src/api.js` — `postHeavyOrRiskRun` / `runViaRiskRun`; all
    live HEAVY UI exports wired; `limitDrilldown` stays sync
  - `frontend/src/api.test.js` — MSW refuse→poll for evaluate / explainPnL
    / reverseStress; evaluate gate-off; dashboard cases preserved
  - `reviews/FINDINGS.md` / `reviews/REMEDIATION_MILESTONE.md` — RF-015
    CLOSED + R0.10.4 COMPLETE (status claims; not treated as proof)
- Confirmed live risk-terminal callers (`ScenarioBuilder.jsx`,
  `Analytics.jsx`, `App.jsx`) only use the wired HEAVY helpers or
  INTERACTIVE `limitDrilldown` / dashboard load. Heatmaps are
  display-only from dashboard data (no live HEAVY POSTs).
- Confirmed `POST /risk/what-if` is HEAVY + gated but has **no** live
  risk-terminal UI caller in this repo slice (documented residual).
- Independently probed sync vs RiskRun payload **top-level keys** for all
  ten new UI `run_type`s via in-process TestClient (gate off; worker
  completes). Keys matched for each. `extra="forbid"` rejects unknown
  knobs. `RISK_RUN_REQUEST_SCHEMAS` bijection with `SUPPORTED_RUN_TYPES`
  holds (18 types).
- Ran the required suites (rerun, not the implementer’s log).

## Required suites (rerun)

```bash
cd /Users/user/src/riskforge-mvp/frontend && npm test -- --run
# Test Files  12 passed (12)
# Tests  93 passed (93)
# ~8.76s
```

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_risk_run_api.py \
  tests/test_backpressure.py \
  tests/test_endpoint_execution_class.py
# 64 passed, 1 warning (pre-existing Starlette/httpx TestClient deprecation)
# ~5.70s
```

## Hunt results

| # | Hunt | Result |
|---|---|---|
| 1 | Live HEAVY UI export still sync-only under gate? | **No** for risk-terminal callers. All ten HEAVY UI exports + `loadDashboard` use refuse→RiskRun. `limitDrilldown` correctly stays INTERACTIVE sync. |
| 2 | New `run_type`s missing from schemas / worker / `extra='forbid'` broken? | **No.** Schemas ↔ `SUPPORTED_RUN_TYPES` bijection; dispatch returns dicts; bogus knobs raise `extra_forbidden`. |
| 3 | Fallback poll never completes / wrong payload vs sync? | **No** under in-process worker. Independent key-parity probe COMPLETED for all ten UI types. UI `ScenarioEvaluationReport.evaluations` shape preserved (not list→`items` wrap). |
| 4 | INTERACTIVE routes forced through RiskRun? | **No.** `limitDrilldown` uses `json()` only. Scenario GETs / `/factors` / LINEAR summary unchanged (prior backpressure tests still green). |
| 5 | Gate-off always goes to RiskRun? | **No.** Helper tries sync first; MSW evaluate gate-off asserts zero `/risk/runs` hits. |
| 6 | RF-015 CLOSED while residual live HEAVY UI still sync-only? | **No** for live risk-terminal HEAVY POSTs. `what-if` API residual has no UI caller; documented; does not reopen RF-015 acceptance for interactive UI fan-out. |
| 7 | Tests only assert URLs, not refuse→poll→result? | **Partial.** Three MSW refuse→poll→payload cases + gate-off evaluate; shared helper covers the rest. Backend dispatch only asserts `dict` (not key parity). Not a product fail given live probe + shared helper. |
| 8 | Backpressure / execution_class miss new run types or formal stress? | **No.** New items are RiskRun `run_type`s, not new HTTP routes. Existing HEAVY routes (formal evaluate/compare, reverse, attribution, ES, VaR-compare, query, what-if) already refuse under the gate; execution_class bijection unchanged and still green. |

## Acceptance criteria

| # | Criterion | Result |
|---|---|---|
| 1 | Shared `postHeavyOrRiskRun` / `runViaRiskRun`; sync first, refuse→create+poll | **Pass.** |
| 2 | All live risk-terminal HEAVY UI POSTs fall back on refuse | **Pass.** evaluate / reverse / reverse-multi / compare / query / attribution / attribution-demo / change-attribution / ES / VaR-compare (+ prior dashboard). |
| 3 | Ten typed `run_type`s in schemas + worker dispatch | **Pass.** |
| 4 | Gate-off sync unchanged; INTERACTIVE stays sync | **Pass.** |
| 5 | RF-015 close candidate: interactive HEAVY UI no longer sync-only under gate | **Pass.** Lead may keep **CLOSED**. |
| 6 | RF-004 / RF-008 / RF-009 untouched / stay CLOSED | **Pass.** |

## Attack results

| # | Attack | Score |
|---|---|---|
| 1 | Some live HEAVY UI export still sync-only (400 when gate on) with no RiskRun fallback | **Fail (attack blocked).** All live HEAVY UI api.js exports wired. |
| 2 | New `run_type`s missing from typed schemas / worker / `extra='forbid'` broken | **Fail (attack blocked).** |
| 3 | Fallback poll never completes or returns wrong payload shape vs sync | **Fail (attack blocked)** for in-process completion + key parity. Unbounded poll without deadline remains a Minor (pre-existing pattern). |
| 4 | INTERACTIVE routes incorrectly forced through RiskRun | **Fail (attack blocked).** |
| 5 | Gate-off path regresses (always RiskRun) | **Fail (attack blocked).** |
| 6 | RF-015 CLOSED while residual HEAVY UI still sync-only | **Fail (attack blocked)** for live UI. `what-if` is API-only residual, not live UI. |
| 7 | Tests only assert URLs, not refuse→poll→result | **Partially true as coverage gap; not a product defect.** Scored Minor, not Important. |
| 8 | Backpressure / execution_class miss new run types or formal stress paths | **Fail (attack blocked).** |

## Residual (not a fail)

| Item | Why it is not a leftover hole for this slice |
|---|---|
| Poll uses fixed `RISK_RUN_POLL_MS` with no max-wait | Same as dashboard fallback; FAILED/COMPLETED still terminate. Hardening later. |
| Analytics Risk Runs dropdown lists older canned types only | Documented. New types are reached via interactive HEAVY UI automatic fallback, not the manual picker. |
| `POST /risk/what-if` HEAVY with no UI caller / no dedicated `run_type` | Explicit non-goal of this UI slice; RF-015 is interactive UI fan-out. Optional follow-up if a what-if terminal lands. |
| Frontend refuse→poll MSW covers 3/10 new exports | Shared helper + run_type wiring verified by inspection + live parity probe. Expand tests as hardening. |
| Backend `test_execute_run_type_dispatch` only asserts `isinstance(payload, dict)` | Independent key-parity probe passed; formal HTTP COMPLETED parity tests would harden RF-009-style confidence for the new types. |
| Deprecated legacy stress POSTs remain gated | Out of scope; not called by live UI (RF-011 track). |

## Findings

### Critical

None. VALID count: **0**.

### Important

None that block APPROVE. VALID count: **0**.

### Minor

1. **Incomplete per-export refuse→poll MSW coverage**
   - File: `frontend/src/api.test.js`
   - Covered: evaluate, explainPnL, reverseStress (+ evaluate gate-off). Missing explicit refuse→poll pins for reverse-multi / compare / query / attribution-demo / change-attribution / ES / VaR-compare.
   - VALID count: **1**.

2. **No client-side poll deadline**
   - File: `frontend/src/api.js` (`pollRiskRunUntilTerminal`)
   - Hung worker → unbounded poll. Documented.
   - VALID count: **1**.

3. **Analytics manual Risk Runs picker omits new `run_type`s**
   - File: `frontend/src/components/Analytics.jsx` (`RISK_RUN_TYPES`)
   - Documented; does not break automatic HEAVY UI fallback.
   - VALID count: **1**.

## Recommendations

- Lead Architect may **keep RF-015 CLOSED** after this APPROVE.
- Do **not** reopen RF-004 / RF-008 / RF-009.
- Optional hardening (non-blocking): expand MSW refuse→poll pins; add
  backend COMPLETED key-parity tests for the ten UI types; expose new
  types on the Analytics picker; add poll max-wait; add `what-if`
  RiskRun only if a UI caller appears.
- Compose smoke under `RISKFORGE_EXTERNAL_WORKER=1` (Scenario Builder
  evaluate + P&L Explain) remains a useful QA follow-up, not a reopen.

## Assessment

**Ready to merge: Yes** (R0.10.4 remaining HEAVY UI → RiskRun fallback).

**RF-015:** Lead may keep **CLOSED**.

## Counts

| Severity | VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 3 |

**Verdict: APPROVE** (zero VALID Important). Lead may keep RF-015 **CLOSED**.
