# R0.7.3 Artifact VaR Independent Review

Slice: RF-008 leftover — derive node VaR/ES from the summed historical
P&L vector on the artifact path (same convention as
`test_var_es_golden.py` / `HistoricalRiskEngine.calculate`). Default
no-artifact `HierarchyEngine.build` still full-reprices and must stay
leftover.
Workspace: uncommitted working tree vs HEAD on `/Users/user/src/quantlineage`
(`r0-core-remediation` @ `c51ac07f4802bc6fc50df4c645e3432f35308134`).
Implementer report: `reviews/r0.7.3-artifact-var-report.md` (claims,
not proof).
Plan: `reviews/REMEDIATION_MILESTONE.md` § R0.7.3 (COMPLETE for opt-in
consume + live producer; this leftover is node VaR/ES from vectors).
Finding: `reviews/FINDINGS.md` RF-008 — Status **IN PROGRESS**. Must
stay open. Default no-artifact `build` still reprices. Do not close
RF-008. R0.7 is not marked COMPLETE.

Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. The only write
is this file.

Other dirty-tree work (Builtin terms in `builtin.py`, HEAVY routes in
`risk.py` / `test_backpressure.py`) is out of scope and was not scored.

Scored only:

- `backend/app/risk/hierarchy.py`
- `backend/app/services/portfolio_service.py`
- `backend/tests/test_hierarchy_artifact_var.py` (new, untracked)
- `backend/tests/test_portfolio_hierarchy_artifacts.py`
- `reviews/r0.7.3-artifact-var-report.md` (new, untracked)

## Verdict: APPROVE

Critical 0 · Important 0 · Minor 0

## Lead Architect adjudication (2026-09-04)

Accepted. Zero VALID Important. RF-008 stays **IN PROGRESS** — default no-artifact `HierarchyEngine.build` still full-reprices. This slice commits artifact-path node VaR/ES from summed vectors only. Do not mark R0.7 COMPLETE.

`_node_from_artifacts` sums the node subset (`TradeCalculationArtifact.add`)
and sets `var_95` / `var_99` / `expected_shortfall_99` from
`_var_es_from_historical_pnl(summed.historical_pnl)`. That helper is
the same four lines as `HistoricalRiskEngine.calculate`:

- loss = −P&L
- `numpy.quantile` default linear interpolation
- VaR and ES floored at 0
- ES = mean of losses ≥ 99% VaR

It is **not** the sum of child VaRs. Controlled two-trade vectors
`(-1,-2,-3,-4)` + `(-10,0,0,0)` give root **9.95 / 10.79 / 11.0**.
Sum of trade `var_99` is **13.67**. Independent hand calc, the
production helper, and `HierarchyEngine.build(artifacts=...)` agree.
Wrong conventions rejected by `test_var_es_golden.py` (1−q tail,
sign flip, off-by-one) do not match.

`PortfolioService.hierarchy` attaches one `approximate_pnl_series` per
already-valued trade (shared `HistoricalRiskEngine` observations,
LINEAR / DELTA_GAMMA). It does not write VaR onto the artifact. Live
two-trade probe: **2 `value` calls**, **0 `value_portfolio`**,
**0 `shocked_value`**, **7 nodes**, root VaR/ES **exactly equal**
both the independently summed series and `HistoricalRiskEngine.calculate`
(20.607243421299675 / 30.907119101630627 / 33.48208802171339).
Not leftover zeros.

Mixed present/absent vectors raise `ValueError` matching
`historical_pnl is present on only one`. All-omitted vectors stay
zeros. Limits stay `[]`. Stress maps stay empty.

`var.py`, `test_var_es_golden.py`, `test_hierarchy.py`,
`trade_artifacts.py`, FINDINGS, and the milestone are byte-identical
to HEAD. RF-008 remains **IN PROGRESS**. Direct `build(...)` without
`artifacts=` on the same book is still **164 `value` / 38
`value_portfolio` / 60 `shocked_value`** and a computed `var_95` ≈
20.61. Do **not** close RF-008.

Zero VALID Important. Do **not** close RF-008. Do **not** mark R0.7
COMPLETE.

## Checks performed

- Read the implementer report as claims only; read § R0.7.3, RF-008
  (acceptance still includes “node VaR equals VaR from the node's
  aggregated scenario P&L vector” and “a hierarchy request does not
  execute one independent full risk run per trade”),
  `HierarchyEngine._node` / `_node_from_artifacts` /
  `_var_es_from_historical_pnl` / `_sum_artifacts`,
  `PortfolioService.hierarchy` / `_historical_pnl_for_valuation`,
  `HistoricalRiskEngine.calculate`, `VaRAnalytics.report`,
  `TradeCalculationArtifact.add` / `_add_historical`,
  `test_var_es_golden.py` methodology block, the new consume-path
  tests, and the updated produce-path test.
- Diffed vs HEAD the scored paths plus `var.py`,
  `test_var_es_golden.py`, `test_hierarchy.py`, `trade_artifacts.py`,
  `FINDINGS.md`, and `REMEDIATION_MILESTONE.md`. Confirmed those six
  untouched paths are empty vs HEAD. `git hash-object` equals the HEAD
  blob for `var.py` (`cefc31e4…`), `test_var_es_golden.py`
  (`ed9c6bae…`), `test_hierarchy.py` (`a87c9d9f…`),
  `trade_artifacts.py` (`a6a916e7…`), FINDINGS (`3b4f2945…`), and the
  milestone (`450539eb…`). `hierarchy.py` is **not** identical to HEAD
  (`7eec0ec2…` vs `4ed13227…`; +48 / −4, helper + `_node_from_artifacts`
  wiring + module docstring). `portfolio_service.py` is **not**
  identical (`7bd0263d…` vs `c114db92…`; +61 / −12, producer series +
  `approximate_pnl_series` import).
- Confirmed RF-008 worktree Status line equals HEAD: **IN PROGRESS**,
  “Remaining: node VaR/ES from historical vectors. Do not close.” No
  `Status: CLOSED` / COMPLETE on RF-008 or R0.7. Milestone § R0.7.3
  still says “Node VaR/ES from vectors remain leftover.” This slice is
  that leftover; they did not flip RF-008 or R0.7.
- Probed in memory only (no production edits):
  - Helper vs hand calc vs goldens G / F / n=101 ES membership.
  - Consume-path controlled vectors: root 9.95 / 10.79 / 11.0; trades
    3.85 / 3.97 / 4.0 and 8.50 / 9.70 / 10.0; parent ≠ sum of trade
    VaR; `_ForbiddenPricing` never invoked; `limits=[]`, `stress=[]`.
  - Mixed present/absent → `ValueError` / `historical_pnl is present
    on only one`.
  - All-omitted vectors → zeros.
  - Omitted-`artifacts` `build`: 164 / 38 / 60; computed VaR ≈ 20.61;
    limits and stress present.
  - Live `PortfolioService.hierarchy`: 2 / 0 / 0; 7 nodes; non-zero
    VaR/ES matching summed `approximate_pnl_series` and
    `HistoricalRiskEngine.calculate` at abs 1e-12; `limits=[]`,
    `stress=[]`; artifact type has no `var_*` fields.
  - FULL_REVAL injected engine: `_historical_pnl_for_valuation`
    returns `None` (documented omit).
- Reran the required suite. Collect-only: 4 + 3 + 12 + 5 + 11 = 35.
  No `skip` / `xfail` in the two scored test files. TDD-red narrative
  in the implementer report was not treated as proof and was not
  reproduced.

Did not mutate index, HEAD, or branch. Did not edit production files.

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | Node `var_95` / `var_99` / ES are leftover zeros, or not VaR/ES of the **summed** trade vectors | **No.** `_node_from_artifacts` calls `_var_es_from_historical_pnl` on `summed.historical_pnl` after `_sum_artifacts`. Consume probe: 9.95 / 10.79 / 11.0 = hand VaR/ES of the sum. Live probe: matches independently reconstructed `series[0]+series[1]` and `calculate()` at 1e-12. All-omitted zeros remain only when every vector is `None`. |
| 2 | Parent VaR is the sum of child VaRs | **No.** Implementation never adds child `var_*`. Consume fixture: parent `var_99` 10.79 ≠ 3.97+9.70. Live two-trade book **is** additive (identical equity factor loadings → identical series up to scale; parent == sum of trade VaRs **and** == `calculate()`). That is correlation, not a sum-of-VaRs implementation. |
| 3 | Live `PortfolioService.hierarchy` value-call regression (not ~1 `value` per position) | **No.** Probe: `value_calls == 2 == n_positions`; nodes == 7; `value_portfolio_calls == 0`; `shocked_value_calls == 0`. Same book omitted-`artifacts`: 164 / 38 / 60. `_historical_pnl_for_valuation` uses already-valued Greeks only. |
| 4 | Mixed present/absent historical vectors do not fail closed | **No.** `add()` → `_add_historical`: one `None` and one series raises `ValueError` matching `historical_pnl is present on only one`. Consume test + independent probe. Length mismatch still fails in unedited `trade_artifacts.py`. |
| 5 | Edited `var.py` / goldens / closed RF-008 / weakened `test_hierarchy.py` | **No.** All four hashes / diffs match HEAD. RF-008 Status line unchanged (**IN PROGRESS**, “Do not close.”). Existing 12 hierarchy tests still collected; 1e-9 VaR / ES / additive goldens unchanged; full-metric tests still call `engine.build` **without** `artifacts`. |
| 6 | Invented VaR in the producer instead of calling `approximate_pnl_series` | **No.** Producer writes `historical_pnl=self._historical_pnl_for_valuation(valuation)`. That helper calls `approximate_pnl_series` on the valued Greeks + shared factor observations. Artifact type has no `var_*` fields. Hierarchy computes the quantile. Produce test reconstructs the two series independently and asserts the summed-vector golden, not a hardcoded 20.61. |
| 7 | Duplicated **wrong** quantile convention | **No.** Helper is a line-for-line copy of `HistoricalRiskEngine.calculate` (not `np.percentile`, not 1−q, not sign-flipped P&L, not strict `>` tail). Independent check: golden G 3.85 / 3.97 / 4.0; F 95.05 / 99.01 / 100.0; n=101 ES 99.5 not 100. `VaRAnalytics.report` floors ES at `var` rather than `0.0`; with `tail = losses[losses >= var]` those are equivalent. Goldens A–D still pin both engines. |

## Findings

None.

## REJECTED

1. **Rejected — leftover zeros on the live / present-vector artifact path.**
   Live root and all seven nodes have non-zero VaR/ES. Consume path
   with fixture vectors is 9.95 / 10.79 / 11.0. Zeros remain only for
   all-omitted vectors (tested) and for FULL_REVAL / `factor_panel`
   (producer returns `None`; documented).
2. **Rejected — parent VaR is the sum of child VaRs.**
   Consume-path numbers prove otherwise. Code path is VaR(sum(vectors)).
3. **Rejected — live hierarchy value-call regression.**
   Probe 2 vs 7 vs omitted 164. Same as the produce-path leftover.
4. **Rejected — mixed vectors silently zero or mix reprice + artifacts.**
   `add()` fails closed before a node is returned. Pricing is not
   invoked on the artifact path.
5. **Rejected — `var.py` / goldens edited, or RF-008 / R0.7 marked
   CLOSED or COMPLETE, or `test_hierarchy.py` weakened.**
   Hashes match HEAD. Status line still **IN PROGRESS** / “Do not
   close.” Full-reprice goldens still 1e-9 on `engine.build` without
   `artifacts`.
6. **Rejected — invented VaR in the producer.**
   Producer attaches a series from `approximate_pnl_series`. Quantile
   lives in `hierarchy.py`. No hardcoded 9.95 / 10.79 / 20.61.
7. **Rejected — duplicated wrong quantile convention.**
   Same convention as `HistoricalRiskEngine.calculate` and
   `test_var_es_golden.py`. Independent goldens G / F / ES101 match.

## Leftovers (not scored as this slice failing)

- **RF-008 default no-artifact path — per-node full recomputation.**
  Omitting `artifacts` still enters `_metrics` / `_stress` / `_limits`
  at every node. Probe: 164 `value` calls on two trades. Expected
  leftover (`test_omitted_artifacts_still_reprices`). Do not close
  RF-008.
- **FINDINGS remaining line is now stale.** Status still says
  “Artifact-path VaR/ES stay omitted zeros; Remaining: node VaR/ES
  from historical vectors.” Live / present-vector artifact path no
  longer has omitted zeros. Correct that they did not flip RF-008 to
  CLOSED. A later status note may record that node VaR/ES from
  summed vectors is done and that the default no-artifact path still
  reprices. Do not close RF-008 when doing so.
- **Milestone § R0.7.3 still says “Node VaR/ES from vectors remain
  leftover.”** This slice is that leftover. They did not re-mark R0.7
  / R0.7.3 or close RF-008.
- **`test_hierarchy_artifacts.py` module docstring still says VaR /
  ES are omitted on the artifact path.** That file was not edited
  (hash matches HEAD). Behavior changed in `hierarchy.py`. The new
  consume-path file owns the VaR asserts. Docstring drift only.
- **Limits stay `[]`.** LimitEngine still prices unless pre-seeded.
  `limits.py` is out of scope. Report is honest.
- **Empty `stress_pnl` / empty node `stress`.** Honest. Not a stress
  run. Previous produce-path Minor (untested `stress == []`) is
  unchanged: the new produce test still pins VaR/ES and `limits == []`,
  not empty stress.
- **Producer omits `historical_pnl` when `self.risk` is not
  `HistoricalRiskEngine`, methodology is `FULL_REVALUATION`, or
  `factor_panel` is set.** Those paths would re-call `value`. Default
  injected engine is DELTA_GAMMA with no panel. Probe: FULL_REVAL
  helper returns `None`.
- **R0.7.4 lazy drilldown.** Not in this slice.

## Items checked and not findings

- `_var_es_from_historical_pnl(None)` and empty array return
  `(0.0, 0.0, 0.0)`. Empty positions (`summed is None`) stay zeros.
- `VaRAnalytics.report` inlines the same quantile; there is no
  extracted helper. Hierarchy copies those four lines rather than
  calling `report()`. That is what the report claims. Convention
  matches `calculate()`, which is the hierarchy no-artifact engine.
- Consume tests do not import the production helper; they reimplement
  the golden four lines. Producer test reconstructs via
  `approximate_pnl_series` on a separate `BuiltinPricingEngine().value`
  (not the counting engine’s return), then applies the same golden
  helper to the sum.
- Live two-trade book is a poor witness for VaR non-additivity
  (identical equity loadings). The consume-path fixture is the witness.
  Live is the witness for “equals `calculate()` / summed series” and
  for value-once.
- `test_hierarchy.py` still uses abs `1e-9` for MV / Greeks / VaR / ES.
  `test_greeks_var_es_stress_limits_on_nodes` still calls
  `engine.build` without `artifacts` and still expects computed
  subset VaR. Not loosened.
- `test_hierarchy_artifact_var.py` and the produce-path file have no
  `skip` / `xfail`.
- Counting `value` is sufficient on the live path: leftover `_metrics`
  goes through `value_portfolio` → `value`; leftover `_stress` goes
  through `value` + `shocked_value` → `value`. A leftover reprice
  cannot keep `value_calls == 2`. Probe also counted the other entry
  points (all 0).
- TDD red (consume `var_95` 0.0 ≠ 9.95; produce 0.0 ≠ ~20.61) is a
  report claim only. Not re-executed as a revert. Current code
  independently produces those non-zero numbers.
- Builtin `value` is dirty on this tree (out of scope). Consume-path
  numbers do not go through Builtin. Live 20.61 matches `calculate()`
  on the same dirty Builtin; the invariant scored here is
  hierarchy-vs-engine, not Builtin-vs-HEAD.

## Claim check

| Claim | Result |
|---|---|
| Node VaR/ES = VaR/ES of the summed trade vectors (golden convention) | **Pass.** Consume 9.95 / 10.79 / 11.0; live matches summed series and `calculate()`. |
| Parent VaR is not the sum of child VaRs | **Pass** on the consume fixture (10.79 ≠ 13.67). |
| Live hierarchy ~1 `value` per position | **Pass.** Probe 2 vs 7. |
| Mixed present/absent vectors fail closed | **Pass.** |
| All-omitted vectors stay zeros | **Pass.** |
| Producer calls `approximate_pnl_series`; does not invent VaR | **Pass.** |
| `var.py` / goldens / `test_hierarchy.py` not edited | **Pass.** |
| RF-008 not closed; default `build` still reprices | **Pass.** Probe 164 `value` calls. Do not close. |
| Required suite 35 passed | **Pass** on independent rerun (35 passed / 35 collected). |

## Commands

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_hierarchy_artifact_var.py \
  tests/test_portfolio_hierarchy_artifacts.py \
  tests/test_hierarchy.py \
  tests/test_hierarchy_artifacts.py \
  tests/test_var_es_golden.py
```

```
35 passed in 1.17s
```

Collect-only: 35 tests. Independent in-memory probes as listed under
Checks performed (not committed).

## Do not close

RF-008 stays **IN PROGRESS**. Direct `HierarchyEngine.build(...)`
without `artifacts=` still independently recomputes full risk at
every node (164 `value` calls on the two-trade fixture). This leftover
gives the artifact path real node VaR/ES from summed vectors. That is
not acceptance evidence that “a hierarchy request does not execute
one independent full risk run per trade” on the default path.

Do not mark R0.7 / R0.7.3 COMPLETE beyond the existing opt-in +
producer notes. Do not treat empty `limits` / empty `stress` as
computed risk.
