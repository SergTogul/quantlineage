# R0.12.4 Independent Re-review

Slice: Nightly / labeled runner, after skip-green / fake-SLA fix.
Workspace: `/Users/user/src/riskforge-mvp` working tree vs HEAD.
Prior review: `reviews/r0.12.4-independent-review.md`.
Orchestrator: Important #1 VALID; Important #2 VALID; Minor #3 deferred; do not close RF-016.
Reviewer did not implement this work. Production files were not edited. Index, HEAD, and branch were not mutated. Only this file was written.

Important #1–2 were **not** assumed closed. Both were re-attacked against the uncommitted tree.

## Verdict: APPROVE

Remaining Critical/Important on items 1–2: **none**.

RF-016 stays **IN PROGRESS**. Do not close it. Minor #3 (comment-blind YAML) remains deferred. Labeled-runner SLA-K1/K2, hierarchy benchmark, and optional QuantLib E2E remain RF-016 leftovers, not this re-review’s close condition.

## Leftover bypasses (items 1–2)

None. The prior GHA skip-green hole (DSN present, Postgres unreachable → 2 skipped, exit 0) is closed. The native job no longer gates on `throughput > 0` / “SLA”; it asserts `impl` + `checksum`. `check_m6_sla.py` is still unused on `ubuntu-latest`.

## Checks performed

- Read uncommitted `nightly.yml`, `test_postgres_two_worker.py`, `test_nightly_ci.py`, `reviews/r0.12.4-report.md`.
- Read `ci.yml` PR-FULL / `backend-pytest`, `conftest.py`, `get_configured_database_url`, `benchmark.cpp` / `risk_kernel.hpp` (checksum identity), FINDINGS RF-016, BUILD_NOTES nightly section.
- `git diff HEAD` for `ci.yml` is two header comments. `nightly.yml` and the two test files are untracked; they were read from the working tree.
- Grepped `.github/workflows/nightly.yml` for `if:` / `continue-on-error` / `throughput` / `SLA-sized` / `check_m6_sla.py`: no `if:` or `continue-on-error`; no throughput/SLA assert; `check_m6_sla.py` appears only in a “do not run on ubuntu-latest” header comment.
- Ran:

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_nightly_ci.py tests/test_pr_full_ci.py tests/test_pr_fast_ci.py
# 15 passed in 1.07s
```

- Reproduced the prior Finding 1 matrix against `tests/test_postgres_two_worker.py` (same GHA DSN, no listener):

| Env | Result |
|---|---|
| unset CI/NIGHTLY + GHA DSN | **2 skipped**, exit 0 |
| `CI=1` + GHA DSN unreachable | **2 failed**, exit 1 |
| `RISKFORGE_NIGHTLY=1` + GHA DSN unreachable | **2 failed**, exit 1 |
| `CI=1` + no DSN | **2 skipped**, exit 0 |

Did not execute GitHub Actions. Did not start Docker/Postgres. Did not mutate index, HEAD, or branch.

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | GHA DSN + unreachable Postgres: does `require_live_postgres` **fail** (not skip) when `RISKFORGE_NIGHTLY=1` or `CI=1` with a postgresql DSN? | **Fail-closed.** `require_live_postgres()` replaced the collection `skipif`. `if nightly or (ci and url)` → `pytest.fail`. Reproduced: GHA DSN, no listener, `CI=1` → 2 failed; `RISKFORGE_NIGHTLY=1` → 2 failed. |
| 2 | Local unset CI/NIGHTLY still **skip**? | **Yes.** Same DSN, both flags unset: 2 skipped, exit 0. Missing DSN also skips. |
| 3 | PR `backend-pytest` with `CI=1` and **no** DSN still skip (must not fail every PR)? | **Yes.** `get_configured_database_url()` does not fall back to `DEFAULT_POSTGRES_DSN`. `CI` + missing DSN → skip. `backend-pytest` is `pytest -q` with no `RISKFORGE_DATABASE_URL`. Reproduced: `CI=1`, unset DSN → 2 skipped. GHA `CI=true` is accepted by `_env_flag`. |
| 4 | Nightly two-worker step actually sets `RISKFORGE_NIGHTLY=1`? | **Yes.** `postgres-two-worker` pytest step env: `RISKFORGE_NIGHTLY: "1"` plus the GHA DSN. Contract asserts `RISKFORGE_NIGHTLY` in that job block. |
| 5 | Native: no `throughput > 0` / “SLA”? Checksum/`impl` asserted? `check_m6_sla.py` unused on ubuntu-latest? | **Yes.** Native python assert is `impl == cpp_header`, `n_exposures==10000`, `n_shocks==1000`, `checksum == -9900`. No `wall_ms` / `throughput` gate. No step title contains SLA; “SLA-sized” echo is gone. `check_m6_sla.py` is not invoked. `checksum == -9900` is the constant-book identity (`10000 * scenario_pnl` = `-9900`), not a dummy. All jobs still `runs-on: ubuntu-latest`. |
| 6 | Still not on `pull_request` / not in PR-FULL `needs:`? | **Yes.** Nightly `on:` is `schedule` + `workflow_dispatch` only. PR-FULL `needs:` is still `backend`, `backend-quantlib`, `frontend`, `lint`, `e2e`, `postgres-smoke`. `ci.yml` vs HEAD is comments only. |

## Findings on items 1–2

### 1. Two-worker skip-green — CLOSED

- Files: `backend/tests/test_postgres_two_worker.py` `require_live_postgres`; `.github/workflows/nightly.yml` L138–143
- Prior hole: module `skipif` + GHA DSN + no listener → 2 skipped, exit 0.
- Now: unreachable postgresql DSN fails when `RISKFORGE_NIGHTLY` is set, or when `CI` is set **and** a postgresql DSN is offered. Local unset still skips. `CI` + no DSN still skips so `backend-pytest` stays green. Nightly sets `RISKFORGE_NIGHTLY=1`.
- The `CI` + missing-DSN skip is required (Attack 3). It is not a remaining GHA bypass: nightly always offers the DSN, and GHA also sets `CI=true`, so removing only `RISKFORGE_NIGHTLY` from the step still fail-closes on an unreachable DSN.

### 2. Native liveness sold as SLA — CLOSED (wording / identity)

- Files: `.github/workflows/nightly.yml` native-benchmark steps; `backend/tests/test_nightly_ci.py` `test_nightly_native_asserts_checksum_identity_not_sla`
- Prior hole: `wall_ms >= 0` and `throughput_ops_per_s > 0` plus “SLA-sized” echo; `check_m6_sla.py` unused.
- Now: checksum/impl identity only. `check_m6_sla.py` still unused on `ubuntu-latest` (correct; host-specific floors flake). Labeled-runner SLA-K1/K2 remains an RF-016 leftover, not a leftover Important on this slice.

## Not remaining Important

- Missing extra unit tests for `RISKFORGE_NIGHTLY=1` fail and `CI=1` + no DSN skip. Behavior was reproduced at pytest file level. Not a bypass.
- Minor #3 (comment-blind `_needs_ids` / `_on_block`; `RISKFORGE_NIGHTLY` substring could live in a job comment) — **deferred**, same class as prior. Current YAML is not comment-only.
- Hierarchy benchmark / QuantLib E2E omitted — RF-016 leftover, not a block of items 1–2.
- RF-016 status is still **IN PROGRESS** (`reviews/FINDINGS.md`). Do not close.

## Finding counts (this re-review, items 1–2)

| Severity | VALID remaining |
|---|---|
| Critical | 0 |
| Important | 0 |

**Verdict: APPROVE**

---

## Orchestrator adjudication (2026-09-03)

Important #1–2 closed. Minor #3 deferred. Stage `.github/workflows/nightly.yml` explicitly. Do **not** close RF-016.
