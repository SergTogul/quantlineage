# Agent Handoff Template

## Task
R0.2 leftover — `what_if` must use one resolved MarketSnapshot

## Owner
Portfolio Risk Engineer / Backend API Engineer

## Summary
`PortfolioService.what_if` now resolves `self.market_snapshot(request.portfolio)` once and passes that object into `what_if_analysis`. `_calculate` / `what_if_analysis` / `incremental_var` thread the same snapshot into `HistoricalRiskEngine.calculate(..., market=...)`, `RiskFactorEngine.calculate`, and `StressEngine.run`. Add/remove after-books no longer re-infer marks from the mutated position set, so SAMPLE what-if add no longer raises `ConflictingSampleMarkError`.

TDD: `test_portfolio_service_what_if_uses_one_resolved_snapshot_before_and_after` failed first (`provider.calls == 0`). Existing `test_service_what_if_returns_before_after_incremental_and_deltas` failed on after-book adapter conflict. Both went green after snapshot threading.

## Files changed
- `backend/app/services/portfolio_service.py`
- `backend/app/risk/incremental_var.py`
- `backend/app/risk/stress.py`
- `backend/tests/test_what_if.py`
- `backend/tests/test_market_authority.py`
- `backend/tests/test_incremental_var.py`
- `reviews/r0.2-what-if-snapshot-report.md`

## Public/interface changes
- `what_if_analysis(..., market: MarketSnapshot | None = None)` — optional; when supplied, before and after use this snapshot.
- `incremental_var(..., market: MarketSnapshot | None = None)` — same contract.
- `_calculate(..., market: MarketSnapshot | None = None)` — passed through to `HistoricalRiskEngine.calculate`.
- `StressEngine.run(..., market: MarketSnapshot | None = None)` — optional; default remains `self.market_data.snapshot(portfolio)`.
- No DTO, HTTP, or domain-model change. API routers were not edited.

## Numerical conventions
- Units: currency loss VaR/ES (same as `HistoricalRiskEngine` / `RiskSummary`).
- Sign convention: incremental = after − before; positive incremental increases risk.
- Day count/calendar if relevant: n/a.
- Tolerances/reference: existing what-if abs 1e-9 vs after − before; remove helper now compares `report.after` to `HistoricalRiskEngine.calculate(after_book, market=base_snapshot)`, not `summary(after_book)` (that would re-infer).

## Tests added/updated
- `test_portfolio_service_what_if_uses_one_resolved_snapshot_before_and_after` — injected `service.market_data` snapshot is the object used for before and after (`provider.calls == 1`, `all(market is snapshot)`), same pattern as contributors/compare. Empty `scenarios` so stress shocks do not allocate derived snapshots.
- `test_what_if_remove_matches_apply_helper` — after-book VaR compared on the base SAMPLE snapshot.
- `test_incremental_var_equals_after_minus_before` / `test_removing_all_risk_positions_reduces_var` — pass `demo_market_snapshot(SAMPLE_PORTFOLIO)` so add/remove do not re-infer.

## Commands executed
```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_what_if.py tests/test_market_authority.py tests/test_api.py
```

RED (before implementation):
```bash
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_market_authority.py::test_portfolio_service_what_if_uses_one_resolved_snapshot_before_and_after \
  tests/test_what_if.py::test_service_what_if_returns_before_after_incremental_and_deltas
```
Failed: `provider.calls == 0`; after-book `ConflictingSampleMarkError` on `rates[USD]`.

Affected incremental VaR (after snapshot threading):
```bash
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_incremental_var.py
```

## Results
- Backend: what-if + API + incremental VaR **all passed**. Exact required command: **37 passed**, **4 failed** — all four are parallel R0.2 equity-vol authority tests (`test_missing_equity_option_vol_raises_when_market_is_supplied`, QuantLib twin). They were added to `test_market_authority.py` by a concurrent pricing slice; this leftover must not edit `backend/app/pricing/**`.
- This leftover’s assertion and all pre-existing authority snapshot-identity tests passed.
- Frontend: n/a
- QuantLib: n/a (not invoked by this leftover)
- C++: n/a
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): **yes for what-if / API / incremental VaR**; required command is not fully green solely due to the parallel vol tests.
- Unexplained failures or skips: none in this leftover. One pre-existing Starlette/`httpx` deprecation warning from `fastapi.testclient`.
- CI is green (all required checks): not started (commit/push out of scope)

## Known limitations / risks
- Direct `incremental_var` / `what_if_analysis` callers that omit `market` still re-infer per book via `demo_market_snapshot`. Empty-book add relies on that fallback (an empty base snapshot has no spots for the added trade).
- `StressEngine.run` now accepts optional `market`; callers that omit it are unchanged.
- What-if factor and stress diffs now price against the **base** snapshot, not a re-inferred after-book snapshot. That is the intended R0.2 leftover.
- Parallel R0.2 equity-vol tests in `test_market_authority.py` fail until Quant Pricing implements supplied-snapshot vol authority. Out of scope here.

## Follow-up / next owner
- Owner: Quant Pricing Engineer (equity-vol authority) / Lead Architect
- Requested action: finish the parallel vol-authority slice so the combined `test_market_authority.py` file is green. No further what-if snapshot work needed from this owner.
- Blocking?: no for this leftover; yes for declaring the exact required three-file command fully green.

## Status
DONE (what-if one-snapshot leftover). Do not commit (per task).
