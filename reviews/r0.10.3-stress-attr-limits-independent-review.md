# R0.10.3 Stress / Attribution / Limits Gate Independent Review

Slice: leftover RF-015 / R0.10.3 — apply the existing `reject_inline_heavy`
gate to HEAVY handlers in `stress.py`, `attribution.py`, and `limits.py`.
Do not treat the implementer’s route table as proof.
Workspace: uncommitted working tree vs HEAD on `/Users/user/src/riskforge-mvp`
(`r0-core-remediation` @ `8a7d968`).
Implementer report: `reviews/r0.10.3-stress-attr-limits-report.md` (claims,
not proof).
Plan: `reviews/REMEDIATION_MILESTONE.md` § R0.10.3.
Finding: `reviews/FINDINGS.md` RF-015 — Status **IN PROGRESS**. Compose
`loadDashboard()` still POSTs dashboard and will 400. Do not close.

Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. The only write
is this file.

Other dirty-tree work (frontend BlockHelp, native tests, README, etc.)
was not scored.

## Verdict: APPROVE

## Checks performed

- Read the implementer report as claims only; read RF-015 acceptance
  (dashboard load must not execute redundant full valuations of the same
  book and snapshot) and the hunt list below.
- Diffed vs HEAD. Scored hunks only:
  - `backend/app/api/stress.py` (`reject_inline_heavy` on all HEAVY POSTs;
    GETs untouched)
  - `backend/app/api/attribution.py` (`/attribution`, `/attribution/demo`,
    `/change-attribution`)
  - `backend/app/api/limits.py` (`/limits` only; `/limits/drilldown`
    untouched)
  - `backend/tests/test_backpressure.py` (live HTTP refuse + INTERACTIVE
    stay-200; dual mounts)
  - `reviews/r0.10.3-stress-attr-limits-report.md`
- Confirmed `backend/app/api/backpressure.py` and
  `backend/app/api/execution_class.py` have **no** diff vs HEAD (0 bytes).
- Confirmed `reviews/FINDINGS.md` and `reviews/REMEDIATION_MILESTONE.md`
  have **no** working-tree diff. RF-015 Status remains **IN PROGRESS**.
- Index empty for scored paths. Branch still `r0-core-remediation`. This
  review did not commit.
- Read every handler in the three routers. Gate is the first statement
  before `service.*` on every HEAVY POST. INTERACTIVE GETs and
  `POST /limits/drilldown` have no gate call.
- Independently probed HTTP with `TestClient(app)` (not the helper
  alone): unset flags, `RISKFORGE_EXTERNAL_WORKER=1`,
  `RISKFORGE_HEAVY_INLINE=0`, and both flags together
  (`EXTERNAL_WORKER=1` + `HEAVY_INLINE=1`). Dual-mount `/api/v1` and
  legacy for all 13 HEAVY POSTs + INTERACTIVE GETs / drilldown.
  In-memory only; no production-file edits.
- Ran the required commands (rerun, not the implementer’s log):

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_backpressure.py \
  tests/test_endpoint_execution_class.py \
  tests/test_dashboard_batch.py
# 44 passed, 1 warning (pre-existing Starlette/httpx TestClient deprecation)
# 6.00s
```

I did not rerun the implementer’s full backend suite or CI.

## Hunt results

| # | Hunt | Result |
|---|---|---|
| 1 | With `RISKFORGE_EXTERNAL_WORKER=1`, do HEAVY stress / attribution / limits POSTs still return 200 with computed payloads? | **No leftover compute on these routes.** All 13 HEAVY POSTs on both mounts are **400**. Body `{code: bad_request, message: Invalid request, details: {use: /risk/runs, route: POST /risk/<name>}}`. No stress rows, evaluations, reverse shocks, attribution items, or limit rows. No `Location`. Gate runs before `service.*`. |
| 2 | INTERACTIVE: `GET /stress/scenarios`, `GET /stress/scenarios/formal`, `POST /limits/drilldown` still 200? | **Yes.** Under `RISKFORGE_EXTERNAL_WORKER=1`, both mounts: scenario GETs **200** (13 rows each; formal length matches legacy); drilldown **200** with `portfolio_id`. |
| 3 | `/limits` gated; `/limits/drilldown` not? | **Yes.** `POST /risk/limits` and `/api/v1/risk/limits` **400**. Drilldown on both mounts **200**. Source: `reject_inline_heavy` only in `risk_limits`, not in `risk_limits_drilldown`. |
| 4 | Edit `backpressure.py` / `execution_class.py` / close RF-015 / enqueue a fake run? | **No on disk.** Those two modules have no diff. RF-015 Status is still **IN PROGRESS**. Refuse is `HTTPException` 400; empty Location; no run id. `RISKFORGE_HEAVY_INLINE=1` does not force-allow when the external-worker flag is on. |
| 5 | Tests helper-only while handlers still compute? | **No.** New cases use `TestClient(app)` POSTs/GETs. Independent probe matches. |
| 6 | Unset flags still allow inline? | **Yes.** Unset: stress / evaluate / limits / attribution/demo / change-attribution / reverse / custom / drilldown all **200** with computed bodies. Required suite 44 passed. |

## Acceptance criteria

| # | Criterion | Result |
|---|---|---|
| 1 | All HEAVY POSTs in stress / attribution / limits call `reject_inline_heavy` when gate on → 400 `Invalid request`, `details.use=/risk/runs` | **Pass.** Thirteen HEAVY POSTs refuse on both mounts with that envelope. |
| 2 | INTERACTIVE GETs and `POST /limits/drilldown` stay 200 with gate on | **Pass.** |
| 3 | `/limits` gated; `/limits/drilldown` not | **Pass.** |
| 4 | `backpressure.py` / `execution_class.py` not unnecessarily edited | **Pass.** No diff vs HEAD. |
| 5 | RF-015 not closed in FINDINGS | **Pass.** Status **IN PROGRESS**; no working-tree edit. |
| 6 | Tests are live HTTP, not helper-only | **Pass.** |
| 7 | Unset flags still allow inline | **Pass.** Live unset probe + suite green. |

## Attack results

| # | Attack | Score |
|---|---|---|
| 1 | Gate on, HEAVY stress / attribution / limits still 200 with computed payloads | **No.** 400 on both mounts; no compute fields. |
| 2 | INTERACTIVE scenarios GETs or limits drilldown refused when the gate is on | **No.** All stay 200. |
| 3 | `/limits/drilldown` gated because `/limits` was gated | **No.** Drilldown has no `reject_inline_heavy`. |
| 4 | New / wrong HTTP message | **No.** Stable `Invalid request`; `details.use=/risk/runs`. |
| 5 | Edit `backpressure.py` / `execution_class.py`, close RF-015, or refuse enqueues a fake run | **No on disk.** Map unchanged. Finding still **IN PROGRESS**. 400 does not enqueue. |
| 6 | Tests only assert `heavy_inline_allowed()` while handlers still compute | **No.** HTTP tests exist; independent probe matches them. |
| 7 | Default unset refuses these HEAVY routes (breaks existing tests) | **No.** Unset 200; required suite 44 passed. |

## Residual (not a fail)

| Item | Why it is not a leftover hole for this slice |
|---|---|
| Compose `loadDashboard()` still POSTs `/api/v1/risk/dashboard` | Already-gated HEAVY batch. Demo Compose dashboard will 400 until a later slice serves a RiskRun or the client calls `/risk/runs`. Documented. Do not close RF-015. |
| FINDINGS RF-015 text still says “Remaining: HEAVY stress / attribution / limits still inline” | Stale after this slice. Implementer correctly did not edit FINDINGS. Lead Architect updates the status line after APPROVE. Do not close. |
| Parametrized / focused HTTP lists omit some dual-mounts (e.g. formal custom / reverse only one mount in tests; `HEAVY_INLINE=0` samples stress + limits only) | Same handlers. Independent probe: both mounts 400 for all 13 HEAVY POSTs; `HEAVY_INLINE=0` refuses stress / limits / attribution/demo / compare. Test gap, not a production hole. |
| `_assert_refused_inline` does not assert `details.route` | Live bodies include `route=POST /risk/...`. Envelope `use=/risk/runs` is asserted. |
| `POST /risk/runs` not refused | Enqueue destination; refusing it would be circular. Not in this slice’s exclusive set. |

## Findings

### Critical

None. VALID count: **0**.

### Important

None that block APPROVE. VALID count: **0**.

### Minor

1. **Partial dual-mount coverage in new backpressure tests**
   - File: `backend/tests/test_backpressure.py`
   - Portfolio-path parametrize covers `/api/v1` + legacy for stress /
     evaluate / attribution/demo / limits, but custom / reverse / compare /
     attribution / change-attribution tests hit only one mount each.
     Independent probe: both mounts refuse. Same class of gap as the prior
     `risk.py` leftover review. VALID count: **1**.

## Recommendations

- Keep RF-015 **IN PROGRESS**. Next owner: Frontend / Risk UX for
  `loadDashboard()` → `/risk/runs`; Lead Architect for RF-015 status text.
  Do not mark R0.10 / RF-015 COMPLETE on this slice alone.
- After this APPROVE, Lead Architect may update the RF-015 status line to
  note stress / attribution / limits are now gated. Do **not** close the
  finding while Compose still POSTs dashboard.
- Do not treat `heavy_inline_allowed()` as proof. Future leftover slices
  must keep HTTP tests on the live routes, including both mounts where
  practical.

## Assessment

**Ready to merge: Yes** (R0.10.3 leftover stress / attribution / limits
refuse-gate slice only).

## Counts

| Severity | VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 1 |

**Verdict: APPROVE** (zero VALID Important).

## Lead Architect adjudication (2026-09-09)

Accepted. Zero VALID Important. Minor deferred. RF-015 stays **IN PROGRESS** — Compose `loadDashboard()` still POSTs dashboard and will 400 when the gate is on. This slice commits stress/attribution/limits refuse-gates only.

The named HEAVY handlers in `stress.py` / `attribution.py` / `limits.py`
refuse inline compute when the gate is on. INTERACTIVE scenario GETs and
`/limits/drilldown` stay 200. `/limits` is gated; drilldown is not. The
400 message is `Invalid request` with `details.use=/risk/runs`. Default
unset still allows inline. Tests are live HTTP. `backpressure.py` and
`execution_class.py` are untouched. RF-015 is not closed. No fake run is
enqueued. Compose dashboard remains leftover — not a fail for this slice.
