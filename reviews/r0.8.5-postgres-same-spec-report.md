# Agent Handoff Template

## Task
R0.8.5 — Postgres lifecycle + same-spec interactive vs worker (RF-009 close candidate)

## Owner
Backend / API Engineer

## Summary
Proved that the same resolved RiskRun spec yields the same numerical result for interactive/API-equivalent execution and worker execution (`summary` and `var`, synthetic dataset, exact float equality on SQLite). Hardened `_execute` so persisted first-class spec columns (`historical_dataset_id` / version / `as_of` / `calculation_config`) are the execute source of truth when present — a tampered or emptied request blob can no longer silently rebind to the process engine (closes the R0.8.4 residual Minor). Added optional live-Postgres lifecycle coverage (save/load, create, claim, COMPLETED/FAILED, identity-on-execute, same-spec parity) that skips locally when the DSN is missing, matching `test_postgres_two_worker.py`. Two-worker SKIP LOCKED remains in the existing nightly module. RF-009 stays **IN PROGRESS** pending independent review — not closed.

## Files changed
- `backend/app/services/risk_factories.py` — `request_blob_for_execute`, `resolve_execute_spec`
- `backend/app/services/risk_run_worker.py` — `_execute` uses column-preferring resolve
- `backend/tests/test_same_spec_parity.py` — new (SQLite parity + columns + identity execute)
- `backend/tests/test_postgres_risk_run_lifecycle.py` — new (live Postgres optional)
- `backend/tests/test_risk_run_factories.py` — `resolve_execute_spec` unit coverage
- `reviews/FINDINGS.md` — RF-009 still IN PROGRESS; R0.8.5 pending review
- `reviews/REMEDIATION_MILESTONE.md` — § R0.8.5 updated
- `reviews/r0.8.5-postgres-same-spec-report.md` — this report

## Public/interface changes
- New helpers: `request_blob_for_execute(run)`, `resolve_execute_spec(run, …)`
- Worker execute path prefers persisted columns over the request JSON blob
- No HTTP/OpenAPI contract changes

## Numerical conventions
- Units: VaR/summary payloads from `PortfolioService` (unchanged formulas)
- Sign convention: n/a (API/lifecycle slice)
- Tolerances/reference: **exact** equality for deterministic synthetic dataset (`observations=250`, `seed=7`) on demo market fixture NVDA@190
- Dataset identity asserted on both interactive rebound and completed worker row

## Tests added/updated
- `test_same_spec_interactive_matches_worker_sqlite` — summary + var
- `test_execute_prefers_persisted_columns_over_tampered_request_blob`
- `test_worker_execute_uses_stored_portfolio_not_posted_attacker`
- `test_resolve_execute_spec_prefers_persisted_columns`
- `test_postgres_*` lifecycle / parity / identity / failed (skip without DSN)

## Commands executed
```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_risk_run_api.py \
  tests/test_risk_run_factories.py \
  tests/test_risk_run_spec.py \
  tests/test_risk_factories.py \
  tests/test_backpressure.py \
  tests/test_postgres_two_worker.py \
  tests/test_same_spec_parity.py \
  tests/test_postgres_risk_run_lifecycle.py
```

## Results
- Backend: **99 passed, 6 skipped, 1 warning** in ~5.7s
- Skips (expected): 2× `test_postgres_two_worker.py` + 4× `test_postgres_risk_run_lifecycle.py` — live Postgres DSN missing/unreachable (PR-green path)
- Frontend / QuantLib / C++: not run (out of scope)
- CI: not started (do not commit / push per task)
- All applicable/affected suites for this task: pass
- Unexplained failures or skips: none

Exact required-suite result:

```text
99 passed, 6 skipped, 1 warning in 5.74s
```

## Known limitations / risks
- Live Postgres tests skip when `QUANTLINEAGE_DATABASE_URL` is missing/unreachable (local / PR backend-pytest); fail-closed under `QUANTLINEAGE_NIGHTLY` or CI-with-DSN per existing nightly rules.
- SQLite claim is single-writer (no SKIP LOCKED); multi-worker exclusivity remains Postgres-only in `test_postgres_two_worker.py`.
- Interactive sync routes outside RiskRun still use the process-global engine; only RiskRun create/execute paths rebind from spec.
- RF-009 must not be CLOSED until independent review APPROVE of this slice.

## Follow-up / next owner
- Owner: Independent reviewer (Backend/Risk) then Lead Architect
- Requested action: Independent APPROVE of R0.8.5; Lead may CLOSE RF-009 if exit criteria met
- Blocking?: yes for RF-009 CLOSE; no for merging this slice after review
