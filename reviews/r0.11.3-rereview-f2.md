# R0.11.3 Finding 2 re-review

Slice: live typed-body byte cap (`WorkloadBodyLimitMiddleware`).
Workspace: uncommitted work on `r0-core-remediation`.
Prior re-review: `reviews/r0.11.3-rereview.md` (Finding 2 still Important).
Orchestrator: ASGI cap **in front of** Pydantic; full-app test (typed-body
route, no Content-Length, 80-byte chunks, cap 64) that fails if more than
the first oversize chunk is received.
Production code was not modified. Index / HEAD / branch were not mutated.

## Verdict: APPROVE

Finding 2 is closed on the live typed-body path. Remaining
Critical/Important on Finding 2: **0**.

`WorkloadBodyLimitMiddleware` is registered last (`app.main`), so it sits
outside `LegacyDeprecationMiddleware` (`BaseHTTPMiddleware`) and FastAPI
body parsing. On `POST /api/v1/market/snapshot` with no `Content-Length`,
the raw client `receive` stops after the first oversize chunk. The inner
HTTP stack is not invoked. Invalid JSON over the cap is 413, not 422.
The new tests drive full `app` ASGI and would fail if the tail were pulled.

## Checks performed

- Read `backend/app/api/workload.py` (`WorkloadBodyLimitMiddleware`,
  `_cache_body_capped`, `_replay_receive`, leftover `_read_body_capped` /
  `enforce_workload_limits`), `backend/app/main.py` middleware order,
  `LegacyDeprecationMiddleware` (`BaseHTTPMiddleware`),
  `test_full_app_chunked_*` in `backend/tests/test_workload_limits.py`.
  Did not treat the implementer tests as proof.
- Independent full-app ASGI (typed `Portfolio` body, no `Content-Length`):
  80-byte chunks, invalid JSON, oneshot, declared `Content-Length: 999999`,
  under-cap control. Logged client `receive`, whether the inner HTTP app
  ran, replay sizes, and whether `enforce_workload_limits` ran. Spies were
  probe-only; production files unchanged.
- Spot-check Finding 1 (`MAX_POSITIONS=2`, 1+3 what-if adds) and Finding 3
  (1200-deep JSON). Finding 4 not reopened.
- `PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_workload_limits.py`
  → **20 passed**.

## Attack results (Finding 2)

| # | Required check | Result |
|---|---|---|
| 1 | Live `POST /api/v1/market/snapshot`, no CL, 80-byte chunks, cap 64: does the raw client stream stop after the first oversize chunk? | **Pass.** Client receive `[(80, True)]` (80 B). 413 `{field: body, limit: 64, actual: 80}`. Inner HTTP app **not** called. Dependency **not** called. Tail not delivered. |
| 2 | Invalid JSON over the cap: 413 not 422? | **Pass.** Same 80-byte stop, 413 `payload_too_large`, not 422. Inner not called. |
| 3 | New tests drive full `app` ASGI, not isolated `Request` / `enforce_workload_limits`? | **Pass.** `_asgi_post_chunked_typed_body` calls `await app(scope, receive, send)` on the live snapshot route. No hand-built `Request`. |
| 4 | Can the receive-log assertion be green while Starlette/FastAPI still joined the body (wrong receive wrapped)? | **No.** The log is the receive passed to `app()`. Overflow: inner HTTP receive never runs, so nothing downstream can join. Under-cap control: inner **is** called with a single replayed chunk (7 B / 10 B), proving the spy sees a join when one happens. `actual != offered` and `len(receive_log)==1` fail if the tail is pulled. |
| 5 | `BaseHTTPMiddleware` still buffering after the outer cap? | **Not on the attack path.** Overflow never reaches `LegacyDeprecationMiddleware`. Under the cap, inner sees only the already-capped replay. `ServerErrorMiddleware` is outermost but does not read `receive`. |
| 6 | Finding 1 what-if add / Finding 3 nesting still 413? | **Pass.** `1+3` adds → 413 `{field: positions, actual: 4}`, no `after`; `1+1` → 200. Legacy `/risk/what-if` same 413. 1200-deep → 413 `{field: nesting, limit: 64, actual: 65}`. |

Oneshot 383 B (first chunk already over the cap) 413s with `actual=383` after that single receive. That is one oversize chunk, not the prior multi-chunk drain.

## Leftover bypasses (Finding 2)

None that restore the prior hole (full client stream delivered into the
process on a live typed-body route, or 422 instead of 413 for invalid JSON
over the cap).

Residual, not Important for this item: a single ASGI chunk larger than the
cap is received once before reject (inherent to pull-based `receive`).
Finding 4 (per-list vs per-request) remains deferred.

## Finding counts (Finding 2 only)

| Severity | Open | Closed this pass |
|---|---|---|
| Critical | 0 | — |
| Important | **0** | 1 (Finding 2) |
| Minor | 0 | — |

Verdict is **APPROVE** because remaining Critical/Important on Finding 2 is 0.

---

## Orchestrator adjudication (2026-09-03)

Finding 2 closed. Finding 4 (per-list vs per-request) remains deferred.
