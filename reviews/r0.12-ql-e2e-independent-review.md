# R0.12 QuantLib E2E Independent Review

Slice: Optional QuantLib E2E on nightly only (R0.12.4 leftover / RF-016).
Workspace: `/Users/user/src/quantlineage` working tree vs HEAD.
Implementer report: `reviews/r0.12-ql-e2e-report.md` (claims, not proof).
Plan: `reviews/REMEDIATION_MILESTONE.md` § R0.12.4 optional QuantLib E2E.
Finding: RF-016.
Reviewer did not implement this work. Production files were not edited. Index, HEAD, and branch were not mutated.

## Verdict: APPROVE

The sibling job `quantlib-e2e` (`nightly-quantlib-e2e`) is real on disk: mandatory `pip install -r requirements.txt` (no no-ql fallback), `import QuantLib`, then `npx playwright test tests/r0-critical-journey.spec.ts` with `QUANTLINEAGE_PRICING_ENGINE=quantlib`. It is not on `pull_request` and is not in PR-FULL `needs:` (still the six PR jobs). Playwright default remains `builtin`. QuantLib hard-gate and PR-FAST bodies are unchanged (ci.yml vs HEAD is one header comment). RF-016 stays **IN PROGRESS**. Do not close it.

Remaining contract weakness is the same comment-blind / token-only class as R0.12.2 / R0.12.4 Minor. It does not hide a missing job.

## Checks performed

- Read `git diff HEAD --` `.github/workflows/nightly.yml`, `.github/workflows/ci.yml`, `e2e/playwright.config.js`, `backend/tests/quantlib_gate.py`, `backend/tests/test_nightly_ci.py`, `backend/tests/test_pr_full_ci.py`, `BUILD_NOTES.md`.
- Read on-disk `nightly.yml` (full), `ci.yml` `on:` / `pr-full` / `e2e` / `backend-quantlib` / `pr-fast`, `e2e/playwright.config.js`, `e2e/tests/r0-critical-journey.spec.ts`, `e2e/package.json`, `backend/tests/quantlib_gate.py`, `backend/app/pricing/factory.py` (`QuantLibPricingEngine()` when env is `quantlib`; raises `QuantLibUnavailableError` if the wheel is missing), `backend/app/api/health.py`, `backend/tests/conftest.py`, `reviews/FINDINGS.md` RF-016, `reviews/qa-review.md` QA-024, `reviews/r0.12-ql-e2e-report.md`.
- Grepped `.github/workflows` for YAML `if:` / `continue-on-error`: none in either file. Only two workflow files exist (`ci.yml`, `nightly.yml`).
- Probed named mutations **in memory** (no production edits, no index/HEAD/branch mutation).
- Ran:

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_nightly_ci.py tests/test_pr_full_ci.py tests/test_pr_fast_ci.py tests/test_quantlib_gate.py
# 21 passed in 1.91s
```

Did not execute GitHub Actions. Did not run Playwright. Did not price the demo book under QuantLib. Did not mutate index, HEAD, or branch.

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | Nightly on `pull_request` or new job in PR-FULL `needs:` | **No on disk.** `on:` is `schedule` (`0 6 * * *`) + `workflow_dispatch` only. PR-FULL `needs:` is exactly `backend`, `backend-quantlib`, `frontend`, `lint`, `e2e`, `postgres-smoke`. Honest add of `quantlib-e2e` fails both `test_pr_full_needs_does_not_include_nightly` and the new exact-six lock. |
| 2 | Job is echo-only / `continue-on-error` / `if: false` | **No on disk.** Install + import + `npx playwright test tests/r0-critical-journey.spec.ts`. No `if:` / `continue-on-error` in either workflow. `continue-on-error` is locked. `if: false` and `echo npx playwright …` stay green in the contract (Finding 1). |
| 3 | QuantLib install has no-ql fallback or skip-green when `QUANTLINEAGE_NIGHTLY=1` | **No on this job.** `quantlib-e2e` is `pip install -r requirements.txt` only (`requirements-no-ql` forbidden in the job block). `require_quantlib_for_nightly()` fail-closes when `QUANTLINEAGE_NIGHTLY=1` (unit-tested with a missing importer). Factory construction of `QuantLibPricingEngine` raises if the wheel is missing, so uvicorn/Playwright cannot skip to builtin. Other nightly jobs still have no-ql fallback (out of scope). |
| 4 | PR e2e now requires QuantLib (default engine changed) | **No.** Playwright is `process.env.QUANTLINEAGE_PRICING_ENGINE \|\| 'builtin'`. PR `e2e-playwright` still sets only `CI: true` (no engine env). Contract forbids an unconditional `'builtin'` overwrite and requires the `\|\| 'builtin'` default. |
| 5 | Hard-gate or PR-FAST weakened | **No.** ci.yml vs HEAD is one header comment. Hard-gate still has `QUANTLINEAGE_REQUIRE_QUANTLIB`, `QUANTLINEAGE_PRICING_ENGINE: quantlib`, no `requirements-no-ql`. PR-FAST file list / builtin env unchanged. `test_pr_fast_ci.py` / `test_quantlib_gate.py` bodies were not edited. |
| 6 | Contract tests comment-blind only | **Same class.** Honest leaks fail. Comment-first `# needs: [six]` plus a real extra `quantlib-e2e`, `if: false`, and `echo npx playwright …` stay green. Does **not** hide a missing job (the YAML job is real). Score Minor. See Finding 1. |
| 7 | RF-016 CLOSED | **No.** `reviews/FINDINGS.md` status remains **IN PROGRESS**. Remaining listed text still includes labeled-runner SLA-K1/K2, hierarchy benchmark, and QuantLib E2E. Report does not close it. Do not close RF-016. |

## Findings

### 1. VALID — Minor — QuantLib E2E contract is still comment-blind and token-wide

- Files: `backend/tests/test_nightly_ci.py` L40–57, L138–151; `backend/tests/test_pr_full_ci.py` L39–68
- What's wrong: Same helpers as R0.12.2 / R0.12.4 Minor (`_needs_ids` unanchored inline `needs: […]`, `_on_block` raw text, job-block substring locks). Specified honest mutations fail. These hunts stay **green**:
  - comment-first `# needs: [backend, backend-quantlib, frontend, lint, e2e, postgres-smoke]` plus listed `needs:` that also includes `quantlib-e2e` (`_needs_ids` returns the comment; exact-six and `quantlib-e2e not in needed` both pass)
  - `if: false` on `quantlib-e2e` (no `if:` lock; only `continue-on-error` is asserted)
  - replace `npx playwright test tests/r0-critical-journey.spec.ts` with `echo npx playwright test tests/r0-critical-journey.spec.ts` (required tokens remain; `echo-only` is a literal string check)
- Why it matters: Acc 1’s exact strings are locked. A later comment-only PR-FULL leak, a skipped job, or an echo that keeps tokens does not fail the suite. GHA itself would error if `ci.yml` `needs:` a job that does not exist in that workflow, so the comment-first leak is not a live PR blocker today. On disk the Playwright line is a real `npx playwright test`, not an echo.
- Suggested fix: Parse `on:` / `needs:` / job steps with a YAML loader. Forbid `if:` / `continue-on-error` on `quantlib-e2e`. Require a `run:` line that is `npx playwright test` (or `npm test`) plus the spec path, not merely those substrings anywhere in the block.

## REJECTED

1. **Rejected — nightly is on `pull_request` or `quantlib-e2e` is in PR-FULL `needs:`.** Not on disk. Adding `pull_request:` under nightly `on:` fails `test_nightly_triggers_are_schedule_and_dispatch_not_pull_request`. Adding `quantlib-e2e` to the real `needs:` list fails both PR-FULL contracts.
2. **Rejected — the new job is echo-only / `continue-on-error` / `if: false`.** On-disk steps install full requirements, import QuantLib, install frontend + Playwright Chromium, and run the critical journey. `continue-on-error` is locked. Contract holes for `if:` / echo are Finding 1, not a fake job.
3. **Rejected — no-ql fallback or skip-green when `QUANTLINEAGE_NIGHTLY=1` on this job.** Missing wheel fails `pip install`, the import step, and `require_quantlib_for_nightly()`. `QUANTLINEAGE_PRICING_ENGINE=quantlib` cannot construct a builtin engine in `create_pricing_engine()`.
4. **Rejected — PR e2e now requires QuantLib.** Default remains builtin. PR `e2e` job body vs HEAD is unchanged.
5. **Rejected — hard-gate or PR-FAST weakened.** Header comment only.
6. **Rejected — close RF-016 because nightly QuantLib E2E exists.** Status is still **IN PROGRESS**. Labeled-runner SLA-K1/K2 (`check_m6_sla.py`) and hierarchy benchmark remain. QA-024’s demo-artifact **range** check is also still OPEN (this slice runs the existing journey, not dollar bands). Do not close RF-016.

## Items checked and not findings

- `check_m6_sla.py` is unused on `ubuntu-latest` (including the new job). That is the accepted leftover, not a skip-green of this slice.
- `QUANTLINEAGE_REQUIRE_QUANTLIB` is a pytest/conftest flag; the API process does not read it. Fail-closed for a missing wheel is pip + `import QuantLib` + `QuantLibPricingEngine()` at uvicorn start. The Playwright step setting the flag is redundant, not a hole.
- `/health` returns `{"status": "ok"}` only. The journey does not assert engine identity. That cannot skip-green a missing QuantLib install (startup fails). It also does not implement QA-024’s artifact-range check — leftover, not this slice’s close condition.
- Per-instrument builtin fallback inside `QuantLibPricingEngine.value` for uncovered types is pre-existing pricing behavior (out of scope). Demo book types used by the journey are the QL adapter’s covered set.
- `test_quantlib_gate.py` still only covers `import_quantlib` / `quantlib_required`. The new fail/skip tests live in `test_nightly_ci.py` and ran in the 21-pass suite.
- BUILD_NOTES also gained an unrelated R0.11.5 auth-profile section in the same dirty tree. Out of scope for this slice.
- Dirty FX / auth / worker / native files were not reviewed except as excluded.

## Leftover skip-green / PR-blocking holes

On disk after this slice: **none** that make nightly QuantLib E2E skip-green or that put `quantlib-e2e` on the PR required path.

Contract leftovers (same Minor class; do not treat as proof the job is missing):

- `if: false` on `quantlib-e2e` stays green.
- `echo npx playwright test tests/r0-critical-journey.spec.ts` stays green.
- Comment-first inline `needs:` plus a real extra `quantlib-e2e` stays green (GHA would still refuse an unknown job id in `ci.yml`).

RF-016 leftovers (do **not** close):

- labeled-runner SLA-K1/K2 (`benchmarks/check_m6_sla.py`)
- hierarchy benchmark
- QA-024 QuantLib E2E vs demo artifact ranges (journey is shape + `base_var_99 !== hedged_var_99`, not bands)

Do not add Nightly or `quantlib-e2e` to branch-protection required checks. Do not add nightly jobs to PR-FULL `needs:`.

## Finding counts

| Severity | VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 1 |

**Verdict: APPROVE**

---

## Orchestrator adjudication (2026-09-03)

Important leftovers: none. Comment-blind contract Minor deferred. Do **not** close RF-016.
