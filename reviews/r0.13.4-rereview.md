# R0.13.4 Re-review (prior findings 1–3 only)

Slice: Current test/benchmark evidence (RF-020), after the review-fix pass.
Workspace: uncommitted docs on `r0-core-remediation` vs HEAD.
Prior review: `reviews/r0.13.4-independent-review.md` (REQUEST CHANGES; 2 Important, 1 Minor). Orchestrator classified all three VALID.
Implementer follow-up: `reviews/r0.13.4-report.md` (Review-fix pass).
Production code was not modified. This review did not mutate the index, HEAD, or branch. The only write is this file.

## Verdict: APPROVE

Remaining on the prior three items: **0 Critical / 0 Important / 0 Minor**.

The first-pass defects were framing: a copied Phase A table titled as today’s suite, 659/688 shown as one current two-engine matrix, and a subset table that omitted recorded lint/build/kernel rows. Those three presentations are gone. The slice still does not execute a live matrix. That leftover is the honesty path the Orchestrator already chose (option b), now labeled on RF-020. It does not reopen Important #1 and does not block APPROVE.

## Checks performed

- Diffed `git diff HEAD -- ROADMAP.md reviews/FINDINGS.md reviews/r0.13.4-report.md`. ROADMAP is insert-only after the existing Current gate paragraph. FINDINGS is a one-line RF-020 residual addition. The report is untracked (diff empty); read on disk.
- Read Current gate (`ROADMAP.md` lines 5–29), RF-020, milestone § R0.13.4, R0 Baseline Verification, Phase A Completion Record.
- Attacked the prior three items against the working tree, not the implementer addendum.
- Confirmed `TASKS.md` is absent.
- Did not re-run pytest, collect-only, ruff, mypy, frontend lint/build, native `g++`, or Postgres smoke. Did not invent or write 801/820 as a pass count.

## Attack on prior items

| # | Attack | Result |
|---|---|---|
| 1 | Is “today's suite size” gone? Does the heading still imply a live re-run? | **Gone. Heading no longer implies a live run.** First-pass `### Current R0 verification (2026-09-03)` is replaced by `### Recorded Phase A baseline (2026-09-03; not re-run)`. Body: copied from the milestone; **not re-run** in this slice. The 160 sentence no longer says “not today's suite size”; it says the figure remains the R0.13.1 historical snapshot and was not rewritten. Parent `## Current gate` is the pre-existing milestone-status heading (R0 **IN PROGRESS**); it is not a verification title claiming a live matrix. |
| 2 | Are 659 and 688 still presented as contemporaneous engines on one tree? | **No.** Lead paragraph and both rows state builtin 659 is **pre-insertion** (before new R0.1 test files) and QuantLib 688 is **after R0.1.1–R0.1.6**. Rows stay separate. QuantLib check column includes `RISKFORGE_PRICING_ENGINE=quantlib` and `RISKFORGE_REQUIRE_QUANTLIB=1`. |
| 3 | Were ruff/mypy/lint/build/`risk_kernel_ok` added as recorded baseline, or claimed as a new run? | **Recorded baseline, not a new run.** Five rows added: ruff exit 0, mypy exit 0, frontend lint exit 0, frontend build OK, native `g++` → **risk_kernel_ok**. Notes column: “Recorded baseline.” Integers match `reviews/REMEDIATION_MILESTONE.md` R0 Baseline Verification. Report commands block still says not re-run. |
| 4 | Did they invent 801/820 as a pass count? | **No.** Absent from ROADMAP and FINDINGS. Report mentions 801/820 only to say a live pass count was not invented from collect-only. |
| 5 | Were historical 160 tables rewritten? Is R0 marked COMPLETE? | **No / no.** `### Baseline verification (2026-09-02)` still has **160 passed** / **10 passed**. Workstream 1 evidence line still has **160 passed**. Progress R0 row, Current gate paragraph, and the new subsection all stay **IN PROGRESS**. RF-020 stays **IN PROGRESS**. |

## Findings (items 1–3 only)

### 1. CLOSED — Important — copied Phase A no longer presented as today's suite

- Files: `ROADMAP.md` (Current gate subsection)
- What changed: heading is the recorded Phase A baseline and states **not re-run**. “Today's suite size” is deleted. 160 is left as dated R0.13.1 history. No 801/820 pass count.
- Why it is closed: the defect was currency framing, not the mere existence of a copy. Option (b) from the prior review / Orchestrator adjudication is what is on disk.

### 2. CLOSED — Important — 659 and 688 are no longer a same-tree engine pair

- Files: `ROADMAP.md` lines 13–18
- What changed: explicit not-contemporaneous note; per-row pre-insertion / post-R0.1 labels; QuantLib env vars restored on the check.
- Why it is closed: a reader of Current gate is told the 29-test gap is insertion timing, not builtin vs QuantLib on one tree.

### 3. CLOSED — Minor — omitted lint/build/kernel rows are now in the table as recorded baseline

- Files: `ROADMAP.md` Current gate table
- What changed: ruff, mypy, frontend lint, frontend build, `g++` → `risk_kernel_ok` appear with “Recorded baseline” notes. Native pytest **8 passed** remains a separate row.
- Why it is closed: the prior gap was an incomplete copy of the recorded matrix, presented without naming the missing checks. The missing checks are now named and are not restated as a new run.

## Residual: milestone still says “record current results, not stale counts”

`reviews/REMEDIATION_MILESTONE.md` § R0.13.4 is unchanged: “Record reproducible current commands and results, not stale counts.” This slice still points at the Phase A recording (pre-insertion 659 / post-R0.1 688 on an earlier tree), not a matrix run on current HEAD.

That is the honesty path (retitle, state not re-run, do not invent collect-only passes), not a live re-run. RF-020 Status now includes: “Current gate is the recorded Phase A baseline, not a live matrix.” Stale counts in older dated ROADMAP tables remain the stated residual.

This leftover does **not** block APPROVE. It is an accepted RF-020 residual, labeled in FINDINGS. Reopening Important #1 would require the table to claim currency again. It does not. A later live matrix on a stated SHA/tree would shrink RF-020 further; it is out of scope for this review-fix verdict.

## Finding counts (prior three items)

| Severity | Remaining VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 0 |

**Verdict: APPROVE**

---

## Orchestrator adjudication (2026-09-03)

Prior three items closed. Residual “no live matrix on HEAD” is accepted RF-020 leftover, labeled in FINDINGS. Do not mark RF-020 CLOSED.
