# R0.6.7 Full-revaluation acceptance benches (record, not SLA)

## Task
R0.6.7 — record FINDINGS RF-007 acceptance fields without inventing SLAs (N×S above 1×120, wall, peak RSS, scenarios/sec, builtin vs QuantLib, warm vs cold, identity checksums).

## Owner
QA & Quant Validation Engineer

## Status
**DONE_WITH_CONCERNS** — PR-safe **10 trades × 50 scenarios** is recorded on the same harness as R0.6.1. Pytest asserts finite numbers, not floors. Close-matrix: wall / scenarios/sec / warm vs cold / identity **MET** at 10×50; N×S, peak RSS, and builtin vs QuantLib **PARTIAL**. RF-007 stays **IN PROGRESS**. Do not close.

## Summary
Extended `benchmarks/run_full_reval_bench.py --json` so the payload still emits the R0.6.1 1×120 builtin identity (`impl`, `n_obs`, `checksum` `6602fa6906f2579f5c89af72a41ab274c07650234fff69387bc2202b5a40534f`, recorded `wall_ms`) and adds an `acceptance` object at **N=10 × S=50**:

| Field | Where | Close score |
|---|---|---|
| N×S | `acceptance.n_positions` / `n_obs` (10×50 > 1×120) plus 1×120 identity | **PARTIAL** (N=100/1k **UNMET** in default PR) |
| wall time | `wall_ms` plus `wall_ms_cold` / `wall_ms_warm` | **MET** at 10×50 (finite number, not a floor) |
| peak RSS | `peak_rss_kib` (KiB; macOS bytes→KiB, Linux KiB) | **PARTIAL** (field exists; process-lifetime `ru_maxrss`; QuantLib includes prior builtin) |
| scenarios/sec | `scenarios_per_sec` from cold `wall_ms` | **MET** at 10×50 (recorded only; no `throughput` key) |
| builtin vs QuantLib | `acceptance.builtin` and `acceptance.quantlib` at the same N×S | **PARTIAL** (same N×S skip-or-run; cash equity is `quantity * spot`, not reconstruction) |
| warm vs cold | two `wall_ms` on the same engine/fixture | **MET** at 10×50 (recorded, not SLA) |
| identity | 1×120 pin + 10×50 engine checksum `a28cf4ee6199bf40da3f2598f4241fc85fa4adc4f86bccbbd97e4938047d7537` | **MET** |

This capture (Darwin, builtin + QuantLib 1.43, not an SLA):

- identity 1×120 builtin: `wall_ms≈16.5`
- 10×50 builtin: `wall_ms_cold≈21.5`, `wall_ms_warm≈20.5`, `peak_rss_kib≈38968`, `scenarios_per_sec≈2326`
- 10×50 QuantLib: `wall_ms_cold≈35.7`, `wall_ms_warm≈31.3`, `peak_rss_kib≈64480` (cumulative `ru_maxrss` after identity + builtin, not QuantLib-only), `scenarios_per_sec≈1400`

`check_m6_sla.py` is not imported or invoked. Not added to `nightly.yml` or PR-FULL `needs:`.

## Files changed
- `benchmarks/run_full_reval_bench.py` — identity kept; acceptance matrix + CLI `--acceptance-n` / `--acceptance-s`
- `backend/tests/test_full_reval_bench.py` — payload + source not-SLA pins
- `benchmarks/README.md` — R0.6.7 pointer
- `reviews/FINDINGS.md` — RF-007 residual (still IN PROGRESS)
- `reviews/REMEDIATION_MILESTONE.md` — § R0.6.7; close gate still KEEP OPEN
- `reviews/r0.6.7-acceptance-benches-report.md`

Not edited: pricing engines, `var.py` / `historical.py` algorithms, C++ kernels, `.github/workflows/`.

## Public/interface changes
- None. Local bench script + pytest. No API or pricing contract change.
- Operator flags `--acceptance-n` / `--acceptance-s` (PR default 10×50). Checksum pin applies only at that default size.

## Numerical conventions
- Units: unit equity `quantity=10`, snapshot spot `100`; P&L = shocked PV − base PV ≡ `N * 10 * 100 * equity_return` (relative return).
- Sign convention: down-move negative P&L.
- Day count/calendar: n/a (cash equity).
- Tolerances/reference: allclose atol `1e-12` (same as R0.6.1 / nightly sample). R0.6.1 checksum unchanged. 10×50 engine digest `a28cf4ee6199bf40da3f2598f4241fc85fa4adc4f86bccbbd97e4938047d7537` (differs from the exact linspace-product digest, same class of residual as R0.6.1). Warm P&L must match cold at atol `1e-12`. `wall_ms` / RSS / scenarios/sec are informational only.

## Tests added/updated
- `test_full_reval_bench_emits_identity_json` — still pins 1×120 checksum; `wall_ms` finite; no `throughput`.
- `test_full_reval_bench_script_is_identity_not_sla` — no `check_m6_sla`; no `throughput > 0`; no wall/RSS/scenarios-sec comparison floors; both checksums in source.
- `test_full_reval_bench_acceptance_records_matrix_without_sla` — 10×50; recorded fields finite; QuantLib full fields when available, else `skip_reason` (builtin fields still required).
- `test_full_reval_bench_quantlib_same_nxs` — `import_quantlib()` hard-gate; same N×S and checksum as builtin.

## Commands executed
```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_full_reval_bench.py
```

## Results
- Focused bench tests: **4 passed** in 2.95s (RED: 3 failed on missing `acceptance` before the harness change).
- Host JSON capture: identity checksum `6602fa69…`; acceptance checksum `a28cf4ee…` on both engines; QuantLib `available=true`.
- Frontend: n/a
- QuantLib: exercised via `QuantLibPricingEngine` at 10×50 when installed; skip-or-run otherwise. Close score remains **PARTIAL** — cash equity is `quantity * spot`, not reconstruction.
- C++: unchanged.
- CI is green (all required checks): not started from this slice.

## Known limitations / risks
- N=100 / 1k and S=750 / 1k are **UNMET** in the PR default (brief: not 1k in PR). CLI can run them; checksum pin is skipped off the 10×50 fixture.
- Builtin vs QuantLib is **PARTIAL**: cash-equity QuantLib path is `quantity * spot` (same as builtin); this is adapter/session overhead, not N×S curve/instrument reconstruction.
- Peak RSS is **PARTIAL**: process-lifetime `ru_maxrss` after identity then builtin then QuantLib in one process. The QuantLib figure includes prior builtin; not a QuantLib-only peak. Units normalized to KiB.
- `scenarios_per_sec` uses cold wall time. Host-specific; not an SLA. Acceptance “cold” is not a process-cold start (runs after 1×120 identity).
- Option A intra-run scenario-block multiprocessing remains PARTIAL.
- RF-007 is **IN PROGRESS**, not CLOSED.

## Follow-up / next owner
- Owner: Lead Architect / QA Task 7 close re-gate.
- Requested action: keep RF-007 IN PROGRESS; do not add a nightly SLA job; do not treat recorded numbers as SLA-K1/K2. Optional: operator 100/1k capture outside PR.
- Blocking?: yes — RF-007 stays open until Task 7 re-gate.
