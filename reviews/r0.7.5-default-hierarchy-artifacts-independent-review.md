# R0.7.5 Default Hierarchy Artifacts — Independent Review

**Verdict: APPROVE** — Critical 0 · Important 0 · Minor 3

Slice: RF-008 leftover — default no-artifact `HierarchyEngine.build` /
`risk_at` must build trade-grain artifacts **once** and aggregate, not
full-reprice every hierarchy node.
Workspace: uncommitted working tree vs HEAD on `/Users/user/src/quantlineage`
(`r0-core-remediation` @ `e04c436828a07caa58a69affbac5510e0f1d6fd8`).
Implementer report: `reviews/r0.7.5-default-hierarchy-artifacts-report.md`
(claims, not proof).
Plan: `reviews/REMEDIATION_MILESTONE.md` § R0.7.5 (COMPLETE pending review).
Finding: `reviews/FINDINGS.md` RF-008 — Status **IN PROGRESS**. Must stay
open. Stress/limits remain omitted on the default producer path; R0.7.4
lazy drilldown is separate. Do **not** mark RF-008 CLOSED. Do **not**
mark R0.7 COMPLETE.

Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. The only write
is this file.

## Summary

When `artifacts` is omitted, `HierarchyEngine.build` / `risk_at` now call
`_trade_artifacts` once (`pricing.value` + shared
`historical_pnl_for_valuation` per position in the scoped portfolio) and
always pass that map into `_node` → `_node_from_artifacts`. Independent
probe on the two-trade same-desk book: **2 `value` calls**, **0
`value_portfolio`**, **0 `shocked_value`**, **0 `_metrics` / `_stress` /
`_limits`**, **7 nodes**. Prior R0.7.3 review recorded the same book on
the old omitted path as **164 / 38 / 60**.

Explicit `artifacts=` is unchanged: complete map required, key/`trade_id`
mismatch fail-closed, `ForbiddenPricing` succeeds, stress still aggregates
when present. Node VaR/ES remain VaR/ES of the **summed** historical
vector (controlled vectors: root `var_99=10.79` vs sum of trade VaRs
`13.67`). `PortfolioService.hierarchy` still produces artifacts itself and
passes them — `_trade_artifacts` is **not** invoked again; engine-default
and service paths agree on MV / VaR at 1e-12 for the probe book.

RF-008 stays **IN PROGRESS** (stress/limits empty; R0.7.4 leftover). Zero
VALID Important. Slice acceptance for “default build does not full-reprice
per node” holds.

## Checks performed

- Read the implementer report as claims only; read § R0.7.5, RF-008
  (acceptance: additives reconcile; node VaR from aggregated P&L vector;
  no independent full risk run per hierarchy node / trade),
  `HierarchyEngine.build` / `risk_at` / `_trade_artifacts` / `_node` /
  `_node_from_artifacts` / `_var_es_from_historical_pnl` /
  `_normalize_artifacts` / `_require_complete_artifacts`,
  `historical_pnl_for_valuation`, `PortfolioService.hierarchy`, and the
  updated hierarchy artifact / hierarchy tests.
- Diffed vs HEAD: `hierarchy.py`, `historical.py`, `trade_artifacts.py`
  (docstring), `portfolio_service.py`, `test_hierarchy.py`,
  `test_hierarchy_artifacts.py`, FINDINGS, milestone. Confirmed
  `var.py`, `test_var_es_golden.py`, `test_hierarchy_artifact_var.py`,
  `test_portfolio_hierarchy_artifacts.py` untouched vs HEAD.
- Confirmed RF-008 Status line remains **IN PROGRESS** (“Do not close
  until independent review APPROVE”). No `Status: CLOSED` on RF-008;
  R0.7 is not marked COMPLETE.
- Re-ran required suite (73 passed). Independent probes below.

## Attack checklist

| # | Attack | Result |
|---|---|---|
| 1 | `build(..., artifacts=None)` still independently reprices / full-risks every node | **No.** Probe: `value_calls == 2 == n_positions`, `n_nodes == 7`; `_metrics`/`_stress`/`_limits` == 0; `value_portfolio`/`shocked_value` == 0. Multi-desk: 3 positions → 3 `value` / 14 nodes. |
| 2 | `risk_at` without artifacts still full-reprices when one-pass artifacts suffice | **No.** Book ref: 2 `value` for 2-position book. Rates Desk subset: `value_calls == 1 == len(subset.positions)`. |
| 3 | Explicit `artifacts=` regresses (incomplete map, VaR of sum, ForbiddenPricing) | **No.** Complete map + `ForbiddenPricing` OK; incomplete → `missing trade ids: ['eq-b']`; key/`trade_id` mismatch → fail-closed; explicit stress still sums (`eq_crash == -15.0`). Artifact-var suite untouched. |
| 4 | Node VaR becomes sum of child VaRs | **No.** Controlled `(-1,-2,-3,-4)+(-10,0,0,0)` → root `var_99=10.79` vs sum of trade `var_99=13.67`. Live omitted path root matches `_var_es_from_historical_pnl(sum)` and `HistoricalRiskEngine.calculate` at 1e-12. |
| 5 | Call-count / ForbiddenPricing regressions are label-only | **No.** Asserts `value_calls == n_positions < n_nodes`, budgeted `_FirstPassThenForbidden`, and zero portfolio/shocked entry points. |
| 6 | RF-008 prematurely CLOSED in FINDINGS | **No.** Status **IN PROGRESS**; explicit “Do not close until independent review APPROVE.” |
| 7 | Numerical/invariant tests weakened without documented invariant | **No.** VaR/ES vs subset `calculate` remain abs 1e-9. Stress/limits expectations flipped to `[]` with documented convergence to the artifact producer path (same as `PortfolioService.hierarchy`). |
| 8 | `PortfolioService.hierarchy` double-builds artifacts or diverges from engine default | **No double-build.** Spy: `_trade_artifacts == 0`, `artifacts=` passed with len 2, `value_calls == 2`. Engine-default vs service MV/VaR equal at 1e-12 on the probe book. Residual producer duplication noted as Minor. |

## Findings

### Critical

None.

### Important

None.

### Minor

1. **Default / live path still omits stress P&L and limits (`[]`).**
   Intentional (matches `PortfolioService.hierarchy`; avoids per-trade
   stress reprice). RF-008 required direction still lists scenario P&L
   and limits against stable node IDs — keep finding **IN PROGRESS**.
   Not a slice blocker for the no-per-node-reprice leftover.

2. **Legacy `_node` full-reprice branch (`_metrics` / `_stress` /
   `_limits`) remains reachable if `artifacts is None`, but `build` /
   `risk_at` always auto-build a map first.** Dead for public entry
   points; leftover confusion / accidental internal use risk.

3. **Empty position `id`: engine default uses map key `""` with
   artifact `trade_id="__empty__"` (skips `_normalize_artifacts`
   key/`trade_id` equality); `PortfolioService.hierarchy` still raises
   `trade_id must be a non-empty string`.** Node-id collision fixtures
   stay green on the engine path; service path diverges on that edge.

## Required suite (re-run by reviewer)

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_hierarchy.py \
  tests/test_hierarchy_artifacts.py \
  tests/test_hierarchy_artifact_var.py \
  tests/test_hierarchy_node_ids.py \
  tests/test_portfolio_hierarchy_artifacts.py \
  tests/test_omit_market.py \
  tests/test_demo_snapshot_adapter.py
```

**Result: 73 passed** in 1.67s. No skips in this run.

## Independent probes (selected)

- Omitted `build`: 2 `value` / 0 portfolio / 0 shocked / 7 nodes;
  `_metrics`/`_stress`/`_limits` = 0.
- Controlled artifact VaR: root 10.79 ≠ sum(trade VaR) 13.67.
- Explicit incomplete / ForbiddenPricing / stress sum: fail-closed / OK /
  −15.0.
- `risk_at` Rates Desk: 1 `value` for 1-position subset.
- `PortfolioService.hierarchy`: 2 `value`, `_trade_artifacts` not called,
  numerical match to engine-default omitted path.

## RF-008 / R0.7 disposition

- **RF-008:** leave **IN PROGRESS** (stress empty, limits `[]`, R0.7.4
  lazy drilldown, dead legacy `_node` branch, FULL_REVALUATION /
  `factor_panel` still omit historical vectors → VaR 0 on those engines).
- **R0.7.5 slice:** APPROVE pending Lead adjudication of this review.
- Do **not** mark R0.7 COMPLETE solely on this APPROVE.

## Files scored

- `backend/app/risk/hierarchy.py`
- `backend/app/risk/historical.py` (`historical_pnl_for_valuation`)
- `backend/app/services/portfolio_service.py` (shared helper wiring)
- `backend/app/risk/trade_artifacts.py` (docstring only)
- `backend/tests/test_hierarchy.py`
- `backend/tests/test_hierarchy_artifacts.py`
- `reviews/FINDINGS.md` / `reviews/REMEDIATION_MILESTONE.md` (status notes)
- `reviews/r0.7.5-default-hierarchy-artifacts-report.md` (claims only)
