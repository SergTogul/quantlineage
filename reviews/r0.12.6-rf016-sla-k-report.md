# R0.12.6 RF-016 labeled-runner SLA-K1/K2

## Task
R0.12.6 labeled-runner SLA-K1/K2 honest residual (RF-016)

## Owner
QA & Quant Validation Engineer

## Status
**IN PROGRESS** (KEEP OPEN; do not CLOSE RF-016)

## Summary
Queried GitHub Actions self-hosted runners for `SergTogul/riskforge-mvp`: `GET /repos/.../actions/runners` returned **`total_count=0`**. Org-level runners are not applicable (user repo, 404). No labeled/self-hosted runner exists. Per the brief, this slice does **not** add a flake-prone `ubuntu-latest` SLA job and does **not** invent host floors. SLA-K1/K2 is scored **PARTIAL**: harness (`benchmarks/check_m6_sla.py`) + `docs/performance.md` + `benchmarks/RESULTS.md` exist; CI does not enforce host floors. Nightly already forbids `check_m6_sla.py` on `ubuntu-latest`. PR-FAST, QuantLib hard-gate, and `postgres-persistence-smoke` are unchanged. RF-016 stays **IN PROGRESS**.

## Runner evidence

```text
gh api repos/SergTogul/riskforge-mvp/actions/runners
→ {"runners":[],"total":0}

gh api orgs/SergTogul/actions/runners
→ HTTP 404 (not an organization)

Workflows: CI, Nightly only. All jobs runs-on: ubuntu-latest.
```

## Files changed
- `backend/tests/test_nightly_ci.py` — residual + ubuntu-latest + harness/docs pins (TDD: residual markers failed first)
- `backend/tests/test_pr_full_ci.py` — PR-FULL `needs:` must not include SLA / `check_m6_sla.py`
- `.github/workflows/nightly.yml` — header documents PARTIAL residual and runner count 0 (no new job)
- `docs/performance.md` — CI does not run `check_m6_sla.py` without a labeled runner
- `BUILD_NOTES.md` — Nightly residual note
- `reviews/FINDINGS.md` — RF-016 **IN PROGRESS**; SLA-K1/K2 **PARTIAL**; not CLOSED
- `reviews/REMEDIATION_MILESTONE.md` — R0.12.6
- `reviews/r0.12.6-rf016-sla-k-report.md`

## Public/interface changes
- None. No new workflow job. No pricing/risk/API change.

## Numerical conventions
- Units: n/a (CI contract). SLA-K1/K2 floors remain those already in `check_m6_sla.py` (serial ≥50× vs python; t4 ≥1.3× vs serial `cpp_ctypes` on `10k_x_1k`). Those floors are reference-host, not `ubuntu-latest` CI gates.
- Sign convention: n/a
- Day count/calendar if relevant: n/a
- Tolerances/reference: `benchmarks/RESULTS.md` laptop captures; not copied onto GitHub-hosted runners.

## Tests added/updated
- `test_ubuntu_latest_jobs_do_not_run_check_m6_sla` — every `runs-on: ubuntu-latest` job in `ci.yml` / `nightly.yml` must not invoke `check_m6_sla.py`
- `test_labeled_sla_job_is_absent_honest_residual` — no SLA job exists; nightly.yml documents PARTIAL + `actions/runners total_count=0`; workflow has no `continue-on-error`
- `test_sla_k_harness_exists_with_documented_floors` — checker encodes SLA-K1 50× / SLA-K2 1.3× / `10k_x_1k`
- `test_sla_k_docs_exist_and_do_not_claim_ubuntu_ci_floors` — performance.md + RESULTS.md exist; residual marker present
- `test_pr_full_needs_does_not_include_sla_k` — PR-FULL `needs:` has no SLA id; `ci.yml` does not name `check_m6_sla.py`
- `test_pr_full_ci.py` extra pin: no SLA job in `needs:`; no `check_m6_sla.py` in `ci.yml`

TDD: residual-marker tests failed first (2 failed, 28 passed). After docs/header, 30 passed.

## Commands executed
```bash
gh api repos/SergTogul/riskforge-mvp/actions/runners

cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_nightly_ci.py tests/test_pr_full_ci.py tests/test_pr_fast_ci.py
# RED: 2 failed, 28 passed (residual markers missing)

PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_nightly_ci.py tests/test_pr_full_ci.py tests/test_pr_fast_ci.py
# GREEN: 30 passed in 2.68s

.venv/bin/ruff check tests/test_nightly_ci.py tests/test_pr_full_ci.py
```

## Results
- Backend: **30 passed** in 2.68s. Ruff: all checks passed.
- Frontend: n/a
- QuantLib: not exercised. Hard-gate job and `RISKFORGE_REQUIRE_QUANTLIB` left intact.
- C++: n/a this slice (native nightly remains checksum/impl identity, not SLA-K1/K2)
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): **yes**
- Unexplained failures or skips: none
- CI is green (all required checks): not started (commit this slice; no push unless requested)

## MET vs PARTIAL vs UNMET (this gate)

| Cell | Score | Evidence |
|---|---|---|
| SLA-K1/K2 harness exists | **MET** | `benchmarks/check_m6_sla.py` floors 50× / 1.3× on `10k_x_1k` |
| Reference-host evidence | **MET** | `docs/performance.md`, `benchmarks/RESULTS.md` |
| Labeled-runner CI enforcement | **UNMET** | `actions/runners total_count=0`; no self-hosted job |
| Invented ubuntu-latest SLA job | **not done** (correct) | nightly/ci ubuntu jobs must not invoke the checker |
| Overall SLA-K1/K2 leftover | **PARTIAL** | harness + docs exist; CI does not enforce host floors |
| PR-FAST / QuantLib hard-gate / postgres-smoke | **unchanged** | contract tests still pass |

## Why IN PROGRESS (not CLOSE)
Required NIGHTLY direction still includes “benchmark/SLA on labeled runner.” That cell is **PARTIAL**, not MET. No labeled runner is registered; adding `check_m6_sla.py` on `ubuntu-latest` would be a fake floor. QA-024 demo-artifact range check is also still a leftover (not this slice). Would not defend CLOSE.

## Known limitations / risks
- SLA-K1/K2 is not a CI gate until a labeled/self-hosted runner exists and a non-flaky job is added.
- `workflow_dispatch` is a trigger, not a runner label.
- Demo-artifact range check (QA-024) remains open.
- GitHub Actions was not executed from this host.

## Follow-up / next owner
- Owner: Lead Architect / DevOps — register a labeled runner if SLA-K1/K2 should become a real nightly job; then pin it in `test_nightly_ci.py` (job exists, not in PR-FULL `needs:`, no `continue-on-error`).
- Requested action: KEEP OPEN; do not CLOSE RF-016; do not add Nightly to branch-protection required checks; do not add nightly jobs to PR-FULL `needs:`.
- Blocking?: no
