# R0.10.3 Backpressure Independent Review

Slice: leftover RF-015 / R0.10.3 — bind HEAVY risk so it cannot monopolize
ordinary HTTP request execution indefinitely. Do not treat the
implementer’s “work left the request thread” language as proof. Do not
assume HEAVY work is off the request thread.
Workspace: uncommitted working tree vs HEAD on `/Users/user/src/riskforge-mvp`
(`r0-core-remediation` @ `5e56557`). Review started at `3642397`; HEAD
advanced during the session via an unrelated in-tree commit
(`feat(r0.7): value each trade once before hierarchy aggregation`).
This review did not commit. The scored R0.10.3 hunks vs current HEAD
are unchanged.
Implementer report: `reviews/r0.10.3-backpressure-report.md` (claims,
not proof).
Plan: `reviews/REMEDIATION_MILESTONE.md` § R0.10.3.
Finding: `reviews/FINDINGS.md` RF-015 — Status **IN PROGRESS**. Other
classified HEAVY routes still inline is leftover. Do not close.

Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. The only write
is this file.

Other dirty-tree work (produce-artifacts, QL value-terms,
`portfolio_service.py` hierarchy artifacts) was not scored.

## Verdict: APPROVE

## Lead Architect adjudication (2026-09-04)

Accepted. Zero VALID Important. Minor deferred. RF-015 stays **IN PROGRESS** — other HEAVY routes still compute inline; Compose dashboard still POSTs `/risk/dashboard` and will 400 when the gate is on. This slice commits the refuse-gate only.

Do **not** close RF-015. Do **not** mark R0.10 COMPLETE.

## Checks performed

- Read the implementer report as claims only; read § R0.10.3 (exit:
  large risk cannot monopolize ordinary HTTP indefinitely), RF-015
  (acceptance: a dashboard load does not execute redundant full
  valuations of the same book and snapshot), and the hunt list below.
- Diffed vs HEAD. Scored production hunks:
  - `backend/app/api/backpressure.py` (new)
  - `backend/app/api/risk.py` (`reject_inline_heavy` on FULL_REVALUATION
    summary only)
  - `backend/app/api/dashboard.py` (`reject_inline_heavy` before
    `service.dashboard`)
  - `backend/tests/test_backpressure.py` (new)
  - `reviews/r0.10.3-backpressure-report.md`
- Confirmed `backend/app/api/execution_class.py` has **no** diff vs HEAD
  (OpenAPI bijection untouched).
- Confirmed `reviews/FINDINGS.md` and `reviews/REMEDIATION_MILESTONE.md`
  have **no** working-tree diff. RF-015 Status remains **IN PROGRESS**.
- Index empty. Branch still `r0-core-remediation`. This review did not
  commit.
- Read `PUBLIC_BAD_REQUEST_MESSAGE` (`Invalid request`) and
  `body_from_http_exception` / `_from_structured_detail`. The refuse
  path reuses that envelope; it does not invent a new client message.
- `external_worker_enabled()` reads `os.environ` per call (not cached
  at import). Monkeypatch / live env take effect on the next request.
- Independently probed HTTP with `TestClient(app)` (not the helper
  alone): unset flags, `RISKFORGE_EXTERNAL_WORKER=1`,
  `RISKFORGE_HEAVY_INLINE=0`, and both flags together. In-memory only;
  no production-file edits.
- Ran the required commands (rerun, not the implementer’s log):

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_backpressure.py \
  tests/test_endpoint_execution_class.py \
  tests/test_dashboard_batch.py
# 20 passed, 1 warning (pre-existing Starlette/httpx TestClient deprecation)
# 3.57s
```

I did not rerun the implementer’s ruff / mypy, the full backend suite,
or CI.

## Hunt results

| # | Hunt | Result |
|---|---|---|
| 1 | With `RISKFORGE_EXTERNAL_WORKER=1`, does `POST /risk/summary?methodology=FULL_REVALUATION` still return 200 with a computed VaR? | **No leftover compute.** Dual-mount `/api/v1` and legacy both **400**. Body `{code: bad_request, message: Invalid request, details: {use: /risk/runs, route: POST /risk/summary}}`. No `var_99`. Gate runs before `service.summary`. |
| 2 | Same for `POST /risk/dashboard`? | **No leftover compute.** `/api/v1/risk/dashboard` and `/risk/dashboard` both **400**, same envelope (`route: POST /risk/dashboard`). No `varReport`. Gate runs before `service.dashboard`. |
| 3 | LINEAR / DELTA_GAMMA still 200? | **Yes.** Under `RISKFORGE_EXTERNAL_WORKER=1`, `POST /api/v1/risk/summary?methodology=LINEAR` and `DELTA_GAMMA` are **200** with `var_99` and the requested `methodology`. |
| 4 | HTTP 400 message is `Invalid request` (not a new string)? | **Yes.** `refuse_inline_heavy` uses `PUBLIC_BAD_REQUEST_MESSAGE`. Live bodies are `message=Invalid request`, `code=bad_request`. Not “Request failed”, not a new copy string. |
| 5 | Default unset still allows inline (so existing dashboard tests pass)? | **Yes.** Unset flags: FULL_REVALUATION summary **200** with `var_99` and `methodology=FULL_REVALUATION`; dashboard **200** with the ten UI keys. `test_dashboard_batch.py` is HTTP, not the helper. Suite result: 20 passed. |
| 6 | Edit `execution_class.py` bijection / close RF-015 / enqueue a fake run? | **No on disk.** `execution_class.py` has no diff. RF-015 Status is still **IN PROGRESS**. Refuse is `HTTPException` 400; no `Location`, no run id, no `RiskRunWorker.submit`. `RISKFORGE_HEAVY_INLINE=1` does not force-allow when the external-worker flag is on. |

## Acceptance criteria

| # | Criterion | Result |
|---|---|---|
| 1 | Large risk cannot monopolize ordinary HTTP indefinitely (this slice) | **Pass for the two leftover sites.** FULL_REVALUATION summary and dashboard refuse request-thread compute when the gate is on. Other HEAVY routes still can; that leftover keeps RF-015 open. |
| 2 | INTERACTIVE LINEAR / Δ-Γ summary stays synchronous | **Pass.** 200 under `RISKFORGE_EXTERNAL_WORKER=1`. |
| 3 | Existing error envelope; message `Invalid request` | **Pass.** `code=bad_request`, `message=Invalid request`, `details.use=/risk/runs`. |
| 4 | Default unset still allows inline | **Pass.** Dashboard batch tests and independent unset probe are 200. |
| 5 | Tests hit live HTTP, not only `heavy_inline_allowed()` | **Pass.** Four refuse cases and LINEAR / Δ-Γ are `TestClient` POSTs against `app.main:app`. The helper test is extra, not the only check. |
| 6 | No OpenAPI bijection change; no fake enqueue | **Pass.** `execution_class.py` untouched. No 202 / Location / queued row from the refuse path. |
| 7 | RF-015 / R0.10 not marked COMPLETE | **Pass.** FINDINGS / milestone have no working-tree diff. Report Status **DONE** is the slice only and states RF-015 is not closed. This review did not edit FINDINGS / milestone. |

## Attack results

| # | Attack | Score |
|---|---|---|
| 1 | Gate on, FULL_REVALUATION summary still 200 with `var_99` | **No.** 400 on both mounts; no VaR fields. |
| 2 | Gate on, dashboard still 200 with `varReport` / hierarchy / contributors | **No.** 400 on both mounts; no batch payload. |
| 3 | LINEAR / Δ-Γ summary refused when the gate is on | **No.** Both still 200 with computed `var_99`. |
| 4 | New / wrong HTTP message (`Request failed`, “use the worker”, …) | **No.** Stable `Invalid request`. |
| 5 | Default unset refuses dashboard / FULL_REVAL (breaks `test_dashboard_batch.py`) | **No.** Unset 200; required suite 20 passed. |
| 6 | Tests only assert `heavy_inline_allowed()` while handlers still compute | **No.** HTTP tests exist; independent probe matches them. Helper-only test does not substitute for the refuse cases. |
| 7 | `execution_class.py` bijection edited, or RF-015 closed, or refuse enqueues a fake run | **No on disk.** Map unchanged. Finding still **IN PROGRESS**. 400 does not enqueue. |

## Leftover HEAVY still inline (not a fail)

These still return **200** with computed bodies when
`RISKFORGE_EXTERNAL_WORKER=1`. That is leftover, not a hole in the two
gated sites. It is why RF-015 must stay **IN PROGRESS**.

| Route | Gate on (`EXTERNAL_WORKER=1`) | Hole for this slice? |
|---|---|---|
| `POST /risk/summary?methodology=FULL_REVALUATION` | **400** | **No** |
| `POST /risk/dashboard` | **400** | **No** |
| `POST /risk/var?methodology=FULL_REVALUATION` | **200**, `methodology=FULL_REVALUATION` | Leftover (documented) |
| `POST /risk/es?methodology=FULL_REVALUATION` | **200**, `methodology=FULL_REVALUATION`, contribution keys present | Leftover |
| `POST /risk/var/compare` | **200**, body includes `FULL_REVALUATION` | Leftover |
| `POST /risk/hierarchy` | **200**, tree `name` present | Leftover |
| `POST /risk/contributors` | **200**, 9 rows | Leftover |
| Other HEAVY map routes (what-if, stress evaluate, reverse multi, …) | Not gated in `risk.py` / `dashboard.py` | Leftover |

### Residual (not a fail)

| Item | Why it is not a leftover hole for this slice |
|---|---|
| Compose `backend` sets `RISKFORGE_EXTERNAL_WORKER=1`; `loadDashboard()` still POSTs `/api/v1/risk/dashboard` | Option A refuse, no frontend change, no enqueue. Demo Compose dashboard will 400 until a later slice serves a RiskRun or the client calls `/risk/runs`. Documented. Do not close RF-015. |
| Plan bullets (outstanding jobs, worker concurrency, scenario count, portfolio size) unchanged | Workload caps stay in `app.api.workload` (R0.11.3). This slice is the request-thread refuse-gate. |
| `RISKFORGE_HEAVY_INLINE=1` does not override an external worker | Matches “unset → follow `RISKFORGE_EXTERNAL_WORKER`.” Truthy inline does not force request-thread FULL_REVAL / dashboard. |
| FINDINGS RF-015 text still says “sequential request-thread compute remains R0.10.3” | Implementer correctly did not edit FINDINGS. Lead Architect updates status after this slice. Do not close. |
| Report summary calls summary+dashboard “the two leftover request-thread sites” | Limitations section lists the other HEAVY routes. Wording is slightly tight; the leftover table above is the contract. |

## Findings

### Critical

None. VALID count: **0**.

### Important

None that block APPROVE. VALID count: **0**.

### Minor

1. **Report summary over-narrows “leftover sites”**
   - File: `reviews/r0.10.3-backpressure-report.md`
   - The summary names only FULL_REVALUATION summary and dashboard as
     leftover request-thread sites. Live probe: `/risk/var`, `/risk/es`,
     `/risk/var/compare`, hierarchy, and contributors still compute
     inline under the same flag. The report’s limitations section
     already says this. Do not read the summary as “HEAVY is off the
     thread.” VALID count: **1**.

## Recommendations

- Keep RF-015 **IN PROGRESS**. Next owner decides whether the remaining
  HEAVY sync routes must share this gate, or whether a real RiskRun
  enqueue replaces the 400. Do not mark R0.10 COMPLETE on this slice
  alone.
- Do not treat `heavy_inline_allowed()` as proof. Future slices must
  keep HTTP tests on the live routes.
- Compose demo dashboard will 400 while `loadDashboard()` still POSTs
  `/risk/dashboard`. That is leftover product wiring, not a reason to
  close the finding.

## Assessment

**Ready to merge: Yes** (R0.10.3 Option A refuse-gate slice only).

The two leftover request-thread sites refuse inline compute when the
gate is on. LINEAR / Δ-Γ stay 200. The 400 message is `Invalid request`.
Default unset still allows inline. Tests are live HTTP, not helper-only.
`execution_class.py` is untouched. RF-015 is not closed. No fake run is
enqueued. Other HEAVY routes still inline — leftover, not a fail.
