# R0.4.5 Scenario Once, Price Many Independent Review

Slice: R0.4.5 — Scenario once, price many (StressEngine / attribution; RF-006 / RF-004).
Workspace: `/Users/user/src/riskforge-mvp` branch `r0-core-remediation`
(HEAD `eac40f4`; scored work is uncommitted working-tree vs that commit).
Implementer report: `reviews/r0.4.5-scenario-once-report.md` (claims only).
Reviewer did not implement this work. Production files were not edited. Index,
HEAD, and branch were not mutated. The only write is this file. RF-006 and
RF-004 were not closed.

Scored only:

- `backend/app/risk/stress.py`
- `backend/app/risk/scenario_attribution.py`
- `backend/tests/test_stress.py` (once-per-scenario assert)
- `reviews/r0.4.5-scenario-once-report.md`
- `reviews/FINDINGS.md` / `REMEDIATION_MILESTONE.md` / `r0.4-architecture-brief.md`
  if touched — RF-006 and RF-004 must **not** be CLOSED

Unrelated dirty tree (frontend BlockHelp, native, README, etc.) ignored.

## Verdict: APPROVE

Critical 0 · Important 0 · Minor 2

`StressEngine.run` / `evaluate` and `_portfolio_pnl_by_position` each call
`apply_scenario` once per scenario (or isolated-factor sub-scenario), then
`PricingEngine.value(p, shocked)` per position. Production `shocked_value`
call sites in risk loops are gone; `PricingEngine.shocked_value` is unchanged.
Structural run test + independent probe show apply count = O(scenarios), not
O(scenarios×positions). Numerical parity vs per-position `shocked_value`
holds. RF-006 and RF-004 stay **IN PROGRESS**. Required suite: **55 passed**.

## Checks performed

- Read implementer report as claims only; diff scored production + tests +
  FINDINGS / milestone / brief vs `eac40f4`.
- Confirmed `run` / `evaluate` no longer call `shocked_value` in the position
  loop; both do `shocked = apply_scenario(...)` then `value(p, shocked)`.
- Confirmed attribution `_portfolio_pnl_by_position` switched the same way;
  `decompose` still skips full-scenario reapply when `by_trade_pnl` is passed
  (evaluate path); factor isolation still one apply per factor key via that
  helper (not N× trades).
- Leftover hunt: `shocked_value(` in `backend/app/` remains only on
  `PricingEngine` / QuantLib override (unchanged vs HEAD). No other production
  risk-loop callers. Out-of-scope stale comment in
  `portfolio_service.hierarchy` still mentions `shocked_value` (Minor #2).
- Confirmed FINDINGS RF-006 / RF-004 Status remain **IN PROGRESS** with
  “Do not close”; milestone / brief mark R0.4.5 COMPLETE (slice only); full
  RF-006 acceptance (bench × scenario count, contribution invariants) still
  called out as open.
- Independent apply-count / parity probe (below).
- Reran the required pytest command (including
  `tests/test_scenario_attribution.py`). Did not execute CI. Did not mutate
  index, HEAD, or branch.

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_stress.py \
  tests/test_scenario_engine.py \
  tests/test_hedge_comparison.py \
  tests/test_crisis_library.py \
  tests/test_scenario_attribution.py
# 55 passed in 2.37s; 55 collected; no skips/xfails
# 1 Starlette/httpx deprecation warning
```

Implementer “45 passed” (suite without `test_scenario_attribution.py`) is not
treated as proof; the fuller required suite was rerun here.

Independent probe:

| Check | Result |
|---|---|
| `run`: N=6 positions × 1 scenario → `stress.apply_scenario` calls | **1** |
| `run`: 3 scenarios × 6 positions → apply calls | **3** |
| `run` P&L vs per-position `shocked_value` | match (`1e-10`) |
| `evaluate`: full-scenario applies (`id == scenario.id`) | **1** |
| `evaluate`: + factor-isolation applies (attr binding) | 1 iso (`eq_down_10__UNIT`) |
| `decompose` (no `by_trade_pnl`): full + iso applies | **1 + 1** (≪ N) |
| FINDINGS RF-006 | **IN PROGRESS**; “Do not close” |
| FINDINGS RF-004 | **IN PROGRESS**; “Do not close” |
| `PricingEngine.shocked_value` / `interfaces/pricing.py` vs HEAD | **unchanged** (diff empty) |

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | Still `shocked_value` per position in `run` / `evaluate` | **Pass.** Shared snapshot + `value`; probe 1 apply for N=6. |
| 2 | Attribution still per-trade rebuild | **Pass.** Diff + probe: one apply per full/iso scenario. |
| 3 | Numerical stress drift vs goldens / shocked_value | **Pass.** Parity assert + required suite green. |
| 4 | Structural test vacuous / wrong binding | **Partial.** `run` assert is exact `len(calls)==1`. Evaluate patches only `stress_mod` (Minor #1). |
| 5 | `shocked_value` removed or semantics changed | **Pass.** Interface + QuantLib override untouched. |
| 6 | RF-006 / RF-004 CLOSED prematurely | **Pass.** Both **IN PROGRESS**; full acceptance still open. |
| 7 | Leftover O(N) apply in scored paths | **Pass.** No production `shocked_value(` in risk loops. |
| 8 | Evaluate re-applies full scenario via contributions | **Pass.** `by_trade_pnl` skips full reapply; probe 1 full. |

## Findings

### Critical (Must Fix)

None.

### Important (Must Fix before APPROVE)

None.

### Minor

#### 1. VALID — Minor — No structural once-per-scenario assert for attribution; evaluate patch is stress-module only

`test_stress_run_applies_scenario_once_not_per_position` firmly pins O(1) apply
for `StressEngine.run`. `test_stress_evaluate_…` monkeypatches
`stress_mod.apply_scenario` only; attribution’s own
`scenario_attribution.apply_scenario` binding is not instrumented, and there is
no dedicated structural test that
`_portfolio_pnl_by_position` / standalone `decompose` apply once for N>1.
Independent probe confirms the attribution fix; this is coverage depth, not a
behavioral miss.

- Suggested fix (later): monkeypatch `scenario_attribution.apply_scenario` (or
  count via a shared seam) for N positions × 1 scenario → exactly one full
  apply; optionally assert iso applies == factor-key count.

#### 2. VALID — Minor — Stale out-of-scope comment still claims `StressEngine.run` uses `shocked_value`

`PortfolioService.hierarchy` docstring still says `StressEngine.run` would
re-call `value` plus `shocked_value` per scenario. Not a scored-path production
bug; leftover documentation after R0.4.5.

- Suggested fix (later): update the comment to “apply once, then `value` per
  position.”

## REJECTED implementer claims

None material. Once-per-scenario apply in `run` / `evaluate` / attribution,
unchanged `shocked_value`, structural run assert, numerical parity, RF-006 /
RF-004 still IN PROGRESS, and deferred full RF-006 acceptance match the tree.
Suite count difference (45 vs 55) is explained by the extra attribution file in
the required command.

## Must-verify matrix

| # | Requirement | Result |
|---|---|---|
| 1 | `StressEngine.run` / `evaluate` apply once per scenario, then `value` per position | **Met** |
| 2 | Scenario attribution path fixed similarly | **Met** |
| 3 | Numerical stress parity preserved (goldens + shocked_value oracle) | **Met** |
| 4 | Structural test proves apply O(scenarios) not O(scenarios×positions) | **Met** (`run`); evaluate/attr coverage weaker (Minor #1) |
| 5 | `PricingEngine.shocked_value` left intact | **Met** |
| 6 | RF-006 / RF-004 not prematurely CLOSED | **Met** |
| 7 | Required suite passes | **Met** (55 passed) |

## Counts

| Severity | Count |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 2 |

REJECTED implementer claims: 0.

**Verdict: APPROVE**

Zero VALID Important. Do **not** close RF-006 or RF-004. Full RF-006
acceptance (bench × scenario count, contribution invariants) and remaining
RF-004 work (R0.4.2 converge, R0.4.1 typed markets) remain open.

## Lead Architect adjudication (2026-09-09)

Accepted. Zero VALID Important. Minors deferred. RF-006 / RF-004 stay **IN PROGRESS**. Next: R0.4.1 typed market structures.
