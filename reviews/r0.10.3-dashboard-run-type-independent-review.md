# R0.10.3 Dashboard RiskRun run_type Independent Review

Slice: leftover RF-015 / R0.10.3 — add `dashboard` as a supported
RiskRun `run_type` returning the coherent batch payload. Do not treat
the implementer’s report as proof.
Workspace: uncommitted working tree vs HEAD on `/Users/user/src/quantlineage`
(`r0-core-remediation` @ `893c937`).
Implementer report: `reviews/r0.10.3-dashboard-run-type-report.md`
(claims, not proof).
Finding: `reviews/FINDINGS.md` RF-015 — Status **IN PROGRESS**. Compose
`loadDashboard()` still POSTs `/risk/dashboard`. Do not close.

Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. The only write
is this file.

Other dirty-tree work (frontend BlockHelp, native tests, README,
docs/superpowers, etc.) was not scored.

## Verdict: APPROVE

## Checks performed

- Read the implementer report as claims only; read RF-015 acceptance
  (dashboard load must not execute redundant full valuations of the same
  book and snapshot) and the must-verify list below.
- Diffed vs HEAD. Scored paths only:
  - `backend/app/services/risk_run_worker.py` (`"dashboard"` in
    `SUPPORTED_RUN_TYPES`; `execute_run_type` branch;
    `_serialize_dashboard_result`; `_serialize_result` unchanged for
    other types)
  - `backend/tests/test_risk_run_api.py` (membership; live HTTP poll
    COMPLETED + key/shape asserts; dual mount)
  - `reviews/r0.10.3-dashboard-run-type-report.md`
- Confirmed `reviews/FINDINGS.md` and `reviews/REMEDIATION_MILESTONE.md`
  have **no** working-tree diff. RF-015 Status remains **IN PROGRESS**.
- Index empty for scored production paths (worker + tests modified
  unstaged; report untracked). Branch still `r0-core-remediation`. This
  review did not commit.
- Read `DashboardBatchResponse` in `backend/app/api/dashboard.py` and
  `PortfolioService.dashboard` return keys; compared to worker
  serialization and COMPLETED payloads.
- Independently probed (in-memory only; no production-file edits):
  - `"dashboard" ∈ SUPPORTED_RUN_TYPES`
  - `_serialize_result(list)` → `{"items": [...]}`; dedicated dashboard
    serializer keeps lists as arrays
  - `execute_run_type(..., "dashboard")` → ten batch keys; stress is a
    list; threats is a report object
  - `execute_run_type(..., "stress")` still `{"items": [...]}`
  - Live HTTP create → poll → COMPLETED on `/risk/runs` and
    `/api/v1/risk/runs` with the ten keys and list-shape asserts
- Ran the required commands (rerun, not the implementer’s log):

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_risk_run_api.py \
  tests/test_dashboard_batch.py \
  tests/test_backpressure.py
# 48 passed, 1 warning (pre-existing Starlette/httpx TestClient deprecation)
# 5.41s
```

I did not rerun the implementer’s full backend suite or CI.

## Hunt results

| # | Hunt | Result |
|---|---|---|
| 1 | Is `"dashboard"` missing from `SUPPORTED_RUN_TYPES` or not dispatched? | **No.** In frozenset. `execute_run_type` calls `_serialize_dashboard_result(portfolio_service.dashboard(portfolio))`. |
| 2 | Does dashboard COMPLETED payload wrap lists as `{"items": [...]}`? | **No.** Dedicated serializer; live HTTP: `stress` / `contributors` / `limits` / `factors` are lists. `_serialize_result` for other types unchanged (`stress` run_type still `{"items": ...}`). |
| 3 | Missing any of the 10 `DashboardBatchResponse` keys? | **No.** Exact set: portfolio, summary, stress, threats, contributors, limits, factors, varReport, hierarchy, attribution. Matches model fields and `PortfolioService.dashboard`. |
| 4 | Tests only unit-assert membership without live HTTP COMPLETED? | **No.** `test_poll_until_completed_with_dashboard_batch_payload` + `test_api_v1_dashboard_run_type` create → poll → COMPLETED. Independent dual-mount probe matches. |
| 5 | RF-015 closed in FINDINGS? | **No.** Status **IN PROGRESS**; no FINDINGS / milestone working-tree edit. |
| 6 | Worker invents / hard-codes risk numbers? | **No.** Dashboard branch only calls `PortfolioService.dashboard`; serializers dump models/lists. |

## Acceptance criteria

| # | Criterion | Result |
|---|---|---|
| 1 | `"dashboard"` in `SUPPORTED_RUN_TYPES`; dispatch to `PortfolioService.dashboard` | **Pass.** |
| 2 | Dashboard lists stay JSON arrays; other run types still use `_serialize_result` | **Pass.** Diff does not alter `_serialize_result`; probe confirms. |
| 3 | COMPLETED payload has the 10 `DashboardBatchResponse` keys | **Pass.** Live HTTP + unit probe. |
| 4 | Live HTTP create → poll → COMPLETED with those assertions | **Pass.** Required suite green; independent probe both mounts. |
| 5 | RF-015 not closed in FINDINGS | **Pass.** |
| 6 | No inventing of risk numbers — only PortfolioService call | **Pass.** |

## Attack results

| # | Attack | Score |
|---|---|---|
| 1 | `"dashboard"` accepted at API but rejected / no-op in worker | **No.** Membership + `execute_run_type` + COMPLETED `result_type=dashboard`. |
| 2 | Dashboard payload uses `{"items": [...]}` for stress (breaks App.jsx shape) | **No.** Lists remain arrays; threats is a dict with `evaluations`, not `items`. |
| 3 | Missing batch keys (e.g. no `varReport` / `attribution`) | **No.** Exact ten-key set. |
| 4 | Only frozenset membership test; no live COMPLETED path | **No.** Live poll tests on both mounts. |
| 5 | FINDINGS / milestone mark RF-015 CLOSED or R0.10 COMPLETE | **No on disk.** Still **IN PROGRESS**. |
| 6 | Quant math or invented numbers in worker for dashboard | **No.** Service call + dump only. |
| 7 | Non-dashboard run types broken by serializer change | **No.** `_serialize_result` body unchanged; stress still wraps `items`. |

## Residual (not a fail)

| Item | Why it is not a leftover hole for this slice |
|---|---|
| Compose `loadDashboard()` still POSTs `/risk/dashboard` | Explicit out of scope. Will 400 under the HEAVY gate until Frontend adopts `run_type: "dashboard"`. Do not close RF-015. |
| Async dashboard uses `PortfolioService.dashboard(portfolio)` defaults (not DI scenario lists from the sync route) | Documented in implementer report. Empty/default DI falls back to the same `DEFAULT_SCENARIOS` / `THREAT_SCENARIOS`. Scenario threading can land with Frontend request envelope if needed. |
| `test_api_v1_dashboard_run_type` asserts fewer nested shapes than the legacy-mount test | Same handler. Independent probe: both mounts COMPLETED with full key set and list stress. Test gap, not a production hole. |
| OpenAPI create examples still show `summary` / `var` | Docs polish; runtime accepts string `run_type`. |

## Findings

### Critical

None. VALID count: **0**.

### Important

None that block APPROVE. VALID count: **0**.

### Minor

1. **Thinner dual-mount assertions on `/api/v1`**
   - File: `backend/tests/test_risk_run_api.py`
   - Legacy mount asserts contributors/limits/factors lists and threats
     shape; `/api/v1` asserts keys + `stress` is a list only.
     Independent probe: both mounts OK. VALID count: **1**.

## Recommendations

- Keep RF-015 **IN PROGRESS**. Next owner: Frontend / Risk UX —
  switch `loadDashboard` to `POST /risk/runs` with `run_type:
  "dashboard"`, poll COMPLETED, consume `results[0].payload`. Do **not**
  close RF-015 until Compose works under the gate-on path.
- After this APPROVE, Lead Architect may note that the async dashboard
  artifact exists. Do **not** mark R0.10 / RF-015 COMPLETE on this slice
  alone.
- Optional: deepen `/api/v1` shape asserts to match the legacy-mount test.

## Assessment

**Ready to merge: Yes** (R0.10.3 leftover `dashboard` RiskRun run_type
slice only).

## Counts

| Severity | VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 1 |

**Verdict: APPROVE** (zero VALID Important).

## Lead Architect adjudication (2026-09-09)

Accepted. Zero VALID Important. Minor deferred. RF-015 stays **IN PROGRESS** — Frontend `loadDashboard()` still POSTs dashboard only. This slice commits the `dashboard` RiskRun run_type only.
