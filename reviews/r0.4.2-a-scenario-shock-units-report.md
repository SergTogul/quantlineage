# R0.4.2-A — Scenario expand uses `shock_units`; stress↔formal parity

## Task
R0.4.2-A — Engine-facing Scenario expand uses `shock_units`; stress↔formal
parity pinned (RF-004 / R0.4.2 first slice)

## Owner
Market Data & Curves Engineer (coordinating with scenario adapters in `app.risk`)

## Summary
Replaced inline bp↔decimal scales in scenario expand/collapse paths with
`bps_to_decimal_rate` / `decimal_rate_to_bps` from `app.risk.shock_units`:
`scenario_model._expand_stress_to_shocks` / `scenario_to_stress`, and the
parallel historical expand/collapse helpers in `scenarios.py`
(`expand_aggregate_change`, `panel_amount_to_bump`, `to_stress_scenario`).
Documented that engine-facing shocks are `FactorShock` in
`MarketSnapshot.bump` units and that StressScenario bp fields convert only
at the adapter boundary. Strengthened a parity golden: same book+base+shocks
via legacy `StressScenario` and formal `Scenario` (plus collapse round-trip)
produce identical marks and `content_hash()` under exact float equality.
StressScenario wire / adapters retained. RF-004 remains **IN PROGRESS**.

## Files changed
- `backend/app/risk/scenario_model.py` — expand/collapse via `shock_units`; docs
- `backend/app/risk/scenarios.py` — aggregate/panel expand + `to_stress_scenario`
- `backend/tests/test_scenario_engine.py` — R0.4.2-A parity golden + rate units
- `backend/tests/test_shock_units.py` — import / no-inline pins for scenario modules
- `reviews/FINDINGS.md` — RF-004 status notes R0.4.2-A (still IN PROGRESS)
- `reviews/REMEDIATION_MILESTONE.md` — R0.4.2-A COMPLETE note
- `reviews/r0.4-architecture-brief.md` — R0.4.2-A row
- `reviews/r0.4.2-a-scenario-shock-units-report.md` (this handoff)

## Public/interface changes
- None. Adapter signatures unchanged; numerical semantics of helper-equivalent
  scales unchanged. StressScenario HTTP wire retained.

## Numerical conventions
- Units:
  - Engine-facing `FactorShock` / `FactorChange`: `MarketSnapshot.bump` units
    (equity/FX relative; vol relative; rates absolute decimal).
  - StressScenario / historical observation bp fields → decimal only via
    `bps_to_decimal_rate` at expand; collapse uses `decimal_rate_to_bps`.
- Sign convention: unchanged.
- Day count/calendar if relevant: n/a
- Tolerances/reference: exact float equality on marks and `content_hash` for
  the parity golden; existing scenario suite tolerances unchanged.

## Tests added/updated
- `test_r042a_stress_formal_parity_exact_marks_and_rate_units` — legacy vs
  formal vs collapse: identical spots/vols/rates + `content_hash`; expanded
  RateZero amounts equal `bps_to_decimal_rate`.
- `test_shock_units.py` — scenario_model / scenarios import helpers; module
  source must not contain raw `/ 10000` or `* 10000`.

## Commands executed
```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_scenario_model.py \
  tests/test_scenario_engine.py \
  tests/test_scenario_wire_api.py \
  tests/test_shock_units.py \
  tests/test_stress.py
```

## Results
- Backend: `53 passed in 2.31s` (1 Starlette/httpx deprecation warning; no skips/xfails)
- Frontend: n/a
- QuantLib: n/a
- C++: n/a
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none
- CI is green (all required checks): not started (no commit/push per task)

## Known limitations / risks
- Full R0.4.2 Scenario converge (delete StressScenario wire / formal-only HTTP)
  is out of scope; adapters remain dual-path.
- Historical crisis library preset magnitudes unchanged.
- RF-004 / RF-006 not closed; R0.4.4 one-pass staging remains separate.

## Follow-up / next owner
- Owner: Market Data & Curves Engineer / Lead Architect
- Requested action: continue R0.4.2 (broader Scenario converge) and R0.4.4
  one-pass snapshot transform when scheduled
- Blocking?: no
