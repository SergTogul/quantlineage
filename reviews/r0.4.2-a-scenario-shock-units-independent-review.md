# R0.4.2-A Scenario Shock Units Independent Review

Slice: R0.4.2-A — Engine-facing Scenario expand uses `shock_units`;
stress↔formal parity pinned (RF-004).
Workspace: `/Users/user/src/riskforge-mvp` branch `r0-core-remediation`
(HEAD `2473d7b` = R0.4.3-B; scored work is uncommitted working-tree vs that
commit).
Implementer report: `reviews/r0.4.2-a-scenario-shock-units-report.md` (claims only).
Reviewer did not implement this work. Production files were not edited. Index,
HEAD, and branch were not mutated. The only write is this file. RF-004 was not
closed.

Scored only:

- `backend/app/risk/scenario_model.py`
- `backend/app/risk/scenarios.py`
- `backend/tests/test_scenario_engine.py` (new parity test)
- `backend/tests/test_shock_units.py` (wiring pins)
- `reviews/r0.4.2-a-scenario-shock-units-report.md`
- `reviews/FINDINGS.md` / `REMEDIATION_MILESTONE.md` /
  `r0.4-architecture-brief.md` if touched — RF-004 must **not** be CLOSED

Unrelated dirty tree (BlockHelp, native tests, README, etc.) ignored.

## Verdict: APPROVE

Critical 0 · Important 0 · Minor 1

Expand/collapse in `scenario_model` / `scenarios` routes bp↔decimal only through
`bps_to_decimal_rate` / `decimal_rate_to_bps` (no raw `/ 10000` or `* 10000` in
scored modules). Helper scales match the pre-helper formulas; legacy
`StressScenario` → formal `Scenario` → collapse share exact marks and
`content_hash`. StressScenario wire/adapters retained. RF-004 stays
**IN PROGRESS**. Required suite: **53 passed**.

## Checks performed

- Read implementer report as claims only; diff scored production + tests +
  FINDINGS / milestone / brief vs `2473d7b`.
- Confirmed `_expand_stress_to_shocks` uses `bps_to_decimal_rate(float(bps))`
  (was `float(bps) / 10000.0`).
- Confirmed `scenario_to_stress` / `to_stress_scenario` use
  `decimal_rate_to_bps(amount)` (was `amount * 10000.0`).
- Confirmed `expand_aggregate_change` / `panel_amount_to_bump` use
  `bps_to_decimal_rate` (was `/ 10000.0`).
- Regex leftover hunt on scored modules: no `/ 10000`, `/ 10_000`,
  `* 10000`, `* 10_000`, `/10000`, `*10000`, `1e4`, or executable `1e-4`
  conversion leftovers (doc examples of result amount `0.0001` only).
- Confirmed `StressScenario` class, `scenario_from_stress` /
  `scenario_to_stress` / `to_stress_scenario`, and
  `wire_to_stress` / `stress_to_wire` still present.
- Confirmed FINDINGS RF-004 Status is still **IN PROGRESS** with
  “Do not close”; milestone / brief mark **R0.4.2-A** COMPLETE (slice only);
  parent **R0.4.2** converge remains open.
- Independent parity probe (engine `apply_scenario` + `shock_snapshot`):
  helpers ≡ `/10000` / `*10000`; legacy / formal / collapse
  `content_hash` and rates identical; collapse restores bp dicts.
- Reran the required pytest command. Did not execute CI. Did not mutate
  index, HEAD, or branch.

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_scenario_model.py \
  tests/test_scenario_engine.py \
  tests/test_scenario_wire_api.py \
  tests/test_shock_units.py \
  tests/test_stress.py
# 53 passed in 2.50s; 53 collected; no skips/xfails
# 1 Starlette/httpx deprecation warning
```

Implementer “53 passed” is not treated as proof; the suite was rerun here.

Independent probe:

| Check | Result |
|---|---|
| Scored modules raw `/ 10000` / `* 10000` | absent |
| `_expand_stress_to_shocks` / aggregate / panel | `bps_to_decimal_rate` |
| `scenario_to_stress` / `to_stress_scenario` | `decimal_rate_to_bps` |
| Helper ≡ pre-helper | `bps/10000` and `rate*10000` |
| Legacy vs formal vs collapse marks / hash | exact equality |
| Collapse `rate_shocks_bps` | USD 50 / EUR 25 restored |
| `StressScenario` wire adapters | retained |
| FINDINGS RF-004 | **IN PROGRESS**; “Do not close” |

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | Inline bp↔decimal still in scored expand/collapse | **Pass.** All four call sites use helpers; source pins added. |
| 2 | Silent numerical retune of rate bumps | **Pass.** Helper-equivalent scales; existing suite + parity golden green. |
| 3 | Parity golden weak (hash-only / no rate unit pin) | **Pass.** Exact spots/vols/rates + `content_hash`; amounts == `bps_to_decimal_rate`. |
| 4 | StressScenario wire / adapters deleted early | **Pass.** Domain model + `scenario_wire` + expand/collapse adapters present. |
| 5 | RF-004 CLOSED in FINDINGS / milestone | **Pass.** Status **IN PROGRESS**; R0.4.2-A slice COMPLETE only. |
| 6 | Engine path bypasses helpers (stale second conversion) | **Pass.** `scenario_engine.expand_scenario(StressScenario)` → `scenario_from_stress` → `_expand_stress_to_shocks`. |
| 7 | Historical aggregate/panel expand left on `/ 10000` | **Pass.** Both wired; pins assert helpers in source. |
| 8 | Report claims false CLEAR of full R0.4.2 converge | **Pass.** Report / FINDINGS keep full converge + R0.4.4 open. |

## Findings

### Critical (Must Fix)

None.

### Important (Must Fix before APPROVE)

None.

### Minor

#### 1. VALID — Minor — `scenario_engine` units docstring still says “÷ 10000”

`backend/app/risk/scenario_engine.py` (not in this slice’s scored file list)
still documents rates as ``StressScenario`` bp fields ÷ 10000. Runtime expand
already goes through `scenario_from_stress` → `bps_to_decimal_rate`. Cosmetic
doc drift only; fix when touching the engine in later R0.4.2 converge, or in a
docs-only follow-up. Does not affect numerical behavior or this APPROVE.

## Must-verify matrix

| # | Requirement | Result |
|---|---|---|
| 1 | Expand/collapse uses helpers; no raw `/ 10000` / `* 10000` in scored modules | **Pass** |
| 2 | Numerical behavior unchanged | **Pass** |
| 3 | Parity golden: legacy vs formal identical marks and/or `content_hash` | **Pass** (both) |
| 4 | StressScenario wire/adapters not deleted | **Pass** |
| 5 | RF-004 still IN PROGRESS | **Pass** |
| 6 | Required suite passes | **Pass** (53) |

## Residual / next

- Keep RF-004 **IN PROGRESS**. Do not close from this slice.
- Full R0.4.2 Scenario converge (formal-only HTTP / delete dual wire) remains
  later, as documented.
- Optional: refresh `scenario_engine` units docstring to name `shock_units`
  (Minor #1).

## Counts

| Severity | VALID | INVALID |
|---|---|---|
| Critical | 0 | 0 |
| Important | 0 | 0 |
| Minor | 1 | 0 |

**Verdict: APPROVE** (zero VALID Important).

## Lead Architect adjudication (2026-09-09)

Accepted. Zero VALID Important. Minor deferred (scenario_engine docstring). RF-004 stays **IN PROGRESS** — next R0.4.4 one-pass snapshot.
