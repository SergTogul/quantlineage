# R0.4.1-A Typed Markets Independent Review

Slice: R0.4.1-A — Pin typed sub-market views; curve bp via `shock_units`
(RF-004 / RF-011).
Workspace: `/Users/user/src/riskforge-mvp` branch `r0-core-remediation` @
`4c99cb8a94c7f9464dc0a156c3fbfb4704578fd7` (HEAD; uncommitted slice on working
tree).
Implementer report: `reviews/r0.4.1-a-typed-markets-report.md` (claims only).
Reviewer did not implement this work. Production files were not edited. Index,
HEAD, and branch were not mutated. The only write is this file. RF-004 and
RF-011 were not closed.

Scored only:

- `backend/app/market/markets.py`
- `backend/app/domain/models.py` (sub-market view properties / docs)
- `backend/app/market/curves.py`
- `backend/tests/test_market_snapshot.py`
- `backend/tests/test_curves.py`
- `backend/tests/test_shock_units.py`
- `reviews/r0.4.1-a-typed-markets-report.md`
- `reviews/FINDINGS.md` / `reviews/REMEDIATION_MILESTONE.md` /
  `reviews/r0.4-architecture-brief.md` (RF-004 / RF-011 must not be CLOSED)

Unrelated dirty tree (BlockHelp frontend, other WIP) ignored.

## Verdict: APPROVE

Critical 0 · Important 0 · Minor 2

Typed views `equity` / `rates_market` / `vol` / `fx` are documented as
canonical in `markets.py` and property docstrings; tests pin concrete view
types, field identity vs flat maps, frozen dataclass, and MappingProxy
immutability. Flat `MarketSnapshot` dict fields are unchanged (no nested
Pydantic rewrite). `parallel_shift_bps` / `key_rate_shift_bps` call
`bps_to_decimal_rate`; `curves.py` has no raw `/ 10_000` for those shifts.
Numerical shift math equals the prior `bps / 10_000.0` scale. RF-004 and
RF-011 remain **IN PROGRESS**. Required suite: **54 passed**.

## Checks performed

- Read implementer report as claims only; diff vs `4c99cb8` for scored paths.
- Confirmed `markets.py` module docs name the four views as canonical over
  flat storage; `EquityMarket` / `RateMarket` / `VolMarket` / `FxMarket`
  remain frozen dataclasses wrapping `Mapping` fields.
- Confirmed `MarketSnapshot` still declares flat dict fields
  (`equity_spots`, `rates`, `curves`, …); properties only gained “Canonical
  typed …” docstrings (no storage rewrite).
- Confirmed `curves.py` imports `bps_to_decimal_rate` and uses it in both
  bp shift methods; ripgrep finds no `/ 10_000` / `/ 10000` in
  `backend/app/market/curves.py`.
- Confirmed `test_submarket_views_expose_grouped_marks` pins types, `is`
  identity to flat maps, and MappingProxy / frozen immutability.
- Confirmed curve tests assert expected zeros via `bps_to_decimal_rate(bps)`;
  tent / immutability behavior retained.
- Confirmed `test_shock_units.py` pins curves methods import the helper and
  module source has no raw bp÷scale literals.
- Confirmed FINDINGS RF-004 / RF-011 Status lines are **IN PROGRESS** with
  “Do not close”; milestone and brief mark R0.4.1-A COMPLETE only.
- Independent probe: `bps_to_decimal_rate(25)==0.0025`,
  `parallel_shift_bps(25)` → `0.04+0.0025`; flat `model_fields` still present.
- Leftover hunt: no raw bp scale left in `curves.py`; nested curve/surface
  dict payloads and broader RF-011 typing remain deferred (documented).
- Reran the required pytest command. Did not execute CI. Did not mutate
  index, HEAD, or branch.

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_market_snapshot.py \
  tests/test_shock_units.py \
  tests/test_curves.py \
  tests/test_vol_surfaces.py
# 54 passed in 1.12s
# No skips/xfails.
```

Implementer “54 passed” is not treated as proof; the suite was rerun here.

Independent probe:

| Check | Result |
|---|---|
| View types `EquityMarket` / `RateMarket` / `VolMarket` / `FxMarket` | isinstance pins + identity to flat maps |
| Flat storage fields retained | `model_fields` still include equity/fx/rates/curves/vol_surfaces maps |
| `curves.py` bp shifts | both use `bps_to_decimal_rate`; no `/ 10_000` in module |
| Scale parity | `bps_to_decimal_rate(25)==0.0025`; shift zeros match prior math |
| FINDINGS RF-004 | **IN PROGRESS**; “Do not close” |
| FINDINGS RF-011 | **IN PROGRESS** (Status line added this slice); “Do not close” |
| Nested curve/surface typing | still dict payloads (deferred; report accurate) |

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | Views not actually canonical / docs-only with weak tests | **Pass.** Module + property docs; types, identity, immutability pinned. |
| 2 | Flat storage rewritten away (compat break) | **Pass.** Diff is docs-only on models properties; fields unchanged. |
| 3 | Curve bp shifts still inline `/ 10_000` | **Pass.** Helper only; shock_units source pin + no literal in `curves.py`. |
| 4 | Silent numerical change to curve shifts | **Pass.** Same `bps/10_000` scale via helper; suite + probe. |
| 5 | RF-004 / RF-011 CLOSED prematurely | **Pass.** Both **IN PROGRESS**; slice COMPLETE language only. |
| 6 | Leftover raw bp scale in scored `curves.py` | **Pass.** None. |
| 7 | Process / doc hygiene on FINDINGS stamps | **Partial.** RF-004 status date backdated 09→08 (Minor #1). |
| 8 | Incomplete immutability pin for all four views | **Partial.** Vol/fx dataclass field reassignment not asserted (Minor #2). |

## Findings

### Critical (Must Fix)

None.

### Important (Must Fix before APPROVE)

None.

### Minor

#### 1. VALID — Minor — RF-004 FINDINGS status date backdated

RF-004 Status stamp changed from `(2026-09-09)` to `(2026-09-08)` while
appending the R0.4.1-A note. Status correctly remains **IN PROGRESS**, but the
date regression is unnecessary process noise relative to prior R0.4.5 work.

- Suggested fix: keep the latest calendar stamp (or use today) when appending
  slice notes; do not rewind earlier COMPLETE dates.

#### 2. VALID — Minor — Vol/fx frozen-field reassignment not pinned

`test_submarket_views_expose_grouped_marks` asserts dataclass field
reassignment failure for `equity` / `rates_market` and MappingProxy mutation
for all four views, but does not assert `vol.* = …` / `fx.* = …` frozen
setattr failures (symmetric coverage gap).

- Suggested fix: add the same `pytest.raises` frozen-field pins for `vol` and
  `fx` when convenient.

## REJECTED implementer claims

None material. Canonical-view docs/tests, flat-storage retention, curves via
`bps_to_decimal_rate`, numerical no-change, RF-004/RF-011 still IN PROGRESS,
and 54-pass suite claim match the tree.

## Must-verify matrix

| # | Requirement | Result |
|---|---|---|
| 1 | Typed views documented canonical; tests pin types + immutability | **Met** |
| 2 | Flat dict storage on MarketSnapshot preserved | **Met** |
| 3 | Curve bp shifts use `bps_to_decimal_rate`; no raw `/ 10_000` | **Met** |
| 4 | Numerical curve behavior unchanged | **Met** |
| 5 | RF-004 / RF-011 still IN PROGRESS | **Met** |
| 6 | Required suite passes | **Met** (54 passed) |

## Counts

| Severity | Count |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 2 |

REJECTED implementer claims: 0.

**Verdict: APPROVE**

## Lead Architect adjudication (2026-09-09)

Accepted. Zero VALID Important. Minors deferred. RF-004 / RF-011 stay **IN PROGRESS**. R0.4 unit/apply/price-many core slices are landed; remaining RF-004 is R0.4.2 formal Scenario HTTP converge (also RF-011). Next: RF-006 full-acceptance residual (factor×scenario structural/bench), then RF-005 panel default.

Zero VALID Important. Do **not** close RF-004 or RF-011. Remaining work:
further R0.4.1 typed curve/surface nesting if scheduled, and R0.4.2 Scenario
converge.
