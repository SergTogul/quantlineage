# Agent Handoff Template

## Task
R0.5.2 / RF-005 leftover — QuantLib production fail-closed on unknown instruments

## Owner
Quant Pricing Engineer

## Summary
`QuantLibPricingEngine.value` no longer delegates to `BuiltinPricingEngine` after
the ten-family `isinstance` chain. An unrecognized position type now raises
`TypeError` (`unsupported instrument for QuantLib production pricing: …`).
The Builtin engine is not imported or called from this path. Family adapters
and their numerical results are unchanged. Factory selection of Builtin via
`RISKFORGE_PRICING_ENGINE=builtin` was not edited.

TDD: new unknown-instrument tests failed first on the live Builtin fallback
(`AssertionError: QuantLibPricingEngine must not fall back to Builtin for
_UnknownPosition`). After the raise, both cases passed.

## Files changed
- `backend/app/pricing/quantlib.py`
- `backend/tests/test_quantlib_unknown_instrument.py` (new)
- `reviews/r0.5.2-ql-fail-closed-report.md` (new)

Not edited: `factory.py`, `builtin.py`, `models.py`, `cache.py`, hierarchy,
FINDINGS, milestone, frontend. Existing QuantLib goldens/process tests were
not changed (no golden weakening).

## Public/interface changes
- No DTO, API, or snapshot schema change.
- `QuantLibPricingEngine.value` fail-closed contract: unknown / unhandled
  position types raise `TypeError` instead of silently pricing through Builtin.
- Message includes `unsupported instrument`, `QuantLib`, and the concrete
  type name.
- Ten handled families unchanged: equity, equity future, European option,
  bond, swap, IR future, cap/floor, swaption, FX forward, FX option.

## Numerical conventions
- Units: unchanged. This slice is adapter dispatch only.
- Sign convention: unchanged.
- Day count/calendar if relevant: unchanged.
- Tolerances/reference: existing QuantLib goldens and without-fallback
  process tests were re-run as-is (rel/abs bands in
  `test_quantlib_golden.py` docstring). No bands were loosened.

## Tests added/updated
- `test_unknown_instrument_raises_without_builtin_fallback` — dummy type
  raises `TypeError`; `BuiltinPricingEngine.value` is monkeypatched to boom
  so a silent fallback cannot pass.
- `test_unknown_instrument_raises_when_market_is_supplied` — same fail-closed
  behavior when a `MarketSnapshot` is provided (overlay path does not create
  a Builtin hole).
- Existing goldens / `_forbid_builtin_fallback` family tests left in place.

## Commands executed
TDD red (new cases only, before production change):

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_quantlib_unknown_instrument.py
```

Required verification:

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_quantlib_unknown_instrument.py \
  tests/test_quantlib_golden.py \
  tests/test_quantlib_pricing.py \
  tests/test_quantlib_process_state.py \
  tests/test_quantlib_gate.py
```

## Results
- Backend: **106 passed** on the required suite (2.44s, then 1.53s replay).
  Breakdown: unknown 2, golden 54, pricing 38, process 10, gate 2. No skips.
- Frontend: not run (not in scope).
- QuantLib: **106 passed** (same suite; goldens not weakened).
- C++: not run (not in scope).
- Build: not run (not in scope).
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none
- CI is green (all required checks): not started (no commit or push)

TDD red result (before `quantlib.py` change): **2 failed** as expected.
`value()` called `BuiltinPricingEngine().value` for `_UnknownPosition`.

## Known limitations / risks
- This is QuantLib adapter fail-closed only. It does not close RF-005.
- Instrument capability registry (R0.5.1) is still a later slice.
- Per-factor historical panel (R0.5.3) is still a later slice.
- Builtin remains the explicit factory engine when
  `RISKFORGE_PRICING_ENGINE=builtin`. That path was not changed.
- `shocked_value` uses the default `PricingEngine` implementation and will
  fail closed via `value()` for an unknown type; it was not given a separate
  adapter.
- A future Position family added to `models.py` will raise here until a
  QuantLib adapter branch is added. That is the intended production behavior.

## Follow-up / next owner
- Owner: Lead Architect / Market Data for R0.5.1 registry and R0.5.3
  per-factor historical panel
- Requested action: do **not** close RF-005 on this slice; keep Builtin
  available only through the factory env switch
- Blocking?: no
