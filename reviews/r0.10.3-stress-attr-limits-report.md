# Agent Handoff Template

## Task
R0.10.3 / RF-015 leftover: gate remaining HEAVY stress / attribution / limits routes when the worker refuse-gate is on

## Owner
Backend / API Engineer

## Summary
Applied the existing `reject_inline_heavy` helper to HEAVY handlers in `stress.py`, `attribution.py`, and `limits.py`. When `QUANTLINEAGE_EXTERNAL_WORKER=1` or `QUANTLINEAGE_HEAVY_INLINE=0`, those POSTs refuse request-thread compute with HTTP **400** `{code: bad_request, message: Invalid request, details.use=/risk/runs}` — same contract as leftover `risk.py` HEAVY routes. INTERACTIVE `GET /stress/scenarios`, `GET /stress/scenarios/formal`, and `POST /limits/drilldown` stay synchronous. `backpressure.py` and `execution_class.py` were not edited. No RiskRun enqueue was invented. RF-015 was not closed in FINDINGS.

## Files changed
- `backend/app/api/stress.py` (`reject_inline_heavy` on all HEAVY POSTs; GETs untouched)
- `backend/app/api/attribution.py` (`/attribution`, `/attribution/demo`, `/change-attribution`)
- `backend/app/api/limits.py` (`/limits` only; `/limits/drilldown` untouched)
- `backend/tests/test_backpressure.py` (live HTTP refuse + INTERACTIVE stay-200 cases, dual mounts)
- `reviews/r0.10.3-stress-attr-limits-report.md` (this file)

Not edited (per task): `backpressure.py`, `execution_class.py`, `FINDINGS.md`, `REMEDIATION_MILESTONE.md`, frontend, native, README. No commit.

## Public/interface changes
- With the gate on, the HEAVY POSTs below return HTTP **400** with `details.use=/risk/runs` and `details.route` set to the logical route string (e.g. `POST /risk/stress`).
- INTERACTIVE GETs for stress scenarios and `POST /risk/limits/drilldown` unchanged (still 200 under the gate).
- Default unset flags: unchanged 200 inline behavior for local tests.
- No new routes, DTOs, env vars, or OpenAPI bijection changes.

## Routes gated (this slice)

| Route | Gate on |
|---|---|
| `POST /risk/stress` | **400** |
| `POST /risk/stress/custom` | **400** |
| `POST /risk/stress/formal/custom` | **400** |
| `POST /risk/stress/evaluate` | **400** |
| `POST /risk/stress/evaluate/custom` | **400** |
| `POST /risk/stress/formal/evaluate/custom` | **400** |
| `POST /risk/stress/reverse` | **400** |
| `POST /risk/stress/reverse/multi` | **400** |
| `POST /risk/stress/compare` | **400** |
| `POST /risk/attribution` | **400** |
| `POST /risk/attribution/demo` | **400** |
| `POST /risk/change-attribution` | **400** |
| `POST /risk/limits` | **400** |

## Routes intentionally not gated

| Route | Class |
|---|---|
| `GET /risk/stress/scenarios` | INTERACTIVE |
| `GET /risk/stress/scenarios/formal` | INTERACTIVE |
| `POST /risk/limits/drilldown` | INTERACTIVE |

Dual-mount `/api/v1` shares the same handlers.

## Numerical conventions
- Units: unchanged
- Sign convention: unchanged
- Day count/calendar if relevant: n/a
- Tolerances/reference: n/a (policy gate; no pricing/VaR numbers)

## Tests added/updated
- `test_stress_attr_limits_portfolio_routes_refused_when_external_worker` — portfolio-body stress / evaluate / attribution/demo / limits on `/api/v1` + legacy mounts
- `test_stress_custom_routes_refused_when_external_worker` — legacy + formal custom / evaluate-custom
- `test_stress_reverse_and_compare_refused_when_external_worker` — reverse, reverse/multi, compare
- `test_attribution_routes_refused_when_external_worker` — attribution + change-attribution
- `test_stress_attr_limits_refused_when_heavy_inline_disabled` — `QUANTLINEAGE_HEAVY_INLINE=0` alone
- `test_interactive_stress_scenarios_stay_sync_when_external_worker` — scenario GETs still 200
- `test_interactive_limits_drilldown_stays_sync_when_external_worker` — drilldown still 200 on both mounts

## Commands executed
```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_backpressure.py \
  tests/test_endpoint_execution_class.py \
  tests/test_dashboard_batch.py
```

## Results
- Backend: **44 passed**, 1 warning in 5.46s (pre-existing Starlette/httpx TestClient deprecation)
- Frontend: unchanged
- QuantLib: unchanged
- C++: unchanged
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none
- CI is green (all required checks): not started (no commit/push per task)

## Known limitations / risks
- RF-015 is **not** closed. Compose `loadDashboard()` still POSTs `/risk/dashboard` and will 400 when the gate is on (frontend leftover; separate slice).
- Refusal does **not** enqueue a RiskRun. The client must `POST /risk/runs` itself.
- `POST /risk/runs` remains the enqueue destination (not refused here; refusing it would be circular).
- Local / test default still allows inline compute (required by existing API tests).

## Follow-up / next owner
- Owner: Frontend / Risk UX Engineer (`loadDashboard` → `/risk/runs`); Lead Architect for RF-015 status
- Requested action: do **not** close RF-015 until dashboard Compose fan-out is fixed
- Blocking?: no
