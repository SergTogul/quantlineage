# R0.5.3 Default Panel Independent Review

Slice: R0.5.3 leftover / RF-005 — production default historical VaR →
`HistoricalFactorPanel`.
Workspace: `/Users/user/src/riskforge-mvp` branch `r0-core-remediation`.
Base (HEAD before slice): `1413272`. Working tree matches that SHA for
unrelated WIP; this slice is uncommitted on top of `1413272`.
Implementer report: `reviews/r0.5.3-default-panel-report.md` (claims only).
Finding: RF-005 — must stay **IN PROGRESS** / CLOSE-ready until Lead acts;
implementer must not CLOSE. Reviewer did not CLOSE RF-005.
Reviewer did not implement this work. Production files were not edited.
Index, HEAD, and branch were not mutated. The only write is this file.

Scored only:

- `backend/app/risk/factor_panel.py`
- `backend/app/services/risk_factories.py`
- `backend/app/risk/historical.py` / `historical_data.py` (docstrings)
- `backend/tests/test_risk_factories.py`
- `backend/tests/test_factor_panel.py` / `test_factor_panel_historical.py`
- `reviews/r0.5.3-default-panel-report.md`
- `reviews/FINDINGS.md` RF-005 status line
- `reviews/REMEDIATION_MILESTONE.md` § R0.5.3 / R0.5.4 notes

Ignored unrelated dirty frontend / native WIP (`conftest.py` native fixture,
`test_native_kernel.py`, `test_historical_scenario_kernel.py`, frontend BlockHelp).

## Verdict: APPROVE

Critical 0 · Important 0 · Minor 2

Production `build_historical_risk_engine()` wires a seeded
`create_synthetic_factor_panel` with `is_per_name_per_tenor_panel is True` and
`projection == "per_factor"`. Independence is real: NVDA ≠ SPY and USD 2Y ≠
USD 10Y on the same observation; LINEAR P&L matches per-name moves, not a
single equity series broadcast onto both names. This is **not** four-macro
broadcast dressed as a panel. Bare `HistoricalRiskEngine()` /
`factor_panel=None` and File/Synthetic datasets keep
`projection="four_macro_demo"`. VaR goldens (`test_var_es_golden.py`) and
`var.py` blobs match `1413272`. FINDINGS RF-005 is still **IN PROGRESS**
(CLOSE-ready pending review) — not CLOSED by the implementer.

**Lead CLOSE recommendation:** Lead **may CLOSE RF-005** after this slice for
the production historical VaR engine default, with an explicit residual that
`VaRAnalytics` / `ESContributionAnalytics` / hierarchy trade-P&L helpers still
consume `dataset.factor_observations()` (four-macro) until R0.5.5. If Lead
applies FINDINGS acceptance literally including “factor contributions trace
to actual factor histories,” keep RF-005 **IN PROGRESS** until that residual
lands — do not treat implementer CLOSE-ready as auto-close.

## Checks performed

- Read implementer report as claims only; read RF-005, § R0.5.3 / R0.5.4,
  factory, synthetic panel builder, `HistoricalRiskEngine.calculate` panel
  branch, `PortfolioService` hist_kwargs / `_historical_pnl_for_valuation`,
  factory tests, panel / wiring tests.
- Diff vs `1413272` on scored paths: `factor_panel.py` +86, `risk_factories.py`
  +22, `historical.py` docstring-only, `historical_data.py` docstring-only,
  test updates + new `test_risk_factories.py`, FINDINGS / milestone status
  notes. `test_var_es_golden.py` / `var.py` empty vs base (hashes match
  `1413272`: goldens `239d8c4d…`, `var.py` `cefc31e4…`).
- FINDINGS RF-005 status: **IN PROGRESS** — CLOSE-ready pending independent
  review. Do **not** CLOSE until orchestrator/independent review. Not CLOSED.
- Grep: factory default sets `factor_panel=`; calculate prefers panel over
  `dataset.factor_observations()`; `expand_aggregate_change` not on panel path.
- Independent runtime probes (no production edits): factory panel projection /
  flag; obs0 NVDA/SPY and 2Y/10Y differ; shocked spots and key-rate pillars
  differ; typed scenario carries distinct RateZero tenors; LINEAR pnl0 matches
  independent name moves (not broadcast of one series); factory VaR numbers
  differ from explicit `factor_panel=None` four-macro engine on the same book.
- Required suite re-run here (implementer “55” included optional
  `test_risk_run_factories`; required list is 50).

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_factor_panel.py \
  tests/test_factor_panel_historical.py \
  tests/test_historical_dataset_projection.py \
  tests/test_risk_factories.py \
  tests/test_var_es_golden.py \
  tests/test_risk.py
# 50 passed in 1.70s
# collect-only: 50. No skips.
```

## Must-verify results

| # | Check | Result |
|---|---|---|
| 1 | Factory default has panel with `is_per_name_per_tenor_panel` / not four_macro | **PASS** — `factor_panel is not None`, flag True, `projection == "per_factor"` |
| 2 | Independence real (two equities + two tenors) | **PASS** — streams via `SeedSequence.spawn`; runtime + factory tests |
| 3 | `factor_panel=None` / four-macro goldens; File/Synthetic labels | **PASS** — bare ctor / explicit None; datasets still `four_macro_demo` |
| 4 | VaR goldens not weakened | **PASS** — file hash == `1413272`; still `factor_panel=None` / array path |
| 5 | Production path not unlabeled four-macro broadcast | **PASS** for `HistoricalRiskEngine.calculate` / `summary` |
| 6 | Required suite | **PASS** — 50 passed |
| 7 | Lead may CLOSE RF-005? | **Yes, with residual** (see verdict) — reviewer did not CLOSE |

## Silent-broadcast hunt

Hunted specifically for “panel that still copies one macro series onto every
name/tenor.”

| Hypothesis | Evidence | Verdict |
|---|---|---|
| Synthetic panel = four-macro columns renamed | `spawn(n_factors)` + per-column `default_rng`; NVDA≠SPY, 2Y≠10Y | **Rejected** |
| `calculate` still reads `factor_observations()` when panel set | Panel branch first; four-macro only when `factor_panel is None` | **Rejected** |
| Full-reval collapses tenors via `to_stress_scenario` | Panel path uses typed `MarketScenario` → `apply_market_scenario`; key_rates 2Y/10Y differ independently | **Rejected** (prior Minor on `to_stress_scenario` remains off this path) |
| Factory VaR equals four-macro on same book | Panel vs bare engine VaR95 differ (≈3035 vs ≈4322 on probe book) | **Rejected** |
| LINEAR P&L = broadcast of one equity move | pnl0 matches Σ δᵢ·rᵢ, not 2·δ·r_shared | **Rejected** |

## Findings

### Important / Critical

None VALID.

### Minor 1 — Dual history identity on the production engine

`build_historical_risk_engine()` still attaches `create_historical_dataset()`
(`projection="four_macro_demo"`, `is_per_name_per_tenor_panel is False`) for
run-spec / dataset id, while VaR P&L comes from a separate synthetic panel.
Intentional for compat; not a broadcast. Consumers that inspect only
`engine.dataset` can misread the active shock source. Prefer documenting
panel identity in run-spec / methodology metadata in a follow-up (RF-009 /
docs), not blocking this slice.

### Minor 2 — Secondary PortfolioService surfaces still four-macro

`PortfolioService` hist_kwargs pass `seed` / `observations` / `dataset` into
`VaRAnalytics` and `ESContributionAnalytics` without `factor_panel`.
`_historical_pnl_for_valuation` correctly returns `None` when a panel is set
(omits rather than silently broadcasting into hierarchy artifacts). Component
VaR / ES contribution paths still use labeled four-macro `factor_observations()`
until R0.5.5. Matches implementer known limitations; residual for CLOSE
judgment (see Lead recommendation).

## Items checked and not findings

- `DEFAULT_PRODUCTION_PANEL_FACTORS` covers demo NVDA/SPY, USD 0Y/2Y/5Y/10Y,
  EURUSD — fail-closed outside that universe (`panel missing required
  factor(s)`). Documented synthetic stand-in, not a vendor feed.
- Cap/floor / swaption still `TypeError` on panel path — fail-closed, prior.
- Native 4-shock kernel unused on panel approximate path — expected.
- Production VaR **numbers change** vs prior four-macro factory default —
  intentional; goldens on explicit none/array path unchanged.
- Report left RF-005 **IN PROGRESS** / CLOSE-ready; did not self-CLOSE.
- `is_per_name_per_tenor_panel` on `HistoricalFactorPanel` is type-level True
  (content independence enforced by builder + tests, not the property).
- Unrelated native/conftest WIP ignored.

## Leftover four-macro / RF-005 close holes

On the **production factory `HistoricalRiskEngine.calculate` / `summary`
path** after this slice: **no** silent four-macro broadcast dressed as a
panel.

Residuals (do not reopen Important for this slice; inform Lead CLOSE):

- `VaRAnalytics` / ES contribution still four-macro via dataset
- Run-spec dataset id still describes the attached demo/synthetic fixture
- Bare ctor remains four-macro by design (labeled fixture path)
- R0.5.5 contribution semantics still open as its own milestone item

## Finding counts

| Severity | VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 2 |

**Verdict: APPROVE**

**CLOSE recommendation for Lead:** CLOSE RF-005 with residual note on
VaRAnalytics / ES / R0.5.5 — **or** keep IN PROGRESS if contribution-trace
acceptance is required before CLOSE. Reviewer did not edit FINDINGS status.

## Lead Architect adjudication (2026-09-09)

Accepted. Zero VALID Important. **RF-005 CLOSED** on this commit: production factory default is a real per-factor panel (independent NVDA/SPY and USD tenors). Residual → **R0.5.5**: `VaRAnalytics` / ES contribution paths still four-macro; factor-contribution trace to panel histories remains open there (not a reopen of the broadcast default).
