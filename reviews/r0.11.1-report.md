# R0.11.1 Safe local defaults

## Task
R0.11.1 Safe local defaults (RF-014 / SEC-004)

## Owner
DevOps / Platform Engineer

## Summary
Default Compose published ports now bind to loopback so `docker compose up` on a laptop does not accidentally expose unauthenticated Postgres, API, or frontend on a shared network. Demo credentials are unchanged. No authentication and no shared-deployment profile YAML were added. Docs now state that default Compose is loopback-bound and still not internet-ready.

## Files changed
- `docker-compose.yml`
- `docs/known_limitations.md`
- `README.md`
- `BUILD_NOTES.md`
- `backend/tests/test_compose_loopback.py`
- `reviews/r0.11.1-report.md`

## Public/interface changes
- None (Compose host bind only; service-to-service URLs and demo DSN unchanged).

## Numerical conventions
- Units: n/a
- Sign convention: n/a
- Day count/calendar if relevant: n/a
- Tolerances/reference: n/a

## Tests added/updated
- `tests/test_compose_loopback.py::test_compose_published_ports_bind_loopback` — parses `docker-compose.yml` (no Docker required) and asserts postgres `127.0.0.1:5432:5432`, backend `127.0.0.1:8000:8000`, frontend `127.0.0.1:5173:80`.

## Commands executed
```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_compose_loopback.py
```

## Results
- Backend: 1 passed in 1.31s (`test_compose_loopback.py`)
- Frontend: n/a
- QuantLib: n/a
- C++: n/a
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none
- CI is green (all required checks): not started (commit/push out of scope for this slice)

## Known limitations / risks
- Loopback bind prevents accidental LAN exposure of published ports; it is not authentication, TLS, or a production IAM boundary.
- Demo credentials remain the documented `riskforge`/`riskforge` pair.
- `docker compose` still does not publish a shared-deployment / non-loopback profile. Binding to `0.0.0.0` would require a local YAML edit.
- Intra-Compose traffic (backend/worker → `postgres:5432`) is unchanged and is not host-loopback-restricted.

## Follow-up / next owner
- Owner: Lead Architect (R0.11 remaining slices)
- Requested action: schedule R0.11.2+ (finite numbers, workload limits, error sanitization, shared-deployment auth gate, container hardening). Do not start those slices from this handoff.
- Blocking?: no

---

# R0.11.1 review fix (Findings 1–2)

## Task
Fix R0.11.1 independent-review Findings 1 (Important) and 2 (Minor) in the Compose loopback test.

## Owner
DevOps / Platform Engineer

## Summary
Rewrote `backend/tests/test_compose_loopback.py` so every `ports:` entry on every service must bind `127.0.0.1` (short-form or long-form `host_ip`). Unparsed entries fail closed. The `endswith` membership fallback is gone. PyYAML is not a backend dependency, so the helper scans every `ports:` value instead of calling `yaml.safe_load`. `docker-compose.yml` was not changed.

## Files changed
- `backend/tests/test_compose_loopback.py`
- `reviews/r0.11.1-report.md`

## Public/interface changes
- None

## Numerical conventions
- Units: n/a
- Sign convention: n/a
- Day count/calendar if relevant: n/a
- Tolerances/reference: n/a

## Tests added/updated
- `test_compose_published_ports_bind_loopback` — current `docker-compose.yml` still passes (postgres/backend/frontend loopback; worker unpublished).
- `test_second_unbound_postgres_mapping_fails` — `127.0.0.1:5432:5432` plus `15432:5432` fails.
- `test_publishing_worker_unbound_fails` — worker `8001:8001` fails.
- `test_extra_service_unbound_publish_fails` — extra service `5432:5433` fails.
- `test_unbound_short_form_without_host_ip_fails` / `test_all_interfaces_host_ip_fails` — Acc 4 `5432:5432` and `0.0.0.0:…` still fail.
- `test_wrong_host_port_does_not_satisfy_required_mapping` — `127.0.0.1:15432:5432` does not satisfy required `5432:5432`.
- `test_tcp_suffix_is_accepted_as_loopback` / `test_long_form_host_ip_is_accepted` — `/tcp` and long-form `host_ip` compare by fields.
- `test_unparsed_port_entry_fails_closed` — unparsed entries fail closed.

## Commands executed
```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_compose_loopback.py
```

## Results
- Backend: 10 passed in 0.80s (`test_compose_loopback.py`)
- Frontend: n/a
- QuantLib: n/a
- C++: n/a
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none
- CI is green (all required checks): not started (commit/push out of scope)

## Known limitations / risks
- Scanner is indent-aware Compose YAML (2-space services / 4-space keys), not a full YAML parser. Flow-style and block `ports:` plus long-form `host_ip` are covered; a later exotic reformat that the scanner cannot parse fails closed.
- Loopback bind is still not authentication.

## Follow-up / next owner
- Owner: Lead Architect (re-review R0.11.1)
- Requested action: re-run the independent review against the rewritten test.
- Blocking?: no
