# R0.5.5 Panel Contributions Independent Review

Slice: R0.5.5 — factor contributions against the panel (RF-005 residual).
Workspace: `/Users/user/src/riskforge-mvp` branch `r0-core-remediation`.
Implementer report: `reviews/r0.5.5-panel-contributions-report.md` (claims, not proof).
HEAD before slice: `ecee86b` (RF-005 **CLOSED**).
Reviewer did not implement this work. Production files were not edited. Index,
HEAD, and branch were not mutated. The only write is this file.

Scored only:

- `backend/app/risk/historical.py` (`approximate_position_pnls_from_panel`)
- `backend/app/risk/var.py`
- `backend/app/risk/es.py`
- `backend/app/services/portfolio_service.py` (panel sharing into var/es)
- `backend/tests/test_panel_contributions.py`
- `reviews/r0.5.5-panel-contributions-report.md`
- `reviews/FINDINGS.md` / `reviews/REMEDIATION_MILESTONE.md` (RF-005 must stay
  CLOSED; R0.5.5 status honest)

Unrelated dirty frontend / native WIP ignored.

## Verdict: APPROVE

Critical 0 · Important 0 · Minor 2

When `factor_panel` is set, VaR position contributions and ES factor
contributions use panel per-name / per-tenor histories. Independent probes
show `_factor_history` / `dataset.factor_observations()` / four-macro full-reval
helpers are **not** entered on those paths. Two equities with opposite panel
returns get different `component_es`; two USD rate tenors likewise. Missing
required factors raise `ValueError` (fail closed) before silent zeros or
interaction dump. `factor_panel=None` still broadcasts via
`dataset.factor_observations()`. `PortfolioService` shares `factor_panel` in
hist kwargs and the `es_contributions` rebuild. Required suite: **53 passed**.
RF-005 remains **CLOSED** (not reopened). R0.5.5 **COMPLETE** is honest.

## Checks performed

- Read implementer report as claims only; scored path diffs vs `ecee86b`;
  FINDINGS RF-005 status line; REMEDIATION § R0.5.5 COMPLETE note.
- Static branch order: `VaRAnalytics._position_pnls` panel-first;
  `ESContributionAnalytics.report` panel-first before
  `factor_observations()` / `_aggregate_factor_pnl_linear` /
  `_aggregate_factor_pnl_full_reval`.
- Runtime leftover hunt (no production edits): monkeypatch /
  helper-wrap probes for `_factor_history`, ES linear vs panel helpers, ES
  full-reval panel vs broadcast helpers, `iter_panel_shocked_snapshots` vs
  `iter_historical_shocked_snapshots`.
- Independence probes: AAA/BBB equity ES contributions; USD 2Y / 10Y bond
  ES contributions (rate probe not in shipped test file; see Minor 2).
- Fail-closed: thin panel missing BBB → `ValueError` on VaR + ES +
  FULL_REVAL panel path.
- `factor_panel=None` equal equity contributions under shared four-macro
  return; ES equity ES matches broadcast Greek sum.
- Service wiring: `svc.var_engine.factor_panel is panel` and
  `svc.es_engine.factor_panel is panel`; `var_report` /
  `es_contributions` keep unequal AAA/BBB traces.
- Hierarchy `_historical_pnl_for_valuation` still returns `None` when panel
  set (omit, not silent broadcast) — out of scope RF-008 / R0.8.4; Minor 1.
- Required pytest command re-run here. Collect-only: **53**. No skips.
- Did not execute GitHub Actions. Did not mutate index, HEAD, or branch.

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_var_methodology.py \
  tests/test_es_contributions.py \
  tests/test_factor_panel_historical.py \
  tests/test_risk_factories.py \
  tests/test_var_es_golden.py \
  tests/test_panel_contributions.py \
  tests/test_component_var.py
# 53 passed, 1 warning in 3.81s
# (Starlette/httpx TestClient deprecation in ES API test)
# collect-only: 53 tests collected
```

Implementer “53 passed” matches this re-run.

Independent leftover probe summary:

| Check | Result |
|---|---|
| Panel LINEAR position P&L AAA vs BBB | `100.0` vs `-200.0` (deltas × panel moves) |
| VaR report with panel: `_factor_history` calls | **0** |
| ES LINEAR with panel: helper used | `_aggregate_factor_pnl_from_panel` only |
| ES equity ES vs panel sum / vs broadcast | matches panel; ≠ broadcast |
| ES FULL_REVAL with panel: helper used | `_aggregate_factor_pnl_full_reval_from_panel` only |
| `factor_panel=None` ES helper | `_aggregate_factor_pnl_linear` only |
| FULL_REVAL snaps with / without panel | `iter_panel_*` / `iter_historical_*` |
| Missing BBB | `ValueError: panel missing required factor(s)` |
| USD 2Y vs 10Y `component_es` under panel | unequal (probe) |
| RF-005 status text | **CLOSED**; “Finding stays **CLOSED** (not reopened)” |

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | When panel set, contribution paths still use `dataset.factor_observations()` four-macro broadcast | **No.** Panel branch precedes broadcast in `var._position_pnls` and `es.report`. Probes: `_factor_history` unused; ES linear/full-reval broadcast helpers unused; full-reval snaps use `iter_panel_shocked_snapshots`. |
| 2 | Two equities with different panel histories still share contribution traces | **No.** Shipped test + probe: `component_es` differ; equal under explicit broadcast dataset. |
| 3 | Missing required factors silently zero into interaction / dump | **No.** `require_panel_covers_portfolio` raises before approximate / full-reval / factor panel paths. FULL_REVAL panel also pre-touches `observation.change` for required factors. |
| 4 | `factor_panel=None` regresses four-macro goldens / fixture behavior | **No.** None-path still uses `_factor_history` / `_aggregate_factor_pnl_linear`. Required goldens / methodology / ES / component suites pass (53). |
| 5 | `PortfolioService` does not share panel into var/es (R0.5.3 residual leftover) | **No.** `hist_kwargs["factor_panel"]` + `es_contributions` rebuild pass `self.risk.factor_panel`. Test + probe confirm identity and unequal contribs via service. |
| 6 | RF-005 reopened or R0.5.5 COMPLETE is dishonest | **No.** FINDINGS keeps **CLOSED** with residual-addressed wording. Milestone COMPLETE matches verified behavior. |
| 7 | Parametric `component_var` on n=1 hides broadcast | **Partial / known.** n=1 → std undefined → parametric components zero; tests correctly assert historical `component_es`. Documented in implementer known limitations. Not a silent four-macro leftover. |

## Findings

### 1. VALID — Minor — Hierarchy trade P&L still omits panel (documented)

- Files: `backend/app/services/portfolio_service.py` `_historical_pnl_for_valuation`
- What's wrong: When `risk.factor_panel is not None`, the helper returns
  `None` instead of panel-backed per-trade series. Hierarchy artifacts omit
  historical P&L rather than broadcasting.
- Why it matters: Out of R0.5.5 scope (RF-008 / R0.8.4 per report). Not a
  contribution-path broadcast leftover. Callers must not assume hierarchy
  trade P&L is panel-complete after this slice.
- Suggested fix: Later hierarchy/panel wire; do not block R0.5.5 COMPLETE.

### 2. VALID — Minor — Shipped contribution tests are equity-only

- Files: `backend/tests/test_panel_contributions.py`
- What's wrong: Independence / panel-vs-broadcast assertions cover two
  equities. Two rate tenors are not in this file (engine-level tenor tests
  remain in `test_factor_panel_historical.py`).
- Why it matters: Exit-criterion prose mentions equities and rate tenors.
  Mechanism is shared (`RateZero` columns in
  `approximate_position_pnls_from_panel`); independent probe shows unequal
  2Y/10Y `component_es`. Coverage gap only — not a live broadcast.
- Suggested fix: Optional add a 2Y/10Y contribution assert to
  `test_panel_contributions.py`. Not required to keep COMPLETE.

## REJECTED

1. **Rejected — four-macro leftover on VaR/ES contribution paths when panel set.**
   Panel-first branches + probes prove broadcast helpers unused.
2. **Rejected — silent interaction dump for missing panel factors.**
   Fail closed via `require_panel_covers_portfolio` / `ValueError`.
3. **Rejected — `factor_panel=None` broken or goldens regress.**
   None-path broadcast preserved; 53 required tests pass.
4. **Rejected — PortfolioService still drops panel (pre-R0.5.5 leftover).**
   Shared in hist kwargs and `es_contributions` rebuild.
5. **Rejected — reopen RF-005.** Status stays CLOSED; residual addressed under
   R0.5.5 only.
6. **Rejected — R0.5.5 COMPLETE is premature.** Contribution residual vs panel
   histories is closed for the scored paths; Minors are hierarchy omit and
   equity-only shipped coverage.
7. **Rejected — ES still rolling up to equity/vol/rate/fx family keys means
   four-macro broadcast.** Family keys are the ES dimension labels; P&L
   inside each family uses typed panel columns (probe: equity ES =
   `δ_AAA·0.1 + δ_BBB·(−0.2)`, not broadcast). FULL_REVAL panel isolates
   typed shocks with explicit `interaction` residual.

## Items checked and not findings

- `approximate_pnl_from_panel` delegates to
  `approximate_position_pnls_from_panel` (aggregate = sum of positions).
- ES imports private `_panel_linear_contribution` — style only; same units as
  panel approximate path.
- Dual dataset+panel identity on production factory (dataset still labeled
  `four_macro_demo`) remains intentional compat; contributions consume panel
  when set.
- Cap/floor / swaption still fail closed on panel factor mapping (prior).
- Unrelated frontend / native / conftest WIP ignored.

## Leftover four-macro / COMPLETE holes

On **VaR/ES contribution paths** after this slice: **no** silent four-macro
broadcast when `factor_panel` is present.

Honest residuals (Minor; do not reopen RF-005; do not block COMPLETE):

- Hierarchy trade P&L omits panel (Finding 1 / RF-008).
- Contribution test file equity-focused (Finding 2).

**R0.5.5 may stay COMPLETE.** RF-005 must stay **CLOSED**.

## Finding counts

| Severity | VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 2 |

**Verdict: APPROVE**

## Lead Architect adjudication (2026-09-09)

Accepted. Zero VALID Important. R0.5.5 COMPLETE stands; RF-005 stays **CLOSED**. Minors deferred. Next: R0.8.4 typed run-request schemas.

**COMPLETE recommendation:** Keep § R0.5.5 COMPLETE. Do not reopen RF-005.
