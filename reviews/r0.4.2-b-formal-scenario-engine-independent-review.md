# R0.4.2-B Formal Scenario Engine Independent Review

**APPROVE** — Critical 0 · Important 0 · Minor 3

Slice: R0.4.2-B — Engine-facing formal `Scenario` path for stress (RF-004 leftover).
Workspace: `/Users/user/src/quantlineage` branch `r0-core-remediation`
(HEAD `ee3a998`; scored work is uncommitted working-tree vs that commit).
Implementer report: `reviews/r0.4.2-b-formal-scenario-engine-report.md` (claims only).
Reviewer did not implement this work. Production files were not edited. Index,
HEAD, and branch were not mutated. The only write is this file. RF-004 was not
closed.

Scored only:

- `backend/app/api/scenario_wire.py`
- `backend/app/api/stress.py`
- `backend/app/risk/stress.py`
- `backend/app/risk/scenario_attribution.py`
- `backend/app/risk/scenario_model.py` (module docstring only)
- `backend/app/services/portfolio_service.py`
- `backend/tests/test_scenario_wire_api.py`
- `reviews/r0.4.2-b-formal-scenario-engine-report.md`
- `reviews/FINDINGS.md` / `REMEDIATION_MILESTONE.md` /
  `r0.4-architecture-brief.md` — RF-004 must **not** be CLOSED

## Verdict summary

`StressEngine.run` / `evaluate` / `ScenarioComparisonEngine.compare` and
PortfolioService stress entry points accept `ScenarioLike`
(`Scenario | StressScenario`). Formal HTTP
`POST /risk/stress/formal/custom` and `.../formal/evaluate/custom` lift wire →
domain `Scenario` via `wires_to_scenarios` and pass that through to the engine
(no `wires_to_stress` / `scenario_to_stress` on that path). Attribution applies
formal / isolated-factor `Scenario` objects directly through `apply_scenario`.
Legacy StressScenario wire endpoints retained. Formal↔legacy P&L matches at
engine and HTTP (custom stress automated; evaluate confirmed by independent
probe). RF-004 stays **IN PROGRESS**. Required suite: **104 passed**.

## Checks performed

- Read implementer report as claims only; diff scored production + tests +
  FINDINGS / milestone / brief vs `ee3a998`.
- Confirmed formal routes call `wires_to_scenarios`, not `wires_to_stress`.
- Confirmed `StressEngine` no longer type-narrows to `list[StressScenario]`;
  apply goes through `scenario_engine.apply_scenario`, which for formal
  `Scenario` calls `scenario_model.apply_scenario` (typed shocks) without
  collapse.
- Confirmed `_evaluation_fields` maps formal category → kind / threshold via
  `category_to_kind` / `threshold.max_loss_pct`.
- Confirmed attribution removed `_to_stress` / apply-path
  `scenario_to_stress`; factor isolation applies formal iso `Scenario`.
- Confirmed `wire_to_stress` / `wires_to_stress` retained as adapters.
- Confirmed FINDINGS RF-004 Status is still **IN PROGRESS** with
  “Do not close”; milestone marks R0.4.2-B COMPLETE pending review and names
  remaining wire converge; brief marks parent R0.4.2 COMPLETE pending review
  while noting dual endpoints remain.
- Independent HTTP P&L + collapse spy probe (below).
- Reran the required pytest command. Did not execute CI. Did not mutate
  index, HEAD, or branch.

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_scenario_wire_api.py \
  tests/test_scenario_model.py \
  tests/test_scenario_engine.py \
  tests/test_scenario_attribution.py \
  tests/test_stress_scenarios_di.py \
  tests/test_scenarios.py \
  tests/test_omit_market.py \
  tests/test_stress.py
# 104 passed in 2.90s; 104 collected; no skips/xfails
# 1 Starlette/httpx deprecation warning
```

Implementer “104 passed” is not treated as proof; the suite was rerun here.

Independent probe (TestClient):

| Check | Result |
|---|---|
| Formal vs legacy `POST .../stress/custom` P&L | exact match (`-91703.27321451333`) |
| Formal vs legacy `POST .../evaluate/custom` pnl/loss/threat/breached/kind | exact match |
| Formal-only `wires_to_stress` / `scenario_to_stress` call count | **0** / **0** |
| FINDINGS RF-004 | **IN PROGRESS**; “Do not close” |
| Legacy `/stress/evaluate/custom` still green in suite | yes |

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | Formal HTTP still collapses via `scenario_to_stress` before apply | **Pass.** Routes use `wires_to_scenarios`; independent spy on formal custom/evaluate: zero `wires_to_stress` / `scenario_to_stress`. |
| 2 | `StressEngine` still StressScenario-only; formal rejected/coerced | **Pass.** `Sequence[ScenarioLike]`; formal runs through `apply_scenario` typed path. |
| 3 | Formal↔legacy P&L parity broken or label-only tests | **Pass (function).** Engine run/evaluate parity + HTTP custom P&L golden; evaluate HTTP contract-only in suite but probe shows full field parity (Minor #1). |
| 4 | Attribution / PortfolioService regress or force collapse | **Pass.** Attribution apply-path collapse removed; PortfolioService takes `ScenarioLike`; formal decompose test still present. |
| 5 | Legacy StressScenario endpoints broken | **Pass.** Legacy custom evaluate regression + suite stress/DI/omit-market green. |
| 6 | RF-004 prematurely CLOSED | **Pass.** Status **IN PROGRESS**; remaining wire converge named. |
| 7 | `shock_units` / one-pass / scenario-once-price-many regress | **Pass.** Required suite includes `test_stress.py` / scenario model+engine (R0.4.2-A / R0.4.4 / R0.4.5 proofs); apply still scenario-once then `value` per position. |
| 8 | “No-collapse” test is fake | **Pass (engine).** Monkeypatch on `scenario_model.scenario_to_stress` during `StressEngine.run` + P&L parity is a real engine-path proof. Not an HTTP spy (Minor #2); independent probe closes the HTTP gap for this review. |

## Issues

### Critical

None.

### Important

None.

### Minor

1. **`test_formal_evaluate_custom_api_contract` is still label/status-only.**
   Engine evaluate parity + independent HTTP evaluate P&L probe are green, but
   the automated HTTP evaluate test does not assert pnl/loss/threat vs legacy
   `evaluate/custom`. Easy follow-up: mirror
   `test_formal_custom_stress_api_matches_legacy_pnl` for evaluate.

2. **Automated no-collapse spy is engine-only.**
   `test_stress_engine_runs_formal_scenario_without_scenario_to_stress` does not
   patch formal HTTP routes or assert `wires_to_stress` is unused. Vestigial
   `monkeypatch.setattr(..., "app.risk.scenario_attribution.scenario_to_stress",
   ..., raising=False)` is dead (import removed). Independent review spy
   confirmed formal HTTP collapse count is zero; optional hardening is an HTTP
   spy in the suite.

3. **Architecture brief parent R0.4.2 row says “COMPLETE pending review (B)”**
   while dual list endpoints / formal-default client path remain. Milestone is
   clearer (“remaining for full R0.4.2 converge”). Not a finding-status error;
   slightly overstates parent-row completeness.

## Residual / handoff

- RF-004 must stay **IN PROGRESS** until full R0.4.2 wire converge (formal
  default client path, retire dual list endpoints, any remaining StressScenario
  wire-only callers) or Lead Architect acceptance.
- Mark R0.4.2-B slice APPROVE in milestone / FINDINGS when convenient; do not
  close RF-004 on this review alone.
- Optional test hygiene: HTTP evaluate P&L parity + formal-route collapse spy
  (Minors #1–#2).

## Reviewer note

No production code was changed. No commit was created.
