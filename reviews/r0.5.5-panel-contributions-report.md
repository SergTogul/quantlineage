# Agent Handoff — R0.5.5 Panel Contributions

## Task
R0.5.5 — Factor contributions against the panel (not four-macro broadcast)

## Owner
Portfolio Risk Engineer

## Summary
When a `HistoricalFactorPanel` is present, VaR/ES contribution paths now attribute using per-name / per-tenor panel histories instead of `dataset.factor_observations()` four-macro broadcast. `PortfolioService` shares `factor_panel` into `VaRAnalytics` / `ESContributionAnalytics` (including the `es_contributions` rebuild path). Missing required factors fail closed via `require_panel_covers_portfolio`. With `factor_panel=None`, the labeled four-macro fixture path is unchanged. RF-005 remains **CLOSED**; residual contribution-trace note updated under R0.5.5 only.

## Files changed
- `backend/app/risk/historical.py` — `approximate_position_pnls_from_panel`; `approximate_pnl_from_panel` delegates to it
- `backend/app/risk/var.py` — optional `factor_panel`; panel position P&L / panel full-reval snaps
- `backend/app/risk/es.py` — optional `factor_panel`; panel linear + full-reval factor attribution with fail-closed coverage
- `backend/app/services/portfolio_service.py` — share `factor_panel` in hist kwargs + `es_contributions`
- `backend/tests/test_panel_contributions.py` — new focused tests
- `reviews/FINDINGS.md` — RF-005 residual note (finding stays CLOSED)
- `reviews/REMEDIATION_MILESTONE.md` — § R0.5.5 COMPLETE
- `reviews/r0.5.5-panel-contributions-report.md` — this handoff

## Public/interface changes
- `VaRAnalytics(..., factor_panel: HistoricalFactorPanel | None = None)`
- `ESContributionAnalytics(..., factor_panel: HistoricalFactorPanel | None = None)`
- `approximate_position_pnls_from_panel(...)` helper (portfolio risk)

No API DTO / endpoint schema changes.

## Numerical conventions
- Units: equity/FX relative returns; rates in basis points; vol relative (same as panel / `approximate_pnl_series`)
- Sign convention: loss = −P&L; VaR/ES non-negative; contributions are mean entity loss on portfolio-tail scenarios
- Tolerances/reference: abs 1e-9–1e-12 on contribution equality / reconciliation; panel vs broadcast inequality asserts on constructed fixtures

## Tests added/updated
- `test_var_position_contributions_differ_with_panel_histories` — two equities’ ES contributions differ under independent panel returns
- `test_es_factor_contributions_use_panel_not_broadcast` — equity factor ES traces to panel (not broadcast)
- `test_panel_missing_required_factor_fails_closed_for_contributions` — missing BBB fails closed
- `test_var_es_four_macro_path_unchanged_without_panel` — `factor_panel=None` still broadcasts
- `test_portfolio_service_shares_factor_panel_into_var_and_es` — service wiring + `es_contributions` forward panel

## Commands executed
```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_var_methodology.py \
  tests/test_es_contributions.py \
  tests/test_factor_panel_historical.py \
  tests/test_risk_factories.py \
  tests/test_var_es_golden.py \
  tests/test_panel_contributions.py \
  tests/test_component_var.py
```

## Results
- Backend: **53 passed**, 1 warning (Starlette/httpx TestClient deprecation in ES API test)
- Frontend: n/a
- QuantLib: n/a
- C++: n/a
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): **yes**
- Unexplained failures or skips: none
- CI is green (all required checks): not started (no commit/push per task)

## Known limitations / risks
- Hierarchy trade-P&L helpers that still omit panel (`_historical_pnl_for_valuation` returns `None` when panel set) remain out of scope (RF-008 / R0.8.4).
- Panel FULL_REVAL factor path isolates aggregate families (equity/vol/rate/fx) from typed panel shocks; interaction residual is explicit. Coverage fail-closed precedes isolation.
- Parametric component VaR on very short panels (n=1) is still zero (std undefined); historical ES contributions are the independence signal used in tests.

## Follow-up / next owner
- Owner: Lead Architect / independent reviewer (optional)
- Requested action: confirm R0.5.5 exit; no RF-005 reopen
- Blocking?: no
