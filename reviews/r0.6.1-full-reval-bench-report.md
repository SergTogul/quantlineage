# R0.6.1 Full-revaluation baseline benchmark (identity, not SLA)

## Task
R0.6.1 — record a baseline FULL_REVALUATION identity bench (checksum / impl / n_obs + recorded `wall_ms`)

## Owner
QA & Quant Validation Engineer

## Status
**DONE** — local script `benchmarks/run_full_reval_bench.py --json` reuses the nightly 120-obs unit-equity sample and emits `impl`, `n_obs`, and a SHA-256 checksum of the rounded engine P&L. `wall_ms` is recorded, not SLA-gated. No nightly job was added (`nightly.yml` remains the labeled-SLA leftover owner). `check_m6_sla.py` is not invoked. A positive throughput reading is not an SLA (`throughput` is omitted from the payload).

## Summary
Added a hierarchy-bench-style identity harness for current full-revaluation execution. Builtin pricing on one UNIT equity, 120 observations (`np.linspace(-0.05, 0.05, 120)`), P&L = shocked PV − base PV. Runtime also allclose-checks the hand identity `10 * 100 * r` at atol `1e-12`. Pricing engines, persistence, cache, QuantLib, `portfolio_service.py`, `nightly.yml`, `FINDINGS.md`, and `REMEDIATION_MILESTONE.md` were not edited. No commit.

This slice does **not** implement the milestone N×S matrix (`N = 10, 100, 1k` / `S = 50, 750, 1k`) or a host throughput floor. Those remain later R0.6 work after reuse/streaming changes.

## Files changed
- `benchmarks/run_full_reval_bench.py` — new identity bench (`impl` / `n_obs` / checksum / recorded `wall_ms`)
- `backend/tests/test_full_reval_bench.py` — pytest that `--json` emits `n_obs` / `checksum` / `impl`; source is identity-not-SLA
- `benchmarks/README.md` — pointer that this harness is identity, not `check_m6_sla.py`
- `reviews/r0.6.1-full-reval-bench-report.md`

Not edited: `reviews/FINDINGS.md`, `reviews/REMEDIATION_MILESTONE.md`, `backend/app/domain/models.py`, `backend/app/pricing/quantlib.py`, `backend/app/pricing/cache.py`, persistence, `backend/app/services/portfolio_service.py`, `.github/workflows/nightly.yml`.

## Public/interface changes
- None. Local bench script + pytest. No API or pricing changes.

## Numerical conventions
- Units: unit equity `quantity=10`, snapshot spot `100`; P&L = shocked PV − base PV ≡ `10 * 100 * equity_return` (relative return).
- Sign convention: down-move negative P&L (first obs −5% → −50; last obs +5% → +50).
- Day count/calendar if relevant: n/a
- Tolerances/reference: allclose atol `1e-12` vs `10 * 100 * linspace(-0.05, 0.05, 120)` (same as `test_nightly_full_reval_sample.py`). SHA-256 of rounded-to-12-decimal engine P&L lines = `6602fa6906f2579f5c89af72a41ab274c07650234fff69387bc2202b5a40534f`. Residual vs the exact linspace product is ~`2.6e-13` (within the nightly identity). `wall_ms` is informational only.

## Tests added/updated
- `test_full_reval_bench_emits_identity_json` — subprocess `--json`; asserts `impl=full_reval_builtin`, `n_obs=120`, pinned checksum; `wall_ms` present as a number; no `throughput` key.
- `test_full_reval_bench_script_is_identity_not_sla` — script calls `full_revaluation_pnl_series`; pins checksum; no `check_m6_sla` import/invoke; no `throughput > 0`.

## Commands executed
```bash
cd /Users/user/src/quantlineage
PYTHONPATH=backend backend/.venv/bin/python benchmarks/run_full_reval_bench.py --json

cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_full_reval_bench.py tests/test_full_reval_golden.py
QUANTLINEAGE_NIGHTLY=1 PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_nightly_full_reval_sample.py
.venv/bin/ruff check tests/test_full_reval_bench.py ../benchmarks/run_full_reval_bench.py
```

## Results
- Full-reval bench (local): `impl=full_reval_builtin`, `n_obs=120`, `n_positions=1`, `pnl_identity_ok=true`, checksum `6602fa6906f2579f5c89af72a41ab274c07650234fff69387bc2202b5a40534f`, `wall_ms≈13.5–19.5` (recorded, not gated).
- Identity pytest + R0.1.4 goldens: **6 passed** in 1.23s.
- Nightly 120-obs sample (`QUANTLINEAGE_NIGHTLY=1`): **1 passed** in 0.67s.
- Ruff: exit 0.
- Frontend: n/a
- QuantLib: not required. Hard-gate / `quantlib.py` left intact.
- C++: unchanged. `check_m6_sla.py` is not invoked.
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none
- CI is green (all required checks): not started (commit/push out of scope)

## Proof this is not a nightly SLA leftover
- `.github/workflows/nightly.yml` was not edited.
- Payload has no `throughput` field; tests forbid `throughput > 0` as a gate.
- Script docstring and pytest forbid `check_m6_sla.py` import/invoke.

## Known limitations / risks
- Workload is the existing nightly 120-obs / 1-trade builtin sample, not the R0.6.1 N×S matrix and not QuantLib vs builtin.
- RSS / cache hit-miss / scenarios-per-sec are not claimed as floors. `wall_ms` is host-specific (this capture: Darwin laptop, builtin engine).
- Full revaluation remains an `N × S` Python `PricingEngine` loop (RF-007). This records current identity; it does not change the execution model.
- RF-007 / RF-006 / RF-015 are **not** CLOSED.

## Follow-up / next owner
- Owner: Portfolio Risk / C++ Performance — R0.6.2 stream/chunk shocked scenarios; later reuse, anti-cache, process parallelism.
- Requested action: do not add a full-reval bench job to `nightly.yml` in this leftover slice; do not treat recorded `wall_ms` as SLA-K1/K2; do not close RF-007 from this baseline.
- Blocking?: no
