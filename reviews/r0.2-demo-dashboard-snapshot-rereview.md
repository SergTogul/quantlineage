# R0.2 Demo Dashboard Snapshot — Re-review (Important #1–2)

Slice: follow-up after dashboard snapshot APPROVE leftovers — thread one snapshot into hedge-compare, reverse-stress, reverse-multi, limit drilldown.
Workspace: uncommitted work vs HEAD. This reviewer did not implement the work.
Prior review: `reviews/r0.2-demo-dashboard-snapshot-independent-review.md` (Important #1–2 VALID). Orchestrator required those fixed before commit. Minors remain deferred.
Implementer report: `reviews/r0.2-demo-dashboard-snapshot-report.md` (context only). Production files were not modified by this review. `sample.py` aggregate seed (`0f91b6d`) not re-litigated.

## Verdict: APPROVE

Counts: **Critical 0 / Important 0 / Minor deferred (not reopened)**

Remaining Critical/Important on items 1–2: **none**.

Important #1 (compare / reverse still DemoAggregate; mixed markets in hedge-compare) and Important #2 (`limit_drilldown` omits `market=` → 400) are closed on the **service / HTTP** paths. Named Acc 5 mutations on those service methods re-break the new locks. Adapter conflict tests still raise. `loadDashboard` POSTs are 200; Minors were not reopened.

## Checks performed

- Diffed vs HEAD the nine scoped files. `demo_snapshot.py` has no diff.
- Read `PortfolioService.compare_scenarios` / `reverse_stress` / `reverse_stress_multi` / `limit_drilldown`, `ScenarioComparisonEngine.compare` / `_risk_summary`, `ReverseStressEngine.solve`, `MultiFactorReverseStressEngine.solve`, `LimitDrilldownEngine.report` / `contributors_for_metric`, `LimitEngine.evaluate` / `_stress_loss_abs`, API routers (`/stress/compare`, `/stress/reverse`, `/reverse/multi`, `/limits/drilldown`), `spyFlatHedgePortfolio` (same-id hedge).
- Probed (no production-file edits): SAMPLE HTTP drilldown root + desk; compare same-id UI hedge and re-id hedge; reverse / reverse-multi; swap PV + book MV explicit vs aggregate vs service; monkeypatch adapter on desk drilldown / compare; in-memory omit-`market=` on service wrappers.
- Ran:

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_demo_portfolios.py \
  tests/test_demo_snapshot_adapter.py \
  tests/test_limit_drilldown.py \
  tests/test_hedge_comparison.py \
  tests/test_reverse_stress.py \
  tests/test_reverse_stress_multi.py
# 63 passed, 1 pre-existing Starlette/httpx TestClient deprecation warning
```

I did not rerun Playwright.

## Attack results

### 1. SAMPLE `POST /api/v1/risk/limits/drilldown` still 400?

**No.** TestClient (builtin + `CachedPricingEngine` default wrap):

| Request | Status |
|---|---|
| `{portfolio: SAMPLE, breaches_only: false}` | **200** — `portfolio_id=global-macro`, `hierarchy_level=portfolio`, 8 items |
| same + desk `HierarchyRef` | **200** — `hierarchy_level=desk` |

No `CachedPricingEngine requires an explicit MarketSnapshot`. Service resolves `market_snapshot(root)` and forwards it into `report` → `calculate(sub)` / `evaluate` / `contributors_for_metric`.

### 2. Hedge-compare still mixed DemoAggregate vs explicit?

**No.** Service snapshots `base` once and passes that object into `compare(..., market=)`. Both `StressEngine.run` legs and both `_risk_summary` → `HistoricalRiskEngine.calculate` legs receive it. Factor exposures use the same snapshot.

| Source | Swap `swap-usd5y` PV | Book PV |
|---|---|---|
| Explicit `demo:global-macro` | 43,000 | 2,632,439.42 |
| Aggregate `demo-aggregate:global-macro` | 21,500 | 2,623,872.41 |
| HTTP/service compare `base_market_value` | — | **2,632,439.42** (explicit) |
| HTTP/service reverse / reverse-multi `base_market_value` | — | **2,632,439.42** |

UI hedge (`spyFlatHedgePortfolio`) keeps SAMPLE id. Re-id hedge (`hedged-global-macro`) also 200. Spy: all four run/calculate calls share one object, `id == demo:global-macro`.

### 3. Reverse / reverse-multi still `DemoAggregateMarketDataProvider`?

**Not on the service path.** `solve(..., market=)` uses `market if market is not None else self.market_data.snapshot`. Service always supplies `market_snapshot(root)`. Injected `service.market_data` is called with `global-macro`; engine `DemoAggregate` spy stays `[]`. Base MV matches explicit, not aggregate.

Engine default remains DemoAggregate when `market=` is omitted. That is the golden/compat path, not the dashboard service path.

### 4. Desk-subset drilldown / compare still invoke the adapter?

**No.** Monkeypatch `DemoSampleMarksSnapshotAdapter.snapshot`: desk-scoped `limit_drilldown`, same-id compare, and re-id compare all have `seen == []`. Adapter on a SAMPLE desk subset still raises `ConflictingSampleMarkError` `rates[USD]` `0.041` vs `0.04` (fail-closed, not weakened).

### 5. Adapter conflict tests still fail on unequal USD marks?

**Yes (they still raise).** `test_unequal_duplicate_usd_marks_raise_in_both_orders`, `test_mixed_usd_demo_uses_explicit_snapshot_successfully` adapter half, and `test_sample_desk_subset_still_raises_conflicting_usd_marks` passed. Zero-line diff on `demo_snapshot.py`.

### 6. Acc 5: omit `market=` on **service** methods (not raw engines)?

**Yes — the new tests would fail.** In-memory rebind of the four service wrappers to call engines without `market=`:

| Service method omitted `market=` | Result |
|---|---|
| `compare_scenarios` + re-id hedge | `ConflictingSampleMarkError`; adapter `seen == ['hedged-global-macro']` |
| `limit_drilldown` + desk ref | `ConflictingSampleMarkError`; adapter `seen == ['desk:Global Macro']` |
| `reverse_stress` | injected `service.market_data` not called; engine falls back to DemoAggregate — `test_reverse_stress_uses_root_market_snapshot_not_aggregate` fails (`calls == []`) |
| `reverse_stress_multi` | same lock as reverse |

`test_compare_stress_and_var_legs_use_same_base_snapshot` also fails if `market` args are `None` (`shared is not None`). HTTP drilldown 200 depends on service forwarding: `evaluate(..., market=None)` still hits `pricing.value(p, None)` → CachedPricingEngine 400.

### 7. Minors → 500 on `loadDashboard`?

**No.** All nine `loadDashboard` POSTs 200 (summary / stress / evaluate / contributors / limits / factors / var / hierarchy / demo-attribution). Minors #1–2 not reopened.

## Leftover bypasses

**None that re-open Important #1–2.**

Engine-level omitted-`market=` defaults are unchanged and intentional:

- `StressEngine.run` / reverse `solve` → `DemoAggregateMarketDataProvider`
- `HistoricalRiskEngine.calculate` / `VaRAnalytics.report` → `demo_market_snapshot(argument book)` (adapter on desk / re-id subsets)
- `LimitDrilldownEngine.report(market=None)` still prices the **subset** via those fallbacks

Dashboard routers go through `PortfolioService`, which now supplies the root snapshot. Acc 5 locks that. Direct engine goldens that omit `market=` keep the old default.

Deferred Minors (not scored): `factors()` still omits `market=` (works on exact SAMPLE id via `DemoPortfolioMarketDataProvider`); `HierarchyEngine._metrics` still drops `market` for a non-`HistoricalRiskEngine`.

## Findings

No new Critical or Important on items 1–2.

---

## Orchestrator adjudication (2026-09-03)

Important #1–2 closed on service/HTTP paths. Engine omitted-`market=` defaults stay for goldens. Minors remain deferred.
