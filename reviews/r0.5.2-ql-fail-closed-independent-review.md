# R0.5.2 QuantLib Fail-Closed Independent Review

Slice: R0.5.2 leftover / RF-005 — QuantLib production mode must not silently switch to Builtin for an unknown instrument.
Workspace: `/Users/user/src/quantlineage` working tree vs HEAD (`r0-core-remediation` @ `422f1cc478bd1bd039c67bbdc03c07624cbaddba`).
Implementer report: `reviews/r0.5.2-ql-fail-closed-report.md` (claims, not proof).
Plan: `reviews/REMEDIATION_MILESTONE.md` § R0.5.2.
Finding: RF-005 (must stay **open**; this slice is adapter dispatch only).
Reviewer did not implement this work. Production files were not edited. Index, HEAD, and branch were not mutated.

Scored only: `backend/app/pricing/quantlib.py`, `backend/tests/test_quantlib_unknown_instrument.py`, `reviews/r0.5.2-ql-fail-closed-report.md`. Other dirty-tree work (`cache.py` / `test_pricing_cache.py`, dashboard batch, frontend) is out of scope.

## Verdict: APPROVE

Critical 0 · Important 0 · Minor 1

`QuantLibPricingEngine.value` no longer imports or calls `BuiltinPricingEngine`. After the same ten-family `isinstance` chain that is on HEAD, it raises `TypeError` (`unsupported instrument for QuantLib production pricing: <TypeName>`). Independent probes: dummy and `SimpleNamespace` unknowns raise that message with **0** `Builtin.value` calls; all ten domain families still price; factory default is still QuantLib (cache-wrapped); `QUANTLINEAGE_PRICING_ENGINE=builtin` still returns `BuiltinPricingEngine`. `factory.py` and `builtin.py` have **no diff vs HEAD**. Goldens/pricing tests have **no diff vs HEAD** (blob hashes match). FINDINGS / milestone unedited — RF-005 is not CLOSED; R0.5 / R0.5.2 are not COMPLETE. Do not close RF-005.

## Checks performed

- `git diff HEAD --` `quantlib.py`, `factory.py`, `builtin.py`, `test_quantlib_unknown_instrument.py`, `test_quantlib_golden.py`, `test_quantlib_pricing.py`. Only `quantlib.py` is dirty (+4/−3). `factory.py` / `builtin.py` / both golden-pricing files: empty diff. Worktree blob hashes of `test_quantlib_golden.py` and `test_quantlib_pricing.py` match `HEAD:`.
- Empty diffs vs HEAD for `reviews/FINDINGS.md` and `reviews/REMEDIATION_MILESTONE.md`.
- Compared HEAD `value()` (`git show HEAD:backend/app/pricing/quantlib.py`) to the working tree. Overlay `elif` chain and session `if` chain are the same ten types. HEAD’s post-`with` Builtin import/call is gone. Working-tree raise sits inside `with self._session` after the last `isinstance`.
- Read on-disk `value()`, `_session` (try/finally, no swallow), `shocked_value` (`super()` only), `PricingEngine.shocked_value` / `value_portfolio`, `create_pricing_engine`, `Position` union (exactly those ten classes), RF-005 text, § R0.5.2, implementer report.
- Disassembled `value` and `shocked_value`: no `IMPORT_NAME` / `IMPORT_FROM`, no Builtin name. `inspect.getsource(QuantLibPricingEngine)` has no `BuiltinPricingEngine`.
- Independent runtime probe (spy on `BuiltinPricingEngine.value`; no production edits). Unknown dummy, unknown + `MarketSnapshot`, and equity-shaped `SimpleNamespace` all raise the QuantLib `TypeError` with **0** Builtin calls. `shocked_value(..., market=snapshot)` same. Ten families each returned a finite PV with **0** Builtin calls.
- Factory: unset `QUANTLINEAGE_PRICING_ENGINE` → `CachedPricingEngine` inner `QuantLibPricingEngine`. `QUANTLINEAGE_PRICING_ENGINE=builtin` + cache off → `BuiltinPricingEngine`. Source still `getenv(..., "quantlib")`.
- New test file: no `skip` / `xfail` / loosened `approx`. Collect-only counts: unknown 2, golden 54, pricing 38, process 10, gate 2 = **106**.
- Ran the requested commands. Did not execute GitHub Actions. Did not mutate index, HEAD, or branch.

```bash
cd /Users/user/src/quantlineage && /usr/bin/git diff HEAD -- \
  backend/app/pricing/quantlib.py \
  backend/app/pricing/factory.py \
  backend/app/pricing/builtin.py \
  backend/tests/test_quantlib_unknown_instrument.py \
  backend/tests/test_quantlib_golden.py \
  backend/tests/test_quantlib_pricing.py
# quantlib.py 1 file, +4 −3
# factory.py / builtin.py / test_quantlib_golden.py / test_quantlib_pricing.py: empty

cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_quantlib_unknown_instrument.py \
  tests/test_quantlib_golden.py \
  tests/test_quantlib_pricing.py \
  tests/test_quantlib_process_state.py \
  tests/test_quantlib_gate.py
# 106 passed in 2.07s
# collect-only: 2 + 54 + 38 + 10 + 2 = 106. No skips.
```

Implementer “106 passed” is not treated as proof; the suite was reran and collected independently. Count matches.

HEAD reconstruction (not a red rerun): HEAD `value()` ended with `from app.pricing.builtin import BuiltinPricingEngine` / `return BuiltinPricingEngine().value(...)`. The new tests monkeypatch `BuiltinPricingEngine.value` to boom and require `TypeError` matching `unsupported instrument`. That HEAD path would fail those tests. TDD-red narrative in the report was not re-executed.

Independent probe (evaluation_date `2026-09-01`; Builtin.value spied):

| Call | Result | Builtin.value calls |
|---|---|---|
| `value(Unknown())` | `TypeError` `unsupported instrument … Unknown` | **0** |
| `value(Unknown(), MarketSnapshot)` | same, type `Unknown` | **0** |
| `value(SimpleNamespace` equity-shaped`)` | same, type `FakePos` | **0** |
| `shocked_value(Unknown(), scenario, market)` | same, type `Unknown` | **0** |
| `shocked_value(Unknown(), scenario)` no market | `ValidationError` Portfolio discriminator `mystery` (demo adapter; never reaches Builtin) | **0** |
| Equity / future / option / bond / swap / IR future / cap / swaption / FX fwd / FX opt | finite PV each | **0** |

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | `BuiltinPricingEngine` is still imported/called from `QuantLibPricingEngine.value` (or `shocked_value` / `value_portfolio`) | **No on disk and not at runtime.** `value()` source and class source have no `BuiltinPricingEngine` / `app.pricing.builtin`. Bytecode of `value` and `shocked_value` has no import and no Builtin name. `shocked_value` is `return super().shocked_value(...)`. `value_portfolio` is not overridden; parent maps to `self.value`. Spy: 0 calls on unknown `value` / `shocked_value(market=...)`. |
| 2 | Unknown type is accepted or routed through a catch-all that prices | **No.** After the ten `if isinstance` arms, `raise TypeError`. Overlay path is `elif` with `updates = {}`; unknown leaves `updates` empty and still hits the raise. Dummy, dummy+market, and `FakePos` all raise; none return a `Valuation`. No `try/except` around the chain. `_session` is `try/finally` only. |
| 3 | A handled family was removed or now raises | **No.** HEAD vs working tree: same ten overlay types and same ten session types. Live: all ten domain classes priced without TypeError and without Builtin. Existing `_forbid_builtin_fallback` family tests in `test_quantlib_pricing.py` (unchanged file) passed on rerun. |
| 4 | Goldens/tolerances in `test_quantlib_golden.py` or `test_quantlib_pricing.py` were loosened | **No.** Both files empty vs HEAD; blob hashes identical to `HEAD:`. Module bands still `_OPT_REL = 2e-3`, `_CIP_REL = 1e-12`, `_BOND_CONTINUOUS_REL = 1e-10`. No skip/xfail added. New file has no `approx`. |
| 5 | RF-005 / R0.5 marked COMPLETE; factory default switched away from QuantLib | **No.** FINDINGS and REMEDIATION_MILESTONE empty vs HEAD. RF-005 has no `Status: CLOSED` (it still has no Status field). § R0.5 / R0.5.2 have no COMPLETE line. Report says do **not** close RF-005. `factory.py` empty vs HEAD; default env is still `"quantlib"`; live factory inner is `QuantLibPricingEngine`. Builtin still selected only via `QUANTLINEAGE_PRICING_ENGINE=builtin`. |

## Findings

### 1. VALID — Minor — New tests pin `value()` only; a later `shocked_value` override that called Builtin would stay green

- Files: `backend/tests/test_quantlib_unknown_instrument.py` L41–59
- What's wrong: Both new cases call `engine.value`. They do not call `shocked_value` or `value_portfolio`. On disk those methods have no Builtin path (`super()` / inherited map to `value()`). Independently, `shocked_value` with an explicit market raises the same `TypeError` with 0 Builtin calls. A future override of `shocked_value` that imported Builtin would not fail this file.
- Why it matters: The leftover under review is the `value()` fallback. That fallback is gone. This is a residual pin, not a live hole.
- Suggested fix: One extra case: `shocked_value(unknown, scenario, market)` with the same Builtin boom. Optional. Not required to keep this leftover.

## REJECTED

1. **Rejected — Builtin still imported or called from `value` / `shocked_value` / `value_portfolio`.** Source, bytecode, and spy are clean. `value_portfolio` is not defined on the QuantLib class.
2. **Rejected — unknown type accepted or catch-all prices.** Raise is after the ten `if`s. Dummy / dummy+market / `FakePos` all TypeError. Overlay does not populate `updates` for an unknown type.
3. **Rejected — a handled family was removed or now raises.** Same ten types as HEAD. All ten priced live.
4. **Rejected — goldens or tolerances loosened.** Empty diff; hashes match HEAD.
5. **Rejected — RF-005 or R0.5 marked COMPLETE; factory default switched.** Docs unedited. Factory default still QuantLib. Builtin only via env.
6. **Rejected — close RF-005.** Report leaves it open. This slice does not add a per-factor historical panel. Do not close RF-005.

## Items checked and not findings

- HEAD Builtin fallback sat **outside** `with self._session` (8-space). The new raise is **inside** the `with` (12-space), after the last `isinstance`. Unknown types now enter the session (eval-date set, restored in `finally`) then raise. That is fail-closed, not a fall-through. Implicit `None` return is not possible: every known arm returns; the miss raises.
- `shocked_value` with `market=None` and an unknown object never reaches `value()`. `PricingEngine.shocked_value` builds a one-trade `Portfolio` for `DemoSampleMarksSnapshotAdapter`; the `Position` discriminator rejects `type='mystery'` (`ValidationError`). Fail-closed, not Builtin. The report’s “will fail closed via `value()`” is exact only when `market` is supplied. Not a silent fallback.
- `Position` is an `Annotated` union of exactly the ten handled classes. There is no eleventh domain family that Builtin can price and QuantLib now refuses. This leftover is preventative for the next family plus fail-closed for non-union objects.
- Builtin’s own unknown path still raises `TypeError(f"Unsupported position: …")`. Without the message pin and the monkeypatch, a leftover Builtin call on `_UnknownPosition` would also raise `TypeError`. The new tests require `unsupported instrument` / `QuantLib` / `_UnknownPosition` and boom if Builtin.value is entered. That is the real discriminator.
- `_ir_future` remains an algebraic STIR mark “same as Builtin” inside the QL session. Comment-only; not an import or call. Pre-existing.
- Overlay `continuous_zero(..., fallback=p.*)` inside cap/swaption components is the R0.2 supplied-market leftover, not a Builtin engine switch. Not scored.
- `create_pricing_engine` still wraps QuantLib in `CachedPricingEngine` when the cache env is on (default). Cache calls `inner.value`; it does not import Builtin. Out-of-scope dirty `cache.py` was not scored.
- New tests use `import_quantlib()` like the other QL files. This run did not skip. Gate behavior is unchanged (`test_quantlib_gate.py` empty vs HEAD, 2 passed).
- Report TDD-red story is consistent with HEAD source but was not re-run here.
- Dirty cache / dashboard / frontend files are other slices. Not scored.
- `reviews/FINDINGS.md` and `reviews/REMEDIATION_MILESTONE.md` were not edited. Do not add COMPLETE/CLOSED from this review.

## Leftover silent-fallback or golden-loosen holes

On disk after this slice: **none** that keep a QuantLib → Builtin production fallback, accept an unknown type, drop a handled family, loosen goldens, close RF-005, or switch the factory default.

Contract leftover (Minor class; do not treat as proof the raise is fake):

- New pytest would stay green if Builtin were reintroduced only on `shocked_value` (Finding 1). Live `shocked_value` has no such path.

Plan leftovers (do **not** fail R0.5.2; do **not** close RF-005):

- R0.5.1 instrument capability registry
- R0.5.3 per-factor historical observations (the actual RF-005 hole)
- R0.5.4 demo four-column projection labeling
- R0.5.5 contribution semantics vs omitted factors

Do not close RF-005. Do not mark R0.5 COMPLETE.

## Finding counts

| Severity | VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 1 |

**Verdict: APPROVE**

## Lead Architect adjudication (2026-09-04)

Accepted. Zero VALID Important. Minor deferred. RF-005 stays **IN PROGRESS** (four-macro history is the remaining hole). This slice commits the QuantLib fail-closed dispatch only.
