# R0.7.6 Hierarchy Artifact Stress — Independent Review

**Verdict: APPROVE** — Critical 0 · Important 0 · Minor 3

Slice: RF-008 residual — default trade-artifact producer fills
`DEFAULT_SCENARIOS` stress (scenario-once / price-many); hierarchy nodes
aggregate stress; limits stay `[]`.
Workspace: uncommitted working tree vs HEAD on `/Users/user/src/quantlineage`
(`r0-core-remediation` @ `2b47403029a666354a5ec4213c88e2c66068bcf5`).
Implementer report: `reviews/r0.7.6-hierarchy-artifact-stress-report.md`
(claims, not proof).
Plan: `reviews/REMEDIATION_MILESTONE.md` § R0.7.6 (COMPLETE pending review).
Finding: `reviews/FINDINGS.md` RF-008 — Status **IN PROGRESS**. Must stay
open in this review. RF-004 / RF-009 stay **CLOSED** — not reopened.
Do **not** mark RF-008 CLOSED here. Do **not** mark R0.7 COMPLETE.

Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. The only write
is this file.

## Summary

Default `HierarchyEngine._trade_artifacts` now values each position once on
the root market, applies each `self.stress_scenarios` entry **once** via
`apply_scenario`, then prices every trade on the shocked snapshot and
stores `shocked_mv - base_mv` under scenario **id**. Aggregation already
sums `stress_pnl`; nodes expose non-empty stress with parent == sum(children).
`PortfolioService.hierarchy` delegates to `build` without a parallel
producer (one `_trade_artifacts` call). Limits remain `[]` (not faked).

Independent probe (2-trade / 7-node book, `S=5`): **12 `value` calls**
(`N×(1+S)`), **5 `apply_scenario`**, **0 `value_portfolio` / `shocked_value`**,
**0 `_metrics` / `_stress` / `_limits`**. Stress P&L matches
`StressEngine.run` after id→name mapping at abs 1e-9. VaR-of-sum invariant
unchanged (`var_99=10.79` vs sum of trade VaRs `13.67`). Explicit
`artifacts=` without `stress_pnl` still yields `stress == []`; explicit
maps with stress still aggregate.

RF-008 stays **IN PROGRESS** on disk. Acceptance-evidence bullets appear
**met** on the default path; required-direction leftovers (limits against
stable node IDs; R0.7.4 lazy drilldown) remain. Lead **may CLOSE** RF-008
only if those are explicitly dispositioned as P1 residuals — this review
does not flip FINDINGS.

## Checks performed

- Read implementer report as claims only; read § R0.7.6, RF-008
  (acceptance + required direction), `HierarchyEngine._trade_artifacts` /
  `_scenario_artifact_key` / `_stress_from_artifacts` / `build` / `risk_at` /
  `_node_from_artifacts`, `StressEngine.run`, `PortfolioService.hierarchy`,
  and updated hierarchy / portfolio hierarchy artifact tests.
- Diffed vs HEAD: `hierarchy.py`, `portfolio_service.py`,
  `test_hierarchy.py`, `test_hierarchy_artifacts.py`,
  `test_portfolio_hierarchy_artifacts.py`, FINDINGS (RF-008 status only),
  milestone § R0.7.6. Confirmed `test_hierarchy_artifact_var.py` /
  `test_hierarchy_node_ids.py` untouched vs HEAD. RF-004 / RF-009 Status
  lines remain **CLOSED** (untouched by this slice).
- Confirmed RF-008 Status remains **IN PROGRESS** (“Do not close until
  independent review APPROVE …”). No `Status: CLOSED` on RF-008.
- Re-ran required suite: **86 passed** in ~1.64s. Independent probes below.

## Attack checklist

| # | Attack | Result |
|---|---|---|
| 1 | Stress still empty on default `build` / `PortfolioService.hierarchy` | **No.** Probe: root stress ids == `{s.id for s in DEFAULT_SCENARIOS}`; non-zero P&L present; service path same; `limits == []`. |
| 2 | Per-node full stress reintroduced (`apply_scenario` ≫ S, or value ≈ N×nodes×S) | **No.** `apply_scenario == 5 == S`; `value_calls == 12 == N×(1+S) ≪ 7×6`; `_metrics`/`_stress`/`_limits` == 0; `shocked_value` == 0. |
| 3 | Parent stress ≠ sum of child trade stress P&Ls | **No.** Recursive additive reconcile on engine + service trees; explicit artifact stress still sums (`eq_crash == -15.0`). |
| 4 | Stress P&L disagrees with StressEngine beyond id vs name | **No.** Same `apply_scenario` + per-position `value` math; P&L equal at 1e-9 via id→name map. Label difference intentional / documented. |
| 5 | VaR/ES / one-pass value regressions (R0.7.5) | **No.** Controlled vectors: root `var_99` == VaR(sum) ≠ sum(trade VaR). Live path budget updated to `N×(1+S)` with forbid-after-budget; suite includes artifact-var / node-id / omit-market / stress. |
| 6 | Limits faked incorrectly, or RF-008 prematurely CLOSED | **No.** Artifact path `limits=[]`; RF-008 **IN PROGRESS**. RF-004 / RF-009 remain **CLOSED**. |
| 7 | Explicit `artifacts=` without stress regresses | **No.** Probe: complete map, no `stress_pnl` → `stress == []`, `ForbiddenPricing` OK, MV sums. With stress keys → aggregate unchanged. |
| 8 | Tests only assert non-empty labels, not additive / call counts | **No.** Tests assert additive stress reconcile, StressEngine P&L parity, `apply_scenario` count == S, value budget `N×(1+S)`, and forbid pricing after first pass. |

## Findings

### Critical

None.

### Important

None.

### Minor

1. **No dedicated regression test that explicit `artifacts=` without
   `stress_pnl` keeps `stress == []`.** Behavior holds under independent
   probe and is documented as caller-owned; a one-liner in
   `test_hierarchy_artifacts.py` would lock the residual. Non-blocking.

2. **R0.7.4 residual unchanged: every hierarchy stress row still carries
   nested `by_position`.** Observed on default-path root (`eq-a` / `eq-b`).
   Expected leftover; keep tracked under R0.7.4 / RF-008 residual
   disposition.

3. **FINDINGS RF-008 status text already asserts acceptance evidence “is
   met” before this review.** Still correctly leaves Status **IN PROGRESS**
   and gates close on independent APPROVE + residual disposition. Prefer
   past-tense “appears met pending review” wording next time; not a
   correctness fail.

## Required suite (re-run by reviewer)

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_hierarchy.py \
  tests/test_hierarchy_artifacts.py \
  tests/test_hierarchy_artifact_var.py \
  tests/test_hierarchy_node_ids.py \
  tests/test_portfolio_hierarchy_artifacts.py \
  tests/test_omit_market.py \
  tests/test_demo_snapshot_adapter.py \
  tests/test_stress.py
```

**Result:** 86 passed in 1.64s.

## Independent probes (not in suite)

Two-trade same-desk book, `DEFAULT_SCENARIOS` length 5:

| Check | Observed |
|---|---|
| Default `build` value / apply / nodes | 12 / 5 / 7; portfolio=0; shocked=0 |
| Legacy `_metrics` / `_stress` / `_limits` | 0 / 0 / 0 |
| StressEngine P&L parity (id→name) | abs 1e-9 |
| `PortfolioService.hierarchy` | 12 value, 5 apply, `_trade_artifacts` once; MV/VaR/stress == engine |
| Explicit artifacts, no stress | `stress == []`, MV 140 |
| Explicit artifacts with stress | `eq_crash == -15.0`; additive |
| VaR of sum vs sum of VaRs | 10.79 vs 13.67 |
| Empty book | `stress == []`, `limits == []` |
| Custom `stress_scenarios` (1) | value=4, apply=1, label=`custom_eq` |
| `risk_at` book ref | value=12, apply=5, limits=[] |

## RF-008 / RF-004 / RF-009 guidance

| Finding | On-disk status | Reviewer guidance |
|---|---|---|
| RF-008 | **IN PROGRESS** | Keep **IN PROGRESS** in this review. Acceptance evidence bullets (additive reconcile; VaR of aggregated historical vector; stable IDs; no independent full risk per trade) appear **satisfied** on the default path with filled stress. Required-direction leftovers: limits against stable node IDs; R0.7.4 lazy drilldown. **Lead may CLOSE** RF-008 only with explicit P1 disposition of those residuals; otherwise leave open. This review does **not** mark CLOSED. |
| RF-004 | **CLOSED** | Must stay CLOSED — untouched. |
| RF-009 | **CLOSED** | Must stay CLOSED — untouched. |

## Must-verify matrix (slice claims)

| Claim | Verdict |
|---|---|
| Default producer fills `DEFAULT_SCENARIOS` stress | **Pass** |
| Scenario-once / price-many (`apply` == S; value == N×(1+S)) | **Pass** |
| Parent stress == sum(children) | **Pass** |
| Stress P&L matches StressEngine (id vs name label only) | **Pass** |
| `PortfolioService.hierarchy` delegates to engine build | **Pass** |
| Limits stay `[]` (not faked) | **Pass** |
| Explicit no-stress artifacts stay empty stress | **Pass** (probe; no dedicated test) |
| VaR/ES R0.7.5 invariants | **Pass** |
| RF-008 not CLOSED; RF-004/009 not reopened | **Pass** |

## Recommendation

**APPROVE** R0.7.6. Zero Critical / Important. Ship the stress-fill slice;
leave RF-008 **IN PROGRESS** until Lead decides residual disposition for
limits + R0.7.4. Do not reopen RF-004 or RF-009.
