# R0.4.3-B Shock Units Independent Review

Slice: R0.4.3-B — Finish rate/vol conversion call sites; kill reverse-stress
magnitude heuristic (RF-004).
Workspace: `/Users/user/src/quantlineage` branch `r0-core-remediation`
(HEAD `8786bfd` = R0.4.3-A; scored work is uncommitted working-tree vs that
commit).
Implementer report: `reviews/r0.4.3-b-shock-units-report.md` (claims only).
Reviewer did not implement this work. Production files were not edited. Index,
HEAD, and branch were not mutated. The only write is this file. RF-004 was not
closed.

Scored only:

- `backend/app/risk/historical.py` (`_panel_linear_contribution`)
- `backend/app/risk/es.py`
- `backend/app/risk/attribution.py`
- `backend/app/risk/reverse_stress.py` (`from_wire_bound`, rates shock build)
- `backend/tests/test_shock_units.py`
- `backend/tests/test_reverse_stress.py`
- `backend/tests/test_approx_pnl_units_golden.py` (comments only if claimed)
- `reviews/r0.4.3-b-shock-units-report.md`
- `reviews/r0.4-architecture-brief.md` / `reviews/FINDINGS.md` if modified —
  RF-004 must **not** be CLOSED

Unrelated dirty tree (BlockHelp, native tests, README, etc.) ignored.

## Verdict: APPROVE

Critical 0 · Important 0 · Minor 2

Panel / ES / attribution vol→points and reverse-stress / attribution rate
bridges call `shock_units` helpers (helper-equivalent scales only; goldens
unmodified numerically). `from_wire_bound` has no `max_shock > 1` branch;
`500` stays search magnitude `500.0`, documented + pinned. ES
`contribution_pct` remains percent-of-total. RF-004 stays **IN PROGRESS**.
Required suite: **72 passed**.

## Checks performed

- Read implementer report as claims only; diff scored production + tests +
  FINDINGS/brief vs `8786bfd`.
- Confirmed `_panel_linear_contribution` EquityVol/FXVol uses
  `relative_vol_move_to_vol_points(move)` (no `move * 100.0`).
- Confirmed `es._aggregate_factor_pnl_linear` uses
  `relative_vol_move_to_vol_points(vol_pct)`;
  `contribution_pct=component / portfolio_es * 100.0` unchanged.
- Confirmed attribution equity/FX vega and rates DV01 paths use
  `relative_vol_move_to_vol_points` / `decimal_rate_to_bps` (no inline
  `* 100.0` / `* 10000.0`).
- Confirmed `from_wire_bound` is identity `float(max_shock)` (no
  `max_shock > 1` / `/ RATES_BP_SCALE`); rates build uses
  `bps_to_decimal_rate(magnitude * RATES_BP_SCALE)`.
- Confirmed FINDINGS RF-004 Status is still **IN PROGRESS** with
  “Do not close”; brief marks R0.4.3-B COMPLETE (slice only).
- Independent parity probe: helpers == pre-helper formulas; breaking
  `from_wire_bound("rates", 500) == 500`; preferred `0.5` → 500 bp wire.
- Leftover hunt: `scenario_model` / `scenarios` still inline bp↔decimal
  (report defers to R0.4.2; out of scored wiring). No production
  `max_shock > 1` residual under `backend/`.
- Reran the required pytest command. Did not execute CI. Did not mutate
  index, HEAD, or branch.

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_shock_units.py \
  tests/test_approx_pnl_units_golden.py \
  tests/test_sensitivities.py \
  tests/test_reverse_stress.py \
  tests/test_factor_panel_historical.py \
  tests/test_attribution.py \
  tests/test_es_contributions.py
# 72 passed in 3.03s (rerun 2.52s); 72 collected; no skips/xfails
# 1 Starlette/httpx deprecation warning
```

Implementer “72 passed” is not treated as proof; the suite was rerun here.

Independent probe:

| Check | Result |
|---|---|
| Panel / ES / attribution vol paths | `relative_vol_move_to_vol_points` |
| Rates reverse build | `bps_to_decimal_rate(mag * RATES_BP_SCALE)` |
| Attribution rates | `decimal_rate_to_bps(r1 - r0)` |
| Helper ≡ pre-helper scales | `*100` / `/10000` / `*10000` parity |
| `from_wire_bound("rates", 500)` | `500.0` (≠ `0.5`) |
| Prefer `0.5` → 500 bp | `to_wire_shock("rates", 0.5) == 500` |
| `max_shock > 1` in `reverse_stress.py` | absent |
| ES `contribution_pct` | still `component / portfolio_es * 100` |
| Golden numerical edits | none (header comments only) |
| FINDINGS RF-004 | **IN PROGRESS**; “Do not close” |

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | Panel/ES/attribution still inline `* 100` / silent golden drift | **Pass.** Helper swap only; goldens pass without numerical retune. |
| 2 | Rates build still `/ 10000` or wrong scale | **Pass.** `bps_to_decimal_rate`; probe `0.5 → 0.05` decimal. |
| 3 | Magnitude heuristic survives (`max_shock > 1` → bp) | **Pass.** Gone from `from_wire_bound` and module; tests pin `500 → 500`. |
| 4 | Breaking `max_shock=500` undocumented / untested | **Pass.** Docstring + report + `test_reverse_stress` / `test_shock_units`. |
| 5 | Contribution-% rewritten as shock-unit conversion | **Pass.** ES percent-of-total line untouched; no attribution % rewrite. |
| 6 | RF-004 CLOSED in FINDINGS | **Pass.** Still **IN PROGRESS**; “Do not close.” |
| 7 | Deferred modules claim falsely cleared | **Partial.** Report discloses `scenario_model`/`scenarios`; Minor #2. |
| 8 | Attribution absolute vol delta mis-scaled via relative helper | **Pass numerically.** Same ×100; naming reuse → Minor #1. |

## Findings

### Critical (Must Fix)

None.

### Important (Must Fix before APPROVE)

None.

### Minor

#### 1. VALID — Minor — Attribution reuses relative-vol helper for absolute vol deltas

`_greek_buckets_for_position` passes absolute level deltas `(v1 - v0)` into
`relative_vol_move_to_vol_points`. Scale is correct (×100 → vol points) and
call-site comments say so, but the helper name/docstring describe relative
moves. Risk is reader confusion, not a numerical change.

- Suggested fix (later): rename/generalize helper (e.g. decimal-vol→points)
  or add a thin alias used by attribution.

#### 2. VALID — Minor — Deferred inline bp conversions remain outside scored paths

`scenario_model.py` / `scenarios.py` still use `/ 10000.0` and `* 10000.0`.
Report correctly defers to R0.4.2; not a R0.4.3-B scored-path miss, but RF-004
unification is incomplete (consistent with FINDINGS remaining work).

## REJECTED implementer claims

None material. Wiring, heuristic removal, breaking `500` semantics, untouched
contribution-%, golden comment-only update, slice COMPLETE vs RF-004 still
IN PROGRESS, and deferred leftovers match the tree.

## Must-verify matrix

| # | Requirement | Result |
|---|---|---|
| 1 | Panel/ES/attribution relative-vol→points via helper; no silent golden change | **Met** |
| 2 | Rate bp↔decimal via `shock_units` helpers | **Met** |
| 3 | `from_wire_bound` has no `max_shock > 1` magnitude branch | **Met** |
| 4 | Breaking `max_shock=500` tested + documented | **Met** |
| 5 | Contribution-% not rewritten as unit conversion | **Met** |
| 6 | RF-004 still IN PROGRESS (not CLOSED) | **Met** |
| 7 | Required suite passes | **Met** (72 passed) |

## Counts

| Severity | Count |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 2 |

REJECTED implementer claims: 0.

**Verdict: APPROVE**

Zero VALID Important. Do **not** close RF-004. Remaining RF-004 work includes
R0.4.2 Scenario converge and broader inline conversion cleanup.

## Lead Architect adjudication (2026-09-09)

Accepted. Zero VALID Important. Minors deferred (broader inline bp leftovers → R0.4.2). RF-004 stays **IN PROGRESS**. Breaking rates `max_shock` semantics accepted (relative-style bound only; documented).
