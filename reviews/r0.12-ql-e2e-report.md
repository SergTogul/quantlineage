# R0.12 QuantLib E2E (nightly only)

## Task
RF-016 leftover — optional QuantLib E2E on nightly only (R0.12.4)

## Owner
QA & Quant Validation / DevOps Platform

## Status
**DONE** — sibling nightly job `quantlib-e2e` (`nightly-quantlib-e2e`) runs the critical Playwright journey with `QUANTLINEAGE_PRICING_ENGINE=quantlib` and `QUANTLINEAGE_REQUIRE_QUANTLIB=1`. Missing QuantLib fails when `QUANTLINEAGE_NIGHTLY=1` (no skip-green). RF-016 stays **IN PROGRESS** (labeled-runner SLA-K1/K2 and hierarchy benchmark remain).

## Summary
Added a real nightly-only QuantLib E2E job. It is **not** on `pull_request` and is **not** in PR-FULL `needs:` (still the six PR jobs). The job installs `backend/requirements.txt` with no no-ql fallback, imports QuantLib, then runs `e2e/tests/r0-critical-journey.spec.ts`. Playwright now honors `QUANTLINEAGE_PRICING_ENGINE` (default **builtin** so PR `e2e-playwright` is unchanged). `check_m6_sla.py` is not run. QuantLib hard-gate and PR-FAST are unchanged. No commit.

## Files changed
- `.github/workflows/nightly.yml` — new job `quantlib-e2e`
- `.github/workflows/ci.yml` — header comment only; PR-FULL `needs:` still the six PR jobs
- `e2e/playwright.config.js` — honor `QUANTLINEAGE_PRICING_ENGINE` (default builtin)
- `backend/tests/quantlib_gate.py` — `require_quantlib_for_nightly` fail-closed helper
- `backend/tests/test_nightly_ci.py` — QuantLib E2E contract + fail/skip unit tests
- `backend/tests/test_pr_full_ci.py` — PR-FULL needs stays exactly the six PR jobs; `quantlib-e2e` forbidden
- `BUILD_NOTES.md` — nightly QuantLib E2E bullet
- `reviews/r0.12-ql-e2e-report.md`

Not edited: `reviews/FINDINGS.md`, `reviews/REMEDIATION_MILESTONE.md`, `backend/app/pricing/*`, E2E product journey assertions, PR-FAST / QuantLib hard-gate bodies.

## Public/interface changes
- None. Nightly-only workflow + e2e harness env default. No API or pricing changes.

## Numerical conventions
- Units: n/a for CI wiring. Journey still asserts UI → API shape and `base_var_99 !== hedged_var_99` from compare JSON (no dollar VaR/ES pins).
- Sign convention: n/a
- Day count/calendar if relevant: n/a
- Tolerances/reference: n/a

## Tests added/updated
- `test_nightly_runs_quantlib_critical_e2e` — job exists; runs Playwright `r0-critical-journey.spec.ts`; sets `QUANTLINEAGE_PRICING_ENGINE: quantlib`, `QUANTLINEAGE_REQUIRE_QUANTLIB`, `QUANTLINEAGE_NIGHTLY`; mandatory `pip install -r requirements.txt` + `import QuantLib`; no `requirements-no-ql` / `continue-on-error` / `check_m6_sla.py`.
- `test_playwright_config_honors_pricing_engine_env` — config uses `process.env.QUANTLINEAGE_PRICING_ENGINE || 'builtin'` (does not unconditionally overwrite to builtin).
- `test_require_quantlib_fails_when_nightly_and_missing` — `QUANTLINEAGE_NIGHTLY=1` + missing QuantLib **fails**.
- `test_require_quantlib_skips_locally_when_missing` — same missing import **skips** when nightly / require flags are unset.
- `test_pr_full_needs_does_not_include_nightly` — nightly job ids including `quantlib-e2e` are not in PR-FULL `needs:`.
- `test_pr_full_needs_production_relevant_jobs` — exact six PR jobs; `quantlib-e2e` absent.

## Commands executed
```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_nightly_ci.py tests/test_pr_full_ci.py tests/test_pr_fast_ci.py

PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_quantlib_gate.py
.venv/bin/python -m ruff check tests/test_nightly_ci.py tests/test_pr_full_ci.py tests/quantlib_gate.py
```

## Results
- Backend: **19 passed** in 3.31s (`test_nightly_ci.py` + `test_pr_full_ci.py` + `test_pr_fast_ci.py`). `test_quantlib_gate.py`: **2 passed**. Ruff: exit 0.
- Frontend: n/a (PR e2e default remains builtin; journey spec not changed)
- QuantLib: hard-gate job and `QUANTLINEAGE_REQUIRE_QUANTLIB` left intact. Nightly E2E requires QuantLib install + import. Playwright QuantLib journey was not executed on this host (GHA nightly is the runner proof).
- C++: unchanged. `check_m6_sla.py` is not invoked on `ubuntu-latest`.
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none
- CI is green (all required checks): not started (commit/push out of scope)

## Proof PR-FULL is not blocked
PR-FULL `needs:` remains exactly:

```text
backend
backend-quantlib
frontend
lint
e2e
postgres-smoke
```

No `quantlib-e2e`. Nightly is a separate workflow file with no `pull_request` trigger.

## What nightly QuantLib E2E actually runs
| Job | Real check |
|---|---|
| `nightly-quantlib-e2e` | `pip install -r requirements.txt` (no no-ql fallback); `import QuantLib`; `require_quantlib_for_nightly()`; Playwright `tests/r0-critical-journey.spec.ts` with QuantLib pricing |

## Known limitations / risks
- GitHub Actions itself was not executed from this host. First `workflow_dispatch` after push is the runner proof that QuantLib wheels + Playwright pass on `ubuntu-latest`.
- Demo Global Macro under QuantLib was not re-run locally in this slice. The journey asserts API/UI shape and hedge-compare inequality, not dollar pins; if QuantLib cannot price the demo book the nightly job must fail (honest), not skip.
- Labeled-runner SLA-K1/K2 (`check_m6_sla.py`) and hierarchy benchmark remain RF-016 leftovers.
- RF-016 is **not** CLOSED.

## Follow-up / next owner
- Owner: Lead Architect / DevOps — labeled-runner SLA-K1/K2; hierarchy benchmark.
- Requested action: do not add Nightly or `quantlib-e2e` to branch-protection required checks; do not add nightly jobs to PR-FULL `needs:`; do not close RF-016.
- Blocking?: no
