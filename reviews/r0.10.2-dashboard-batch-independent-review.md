# R0.10.2 Dashboard Batch Independent Review

Slice: leftover RF-015 / R0.10.2 — stop `loadDashboard()` from launching
nine overlapping POSTs against the same book. Prefer one coherent batch
or a completed risk-run artifact. Do not treat the implementer’s batch
as a real single request until the client and live OpenAPI say so.
Workspace: uncommitted working tree vs HEAD on `/Users/user/src/quantlineage`
(`r0-core-remediation` @ `553e09c`).
Implementer report: `reviews/r0.10.2-dashboard-batch-report.md` (claims,
not proof).
Plan: `reviews/REMEDIATION_MILESTONE.md` § R0.10.2.
Finding: `reviews/FINDINGS.md` RF-015 — Status **IN PROGRESS**. Do not close.
Request-thread compute is R0.10.3.

Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. The only write
is this file.

Other dirty-tree work (`cache.py`, `quantlib.py`, `FINDINGS.md` RF-005
status, `REMEDIATION_MILESTONE.md`) was not scored.

## Verdict: APPROVE

## Lead Architect adjudication (2026-09-04)

Accepted. Zero VALID Important. RF-015 stays **IN PROGRESS** (request-thread compute is R0.10.3). This slice commits the one-POST dashboard batch only.

`loadDashboard()` is one `POST /api/v1/risk/dashboard`. There is no
`Promise.all` of the nine old risk POSTs in `frontend/src`. Live
`app.main:app` OpenAPI lists both dual-mounts. `classify` returns HEAVY
on `/risk/dashboard` and `/api/v1/risk/dashboard`; `is_interactive_only`
is False. The OpenAPI ↔ `ENDPOINT_EXECUTION_CLASS` bijection is empty on
both sides (33 owned canonical routes). The batch calls existing
`PortfolioService` methods that still snapshot internally; it does not
omit `market=` on a production engine. The client returns JSON keys only.
RF-015 and milestone R0.10 were not marked COMPLETE.

This is one HTTP request wrapping sequential existing methods, not a
shared-snapshot RiskRun. That leftover is documented and belongs to
R0.10.3. It is not a fail for this slice.

Do **not** close RF-015. Do **not** mark R0.10 COMPLETE.

## Checks performed

- Read the implementer report as claims only; read § R0.10.2, RF-015
  (acceptance: a dashboard load does not execute redundant full
  valuations of the same book and snapshot), and the six attacks.
- Diffed vs HEAD. Scored production hunks:
  - `backend/app/api/dashboard.py` (new)
  - `backend/app/api/risk.py` (`include_router`)
  - `backend/app/api/execution_class.py` (`POST /risk/dashboard` → HEAVY)
  - `backend/app/services/portfolio_service.py` (`dashboard()` only)
  - `frontend/src/api.js` (`loadDashboard` → one batch POST)
  - `backend/tests/test_dashboard_batch.py` (new)
  - `backend/tests/test_endpoint_execution_class.py` (dashboard HEAVY)
  - `frontend/src/api.test.js` (new)
  - `BUILD_NOTES.md` (R0.10.2 note)
  - `reviews/r0.10.2-dashboard-batch-report.md`
- Confirmed `backend/app/domain/models.py` has **no** diff vs HEAD.
- Confirmed `frontend/src/App.jsx` has **no** diff vs HEAD. Destructure
  keys remain `{portfolio, summary, stress, threats, contributors,
  limits, factors, varReport, hierarchy, attribution}`.
- Index empty. Branch still `r0-core-remediation`. This review did not
  commit. HEAD advanced from `422f1cc` → `553e09c` during the session
  via unrelated in-tree commits (`r0.5` QuantLib fail-closed, `r0.2`
  terms cache). Those commits are out of scope; the R0.10.2 working-tree
  hunks vs current HEAD are unchanged.
- Loaded live OpenAPI via `from app.main import app; app.openapi()`
  (title QuantLineage API 0.3.0; 66 method+path rows, 33 owned canonical
  after `/api/v1` strip). Compared `ENDPOINT_EXECUTION_CLASS` to that
  set: missing `[]`, extra `[]`.
- Probed `classify` / `is_interactive_only` on both dashboard mounts
  and the nine standalone routes the old client used. In-memory only.
- Grepped `frontend/src` for `Promise.all` (zero matches) and for the
  nine old dashboard POSTs (only drill-down helpers and comments remain).
- Confirmed `frontend/src/test/setup.js` uses
  `server.listen({ onUnhandledRequest: 'error' })`. Default MSW handlers
  do not cover the nine old dashboard POSTs.
- Ran the required commands (rerun, not the implementer’s log):

```bash
cd /Users/user/src/quantlineage && /usr/bin/git diff HEAD -- \
  backend/app/api/dashboard.py backend/app/api/risk.py \
  backend/app/api/execution_class.py \
  backend/app/services/portfolio_service.py \
  frontend/src/api.js frontend/src/App.jsx backend/app/domain/models.py
# models.py / App.jsx: empty. Remaining hunks match the scored set.

cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_dashboard_batch.py tests/test_endpoint_execution_class.py
# 13 passed, 1 warning (pre-existing Starlette/httpx TestClient deprecation)

cd /Users/user/src/quantlineage/frontend
npm test -- src/api.test.js
# 1 passed (1 file)
```

I did not rerun the implementer’s broader API suite, ruff, mypy, full
`npm test`, or CI.

## Acceptance criteria

| # | Criterion | Result |
|---|---|---|
| 1 | One batch POST returns the ten UI keys | **Pass.** Live `POST /api/v1/risk/dashboard` (with GET-portfolio body and empty body) returns exactly `{portfolio, summary, stress, threats, contributors, limits, factors, varReport, hierarchy, attribution}`. `DashboardBatchResponse` preserves camelCase `varReport`. |
| 2 | `loadDashboard()` calls that path only | **Pass.** `api.js` is `return json(\`${API_V1}/risk/dashboard\`, {method: 'POST'})`. No `Promise.all` in `frontend/src`. `App.jsx` still calls `loadDashboard()` once on mount. |
| 3 | Classified HEAVY; not INTERACTIVE-only | **Pass.** Map entry and `classify` are HEAVY on both mounts. `is_interactive_only("POST", "/risk/dashboard")` is False. |
| 4 | Live OpenAPI bijection includes the new route | **Pass.** OpenAPI rows: `POST /risk/dashboard` and `POST /api/v1/risk/dashboard`. Independent dump: 33 owned canonical; map ↔ owned is empty on both sides. `test_every_owned_route_is_classified` loads `app.openapi()`. |
| 5 | Dual-mount `/api/v1` shares the class | **Pass.** `normalize_path` strips `/api/v1`. Probed both strings → HEAVY. Raw dict has only the canonical key (same pattern as every other owned route). |
| 6 | Market-required semantics unchanged | **Pass.** `dashboard()` calls existing service methods. Those methods still `market_snapshot(portfolio)` and pass `market=` into the engines that require it. No new `market=None` / `demo_market_snapshot` path. HTTP body is still an optional `Portfolio`, same as the standalone POSTs. |
| 7 | Client does not compute or pin risk numbers | **Pass.** `loadDashboard` returns `res.json()`. `api.test.js` fixtures are `{ ok: key }` / `{ id: 'demo', … }`. No dollar goldens. |
| 8 | RF-015 / R0.10 not marked COMPLETE | **Pass.** RF-015 Status remains **IN PROGRESS**. § R0.10 has no COMPLETE row; § R0.10.2 has no COMPLETE marker. Report Status **DONE** is the slice only and states RF-015 is not closed. `BUILD_NOTES.md` says work still runs on the request thread (R0.10.3). This review did not edit FINDINGS / milestone. |
| 9 | Not a RiskRun / not off the request thread | **Pass.** Sync `def risk_dashboard` → sequential `PortfolioService.dashboard()`. No enqueue. Documented leftover for R0.10.3. |

## Attack results

| # | Attack | Score |
|---|---|---|
| 1 | `loadDashboard` still fans out multiple risk POSTs, or the batch client still `Promise.all`s the nine routes | **No leftover in `frontend/src`.** `loadDashboard` is one POST. Zero `Promise.all` matches under `frontend/src`. The nine old paths are not called from `loadDashboard`. `api.test.js` records posted pathnames and expects exactly `/api/v1/risk/dashboard`. A leftover GET `/portfolio` or any of the nine old POSTs would be an MSW unhandled-request error (`onUnhandledRequest: 'error'`; those paths are not in the default handlers). |
| 2 | `POST /risk/dashboard` missing from live OpenAPI or classified INTERACTIVE-only | **No.** Both dual-mount paths are in `app.openapi()["paths"]`. Class is HEAVY. `is_interactive_only` is False. |
| 3 | Dual-mount `/api/v1` is unclassified or a different class | **No.** `classify("POST", "/api/v1/risk/dashboard")` is HEAVY, same as the unprefixed path. Unknown `/api/v1/...` that do not normalize to a mapped route still raise `KeyError`. |
| 4 | Batch omits a required explicit market on a production method (silent demo inference) | **No.** `dashboard()` does not call `HistoricalRiskEngine.calculate` / `VaRAnalytics.report` / `HierarchyEngine.build` / stress evaluate without `market`. It delegates to `summary`, `stresses`, `threat_evaluation`, `contributors`, `limits`, `factors`, `var_report`, `hierarchy`, `demo_attribution` — each of which snapshots the same way the standalone routes already do. Empty-body default book is `get_default_portfolio` (same as `GET /portfolio`), not a new demo-market bypass. |
| 5 | Frontend computes or hardcodes risk numbers | **No.** Transport only. New UI test uses structural fixtures, not dollar pins. |
| 6 | RF-015 / R0.10 marked COMPLETE; goldens/dollar pins added in UI tests | **No on disk.** RF-015 stays **IN PROGRESS**. R0.10 is not COMPLETE. No dollar pins in `api.test.js`. Dirty `FINDINGS.md` hunk is an RF-005 status line (out of scope), not an RF-015 close. |

## Leftover client fan-out / OpenAPI holes

**None on the R0.10.2 dashboard-load path.**

| Route / client call | OpenAPI? | Class | Hole? |
|---|---|---|---|
| `POST /risk/dashboard` | yes | HEAVY | **No** |
| `POST /api/v1/risk/dashboard` | yes | HEAVY (via `normalize_path`) | **No** |
| `loadDashboard()` → nine POSTs | n/a | n/a | **No** (removed) |
| Standalone `POST /risk/summary` … `/attribution/demo` | yes | unchanged | **No** (drill-down leftovers, allowed) |

### Residual (not a fail)

| Item | Why it is not a leftover hole for this slice |
|---|---|
| `dashboard()` still calls nine service methods sequentially; each takes its own snapshot | Plan allowed “one completed risk-run artifact **or** a coherent batch service.” HTTP fan-out is gone. Shared-snapshot / off-request-thread is R0.10.3. RF-015 acceptance evidence is not fully met; that is why the finding stays open. |
| `classify` is still not wired to routers | Same as R0.10.1. HEAVY is a map label, not a queue. |
| Drill-down POSTs remain (`evaluateCustomScenario`, `explainPnLDemo`, `VaRCompare`, …) | R0.10.2 scoped the dashboard **load**. Standalone routes stay for other UI actions. |
| `frontend/runtime-dist/index.html` still GETs `/portfolio` then POSTs `/risk/stress/evaluate` | Not in the scored file set; not the React `loadDashboard` path. |
| `api.test.js` does not name the nine old POSTs | Protection is global MSW `onUnhandledRequest: 'error'` plus `posted === [dashboard]`. Sufficient; see Minor 1. |
| RF-015 text still says “dashboard fan-out remain R0.10.2 / R0.10.3” | Implementer correctly did not edit FINDINGS. Lead Architect updates status after this slice. Do not close. |

## Findings

### Critical

None.

### Important

None that block APPROVE.

### Minor

1. **Fan-out guard is implicit, not a named deny list**
   - File: `frontend/src/api.test.js`
   - The implementer report says “MSW errors if the nine old POSTs fire.”
     That is true only because `setup.js` sets `onUnhandledRequest: 'error'`
     and the default handlers do not include those nine paths. The test
     itself does not register failing handlers for `/risk/summary`,
     `/stress`, `/var`, `/hierarchy`, etc. A later default-handler add
     could weaken the tripwire. `posted === [dashboard]` would still fail
     if those calls were the only traffic.
2. **R0.10.1 milestone line still says 32 owned routes**
   - File: `reviews/REMEDIATION_MILESTONE.md` § R0.10.1 (out of this
     slice’s edit set)
   - Live owned count is now 33. Not a bijection hole; the map matches
     OpenAPI. Do not treat the stale “32” sentence as the contract.

## Recommendations

- Keep RF-015 **IN PROGRESS**. Next owner is R0.10.3 (queue /
  backpressure / moving heavy work off the HTTP thread). Do not mark
  R0.10 COMPLETE until that lands if request-thread monopolization
  remains the finding’s remaining acceptance.
- Optional: name the nine old dashboard POSTs as MSW error handlers in
  `api.test.js` so the tripwire does not depend on default-handler set.
- When a later slice consumes `classify`, keep calling `classify` (with
  methodology when present). Do not treat the raw dict as complete for
  `POST /risk/summary`.

## Assessment

**Ready to merge: Yes** (R0.10.2 dashboard-batch slice only).

The client no longer fans out nine overlapping risk POSTs. The new
route exists on both mounts, is HEAVY, and is in the live OpenAPI
bijection. Market-required call shape is unchanged. The six attacks do
not land. RF-015 / R0.10 remain open because the batch is still
sequential request-thread compute, not a RiskRun.
