# R0.12.2 PR-FULL

## Task
R0.12.2 PR-FULL aggregator (RF-016)

## Owner
DevOps / Platform Engineer

## Summary
Added an explicit GitHub Actions job named `PR-FULL` so a green check of that name means the production-relevant jobs have all succeeded: `backend` (backend-pytest), `backend-quantlib` (backend-quantlib-hard-gate), `frontend` (frontend-test-build, already includes `npm test` and `npm run build`), `lint` (lint-static-analysis), `e2e` (e2e-playwright), and `postgres-smoke` (postgres-persistence-smoke). The job is a `needs:` aggregator with a no-op `echo`; it does not re-run pytest. No `if:` skip on missing QuantLib. `backend-quantlib-hard-gate` is unchanged and still sets `QUANTLINEAGE_REQUIRE_QUANTLIB`. This does not claim nightly (R0.12.4) or native ABI (R0.12.5).

## Files changed
- `.github/workflows/ci.yml`
- `backend/tests/test_pr_full_ci.py`
- `reviews/r0.12.2-report.md`

## Public/interface changes
- None (CI aggregator job + workflow-contract test only).

## Numerical conventions
- Units: n/a
- Sign convention: n/a
- Day count/calendar if relevant: n/a
- Tolerances/reference: n/a

## Tests added/updated
- `tests/test_pr_full_ci.py` — finds job `pr-full` with display name containing PR-FULL, asserts `needs:` includes the six production-relevant job ids, and asserts `backend-quantlib-hard-gate` still has `QUANTLINEAGE_REQUIRE_QUANTLIB` (no no-QL fallback).

## Commands executed
```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_pr_fast_ci.py tests/test_pr_full_ci.py
```

## Results
- Backend: **7 passed** in 0.83s (`test_pr_fast_ci.py` + `test_pr_full_ci.py`)
- Frontend: n/a (not re-run; `frontend-test-build` already covers `npm test` / `npm run build`)
- QuantLib: not re-run here. `backend-quantlib-hard-gate` left intact (`QUANTLINEAGE_REQUIRE_QUANTLIB`, mandatory `pip install -r requirements.txt`, no `requirements-no-ql` fallback).
- C++: n/a (native compile remains inside `backend-pytest`; not claimed as R0.12.5 ABI)
- Build: n/a (production build remains `frontend-test-build`)
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none
- CI is green (all required checks): not started (commit/push out of scope; GHA cannot be run here)

## Known limitations / risks
- A green PR-FULL is the existing production-relevant job set succeeding, not a new pytest matrix. If a needed job fails, GitHub skips the aggregator by default (no `if: always()`); treat `PR-FULL` as the single required check so a skip/fail still blocks merge.
- Native compile/parity stays inside `backend-pytest`; this slice does not add a separate native job or ABI checks (R0.12.5).
- Nightly / labeled-runner work (Postgres two-worker, larger FULL_REVALUATION, benchmarks) is R0.12.4 and is not claimed.
- GitHub Actions itself was not executed from this host. Branch protection must list the check whose display name contains **PR-FULL** as required for the aggregator to be the merge gate.

## Follow-up / next owner
- Owner: DevOps / QA (R0.12.4 nightly / R0.12.5 native ABI)
- Requested action: add nightly/labeled-runner coverage and native ABI validation. Do not treat a green PR-FULL as those having run. Mark RF-016 remaining tiers accordingly.
- Blocking?: no
