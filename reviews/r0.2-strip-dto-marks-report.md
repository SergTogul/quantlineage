# Agent Handoff Template

## Task
R0.2 Phase B — strip mark fields from `*Position` DTOs (RF-001)

## Owner
Quant Pricing Engineer

## Summary
Removed live market mark fields from all `*Position` DTOs (`extra='forbid'`). Builtin and QuantLib production pricing now build a `types.SimpleNamespace` working view from `terms.model_dump()` + `_snapshot_marks_from_terms(terms, market)` (+ Position `duration` when present) and never read marks from the Position DTO. `LegacyDemoPricingAdapter` deleted. `DemoSampleMarksSnapshotAdapter` / ad-hoc `demo_market_snapshot` raise `SampleMarksRemovedError`. Demo SAMPLE portfolios are economics-only; marks live only in `_DEMO_MARKETS` / `_DEMO_AGGREGATE_MARKETS`. Attribution no longer falls back to Position spots/vols.

## Files changed
- `backend/app/domain/models.py` — strip mark fields; `PositionHierarchyMixin.extra='forbid'`
- `backend/app/pricing/builtin.py` — `_pricing_view` SimpleNamespace path
- `backend/app/pricing/quantlib.py` — same
- `backend/app/interfaces/pricing.py` — remove `LegacyDemoPricingAdapter`
- `backend/app/market/demo_snapshot.py` — raise `SampleMarksRemovedError`
- `backend/app/market/snapshot.py` — docstring for deprecated provider
- `backend/app/sample.py` — economics-only demos; ad-hoc path raises
- `backend/app/risk/attribution.py` — market-only spot/vol fallbacks
- `backend/tests/*` — Phase B migrations (markets explicit; regressions)
- `backend/tests/market_fixtures.py` — shared test market helpers
- `reviews/FINDINGS.md` — Phase B done pending review (RF-001 not closed)
- `reviews/r0.2-strip-dto-marks-report.md` — this handoff

## Public/interface changes
- Position DTO constructors reject former mark kwargs (`ValidationError` / `extra_forbidden`)
- `PricingEngine.value(position, market)` remains required; no DTO-mark pricing path
- `LegacyDemoPricingAdapter` removed (breaking for any external importer)
- `DemoSampleMarksSnapshotAdapter.snapshot` always raises `SampleMarksRemovedError`
- `demo_market_snapshot` still returns canned snapshots for exact demo identities

## Numerical conventions
- Units: unchanged (decimals for rates/vols; cash MV)
- Sign convention: unchanged (payer swap, long STIR, etc.)
- Day count/calendar: unchanged (Actual365Fixed / existing QL policy)
- Tolerances/reference: existing goldens retained; local-vs-snapshot divergence tests now compare two explicit snapshots

## Tests added/updated
- `test_value_requires_market.py` — Phase B regression: mark fields absent / ValidationError
- `test_demo_snapshot_adapter.py` — adapter raises; no LWW from positions; mixed-USD demo honest
- Pricing / market authority / terms / hierarchy / stress / omit-market suites migrated to economics + explicit `MarketSnapshot`

## Commands executed
```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=line \
  tests/test_value_requires_market.py \
  tests/test_builtin_pricing.py tests/test_quantlib_pricing.py \
  tests/test_builtin_terms_value.py tests/test_quantlib_terms_value.py \
  tests/test_pricing.py tests/test_market_authority.py \
  tests/test_demo_snapshot_adapter.py tests/test_omit_market.py \
  tests/test_instrument_terms.py tests/test_risk.py \
  tests/test_hierarchy.py tests/test_stress.py
```

## Results
- Backend: **251 passed** in ~2.7s
- Frontend: not run (out of scope)
- QuantLib: included via quantlib pricing/terms tests
- C++: not run
- Build: not run
- All tests pass (all applicable/affected suites required by the task): **yes** (targeted list)
- Unexplained failures or skips: none
- CI is green (all required checks): not started (no commit/push per instructions)

## Known limitations / risks
- Broader `tests/` beyond the targeted list may still reference removed mark kwargs / `LegacyDemoPricingAdapter` (e.g. golden/property/cache suites) — orchestrator should expand CI coverage
- `model_copy(update={mark: ...})` does not always fail closed under pydantic; reconstruction via `model_validate` does (covered by instrument_terms tests)
- Attribution theta and other risk paths outside the targeted list may need follow-up if they still assume Position marks

## Follow-up / next owner
- Owner: Lead Architect / Orchestrator
- Requested action: independent review; close RF-001 in FINDINGS if approved; expand full `tests/` fix-up and commit
- Blocking?: no for Phase B targeted suite; yes for declaring RF-001 fully closed repo-wide until remaining test files are migrated

---

## Full-suite test migration (Phase B leftovers)

### Summary
Migrated remaining tests off Position DTO marks and ad-hoc `demo_market_snapshot` / default `DemoPortfolioMarketDataProvider` paths. Restored intentional `ValidationError` / `extra_forbidden` forbid-mark tests. Injected explicit `MarketSnapshot` via `tests.market_fixtures.FixedMarketProvider` / `equity_spot_market` / `equity_spots_market` for ad-hoc books. Stripped leftover JSON `price` fields. Regenerated `data/demo_risk_artifact.json`. Fixed bare `TestClient(app)` (no lifespan) pollution after persistence DI tests. Did **not** revert production Phase B or `market_data=` constructor. RF-001 left open. No commit.

### Commands executed
```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=line
```

### Results
- **1208 passed, 4 skipped, 0 failed, 0 errors** in ~177s
- Skips: pre-existing intentional only

### Files touched in this leftover pass (tests / fixtures / goldens / bench)
- `backend/tests/market_fixtures.py` — `FixedMarketProvider`, `equity_spots_market`
- Forbid restores: `test_value_requires_market.py`, `test_demo_snapshot_adapter.py`, `test_builtin_terms_value.py`, `test_quantlib_terms_value.py`, `test_finite_scalars.py`
- Explicit markets / `FixedMarketProvider`: hierarchy* / portfolio hierarchy artifacts, m9 properties, component/marginal/ES/scenario attribution, incremental/var_compare, durable_worker, risk_run_api, persistence_di, workload_limits, risk_attribution
- JSON strip: `test_what_if.py`, `test_finite_scalars.py`
- Lifespan-safe TestClient: `test_reverse_stress.py`, `test_reverse_stress_multi.py`, `test_risk_attribution.py`
- `benchmarks/run_full_reval_bench.py` — economics-only EquityPosition
- `data/demo_risk_artifact.json` — regenerated for Phase B marks
