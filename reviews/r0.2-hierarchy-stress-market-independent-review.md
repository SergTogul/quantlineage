# R0.2 Hierarchy / Stress / Reverse Omitted-Market Independent Review

Slice: leftover RF-001 after VaR/ES explicit-market APPROVE — production
`HierarchyEngine`, `StressEngine`, reverse-stress, and reverse-stress-multi
must require an explicit `MarketSnapshot` and must not call
`demo_market_snapshot` / `DemoAggregateMarketDataProvider.snapshot` when
`market` is omitted or `None`.
Workspace: uncommitted work on `r0-core-remediation` (`5b4aa58`) vs HEAD.
Implementer report: `reviews/r0.2-hierarchy-stress-market-report.md`
(claims, not proof).
Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. RF-001 was not
closed. The only write is this file.

Other dirty production files on this workspace (pricing, models, kernel,
native, sample) are out of scope for this slice and were not treated as
implementer evidence.

## Verdict: APPROVE

The claimed production methods fail closed. Omitting `market=` or passing
`market=None` raises
`ValueError("production risk calculation requires an explicit MarketSnapshot")`
— the same type and wording as VaR/ES. They no longer call
`demo_market_snapshot` or `DemoAggregateMarketDataProvider.snapshot`.
`PortfolioService` was not edited and still resolves one root snapshot
and threads it into hierarchy, stress, reverse, reverse-multi, compare,
and dashboard paths.

`ScenarioAttributionEngine.decompose` still infers if called directly.
That is not scored as leftover: it is not a production entry point
without a snapshot from stress. `StressEngine.contributions` /
`evaluate` require a snapshot before calling it.

No leftover `market if market is not None else demo_market_snapshot(...)`
or `self.market_data.snapshot(portfolio)` remains on the claimed
hierarchy / stress / reverse path.

Do **not** close RF-001. Bond/swap/IR family fallbacks and the terms
split remain open.

## Checks performed

- Diffed `backend/app/risk/hierarchy.py`, `backend/app/risk/stress.py`,
  `backend/app/risk/reverse_stress.py`,
  `backend/app/risk/reverse_stress_multi.py`, and the
  `require_explicit_market` docstring in `backend/app/risk/historical.py`
  against HEAD. Confirmed `backend/app/services/portfolio_service.py`,
  `reviews/FINDINGS.md`, and `reviews/REMEDIATION_MILESTONE.md` have no
  working-tree diff.
- Read `require_explicit_market`, `HierarchyEngine.build` / `risk_at`,
  `StressEngine.run` / `evaluate` / `contributions`,
  `ScenarioComparisonEngine.compare`, `ReverseStressEngine.solve`,
  `MultiFactorReverseStressEngine.solve`,
  `ScenarioAttributionEngine.decompose`, PortfolioService hierarchy /
  stress / reverse / compare / limits / dashboard callers, RF-001 in
  `reviews/FINDINGS.md` / `reviews/REMEDIATION_MILESTONE.md`, and the
  prior VaR/ES review `reviews/r0.2-omit-market-independent-review.md`.
- Re-read brief § production contract (no production risk method may
  infer a market) and the attack list. TDD red narrative in the
  implementer report was not treated as proof.
- Source-hunted `demo_market_snapshot`, `market_data.snapshot`, and
  `market if market is not None` under `backend/app/risk/`.
- Probed runtime (no production-file edits). Helper rebinds were
  in-memory only and restored.
- Ran:

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_hierarchy.py tests/test_stress.py \
  tests/test_reverse_stress.py tests/test_omit_market.py
# 49 passed, 1 existing Starlette/httpx deprecation warning
```

I did not rerun the implementer's broader caller suite, QuantLib, or CI.

## Acceptance criteria

| # | Criterion | Result |
|---|---|---|
| 1 | `HierarchyEngine.build` / `risk_at` omitted / `None` raise the stated `ValueError` | **Pass.** Probe and `test_hierarchy_build_omitted_market_raises` / `_market_none_raises` / `test_hierarchy_risk_at_omitted_market_raises` / `_market_none_raises`. Message is exactly `production risk calculation requires an explicit MarketSnapshot`. |
| 2 | `StressEngine.run` / `evaluate` / `contributions` omitted / `None` raise the same `ValueError` | **Pass.** Probe (including `evaluate` / `contributions` `market=None`). Tests in `test_omit_market.py` and `test_stress.py` hit `run` / `evaluate` omit+`None` and `contributions` omit. |
| 3 | `ReverseStressEngine.solve` / `MultiFactorReverseStressEngine.solve` omitted / `None` raise | **Pass.** Probe both omit and `None`. Tests cover reverse omit+`None` and reverse-multi omit (`None` probed, not a separate test). |
| 4 | `ScenarioComparisonEngine.compare` omitted / `None` raise | **Pass.** Diff adds `require_explicit_market` at the top of `compare`. Probe: omit and `market=None` raise the same message. No dedicated omit test; not required once the production method fails closed. |
| 5 | Claimed methods no longer call `demo_market_snapshot` or `DemoAggregateMarketDataProvider.snapshot` | **Pass.** Diff removes the fallback lines. Source hunt: zero `demo_market_snapshot` / `market_data.snapshot` in `hierarchy.py`; zero `.snapshot` calls in `stress.py` / `reverse_stress.py` / `reverse_stress_multi.py` methods. Monkeypatch of both helpers: omit still raised `ValueError`, call counts stayed 0. |
| 6 | Explicit snapshot still values | **Pass.** Probe: unit equity `market_value == 1.0` on hierarchy / reverse / reverse-multi. `test_hierarchy_stress_reverse_explicit_market_still_values` and `test_stress.py` explicit run/evaluate. |
| 7 | Tests hit the production classes, not only a wrapper | **Pass.** `test_omit_market.py` and `test_stress.py` construct `HierarchyEngine`, `StressEngine`, `ReverseStressEngine`, and `MultiFactorReverseStressEngine` and call `build` / `risk_at` / `run` / `evaluate` / `contributions` / `solve` directly. |
| 8 | `PortfolioService` still threads the root snapshot | **Pass.** No diff vs HEAD. `hierarchy`, `stresses`, `threat_evaluation`, `reverse_stress`, `reverse_stress_multi`, `compare_scenarios`, `what_if`, `limits`, and `limit_drilldown` still call `self.market_snapshot(portfolio)` and pass `market=`. |
| 9 | RF-001 not marked CLOSED | **Pass.** `reviews/FINDINGS.md` RF-001 remains **IN PROGRESS**. `reviews/REMEDIATION_MILESTONE.md` still lists hierarchy/stress omitted `market=` as OPEN (stale remaining-line; this review did not edit it). This review did not edit those files. |
| 10 | Restoring inference would make omit tests fail | **Pass.** Rebinding `require_explicit_market` to `market if market is not None else demo_market_snapshot(BOOK)` priced omit-`market=` on hierarchy / stress / reverse / reverse-multi (`mv=1.0`). Restoring the helper raised again. |

## Attack results

1. **`HierarchyEngine` / stress / reverse still `market if market is not None else demo_market_snapshot(...)`.** Gone. Hierarchy replaced both `build` and `risk_at` fallbacks with `require_explicit_market`. Stress `run` / `evaluate` / `contributions` and both reverse `solve` methods replaced `self.market_data.snapshot(portfolio)` the same way. `ScenarioComparisonEngine.compare` now guards at the top.
2. **`market=None` still prices.** Default remains (`MarketSnapshot | None = None`) so the failure is a domain `ValueError`, not a `TypeError`. It does **not** price. Probe: every claimed method with `market=None` raises the exact message.
3. **Tests only cover wrappers; engines still infer.** Not found. New tests call the production classes. `PortfolioService` is a wrapper that already threads a snapshot; it was not the only coverage.
4. **RF-001 marked CLOSED.** Not found.
5. **`PortfolioService` stopped threading the root snapshot (regression).** Not found. File is untouched vs HEAD and still passes `market=self.market_snapshot(...)`.

## Leftover omitted-market inference on hierarchy / stress / reverse

**None on the claimed production path.**

Source hunt after the change:

- `backend/app/risk/hierarchy.py`: no `demo_market_snapshot`; `build` and `risk_at` call `require_explicit_market`; `_node` / `_stress` / `_limits` receive the required snapshot.
- `backend/app/risk/stress.py`: `run` / `evaluate` / `contributions` / `ScenarioComparisonEngine.compare` call `require_explicit_market`. No `.snapshot(` in those methods.
- `backend/app/risk/reverse_stress.py`: `solve` calls `require_explicit_market`. No `.snapshot(` after the change.
- `backend/app/risk/reverse_stress_multi.py`: same.

Not scored as leftover for this slice:

- `ScenarioAttributionEngine.decompose` still does
  `market if market is not None else self.market_data.snapshot(portfolio)`.
  Probe: direct `decompose(BOOK, PRICING, scenario)` returned
  `portfolio_pnl ≈ -0.1`. The only production caller is
  `StressEngine.contributions`, which now does
  `market=require_explicit_market(market)` before the call.
  `evaluate` passes the already-required snapshot into `contributions`.
  Per brief: score this only if it is a production entry point without a
  snapshot from stress. It is not.

Out of this slice (not scored):

- `LimitEngine` / `contributors_for_metric` still type
  `market: MarketSnapshot | None = None` and forward `None`. They fail
  when they reach `StressEngine.run` or VaR/ES; they do not infer.
- `limit_drilldown._key_rate_dv01_amounts`, `RiskFactorEngine`,
  `SensitivityEngine`, and P&L attribution engines still infer via
  `market_data.snapshot`. Those are not hierarchy / stress / reverse
  entry points.
- Engines still construct unused `DemoAggregateMarketDataProvider`
  attributes. `snapshot` is not invoked on the claimed methods.

## Findings

None that change the verdict.

### Items checked and not findings

- `require_explicit_market` is a thin `is None` guard. An empty
  `MarketSnapshot(id="empty")` is explicit and accepted (reverse-stress
  empty-book test already passes one). Missing *factors* remain a
  pricer `MissingMarketDataError`.
- Keeping `market=None` on the signature is the claimed contract (domain
  `ValueError` with the same “explicit MarketSnapshot” wording as
  VaR/ES). It is not leftover inference.
- `test_omit_market.py` / `test_hierarchy.py` / `test_stress.py` use
  `demo_market_snapshot` only on explicit happy paths. That is the named
  demo helper, not production inference.
- `test_omit_market.py` has no `ScenarioComparisonEngine` omit case and
  no reverse-multi / contributions `market=None` case. Probes cover
  those paths. Same pattern as the VaR/ES review (helper-level guard;
  not every keyword combination needs its own test).
- Unused `self.market_data = DemoAggregateMarketDataProvider()` on
  stress / reverse engines is dead construction, not a silent default
  on `run` / `solve`.
- `FINDINGS.md` RF-001 remaining text still lists
  “hierarchy/stress/reverse omitted-`market=` inference.” That line is
  stale relative to this slice and was correctly left unedited.
  Stale remaining-text is not a close of RF-001.
- I did not close RF-001. Hierarchy / stress / reverse omitted-`market=`
  inference on the claimed methods is done; bond/swap/IR families and
  the terms split remain.

## REJECTED items (implementer report)

- **TDD red count as proof.** Not used. Diff, source hunt, mutation
  probes, and pytest are the evidence. The red narrative is consistent
  with the pre-change fallbacks but is not accepted as verification.

## Finding counts

| Severity | VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 0 |

REJECTED implementer claims: 1 (TDD red as proof).

**Leftover omitted-market inference on the claimed hierarchy / stress / reverse path: none.**

**Verdict: APPROVE**

Do **not** close RF-001.

---

## Orchestrator adjudication (2026-09-03)

Verdict stands **APPROVE**. `decompose` leftover not a production entry point. Do **not** close RF-001 (terms split remains).
