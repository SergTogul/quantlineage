# R0.4.2-C Formal Default Wire Independent Review

**APPROVE** — Critical 0 · Important 0 · Minor 2

Slice: R0.4.2-C — Formal `ScenarioWire` as default HTTP list wire (RF-004 leftover).
Workspace: `/Users/user/src/riskforge-mvp` branch `r0-core-remediation`
(HEAD `7b24d58`; scored work is uncommitted working-tree vs that commit).
Implementer report: `reviews/r0.4.2-c-formal-default-wire-report.md` (claims only).
Reviewer did not implement this work. Production files were not edited. Index,
HEAD, and branch were not mutated. The only write is this file. RF-004 / RF-011
were not closed. RF-009 was not reopened.

Scored only:

- `backend/app/api/stress.py`
- `backend/app/api/scenario_wire.py` (module docstring)
- `backend/app/risk/scenario_model.py` (module docstring)
- `backend/tests/test_scenario_wire_api.py`
- `backend/tests/test_api.py`
- `backend/tests/test_backpressure.py`
- `backend/tests/test_stress_scenarios_di.py`
- `reviews/r0.4.2-c-formal-default-wire-report.md`
- `reviews/FINDINGS.md` / `REMEDIATION_MILESTONE.md` /
  `r0.4-architecture-brief.md` — RF-004 / RF-011 must **not** be CLOSED;
  RF-009 must stay CLOSED

## Verdict summary

`GET /risk/stress/scenarios` (and the unversioned dual-mount) returns
`list[ScenarioWire]` via shared `_formal_scenario_wires` / `stress_to_wire`.
`GET .../scenarios/formal` is an identical JSON alias (same helper, same DI).
OpenAPI `response_model` and route description advertise formal wire and the
breaking change. Legacy StressScenario **POST** bodies on custom / evaluate /
compare are retained. Frontend has no GET list consumer. Formal↔legacy custom
stress P&L parity remains green. RF-004 and RF-011 stay **IN PROGRESS**.
RF-009 stays **CLOSED**. Required suite: **108 passed**.

Lead may mark this slice APPROVE in milestone / FINDINGS. Lead should **not**
close RF-004 on list-only converge while dual StressScenario POST bodies remain
(unless Lead explicitly accepts that residual as out of RF-004 exit). Do not
close RF-011.

## Checks performed

- Read implementer report as claims only; diff scored production + tests +
  FINDINGS / milestone / brief vs `7b24d58`.
- Confirmed default GET uses `response_model=list[ScenarioWire]` and
  `_formal_scenario_wires`; no longer returns raw DI `StressScenario` list.
- Confirmed `/scenarios/formal` calls the same helper (alias, not a second
  projection path).
- Confirmed OpenAPI component `ScenarioWire` exposes `category` / `shocks[]`,
  not `equity_shock` / `rates_shift_bps` / `vol_shock`.
- Grepped `frontend/` — no consumer of `GET .../stress/scenarios`; UI still
  POSTs legacy `scenarioPayload` to evaluate/custom (documented residual).
- Confirmed FINDINGS RF-004 / RF-011 Status **IN PROGRESS** with “Do not
  close”; RF-009 Status **CLOSED** unchanged.
- Independent HTTP probe (below).
- Reran the required pytest command. Did not execute CI. Did not mutate
  index, HEAD, or branch.

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_scenario_wire_api.py \
  tests/test_scenario_model.py \
  tests/test_scenario_engine.py \
  tests/test_stress_scenarios_di.py \
  tests/test_scenarios.py \
  tests/test_stress.py \
  tests/test_api.py \
  tests/test_backpressure.py
# 108 passed in 3.34s; 108 collected; no skips/xfails
# 1 Starlette/httpx deprecation warning
```

Implementer “73 passed” (narrower suite) is not treated as proof; the required
suite was rerun here (108).

Independent probe (TestClient):

| Check | Result |
|---|---|
| Default GET formal keys; no `equity_shock` / scalars | **Pass** (13 rows; `shocks` present) |
| `/scenarios/formal` JSON `==` default | **Pass** |
| Unversioned `/risk/stress/scenarios` same ids + formal shape | **Pass** |
| OpenAPI 200 items → `ScenarioWire` (both paths) | **Pass** |
| Formal vs legacy `POST .../stress/custom` P&L | exact match (`-91703.27321451333`) |
| FINDINGS RF-004 / RF-011 | **IN PROGRESS**; “Do not close” |
| FINDINGS RF-009 | **CLOSED** (untouched) |

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | Default GET `/scenarios` still legacy StressScenario / mixed | **Pass.** `response_model=list[ScenarioWire]`; tests + probe: `shocks`/`category` present; legacy scalars absent. |
| 2 | `/scenarios/formal` diverges from default (not alias) | **Pass.** Shared `_formal_scenario_wires`; suite + probe `formal.json() == default.json()`. |
| 3 | Breaking change undocumented / tests still expect legacy list | **Pass (API).** OpenAPI description documents breaking; DI/API/backpressure/wire tests updated. Historical ADR/ROADMAP notes still dual-list (Minor #1). |
| 4 | Formal↔legacy custom stress P&L parity regresses | **Pass.** `test_formal_custom_stress_api_matches_legacy_pnl` + independent probe exact match. |
| 5 | RF-004 or RF-011 prematurely CLOSED | **Pass.** Both **IN PROGRESS**. RF-009 remains **CLOSED**. |
| 6 | OpenAPI / DI advertise legacy list without formal shocks | **Pass.** OpenAPI schema is `ScenarioWire` with `shocks[]`. DI still stores `StressScenario` internally; HTTP projects via `stress_to_wire` (correct boundary). |
| 7 | Frontend or other callers silently broken | **Pass.** No React GET list consumer; POST `scenarioPayload` path unchanged. |

## Issues

### Critical

None.

### Important

None.

### Minor

1. **Historical docs still describe dual-list semantics.**
   ADR 004 consequences, `docs/agents/HANDOFF_SCENARIO_WIRE_LEAD.md`, and
   older ROADMAP progress notes still say formal list lives only on
   `/scenarios/formal` and legacy GET is unchanged. Live OpenAPI + milestone /
   FINDINGS / brief for this slice are correct. Optional follow-up: amend ADR
   004 consequences for R0.4.2-C (default list = formal; `/formal` alias).

2. **Carry-forward: formal evaluate HTTP P&L still contract-only.**
   From R0.4.2-B Minor #1 — not introduced or worsened by this list-wire
   slice. Custom stress HTTP P&L parity remains automated; evaluate custom
   remains label/status assertions. Optional hygiene only.

## Residual / handoff

- **RF-004:** stays **IN PROGRESS**. List-wire converge for R0.4.2-C is ready
  to mark APPROVE. Residual dual StressScenario POST bodies on
  `/stress/custom`, `/evaluate/custom`, `/compare` (UI `scenarioPayload`)
  remain. Lead **should not** close RF-004 from this review alone unless
  accepting list-only converge as sufficient for the RF-004 exit bar.
- **RF-011:** stays **IN PROGRESS** (flat dict market storage / broader
  nesting; dual POST residual also named). Do not close.
- **RF-009:** stays **CLOSED** — do not reopen.
- Mark R0.4.2-C slice APPROVE in milestone / FINDINGS / brief when convenient.

## Reviewer note

No production code was changed. No commit was created.
