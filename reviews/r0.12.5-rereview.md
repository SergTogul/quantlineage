# R0.12.5 Re-review

Slice: Native ABI safety (RF-017 ABI slice), after the review-fix pass.
Workspace: `/Users/user/src/riskforge-mvp` working tree vs HEAD.
Prior review: `reviews/r0.12.5-independent-review.md` REQUEST CHANGES. Orchestrator: all five findings VALID.
Reviewer did not implement this work. Production files were not edited. Index, HEAD, and branch were not mutated. The only write is this file.

Do **not** close RF-017.

## Verdict: APPROVE

Remaining on the prior five items: **0 Critical / 0 Important**.

## Checks performed

- `git diff HEAD` on the requested paths. Confirmed `backend/native/include/risk_kernel_capi.h` exists (`??`, 1738 bytes).
- Read `ci.yml` native-smoke, `test_native_ci.py`, `kernel_test.cpp`, `risk_kernel_capi.cpp`, `risk_kernel_capi.h`, wrap/tight tests in `test_native_kernel.py`, `portfolio_scenarios_flat_into`.
- Did not treat `reviews/r0.12.5-report.md` as proof.

```bash
cd /Users/user/src/riskforge-mvp/backend
g++ -std=c++20 -O2 -pthread -I native/include \
  native/tests/kernel_test.cpp native/src/risk_kernel_capi.cpp \
  -o /tmp/kernel_test_rereview && /tmp/kernel_test_rereview
# risk_kernel_ok, exit 0

g++ -std=c++20 -O2 -pthread -I native/include \
  native/tests/kernel_test.cpp -o /tmp/kernel_test_old
# undefined _riskforge_kernel_abi_version, _riskforge_portfolio_scenarios; exit 1

PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_native_kernel.py tests/test_native_ci.py
# 17 passed in 35.09s
```

Attacks (copies under `/tmp` only): drop capi from the smoke `g++` line in memory; delete the overflow guard; delete the length predicate and run the tight `n_out` case (plain + ASan).

## Attack on prior items

| # | Attack | Result |
|---|---|---|
| 1 | Does GHA native-smoke `g++` list both TUs? Local two-TU link? | **Yes.** `ci.yml` L74–76: `kernel_test.cpp` + `risk_kernel_capi.cpp`. Two-TU binary prints `risk_kernel_ok`. One-file list still fails to link. |
| 2 | Does `test_native_ci.py` fail if the capi TU is dropped from **that** step? | **Yes.** Helpers take `g++` + `kernel_test.cpp` and exclude `-shared`. Dropping capi from the smoke argv (shared-lib line still names it) leaves `risk_kernel_capi.cpp` absent from that command; the assert fails. |
| 3 | Wrap-sized counts → `ERR_LENGTH`, sentinel unchanged, no huge alloc? Delete overflow guard → tests fail? | **Yes.** Pytest + `kernel_test.cpp` pass `SIZE_MAX/5+1` with `n_exposure_doubles=4` and `SIZE_MAX/4+1` with `n_shock_doubles=0` / `n_out=wrap_shocks` on 5/4/1-double buffers. Live path: `rc==ERR_LENGTH`, sentinel `99`. Wrap math: `*5→4`, `*4→0`. Unguarded `/tmp` copy: SIGSEGV (exit 139), not `ERR_LENGTH`. |
| 4 | Tight-buffer mismatch: skipped length predicate overruns (ASan), not only wrong `rc`? | **Yes.** `n_shocks=2` / `n_out=1` / 1-double `out`; `n_exposures=2` / 5-double buffer. `/tmp` copy with the equality check deleted: `rc=0` and `out[0]` written (`-159`); ASan `stack-buffer-overflow` WRITE past `out`. Tests would fail. Inner walk still uses counts only; predicates now lock it. |
| 5 | Header in the change set (not missing from the commit-able tree)? | **On disk.** `backend/native/include/risk_kernel_capi.h` is present and `#include`d. Still `??` (see leftover). |
| 6 | `kernel_test.cpp` NULL shocks/out and `n_shock_doubles`? | **Yes.** `n_shock_doubles=3`; NULL shocks; NULL out; wrap + tight cases. |
| 7 | Happy-path `KERNEL_ABI_*` still passing? | **Yes.** 17 passed (`test_native_kernel.py` + `test_native_ci.py`), 35.09s. |

## Findings (prior five items)

### 1. CLOSED — Critical — GHA native smoke builds both TUs

- Files: `.github/workflows/ci.yml` L65–76; `backend/tests/test_native_ci.py`
- What changed: Smoke `g++` lists `native/tests/kernel_test.cpp native/src/risk_kernel_capi.cpp`. Comment matches pytest/README.
- Why it is closed: Reproduced two-TU link. Old one-file argv still undefined-symbol.

### 2. CLOSED — Important — Wrap guard and tight-buffer mismatch are tested

- Files: `backend/tests/test_native_kernel.py` (`test_native_wrap_sized_counts_fail_closed`, `test_native_tight_buffer_mismatch_fails_closed`); `backend/native/tests/kernel_test.cpp` L104–135
- What changed: Wrap-sized counts expect `ERR_LENGTH` + sentinel `99` with no wrap-sized allocation. Tight `n_out` / `n_exposures` cases make a skipped predicate an actual overrun.
- Why it is closed: Deleting L32–35 SIGSEGVs the wrap call. Deleting the length equality returns `OK` and ASan-aborts on the 1-double `out`. Suggested fix from the prior review is in.

### 3. CLOSED — Important — `risk_kernel_capi.h` exists in the working-tree change set

- Files: `backend/native/include/risk_kernel_capi.h` (`??`)
- What changed: Header is on disk (ABI, strides, error macros, prototype). Both TUs include it.
- Why it is closed: Orchestrator required the file in the same change. It is present. Still untracked until commit (leftover, not a missing file).

### 4. CLOSED — Minor — C++ smoke covers NULL shocks/out and `n_shock_doubles`

- Files: `backend/native/tests/kernel_test.cpp` L92–96, L143–151
- Why it is closed: Mirrors the three Python null cases and the shock-double mismatch.

### 5. CLOSED — Minor — Native-smoke `g++` argv is locked

- Files: `backend/tests/test_native_ci.py` (not `test_pr_*_ci.py`; equivalent lock, collected by `pytest -q`)
- Why it is closed: Dropping the capi TU from the smoke step fails the suite. Shared-lib line is excluded via `-shared`.

## Leftover bypasses (not Critical/Important on the prior five)

1. **Header still `??`.** `git diff HEAD` still omits it. `git add -u` would ship a tree that does not compile. Stage `backend/native/include/risk_kernel_capi.h` with the rest.
2. **`test_native_ci.py` is a YAML string scan.** A `# risk_kernel_capi.cpp` comment on the smoke `g++` line would pass the contract test. The actual GHA link would still fail (undefined symbols). Same class of lock as `test_pr_*_ci.py`.
3. **Inner `portfolio_scenarios_flat_into` still indexes from counts only.** Declared `n_*_doubles` bound the walk only via the C ABI predicates. Those predicates are now wrap- and tight-buffer tested.

## RF-017

ABI version / error / length is one RF-017 bullet. Contiguous NumPy, per-call thread setup, and “more C++ is not the priority” remain. **Do not close RF-017.**

## Finding counts (prior five items)

| Severity | Remaining VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 0 |

**Verdict: APPROVE**

---

## Orchestrator adjudication (2026-09-03)

Prior five findings closed. Leftover: stage `risk_kernel_capi.h` explicitly (do not `git add -u`). YAML comment-blind contract test: VALID, deferred (same class as PR-FAST/PR-FULL). Do **not** close RF-017.
