# RF-007 close gate (R0.6.9, after reconstruction benches)

**Owner:** QA & Quant Validation Engineer  
**Date:** 2026-09-09  
**Branch:** `r0-core-remediation`  
**BASE:** `78c1d0e` (Task 7 APPROVE)  
**Finding:** RF-007  
**Recommendation:** **CLOSE**

This is an independent QA re-score. Implementer reports (Task 5 KEEP OPEN, Task 6/7 benches) were treated as unverified until re-read against on-disk tests and a fresh close-gate suite. No production/pricing/risk algorithm change. No SLA invented. This text is **not** a restamp of the rejected CLOSE at `970d669` (that CLOSE treated an unrecorded bench matrix as a P1 leftover).

## Recommendation

**CLOSE** RF-007. P0 required direction is honestly met (6 MET; process-chunk option A remains **PARTIAL** and is a **P1 residual**, same bound Task 4 authorized). Reconstruction, N×S *recording*, and numerical identity are not UNMET.

Acceptance matrix after Task 7 APPROVE: reconstruction builtin vs QuantLib **MET**; identity **MET**; N×S **PARTIAL** (N=100 **MET** in nightly; N=1k **UNMET** — not a fake 1k SLA, not automatic KEEP OPEN); wall / scenarios/sec / warm vs cold **MET** at 10×50 (finite numbers, not floors); peak RSS **PARTIAL** (isolated child `ru_maxrss` — documented measurement limit, **not** scored MET).

Controller must not treat CLOSED as final until this task’s independent review **APPROVE**.

## Required direction (7 bullets)

| # | Required direction | Score | Evidence |
|---|---|---|---|
| 1 | stream/chunk scenarios | **MET** | `backend/app/risk/scenarios.py` `iter_shocked_snapshots` / `iter_historical_shocked_snapshots` are generators. `backend/app/risk/historical.py` `full_revaluation_pnl_series` and `backend/app/risk/var.py` `_full_reval_position_pnls` consume the iterator; list helpers remain wrappers. R0.6.2. |
| 2 | build scenario market state once | **MET** | RF-006 CLOSED. One-pass `MarketSnapshot.apply`; Stress `apply_scenario` count == S, not P×S. Full-reval iterates one shocked snapshot at a time. |
| 3 | reuse instrument structures or relinkable market handles where safe | **MET** | R0.6.3 APPROVE (`reviews/sdd-briefs/task-1-r0.6.3-review.md`). Scalar equity/FX options reuse `VanillaOption` / process via live `SimpleQuote` handles. Surfaces / snapshot curves / non-scalar instruments still rebuild — documented safety remainder. Evidence: `backend/tests/test_quantlib_reuse.py` (re-run in close-gate suite). |
| 4 | parallelize QuantLib scenario partitions by process, not in-process threads | **PARTIAL** | R0.6.5 APPROVE choice **B** (`reviews/sdd-briefs/task-4-r0.6.5-review.md`). HEAVY full-reval runs in the Compose `worker` OS process; in-process QuantLib stays `_QL_PROCESS_LOCK` serialized (`backend/app/pricing/quantlib.py`). Intra-run scenario-block `ProcessPoolExecutor` (option A) is **not** implemented (`backend/tests/test_r065_process_partition.py`). Extra replicas are **job** parallelism, not scenario-index partitioning inside one FULL_REVALUATION. **P1 residual.** |
| 5 | move large full-revaluation jobs to `RiskRun` | **MET** | RF-015 CLOSED. `classify(..., methodology=FULL_REVALUATION)` is HEAVY (`backend/app/api/execution_class.py`); refuse-inline when the gate is on; worker still executes FULL_REVALUATION. Evidence: `backend/tests/test_r065_process_partition.py`. |
| 6 | keep interactive LINEAR / DELTA_GAMMA as the fast default | **MET** | `backend/tests/test_var_methodology.py::test_default_methodology_is_delta_gamma` and `test_service_and_api_default_methodology`. LINEAR / Δ-Γ stay interactive; FULL_REVALUATION is HEAVY. |
| 7 | remove/disable caches that make unique-shock workloads slower | **MET** | R0.6.4 APPROVE (`reviews/sdd-briefs/task-2-r0.6.4-review.md`). Unique-shock FULL_REVALUATION enters `bypass_valuation_lru` (`backend/app/risk/historical.py`, `backend/app/risk/var.py`). Evidence: `backend/tests/test_pricing_anti_cache.py`. |

**Required-direction summary:** 6 MET, 1 PARTIAL (option B job process, not intra-run chunks). The PARTIAL bullet is the documented bound, not a missing P0 architecture. Same class of residual Task 4 already accepted as post-bench P1.

Contribution scaling (R0.6.6 APPROVE): family path is O(N) base `value` calls, not O(N×S×F) extra books. Joint full-reval remains O(N×S). Factor buckets under FULL_REVAL are Δ-Γ-plus-residual of the joint P&L.

## Acceptance evidence (benchmark matrix)

Recorded vs missing. **Do not set a fake universal SLA.** FINDINGS never set a 1k wall/RSS floor.

| Item | Score | What is recorded | Still missing / residual |
|---|---|---|---|
| N trades × S scenarios | **PARTIAL** | Identity **1×120** builtin (`benchmarks/run_full_reval_bench.py` `N_POSITIONS`/`N_OBS`). PR reconstruction **10×50** European options (`RECONSTRUCTION_N_*`). Cash-equity acceptance **10×50**. Nightly **N=100×50** (`backend/tests/test_nightly_full_reval_n100.py`, `.github/workflows/nightly.yml` job `full-reval-n100`; skip unless `QUANTLINEAGE_NIGHTLY=1`; not in PR-FULL `needs:` per `backend/tests/test_pr_full_ci.py`). Independent re-run of nightly test: **1 passed**. | N=1k **UNMET**. S=750/1k **UNMET** (FINDINGS “where practical”). Not a close blocker: N=100 nightly is recorded; no fake 1k SLA. |
| wall time | **MET** | `wall_ms` / `wall_ms_cold` / `wall_ms_warm` on reconstruction and acceptance rows. Pytest asserts finite, not a floor (`backend/tests/test_full_reval_bench.py` `_assert_recorded_finite`). | Host-controlled 1k walls unrecorded. Not an SLA. |
| peak RSS | **PARTIAL** | Reconstruction `peak_rss_kib` from a **subprocess per impl** (`--isolated-impl` / `_spawn_isolated_impl`). Tests assert `rss_isolated` and distinct `rss_pid` when QuantLib is available (`test_full_reval_bench_reconstruction_rss_is_isolated_per_impl`). | Still process-lifetime `resource.getrusage(RUSAGE_SELF).ru_maxrss` **inside that child** (interpreter + import + run). Not incremental kernel RSS. **Must not be scored MET.** Cash-equity `acceptance.quantlib.peak_rss_kib` remains cumulative in the parent (R0.6.7 path). |
| scenarios/sec | **MET** | `scenarios_per_sec` from cold `wall_ms`. No `throughput` key. Source grep rejects comparison floors. | Not an SLA. |
| built-in vs QuantLib | **MET** | Reconstruction book is `EuropeanOptionPosition` with live spot/vol/rate/div (`reconstruction_book`). Harness **rejects** P&L that matches cash-equity `quantity * spot`. Per-engine checksums: builtin `3148a41a0b3b4bb515c75d07991a96ecc186fb9d2294ccb20347df8f5322526e`, QuantLib `0594ecd65f68e33a800dbf5c331ead44b1fd353dadb198591721cc445f745eef`. P&L gap at option-match `rel=2e-3` (`test_full_reval_bench_reconstruction_records_pnl_gap`). Skip-or-run if QuantLib missing; `QUANTLINEAGE_REQUIRE_QUANTLIB=1` fail-closed. | Cash-equity `acceptance` path is still `qty*spot` (R0.6.7); that path is **not** the reconstruction cell. |
| warm vs cold | **MET** | Two `wall_ms` on the same engine/fixture in `_reconstruction_row` / `_engine_row`. Warm P&L must allclose cold at atol `1e-12`. Isolated reconstruction child is first use of that impl in that process. | Not a process-cold of the parent interpreter. Recorded, not SLA. |
| identical numerical results to pre-change goldens | **MET** | Close-gate re-run: **96 passed** (see Commands). R0.6.1 checksum `6602fa6906f2579f5c89af72a41ab274c07650234fff69387bc2202b5a40534f`. Cash-equity 10×50 `a28cf4ee6199bf40da3f2598f4241fc85fa4adc4f86bccbbd97e4938047d7537`. Reconstruction pins as above. Goldens: `backend/tests/test_full_reval_golden.py`. | n/a |

## Why CLOSE (and why this is not `970d669`)

R0.6 exit: full revaluation may stay expensive if scaling is **explainable**, **benchmarked**, **bounded**, and **appropriate for a risk-run job**.

| Exit word | Judgment |
|---|---|
| explainable | **Yes.** O(N×S) joint `PricingEngine.value` + O(N) Greeks. Not O(N×S×F). Not LRU hash/get/put on unique snaps. Not list-materializing all shocked markets. |
| benchmarked | **Yes** at PR-safe 10×50 reconstruction (European options) plus 1×120 / 10×50 cash-equity identity, with N=100×50 recorded in nightly. Peak RSS remains a **PARTIAL** measurement, not a MET claim. |
| bounded | **Yes.** HEAVY FULL_REVALUATION is a `RiskRun` job; HTTP refuses inline when the gate is on. |
| appropriate for a risk-run job | **Yes.** Interactive default remains DELTA_GAMMA. |

The rejected CLOSE (`970d669`) closed while peak RSS / scenarios/sec / builtin vs QuantLib were **UNMET** and N×S / wall / warm-cold were only **PARTIAL** at 1×120 builtin. Task 5 correctly KEEP OPEN’d. Task 7 APPROVE recorded the missing reconstruction, isolated RSS, and N=100 nightly. Those cells are no longer UNMET. N=1k remaining UNMET does not reopen P0: FINDINGS never set a 1k SLA.

Would this gate defend CLOSE to a second reviewer? **Yes**, provided peak RSS stays **PARTIAL** and option A / N=1k stay named P1 residuals — not relabeled MET.

## P1 residuals (not blocking close)

1. **Intra-run scenario-block process partition (option A).** Not implemented. Scale-out remains extra Compose `worker` processes claiming distinct RiskRuns. Do not add unused multiprocessing without exact P&L identity vs serial and a new ADR.
2. **Peak RSS measurement limit.** Isolated per impl; still process-lifetime `ru_maxrss` in the child. Documented PARTIAL. Not MET.
3. **N=1k (and S=750/1k) unrecorded.** Optional same skip rule; FINDINGS never set a fake 1k SLA. Do not invent host floors from recorded `wall_ms`.

Documented remainders (not P1 of this finding):

- QuantLib vol surfaces, snapshot curves/key-rate handles, and non-scalar instruments still rebuild per valuation (R0.6.3 safety).
- Reverse-stress / hierarchy `value()` on unique snaps still uses the NPV LRU unless they enter `bypass_valuation_lru` / `shocked_value` (different execution class; R0.6.4).
- FULL_REVAL factor *bucket* amounts on nonlinear books are Δ-Γ-plus-residual, not isolated reval (R0.6.6; portfolio VaR/ES identity unchanged).
- QuantLib reconstruction checksum is pinned to repo `QuantLib>=1.43`; a different patch may need a pin update.
- Nightly job sets `QUANTLINEAGE_PRICING_ENGINE: builtin` even though engines are constructed directly (Task 7 review Minor; not a matrix miss).

## Commands executed

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_full_reval_bench.py \
  tests/test_full_reval_golden.py \
  tests/test_quantlib_reuse.py \
  tests/test_pricing_anti_cache.py \
  tests/test_contribution_reuse.py \
  tests/test_r065_process_partition.py \
  tests/test_var_methodology.py \
  tests/test_es_contributions.py \
  tests/test_scenario_attribution.py \
  tests/test_nightly_ci.py \
  tests/test_pr_full_ci.py

QUANTLINEAGE_NIGHTLY=1 PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_nightly_full_reval_n100.py
```

## Results

- Backend focused close-gate suite: **96 passed**, 1 pre-existing Starlette `TestClient` deprecation warning, 12.18s.
- Nightly N=100 reconstruction (this host, `QUANTLINEAGE_NIGHTLY=1`): **1 passed**, 5.45s.
- Frontend: n/a (docs-only).
- QuantLib: reuse + reconstruction gap tests included; no methodology change.
- C++: unchanged.
- All applicable/affected suites required by this docs gate: **yes**.
- Unexplained failures or skips: **none**.
- CI: not started from this subtask (docs commit only).

## Follow-up

- **Owner:** independent Task 8 reviewer, then Lead Architect.
- **Requested action:** APPROVE or KEEP OPEN this close gate. Controller applies CLOSED only on APPROVE.
- **Blocking?:** no for further P0 architecture; yes for declaring the finding closed until review APPROVE.
