# R0.10.3 Remaining HEAVY Routes Independent Review

Slice: leftover RF-015 / R0.10.3 — apply the existing `reject_inline_heavy`
gate to remaining HEAVY handlers in `app/api/risk.py`. Do not treat the
implementer’s “HEAVY `/risk/*` refuse” language as proof that every HEAVY
route is off the request thread.
Workspace: uncommitted working tree vs HEAD on `/Users/user/src/quantlineage`
(`r0-core-remediation` @ `c51ac07`).
Implementer report: `reviews/r0.10.3-heavy-routes-report.md` (claims,
not proof).
Plan: `reviews/REMEDIATION_MILESTONE.md` § R0.10.3.
Finding: `reviews/FINDINGS.md` RF-015 — Status **IN PROGRESS**. Compose
dashboard 400 and ungated stress / attribution / limits are leftovers.
Do not close.

Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. The only write
is this file.

Other dirty-tree work (Builtin terms, artifact VaR, `builtin.py`,
`hierarchy.py`, `portfolio_service.py`) was not scored.

## Verdict: APPROVE

## Checks performed

- Read the implementer report as claims only; read § R0.10.3, RF-015
  (acceptance: a dashboard load does not execute redundant full
  valuations of the same book and snapshot), and the hunt list below.
- Diffed vs HEAD. Scored hunks only:
  - `backend/app/api/risk.py` (`reject_inline_heavy` at the start of
    `/var`, `/es`, `/var/compare`, `/what-if`, `/hierarchy`, `/query`,
    `/contributors`)
  - `backend/tests/test_backpressure.py` (leftover HEAVY HTTP refuse +
    INTERACTIVE factors)
  - `reviews/r0.10.3-heavy-routes-report.md`
- Confirmed `backend/app/api/backpressure.py` and
  `backend/app/api/execution_class.py` have **no** diff vs HEAD.
- Confirmed `reviews/FINDINGS.md` and `reviews/REMEDIATION_MILESTONE.md`
  have **no** working-tree diff. RF-015 Status remains **IN PROGRESS**.
- Index empty. Branch still `r0-core-remediation`. This review did not
  commit.
- Read every handler in `risk.py`. Gate is the first statement before
  `service.*`. INTERACTIVE `/summary` (LINEAR / DELTA_GAMMA) and
  `/factors` are not gated. FULL_REVALUATION summary remains gated as
  before.
- Independently probed HTTP with `TestClient(app)` (not the helper
  alone): unset flags, `QUANTLINEAGE_EXTERNAL_WORKER=1`,
  `QUANTLINEAGE_HEAVY_INLINE=0`, and both flags together
  (`EXTERNAL_WORKER=1` + `HEAVY_INLINE=1`). Dual-mount `/api/v1` and
  legacy. In-memory only; no production-file edits.
- Ran the required commands (rerun, not the implementer’s log):

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_backpressure.py \
  tests/test_endpoint_execution_class.py \
  tests/test_dashboard_batch.py
# 32 passed, 1 warning (pre-existing Starlette/httpx TestClient deprecation)
# 8.51s
```

I did not rerun the implementer’s ruff / mypy, the full backend suite,
or CI.

## Hunt results

| # | Hunt | Result |
|---|---|---|
| 1 | With `QUANTLINEAGE_EXTERNAL_WORKER=1`, do `/risk/var`, `/es`, `/var/compare`, `/hierarchy`, `/contributors`, `/what-if`, `/query` still return 200 with computed payloads? | **No leftover compute on these routes.** Dual-mount `/api/v1` and legacy are all **400**. Body `{code: bad_request, message: Invalid request, details: {use: /risk/runs, route: POST /risk/<name>}}`. No `var_99` / `before` / `intent` / contributor rows. No `Location`. Gate runs before `service.*`. `/var?methodology=LINEAR` is also **400** (route is HEAVY). |
| 2 | LINEAR summary and `/risk/factors` still 200? | **Yes.** Under `QUANTLINEAGE_EXTERNAL_WORKER=1`, `POST /api/v1/risk/summary?methodology=LINEAR` and `DELTA_GAMMA` are **200** with `var_99` and the requested `methodology`. `/api/v1/risk/factors` and `/risk/factors` are **200** with 8 factor rows (`factor`, `exposure`, …). `POST /risk/limits/drilldown` (INTERACTIVE, other router) is also **200**. |
| 3 | HTTP 400 message is `Invalid request`? | **Yes.** Live bodies are `message=Invalid request`, `code=bad_request`. Reuses `PUBLIC_BAD_REQUEST_MESSAGE`. Not “Request failed”, not a new copy string. |
| 4 | Edit `backpressure.py` / `execution_class.py` / close RF-015 / enqueue a fake run? | **No on disk.** Those two modules have no diff. RF-015 Status is still **IN PROGRESS**. Refuse is `HTTPException` 400; empty headers; no `Location`, no run id, no `RiskRunWorker.submit`. `QUANTLINEAGE_HEAVY_INLINE=1` does not force-allow when the external-worker flag is on. |
| 5 | Gate INTERACTIVE routes by mistake? | **No.** LINEAR / Δ-Γ summary, `/risk/factors`, and `/risk/limits/drilldown` stay 200 under the gate. Only `methodology=FULL_REVALUATION` upgrades summary to the refuse path. |

## Acceptance criteria

| # | Criterion | Result |
|---|---|---|
| 1 | Named leftover HEAVY handlers in `risk.py` refuse request-thread compute when the gate is on | **Pass.** `/var`, `/es`, `/var/compare`, `/what-if`, `/hierarchy`, `/query`, `/contributors` refuse on both mounts. |
| 2 | INTERACTIVE LINEAR / Δ-Γ summary and `/risk/factors` stay synchronous | **Pass.** 200 with computed payloads under `QUANTLINEAGE_EXTERNAL_WORKER=1`. |
| 3 | Existing error envelope; message `Invalid request` | **Pass.** `code=bad_request`, `message=Invalid request`, `details.use=/risk/runs`. |
| 4 | Default unset still allows inline | **Pass.** Unset: all seven leftover routes **200** with computed bodies; FULL_REVALUATION summary and dashboard **200**. `test_dashboard_batch.py` is HTTP. Suite result: 32 passed. |
| 5 | Tests hit live HTTP, not only `heavy_inline_allowed()` | **Pass.** Leftover refuse cases, LINEAR `/var`, what-if, query, `HEAVY_INLINE=0` `/es`, and INTERACTIVE factors are `TestClient` POSTs against `app.main:app`. |
| 6 | No OpenAPI bijection change; no fake enqueue; no RF-015 close | **Pass.** `execution_class.py` and `backpressure.py` untouched. FINDINGS / milestone have no working-tree diff. 400 does not enqueue. |
| 7 | RF-015 / R0.10 not marked COMPLETE | **Pass.** Report Status **DONE** is the slice only and states RF-015 is not closed. This review did not edit FINDINGS / milestone. |

## Attack results

| # | Attack | Score |
|---|---|---|
| 1 | Gate on, `/var` `/es` `/var/compare` `/hierarchy` `/contributors` `/what-if` `/query` still 200 with computed payloads | **No.** 400 on both mounts; no VaR / ES / tree / contributor / what-if / query fields. |
| 2 | LINEAR summary or `/risk/factors` refused when the gate is on | **No.** Both still 200 with computed payloads. |
| 3 | New / wrong HTTP message (`Request failed`, “use the worker”, …) | **No.** Stable `Invalid request`. |
| 4 | Edit `backpressure.py` / `execution_class.py`, close RF-015, or refuse enqueues a fake run | **No on disk.** Map unchanged. Finding still **IN PROGRESS**. 400 does not enqueue. |
| 5 | INTERACTIVE routes accidentally gated | **No.** LINEAR / Δ-Γ summary, factors, limits drilldown stay 200. |
| 6 | Tests only assert `heavy_inline_allowed()` while handlers still compute | **No.** HTTP tests exist; independent probe matches them. |
| 7 | Default unset refuses leftover HEAVY / dashboard (breaks existing tests) | **No.** Unset 200; required suite 32 passed. |

## Leftover HEAVY still inline (not a fail)

These still return **200** with computed bodies when
`QUANTLINEAGE_EXTERNAL_WORKER=1`. They are not in this slice’s exclusive
`risk.py` set. That leftover keeps RF-015 **IN PROGRESS**.

| Route | Gate on (`EXTERNAL_WORKER=1`) | Hole for this slice? |
|---|---|---|
| `POST /risk/summary?methodology=FULL_REVALUATION` | **400** | **No** (already gated) |
| `POST /risk/dashboard` | **400** | **No** (already gated; Compose client leftover) |
| `POST /risk/var` (any methodology) | **400** | **No** |
| `POST /risk/es` | **400** | **No** |
| `POST /risk/var/compare` | **400** | **No** |
| `POST /risk/what-if` | **400** | **No** |
| `POST /risk/hierarchy` | **400** | **No** |
| `POST /risk/query` | **400** | **No** |
| `POST /risk/contributors` | **400** | **No** |
| `POST /risk/stress` | **200**, 5 rows | Leftover (other router) |
| `POST /risk/stress/evaluate` | **200**, `evaluations` / `worst_scenario` | Leftover |
| `POST /risk/stress/reverse` | **200**, `converged` / `required_shock` | Leftover |
| `POST /risk/stress/reverse/multi` | **200**, `shocks` / `factors` | Leftover |
| `POST /risk/attribution` | **200**, `residual` / `items` | Leftover |
| `POST /risk/attribution/demo` | **200**, `total_change` | Leftover |
| `POST /risk/change-attribution` | **200**, waterfall keys | Leftover |
| `POST /risk/limits` | **200**, 8 rows | Leftover |
| `POST /risk/runs` | enqueue destination | Not a refuse target |

### Residual (not a fail)

| Item | Why it is not a leftover hole for this slice |
|---|---|
| Compose `loadDashboard()` still POSTs `/api/v1/risk/dashboard` | Already-gated HEAVY batch. Demo Compose dashboard will 400 until a later slice serves a RiskRun or the client calls `/risk/runs`. Documented. Do not close RF-015. |
| Ungated stress / attribution / limits | Live in `stress.py` / `attribution.py` / `limits.py`. Exclusive-file-out of this slice. Documented in the report. |
| FINDINGS RF-015 text still says “other HEAVY routes still inline; Compose UI still POSTs dashboard” | Still true after this slice. Implementer correctly did not edit FINDINGS. Lead Architect updates status after APPROVE. Do not close. |
| Parametrized leftover HTTP list omits some dual-mounts (`/es`, `/var/compare`, `/contributors` legacy; what-if / query only on `/api/v1`) | Same handlers. Independent probe: both mounts 400. Test gap, not a production hole. |
| INTERACTIVE factors test asserts status 200 only | Independent probe: 8 factor rows with `factor` / `exposure`. LINEAR summary live probe has `var_99`. |
| `QUANTLINEAGE_HEAVY_INLINE=1` does not override an external worker | Matches prior R0.10.3 contract. Live `/var` still 400. |

## Findings

### Critical

None. VALID count: **0**.

### Important

None that block APPROVE. VALID count: **0**.

### Minor

1. **Test module docstring over-broadens “HEAVY `/risk/*`”**
   - File: `backend/tests/test_backpressure.py` (module docstring)
   - The new wording says HEAVY `/risk/*` handlers refuse request-thread
     compute. Live probe: stress / attribution / limits still compute
     inline under `QUANTLINEAGE_EXTERNAL_WORKER=1`. The implementer report’s
     limitations section already lists those leftovers. Do not read the
     docstring as “HEAVY is off the thread.” VALID count: **1**.

## Recommendations

- Keep RF-015 **IN PROGRESS**. Next owner: Backend / API for remaining
  HEAVY stress / attribution / limits routers; Frontend for
  `loadDashboard()` → `/risk/runs`. Do not mark R0.10 COMPLETE on this
  slice alone.
- After this APPROVE, Lead Architect may update the RF-015 status line
  to name the newly gated `risk.py` routes. Do **not** close the finding.
- Do not treat `heavy_inline_allowed()` as proof. Future leftover slices
  must keep HTTP tests on the live routes, including both mounts.

## Assessment

**Ready to merge: Yes** (R0.10.3 leftover `risk.py` refuse-gate slice
only).

## Lead Architect adjudication (2026-09-04)

Accepted. Zero VALID Important. RF-015 stays **IN PROGRESS** — stress / attribution / limits still inline; Compose `loadDashboard()` still POSTs dashboard. This slice commits the remaining `risk.py` gates only.

The named leftover HEAVY handlers in `risk.py` refuse inline compute
when the gate is on. LINEAR / Δ-Γ summary and `/risk/factors` stay 200.
The 400 message is `Invalid request`. Default unset still allows inline.
Tests are live HTTP, not helper-only. `backpressure.py` and
`execution_class.py` are untouched. RF-015 is not closed. No fake run is
enqueued. Stress / attribution / limits and Compose dashboard remain
leftover — not a fail.
