# R0.5.1 Instrument Capabilities Independent Review

Slice: R0.5.1 leftover / RF-005 — static instrument capability registry (lookup-only; not a live pricing or history contract).
Workspace: `/Users/user/src/riskforge-mvp` working tree vs HEAD (`r0-core-remediation` @ `8df35eaecdb80069becf0a023f610ec103f53d5f`).
Implementer report: `reviews/r0.5.1-instrument-capabilities-report.md` (claims, not proof).
Plan: `reviews/REMEDIATION_MILESTONE.md` § R0.5.1.
Finding: RF-005 (must stay **open**; four-macro history is R0.5.3).
Reviewer did not implement this work. Production files were not edited. Index, HEAD, and branch were not mutated.

Scored only: `backend/app/pricing/instrument_capabilities.py`, `backend/tests/test_instrument_capabilities.py`, `reviews/r0.5.1-instrument-capabilities-report.md`. Other dirty-tree work (RiskRun spec on `models.py`, R0.5.4 demo-projection labels on `historical_data.py`, persistence/migration, demo historical tests) is out of scope.

## Verdict: APPROVE

Critical 0 · Important 0 · Minor 1

The new module is a frozen ten-row lookup. Independent comparison against `Position` discriminator literals, `InstrumentTerms` union literals, and `trade_cache_key` `_TRADE_CACHE_SCHEMAS` shows the **same ten families** and **no extra registry key**. `get_capability` has no default row: unknown strings raise `KeyError`; non-strings raise `TypeError`. Equity option / bond / FX option required kinds include spot, vol, and rates as claimed. AST imports are `__future__`, `dataclass`, and five `factor_types` classes — no `QuantLibPricingEngine` / `BuiltinPricingEngine`. `value()` in QuantLib and Builtin does not call `get_capability`. FINDINGS / milestone empty vs HEAD — RF-005 is still **IN PROGRESS**; R0.5 / R0.5.1 are not COMPLETE. Do not close RF-005.

## Checks performed

- `git diff HEAD --` `instrument_capabilities.py`, `quantlib.py`, `models.py`, `historical_data.py`. Capabilities file is untracked (new). `quantlib.py` empty vs HEAD; worktree blob hash matches `HEAD:` (`583f8dc5…`). `cache.py` empty vs HEAD; hash matches `HEAD:` (`c4603693…`). `models.py` dirty (+48/−2) but every hunk is `RiskRun` / `RiskRunView` spec fields (line 1482+). `historical_data.py` dirty (+75/−5) but every hunk is R0.5.4 `four_macro_demo` labeling. Neither dirty file mentions `instrument_capabilities` / `get_capability` / family rows.
- Empty `git diff HEAD --name-only` for `quantlib.py`, `builtin.py`, `factory.py`, `cache.py`, `test_quantlib_golden.py`, `test_quantlib_pricing.py`, `test_approx_pnl_units_golden.py`, `reviews/FINDINGS.md`, `reviews/REMEDIATION_MILESTONE.md`.
- Read on-disk registry, `get_capability`, `_TRADE_CACHE_SCHEMAS`, `InstrumentTerms` union, `Position` union, QuantLib overlay + session `isinstance` chains, Builtin `value()` chain, `RiskFactorEngine.calculate_typed`, RF-005 text, § R0.5 / R0.5.1, implementer report.
- Independent runtime probe (no production edits): family-set equality vs Position / InstrumentTerms / cache schemas; unknown / empty / alias / non-string keys; required kinds for the three attack families; AST imports; `value()` source does not mention this module.
- New test file: no `skip` / `xfail` / `approx`. Collect-only counts: capabilities 6, instrument terms 31, factor types 6 = **43**.
- Ran the requested commands. Did not execute GitHub Actions. Did not mutate index, HEAD, or branch.

```bash
cd /Users/user/src/riskforge-mvp && /usr/bin/git diff HEAD -- \
  backend/app/pricing/instrument_capabilities.py \
  backend/app/pricing/quantlib.py \
  backend/app/domain/models.py \
  backend/app/risk/historical_data.py
# instrument_capabilities.py: untracked (new)
# quantlib.py: empty
# models.py: RiskRun spec only (out of scope)
# historical_data.py: R0.5.4 demo labels only (out of scope)

cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_instrument_capabilities.py \
  tests/test_instrument_terms.py \
  tests/test_factor_types.py
# 43 passed in 0.75s
# collect-only: 6 + 31 + 6 = 43. No skips.
```

Implementer “43 passed” is not treated as proof; the suite was reran and collected independently. Count matches. The registry is not assumed to be a live contract: family coverage was checked against Position / InstrumentTerms / cache schemas, not against the test’s own hardcoded list.

TDD-red narrative in the report was not re-executed. The module is on disk now. HEAD has no `app.pricing.instrument_capabilities`; a pre-module import would raise `ModuleNotFoundError` as claimed.

Independent probe (import capabilities first; Builtin/QuantLib not yet in `sys.modules`):

| Check | Result |
|---|---|
| `Position` / `InstrumentTerms` / `_TRADE_CACHE_SCHEMAS` / `PRODUCTION_FAMILIES` / `_REGISTRY` | same ten ids; extra registry keys **empty**; missing vs each source **empty** |
| `get_capability("convertible_bond")` / `""` / `" "` / `"EQUITY"` / `"equity_option"` / `"default"` / `"*"` | `KeyError` `unknown instrument family: …` |
| `get_capability(None)` / `object()` / `123` | `TypeError` `unknown instrument family: <type>` |
| `_REGISTRY.get("nope")` / `__missing__` | sentinel / no `__missing__` (lookup has no default row) |
| `european_option` kinds | `EquitySpot`, `EquityVol`, `RateZero` |
| `bond` kinds | `RateZero`; maps include `rates` |
| `fx_option` kinds | `FXSpot`, `FXVol`, `RateZero` |
| AST imports | `__future__`, `dataclass`, `EquitySpot/EquityVol/FXSpot/FXVol/RateZero` only |
| `QuantLibPricingEngine.value` / `BuiltinPricingEngine.value` | no `get_capability` / `instrument_capabilities` |
| snapshot map names | all ⊂ `MarketSnapshot` fields |

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | A `trade_cache_key` / `InstrumentTerms` family is missing or catch-all | **No.** Live literals and `_TRADE_CACHE_SCHEMAS` are exactly the ten ids in `PRODUCTION_FAMILIES` and `_REGISTRY`. QuantLib overlay/session and Builtin `value()` handle the same ten classes and then raise. No `"*"` / `"default"` / empty-string row. `terms_from_position` already fail-closes on an eleventh type (`TypeError`); this table does not add a catch-all that would accept it. |
| 2 | Unknown family is accepted or returns a default row | **No.** `get_capability` is `isinstance(str)` then `_REGISTRY[family]` with `KeyError` re-raise. Probes: seven unknown strings all `KeyError`; three non-strings all `TypeError`. No `__missing__`. No public getter with a default. |
| 3 | Required factors for equity option / bond / FX option omit spot, vol, or rates | **No.** `european_option`: `EquitySpot`, `EquityVol`, `RateZero` plus maps `equity_spots`, `equity_vols`, `rates` (also `dividend_yields`, `vol_surfaces`). `bond`: `RateZero` plus `rates` (also `key_rates`, `curves`). `fx_option`: `FXSpot`, `FXVol`, `RateZero` plus `fx_spots`, `fx_vols`, `rates` (also `vol_surfaces`). Matches QuantLib overlay reads (`_required_equity_spot` / `required_equity_option_vol` / `_required_settlement_rate`; `required_continuous_zero`; `_required_fx_spot` / `required_fx_option_vol` / domestic+foreign rates). |
| 4 | Module imports `QuantLibPricingEngine` or `BuiltinPricingEngine` | **No.** AST and source have neither name and no `app.pricing.builtin` / `app.pricing.quantlib` / `QuantLib` import. Fresh import leaves those modules out of `sys.modules`. Adapter fields are the strings `"quantlib"` / `"builtin"` on every row. |
| 5 | RF-005 / R0.5 marked COMPLETE; goldens or pricing dispatch changed | **No.** FINDINGS and REMEDIATION_MILESTONE empty vs HEAD. RF-005 is **IN PROGRESS** (no `Status: CLOSED`). § R0.5 has no COMPLETE line; § R0.5.1 has no COMPLETE line. Report says do **not** close RF-005. `quantlib.py` / `builtin.py` / `factory.py` / golden pricing tests empty vs HEAD. Registry is not imported from `value()`. |

## Findings

### 1. VALID — Minor — New tests pin a hardcoded ten-name list and subset/either-exception checks

- Files: `backend/tests/test_instrument_capabilities.py` L15–26, L98–100, L139–145
- What's wrong: Coverage is compared to `PRODUCTION_FAMILY_TYPES` copied in the test file, not to `InstrumentTerms` / `_TRADE_CACHE_SCHEMAS`. Factor and map asserts are `<=` (registry may add extras). Unknown-string uses `pytest.raises((KeyError, TypeError), …)` so a string unknown that raised `TypeError` would still pass. Adapter asserts are `in {"quantlib", "builtin"}` rather than production=`quantlib` / reference=`builtin`.
- Why it matters: Today the three domain sources match the table, unknown strings raise `KeyError`, and adapters are assigned correctly. An eleventh family, or a swap of exception type for unknown strings, would not fail this file. This is a residual pin, not a live missing-family or silent-accept hole.
- Suggested fix: Derive expected ids from `InstrumentTerms` / `_TRADE_CACHE_SCHEMAS`; pin unknown strings to `KeyError` and non-strings to `TypeError`; pin adapter fields exactly. Optional. Not required to keep this leftover.

## REJECTED

1. **Rejected — a production family is missing or the table is a catch-all.** Position, InstrumentTerms, cache schemas, and both engine `isinstance` ladders are the same ten ids. Registry has no extra key.
2. **Rejected — unknown family accepted or default row returned.** Seven unknown strings `KeyError`; three non-strings `TypeError`; no `__missing__`.
3. **Rejected — equity option / bond / FX option omit spot, vol, or rates.** All three required-kind sets include the claimed types; maps include the corresponding snapshot fields. Cross-checked against QuantLib overlay reads.
4. **Rejected — module imports a pricing engine.** AST/source/sys.modules clean. Name strings only.
5. **Rejected — RF-005 or R0.5 marked COMPLETE; goldens or dispatch changed.** Docs unedited vs HEAD. RF-005 remains IN PROGRESS. Pricing/golden files empty vs HEAD.
6. **Rejected — close RF-005.** Report leaves it open. This slice does not add a per-factor historical panel. Do not close RF-005.

## Items checked and not findings

- The registry is **not** a runtime contract. Nothing in `value()`, `trade_cache_key`, `RiskFactorEngine`, or `historical_data` imports it. Plan § R0.5.1 asked for a coherent table, not a plugin or a `value()` wire. R0.5.2 already fail-closes unknown QuantLib types. Unused lookup is in scope for this leftover.
- No typed `DividendYield` or `IRVol` exists in `factor_types.RiskFactor`. Equity-option dividends and cap/floor/swaption IR vol are snapshot-map only (`dividend_yields`, `ir_vols`, `vol_surfaces`). That matches the typed taxonomy; it is not an omitted `EquityVol` / `FXVol` / `RateZero`.
- `cap_floor` / `swaption` required kinds are `RateZero` only. Engines also read IR vol via `required_ir_option_vol`. There is no `IRVol` class to declare. Maps include `ir_vols`. Attack 3 named equity option / bond / FX option only.
- `RiskFactorEngine.calculate_typed` still skips cap/floor/swaption and does not emit `RateZero` for options. Pre-existing four-macro / extractor hole (RF-005 / R0.5.3). The table is more complete than the extractor; it is not wired into it.
- `rate_spreads` exists on `MarketSnapshot` and is not in any row. Engines do not require it for the scored families.
- `models.py` Position union (line 448) has no hunk vs HEAD. Dirty hunks start at 1482 (`RiskRunCalculationConfig`). Not attributable to this slice.
- `historical_data.py` dirty text is R0.5.4 `projection="four_macro_demo"` / `MVP_AGGREGATE_FACTORS`. No capability symbols. Not attributable to this slice. RF-005 remains the four-macro history hole.
- Report TDD-red story is consistent with HEAD lacking the module but was not re-run here.
- Dirty RiskRun persistence / migration / demo-historical tests are other slices. Not scored.
- `reviews/FINDINGS.md` and `reviews/REMEDIATION_MILESTONE.md` were not edited. Do not add COMPLETE/CLOSED from this review.

## Leftover missing-family, silent-accept, or RF-005-close holes

On disk after this slice: **none** that drop a `trade_cache_key` / `InstrumentTerms` family, accept an unknown family, omit spot/vol/rates for equity option / bond / FX option, import a pricing engine, change goldens/dispatch, or close RF-005.

Contract leftover (Minor class; do not treat as proof the table is a live contract):

- New pytest would stay green if an eleventh domain family landed without a registry row, or if unknown strings started raising `TypeError` (Finding 1). Live lookup still fail-closes the current unknown strings.

Plan leftovers (do **not** fail R0.5.1; do **not** close RF-005):

- R0.5.3 per-factor historical observations (the actual RF-005 hole)
- R0.5.4 demo four-column projection labeling (dirty `historical_data.py` is that slice, not this one)
- R0.5.5 contribution semantics vs omitted factors
- Later: consume this table from factor extraction / snapshot construction once the typed history panel exists

Do not close RF-005. Do not mark R0.5 COMPLETE.

## Finding counts

| Severity | VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 1 |

**Verdict: APPROVE**

## Lead Architect adjudication (2026-09-04)

Accepted. Zero VALID Important. Minor deferred. RF-005 stays **IN PROGRESS** (four-macro history is R0.5.3). This slice commits the static registry only.
