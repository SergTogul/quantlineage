# Verdict: APPROVE

Remaining Critical/Important findings: **0**

This re-review is limited to the four previously reported Important findings. No already-approved golden was reopened.

## Important finding disposition

1. **Satisfied — overlapping QuantLib sessions.** The event-controlled test overlaps sessions from two distinct `QuantLibPricingEngine` instances. Engine A samples the process-global evaluation date only after engine B has entered its session, and observes B's 2024-06-14 date rather than A's 2020-01-02 date. This directly demonstrates cross-instance contamination.

2. **Satisfied — HistoricalRiskEngine ES99 membership.** The engine-facing test supplies 101 losses from 0 through 100 and pins `var_99 = 99.0` and `expected_shortfall_99 = 99.5`; the explicit inequality against 100.0 distinguishes `>= VaR` membership from strict `> VaR`.

3. **Satisfied — FULL_REVALUATION routing.** The option case exercises `HistoricalRiskEngine.calculate` under both `LINEAR` and `FULL_REVALUATION`, asserts their VaR99 results diverge, and ties the full-revaluation result to scenario P&L independently constructed as shocked PV minus base PV.

4. **Satisfied — exact key-rate DV01 units.** The swap golden pins `5_000_000 × 4.3 × 0.0001 = 2_150`, the +1bp 5Y shocked-PV P&L to 2,150, the off-pillar 10Y P&L to approximately zero, and distinguishes a 100bp decimal bump (`0.01`) from a `1.00` decimal-rate bump.

## Verification

Command:

`PYTHONPATH=. RISKFORGE_PRICING_ENGINE=quantlib .venv/bin/python -m pytest -q tests/test_var_es_golden.py tests/test_approx_pnl_units_golden.py tests/test_full_reval_golden.py tests/test_quantlib_process_state.py`

Result: **29 passed in 1.93s**.

Finding 6 remains rejected. This limited re-review found no new Critical/Important reason to reopen it.
