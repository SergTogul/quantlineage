# R0.2 Decompose Omitted-Market Independent Review

Slice: leftover RF-001 after hierarchy / stress / reverse explicit-market
APPROVE — production `ScenarioAttributionEngine.decompose` must require
an explicit `MarketSnapshot` and must not call `demo_market_snapshot` or
`DemoAggregateMarketDataProvider.snapshot` when `market` is omitted or
`None`.
Workspace: uncommitted work on `r0-core-remediation` (`660d49eb`) vs HEAD.
Implementer report: `reviews/r0.2-decompose-market-report.md` (claims, not
proof).
Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. RF-001 was not
closed. The only write is this file.

Other dirty production files on this workspace (typed `as_of`, pricing,
models, persistence, cache, PortfolioService `as_of`, benches, endpoint
classification) are out of scope for this slice and were not treated as
implementer evidence or scored.

## Verdict: APPROVE

`ScenarioAttributionEngine.decompose` fails closed. Omitting `market=` or
passing `market=None` raises
`ValueError("production risk calculation requires an explicit MarketSnapshot")`
— the same type and wording as VaR / hierarchy / stress. It no longer
calls `demo_market_snapshot` or `DemoAggregateMarketDataProvider.snapshot`.
Empty books still require an explicit snapshot before the empty-contribution
early return. `StressEngine.contributions` / `evaluate` still require a
snapshot and still pass it into `decompose`. `stress.py` has no working-tree
diff vs HEAD.

No leftover `market if market is not None else demo_market_snapshot(...)`
or `self.market_data.snapshot(portfolio)` remains on `decompose`.

Do **not** close RF-001. Typed `InstrumentTerms` models and compatibility
DTOs that still carry marks remain open.

## Checks performed

- Diffed `backend/app/risk/scenario_attribution.py`,
  `backend/tests/test_omit_market.py`, and
  `backend/tests/test_scenario_attribution.py` against HEAD. Confirmed
  `backend/app/risk/stress.py`, `reviews/FINDINGS.md`, and
  `reviews/REMEDIATION_MILESTONE.md` have no working-tree diff for this
  slice. `portfolio_service.py` dirty hunk is typed `as_of` (`t0`), not
  decompose / market threading.
- Read `require_explicit_market`, `ScenarioAttributionEngine.decompose`,
  helpers `_to_formal` / `_to_stress` / `_portfolio_pnl_by_position` /
  `_factor_isolated_pnl`, `StressEngine.contributions` / `evaluate`,
  RF-001 in `reviews/FINDINGS.md` / `reviews/REMEDIATION_MILESTONE.md`,
  and the prior hierarchy / stress review
  `reviews/r0.2-hierarchy-stress-market-independent-review.md`.
- Source-hunted `.decompose(`, `demo_market_snapshot`,
  `market_data.snapshot`, and `market if market is not None` under
  `backend/app/risk/` and tests. Production `decompose` callers: only
  `StressEngine.contributions`.
- Re-read the attack list. TDD red narrative in the implementer report
  was not treated as proof.
- Probed runtime (no production-file edits). Helper rebinds were
  in-memory only and restored.
- Ran:

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_scenario_attribution.py tests/test_omit_market.py
# 32 passed
```

I did not rerun QuantLib, C++, frontend, or CI.

## Acceptance criteria

| # | Criterion | Result |
|---|---|---|
| 1 | `decompose` omitted / `None` raises the stated `ValueError` | **Pass.** Probe and `test_scenario_attribution_decompose_omitted_market_raises` / `_market_none_raises`. Message is exactly `production risk calculation requires an explicit MarketSnapshot`. |
| 2 | Same `ValueError` via `require_explicit_market` | **Pass.** Diff replaces `market if market is not None else self.market_data.snapshot(portfolio)` with `require_explicit_market(market)`. |
| 3 | Empty book omitted / `None` still raises (before empty return) | **Pass.** Probe and `test_scenario_attribution_decompose_empty_book_omitted_market_raises`. Check is the first statement in `decompose`. |
| 4 | No `demo_market_snapshot` / `DemoAggregateMarketDataProvider.snapshot` on `decompose` | **Pass.** File no longer imports `demo_market_snapshot`. No `self.market_data.snapshot`. Monkeypatch of both helpers: omit / `None` still raised; call counts stayed 0. |
| 5 | Tests hit `decompose` directly, not only a wrapper | **Pass.** `test_omit_market.py` constructs `ScenarioAttributionEngine` and calls `decompose`. Existing `test_scenario_attribution.py` cases also call `decompose` with an explicit fixture / `demo_market_snapshot`. |
| 6 | Explicit snapshot still values | **Pass.** Probe: unit equity `portfolio_pnl ≈ -0.1`. Empty `MarketSnapshot(id="empty")` is accepted as explicit for an empty book. `test_hierarchy_stress_reverse_explicit_market_still_values` now also asserts `decompose`. |
| 7 | Stress path that calls `decompose` still passes a snapshot | **Pass.** `stress.py` unchanged vs HEAD. `contributions` does `market=require_explicit_market(market)` before `decompose`. `evaluate` requires a snapshot then passes `market=market`. Probe: omit / `None` on `contributions` / `evaluate` raise; explicit still values. |
| 8 | RF-001 not marked CLOSED | **Pass.** `reviews/FINDINGS.md` RF-001 remains **IN PROGRESS**. Milestone remaining line is typed InstrumentTerms / DTO marks, RF-001 **OPEN**. Neither file has a working-tree diff. This review did not edit those files. |
| 9 | Restoring inference would make omit tests fail | **Pass.** Rebinding `require_explicit_market` to `market if market is not None else demo_market_snapshot(BOOK)` priced omit-`market=` (`pnl ≈ -0.1`). Restoring the helper raised again. |

## Attack results

1. **Still `market if market is not None else demo_market_snapshot(...)`.** Gone. Replaced by `require_explicit_market(market)`. Source hunt: no fallback line, no `demo_market_snapshot` import, no `self.market_data.snapshot` in `scenario_attribution.py`.
2. **`market=None` still decomposes.** Default remains (`MarketSnapshot | None = None`) so the failure is a domain `ValueError`, not a `TypeError`. It does **not** decompose. Probe: omit and `market=None` raise the exact message, including empty book.
3. **Tests only hit wrappers; `decompose` still infers.** Not found. New omit / `None` / empty-book tests call `ScenarioAttributionEngine.decompose` directly. Happy-path tests in `test_scenario_attribution.py` were updated to pass `SAMPLE_MARKET` or `demo_market_snapshot(...)` into `decompose`, not a wrapper.
4. **RF-001 marked CLOSED.** Not found.
5. **Stress path that calls `decompose` no longer passes a snapshot.** Not found. `stress.py` has no slice diff. `contributions` still forwards `require_explicit_market(market)`. `evaluate` still passes the already-required snapshot.

## Leftover omitted-market inference on `decompose`

**None.**

Source hunt after the change:

- `backend/app/risk/scenario_attribution.py`: `decompose` calls
  `require_explicit_market` before `_to_formal` / `_to_stress` / empty
  return / pricing. Helpers take a required `MarketSnapshot`. No
  `demo_market_snapshot`. No `.snapshot(` after the change.
- Production caller: `StressEngine.contributions` already required and
  forwarded a snapshot at HEAD; `evaluate` still threads that snapshot.

Out of this slice (not scored):

- `LimitEngine` / `contributors_for_metric` still type
  `market: MarketSnapshot | None = None` and forward `None`. They fail
  when they reach `StressEngine.run` (or VaR/ES); they do not infer.
- `RiskFactorEngine`, `SensitivityEngine`, and P&L attribution engines
  still infer via `market_data.snapshot`. Those are not `decompose`.
- `PricingEngine.shocked_value` / `value_portfolio` still accept optional
  `market` with a demo-adapter fallback. `decompose` does not take that
  path: it passes the required snapshot into `value` / `shocked_value`.
- `DemoAggregateMarketDataProvider` remains an unused constructor
  attribute on `ScenarioAttributionEngine` (same pattern as
  `StressEngine` / `ReverseStressEngine`). `snapshot` is not invoked.

## Findings

None that change the verdict.

### Items checked and not findings

- `require_explicit_market` is a thin `is None` guard. An empty
  `MarketSnapshot(id="empty")` is explicit and accepted. Missing
  *factors* remain a pricer `MissingMarketDataError`.
- Keeping `market=None` on the signature is the claimed contract (domain
  `ValueError` with the same “explicit MarketSnapshot” wording as
  VaR / hierarchy / stress). It is not leftover inference.
- `test_omit_market.py` / `test_scenario_attribution.py` use
  `demo_market_snapshot` only on explicit happy paths. That is the named
  demo helper, not production inference.
- Unused `self.market_data = DemoAggregateMarketDataProvider()` is dead
  construction, not a silent default on `decompose`.
- `FINDINGS.md` RF-001 remaining text still lists
  “`ScenarioAttributionEngine.decompose` still infers if called
  directly.” That line is stale relative to this slice and was correctly
  left unedited. Stale remaining-text is not a close of RF-001.
- I did not close RF-001. `decompose` omitted-`market=` inference is
  done; typed InstrumentTerms / compatibility DTO marks remain.

## REJECTED items (implementer report)

- **TDD red count as proof.** Not used. Diff, source hunt, mutation
  probes, and pytest are the evidence. The red narrative is consistent
  with the pre-change `self.market_data.snapshot(portfolio)` fallback
  but is not accepted as verification.

## Finding counts

| Severity | VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 0 |

REJECTED implementer claims: 1 (TDD red as proof).

**Leftover omitted-market inference on `decompose`: none.**

**Verdict: APPROVE**

Do **not** close RF-001.

---

## Orchestrator adjudication (2026-09-03)

Verdict stands **APPROVE**. Do **not** close RF-001 (typed InstrumentTerms / DTO marks remain).
