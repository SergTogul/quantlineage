# R0.4.3-A Shock Units Independent Review

Slice: R0.4.3-A — Canonical shock-unit conversion helpers (RF-004; no numerical change).
Workspace: `/Users/user/src/riskforge-mvp` branch `r0-core-remediation` @ `f898a299629d7082131db0bdbad8b02257259c17`.
Implementer report: `reviews/r0.4.3-a-shock-units-report.md` (claims only).
Architecture brief: `reviews/r0.4-architecture-brief.md` (context; scored if contradicting code).
Reviewer did not implement this work. Production files were not edited. Index,
HEAD, and branch were not mutated. The only write is this file. RF-004 was not
closed.

Scored only:

- `backend/app/risk/shock_units.py`
- `backend/app/risk/historical.py` (`approximate_pnl_series` wiring only)
- `backend/app/risk/sensitivities.py` (rate bump wiring only)
- `backend/tests/test_shock_units.py`
- `reviews/r0.4.3-a-shock-units-report.md`
- `reviews/r0.4-architecture-brief.md` (context / contradiction check)

Unrelated dirty tree (BlockHelp, native tests, README, etc.) ignored.

## Verdict: APPROVE

Critical 0 · Important 0 · Minor 3

Helpers exist for bp↔decimal and relative-vol→vol-points; the module docstring
reproduces the brief’s frozen dual vol conventions (`MarketSnapshot.bump`
relative vs SensitivityEngine absolute FD). `approximate_pnl_series` calls
`relative_vol_move_to_vol_points` (no inline `* 100` on that path).
SensitivityEngine parallel and key-rate bumps call `bps_to_decimal_rate` (no
inline `/ 10_000`). Snapshot vol bump remains relative; SensitivityEngine vol
FD remains absolute add. Wrong-scale pins fail equality; goldens still pass.
FINDINGS RF-004 stays **IN PROGRESS**. No inventing of risk numbers.

## Checks performed

- Read implementer report as claims only; read architecture brief frozen table
  and R0.4.3-A slice goal; read FINDINGS RF-004 status.
- Diff vs HEAD for scored production wiring:
  - `historical.py`: import + `vol_points = relative_vol_move_to_vol_points(...)`
    replacing `np.asarray(vol_pct) * 100.0` inside `approximate_pnl_series` only.
  - `sensitivities.py`: import + `_bump_parallel_rate` / `_bump_key_rate` use
    `bps_to_decimal_rate` replacing `bps / 10_000.0`.
  - New: `shock_units.py`, `test_shock_units.py`, report, brief.
- Confirmed `MarketSnapshot.bump` EquityVol/FXVol still
  `vol * (1.0 + amount)` (relative); SensitivityEngine `_bump_equity_vol` /
  `_bump_fx_vol` still `vol + absolute` with default `vol_bump=0.01`.
- Confirmed FINDINGS.md RF-004 line unchanged vs HEAD:
  `Status: **IN PROGRESS** (R0.1.3 unit goldens; production unification is R0.2/R0.4)`.
  No RF-004 CLOSED edit in this tree.
- Independent parity probe (scale-only; no invented P&L): helpers equal the
  pre-helper formulas; wrong scales diverge; `approximate_pnl_series` body has
  no `* 100` conversion; sensitivities source has no `bps / 10_000`.
- Leftover hunt: sibling `historical.py` `_panel_linear_contribution` still
  `move * 100.0` (out of narrow wiring scope; see Minor #1). Broader
  `scenario_model` / `scenarios` / `attribution` / `es` / `reverse_stress`
  inline conversions match the report’s R0.4.3-B deferral.
- Brief table matches `shock_units` docstring; no brief↔code contradiction on
  dual vol or rate conventions.
- Reran the required pytest command. Did not execute CI. Did not mutate index,
  HEAD, or branch.

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_shock_units.py \
  tests/test_approx_pnl_units_golden.py \
  tests/test_sensitivities.py
# 26 passed in 1.49s
# collect-only: 26 tests collected. No skips/xfails.
```

Implementer “26 passed” is not treated as proof; the suite was rerun here.

Independent probe:

| Check | Result |
|---|---|
| `bps_to_decimal_rate(1.0)` | `== 1.0/10_000`; `!= 0.01` |
| `relative_vol_move_to_vol_points([0,0.01,-0.02,0.25])` | `== *100` array |
| `approximate_pnl_series` body | helper call; no inline `* 100` |
| SensitivityEngine rate bumps | both sites use `bps_to_decimal_rate`; no `/ 10_000` |
| Snapshot EquityVol/FXVol bump | relative `*(1+amount)` |
| SensitivityEngine equity/FX vol bump | absolute `+ absolute` |
| FINDINGS RF-004 | still **IN PROGRESS** |
| `_panel_linear_contribution` | leftover `move * 100.0` (Minor) |

## Attack scores

| # | Attack | Score |
|---|---|---|
| 1 | Helpers missing or wrong scale (bp-as-%, ÷100, forgotten ×100) | **Pass.** Unit tests pin correct values and assert wrong scales fail equality. |
| 2 | `approximate_pnl_series` still inline `* 100` / numerical drift | **Pass.** Diff is helper swap only; goldens pass; probe parity with `* 100`. |
| 3 | SensitivityEngine rate bumps still inline `/ 10_000` | **Pass.** Both parallel and key-rate paths converted. |
| 4 | Silent change to snapshot relative vol or SE absolute vol FD | **Pass.** Unchanged semantics; dual conventions documented in module + SE docstring. |
| 5 | RF-004 closed prematurely / FINDINGS edited | **Pass.** Still IN PROGRESS; FINDINGS clean vs HEAD for RF-004. |
| 6 | LLM-invented risk numbers in helpers/tests | **Pass.** Pure scale constants and equality pins only. |
| 7 | Same-file leftover `* 100` in panel approx path | **Partial.** Present; out of scored wiring scope → Minor #1. |
| 8 | Brief contradicts code on dual vol | **Pass.** Brief and `shock_units` docstring agree; code preserves both. |

## Findings

### Critical (Must Fix)

None.

### Important (Must Fix before APPROVE)

None.

### Minor

#### 1. VALID — Minor — Same-file panel path still inlines `×100`

`_panel_linear_contribution` in `historical.py` still does
`float(valuation.vega) * (move * 100.0)` while claiming units match
`approximate_pnl_series`. Out of R0.4.3-A’s scored wiring
(`approximate_pnl_series` only) and consistent with the report’s “other call
sites later” posture, but the report’s leftover list omits this sibling.

- Suggested fix (R0.4.3-B or follow-up): route through
  `relative_vol_move_to_vol_points(move)`.

#### 2. VALID — Minor — Golden comments still describe inline literals

`test_approx_pnl_units_golden.py` header still says `vol_points = vol_pct * 100`
and `bps / 10_000`. Behavior is unchanged; comments are stale relative to the
helpers.

- Suggested fix: point comments at `shock_units` helpers when convenient.

#### 3. VALID — Minor — No direct import/wiring pin in new tests

`test_shock_units.py` pins helper math only. Wiring is verified by code review
+ existing goldens, not by an assert that `approximate_pnl_series` /
rate-bump helpers call the module.

- Suggested fix (optional): one source or monkeypatch pin that a wrong inline
  restore would fail without relying solely on goldens.

## REJECTED implementer claims

None material. Scope, dual-vol non-unification, numerical no-change, leftover
deferral to R0.4.3-B, and “RF-004 remains IN PROGRESS” match the tree. The
leftover inventory understates the same-file panel path (Minor #1), not a
false numerical claim.

## Must-verify matrix

| # | Requirement | Result |
|---|---|---|
| 1 | Helpers bp↔decimal + relative-vol→vol-points; dual vol documented | **Met** |
| 2 | `approximate_pnl_series` uses helper; goldens unchanged numerically | **Met** |
| 3 | SensitivityEngine rate bumps use `bps_to_decimal_rate` | **Met** |
| 4 | No semantic change to snapshot relative vol / SE absolute vol FD | **Met** |
| 5 | Wrong-scale tests fail equality; goldens pass | **Met** (26 passed) |
| 6 | RF-004 not closed in FINDINGS | **Met** |
| 7 | No inventing of risk numbers | **Met** |

## Counts

| Severity | Count |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 3 |

REJECTED implementer claims: 0.

**Verdict: APPROVE**

Zero VALID Important. Do **not** close RF-004. R0.4.3-B remains the next
slice for remaining magnitude / inline conversion leftovers.

## Lead Architect adjudication (2026-09-09)

Accepted. Zero VALID Important. Minors deferred to R0.4.3-B (panel `move * 100`, stale comments, wiring pin). RF-004 stays **IN PROGRESS**.
