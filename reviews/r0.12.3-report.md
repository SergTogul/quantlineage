# R0.12.3 Critical E2E journey

## Task
R0.12.3 Critical E2E journey (RF-016 / QA-014) — independent-review fix (REQUEST CHANGES)

## Owner
QA & Quant Validation Engineer

## Status
**DONE** — journey is green against the live builtin API. Compare JSON now asserts changed risk (`base_var_99 !== hedged_var_99`). Custom and compare POSTs are paired with `waitForResponse` + `ok()`. No frontend or backend product code changed. No commit.

## Summary
Independent review of R0.12.3 was **REQUEST CHANGES**. Orchestrator adjudication accepted both findings. This pass updates only `e2e/tests/r0-critical-journey.spec.ts` (and this report):

- **Important (QA-014):** After Compare hedge, `waitForResponse` on `POST /api/v1/risk/stress/compare` with `ok()`, then from the response JSON assert `base_var_99 !== hedged_var_99` and `var_improvement !== 0`. No dollar VaR/ES pins. No client-side VaR computation.
- **Minor:** Custom-scenario `POST /api/v1/risk/stress/evaluate/custom` is paired with `waitForResponse` + `ok()`. Dashboard KPI / contributor panels wait for `POST /api/v1/risk/var` `ok()` before treating them as API-backed. Load ES waits for `POST /api/v1/risk/es` `ok()`.

The journey remains:

```text
load demo portfolio
→ inspect dashboard VaR/ES (after POST /risk/var ok)
→ open Scenario Builder, run a named custom scenario (request units + response ok), see Loss/NAV
→ inspect Component VaR + ES contribution tables (Load ES waits /risk/es ok)
→ run existing Hedge Compare (SPY-flat what-if)
→ verify changed risk from compare JSON (base_var_99 !== hedged_var_99)
```

Hedge / what-if UI **is present** (`HedgeCompare` on `#scenario-builder`: SPY equity qty → 0, POST `/api/v1/risk/stress/compare`). There is no free-form “add a custom hedge trade” control; the spec uses the existing demo hedge path.

## Files changed
- `e2e/tests/r0-critical-journey.spec.ts` (response `ok()` + QA-014 JSON invariant)
- `reviews/r0.12.3-report.md` (this pass)

Not edited: `playwright.config.js`, `.github/workflows/ci.yml`, frontend product code, backend quant.

## Public/interface changes
- None.

## Numerical conventions
- Units: scenario builder display is Equity/Vol/FX **percent** and Rates **bp**. API body must be `equity_shock` / `vol_shock` / `fx_shock` / `max_loss_pct` as **decimal fractions**, `rates_shift_bps` as **bp**. Hedge default Crash is `equity_shock: -0.2`.
- Sign convention: equity crash is negative (display −20 → API −0.2).
- Day count/calendar if relevant: n/a
- Tolerances/reference: `toBeCloseTo(..., 10)` on request decimals only. **No dollar VaR/ES pins.** Hedge change is a relational compare of two API fields (`!==`), not a computed or invented VaR.

## Tests added/updated
- `e2e/tests/r0-critical-journey.spec.ts` — same single journey, plus:
  1. `/` waits `POST /api/v1/risk/var` `ok()` before Overview `$` KPIs
  2. Named custom scenario: request units **and** `waitForResponse` + `ok()`
  3. Load ES: `POST /api/v1/risk/es` `ok()` before treating the ES table as API-backed
  4. Compare hedge: request body (equity −0.2, SPY qty 0) **and** `waitForResponse` + `ok()`
  5. **QA-014:** `expect(compareJson.base_var_99).not.toBe(compareJson.hedged_var_99)` and `expect(compareJson.var_improvement).not.toBe(0)`

JSON assert added (from compare API, not UI):

```ts
const compareJson = await hedgeRes.json()
expect(compareJson.base_var_99).not.toBe(compareJson.hedged_var_99)
expect(compareJson.var_improvement).not.toBe(0)
```

## Commands executed
```bash
cd /Users/user/src/riskforge-mvp/e2e && npm test -- tests/r0-critical-journey.spec.ts
```

Playwright started uvicorn `:8000` (builtin, `RISKFORGE_PRICING_ENGINE=builtin`) + Vite `:5173`. Chrome channel from `playwright.config.js`. Suite started. SPY-flat did **not** leave `base_var_99 === hedged_var_99` (invariant held; not BLOCKED).

## Results
- Critical journey: **1 passed** in 9.0s (`r0-critical-journey.spec.ts`)
  - `✓ [chromium] › tests/r0-critical-journey.spec.ts:27:7 › R0.12.3 critical journey › demo portfolio → VaR/ES → custom scenario → contributors → hedge compare (3.7s)`
- Frontend unit / backend pytest / QuantLib / native / dashboard smoke: not required for this review-fix pass; not re-run
- All tests pass (all applicable/affected suites required by the task): **yes**
- Unexplained failures or skips: none
- CI is green (all required checks): not started (this QA slice does not push)

## Known limitations / risks
- Hedge Compare is a **preset** SPY-flat what-if, not an interactive “add hedge trade” editor. That is the longest hedge prefix already in the UI.
- The change-risk check is relational on API fields only. If a future demo book left `base_var_99 === hedged_var_99` after flattening SPY equity, this spec must fail (BLOCKED) — do not weaken the invariant.
- Dashboard `/risk/var` wait does not lock `/risk/summary` or `/risk/contributors` paths separately; Component VaR rows remain cardinality + visibility after the dashboard `var` `ok()`.
- Test timeout is 120s on the spec only; `playwright.config.js` unchanged.
- Playwright artifacts: `e2e/test-results/` (local; not part of the task files).

## Follow-up / next owner
- Owner: none required for R0.12.3
- Requested action: none — review findings addressed; journey green
- Blocking?: **no**

## Handoff
Independent-review Important #1 and Minor #2 are closed in this spec. Execution is green. Do not commit (per task).

```bash
cd /Users/user/src/riskforge-mvp/e2e && npm test -- tests/r0-critical-journey.spec.ts
# 1 passed (9.0s)
```
