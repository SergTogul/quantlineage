# R0.12.3 Critical E2E journey

## Task
R0.12.3 Critical E2E journey (RF-016 / RF-017 verification slice)

## Owner
QA & Quant Validation Engineer

## Status
**DONE** — journey is green against the live builtin API after `fd09326` (root snapshot threaded through stress / hierarchy / compare / reverse / drilldown). Existing `dashboard.spec.ts` smoke is also green. No frontend or backend product code changed in this re-run.

## Summary
Re-ran the existing Playwright journey (`e2e/tests/r0-critical-journey.spec.ts`) after the R0.2 snapshot-threading fix (`fd09326`, after `0f91b6d`). The spec was previously **BLOCKED** on dashboard `Promise.all` 500s (`API error: Failed to fetch` from missing `dividend_yields[SPY]` and conflicting sample rates).

This re-run did **not** change the spec. The same UI → API → rendered-results path now completes:

```text
load demo portfolio
→ inspect dashboard VaR/ES
→ open Scenario Builder, run a named custom scenario, see Loss/NAV
→ inspect Component VaR + ES contribution tables
→ run existing Hedge Compare (SPY-flat what-if)
→ confirm VaR/ES figures are visible after the hedge rerun
```

Assertions remain UI → API → rendered results only. No invented dollar VaR/ES. Request units stay pinned where % / bp conversion matters (display percent → API decimal; rates stay bp). Selectors are reused from `dashboard.spec.ts`, `scenario-builder.spec.ts`, and `m8-panels.spec.ts`.

Hedge / what-if UI **is present** (`HedgeCompare` on `#scenario-builder`: SPY equity qty → 0, POST `/api/v1/risk/stress/compare`). There is no free-form “add a custom hedge trade” control; the spec uses the existing demo hedge path.

## Files changed
- `reviews/r0.12.3-report.md` (this re-run: status, commands, results)
- `e2e/tests/r0-critical-journey.spec.ts` (unchanged this re-run; written in the blocked first pass)

Not edited: `playwright.config.js`, `.github/workflows/ci.yml`, frontend product code, backend quant.

## Public/interface changes
- None.

## Numerical conventions
- Units: scenario builder display is Equity/Vol/FX **percent** and Rates **bp**. API body must be `equity_shock` / `vol_shock` / `fx_shock` / `max_loss_pct` as **decimal fractions**, `rates_shift_bps` as **bp**. Hedge default Crash is `equity_shock: -0.2`.
- Sign convention: equity crash is negative (display −20 → API −0.2).
- Day count/calendar if relevant: n/a
- Tolerances/reference: `toBeCloseTo(..., 10)` on request decimals only. **No dollar VaR/ES pins.** Rendered risk is asserted as present (`$` KPI text, Loss/NAV labels, contribution / hedge tables).

## Tests added/updated
- `e2e/tests/r0-critical-journey.spec.ts` — single journey (unchanged this re-run):
  1. `/` → Global Macro Demo; Overview KPIs Market Value / 99% VaR / 99% ES (money-formatted, not a specific amount)
  2. Main nav → Scenario Builder; name `R0 Critical Journey`; Equity −20%, Rates 100 bp
  3. Payload preview + intercepted POST `/api/v1/risk/stress/evaluate/custom` units
  4. Scenario result: name, Loss, NAV, threat (no dollar assert)
  5. Main nav → VaR & ES: Component VaR Contributors rows + Load ES table (99% VaR / 99% ES labels)
  6. Main nav → Scenario Builder: Compare hedge; intercepted POST `/api/v1/risk/stress/compare` (`equity_shock === -0.2`, SPY equity qty 0); Hedge cost / 99% VaR / 99% ES / table after rerun

## Commands executed
```bash
cd /Users/user/src/riskforge-mvp/e2e && npm test -- tests/r0-critical-journey.spec.ts
cd /Users/user/src/riskforge-mvp/e2e && npm test -- tests/dashboard.spec.ts
```

Playwright started uvicorn `:8000` (builtin, `RISKFORGE_PRICING_ENGINE=builtin`) + Vite `:5173`. Ports were free at first run; Chrome channel from `playwright.config.js`. Suite **did start**. HEAD at re-run: `fd09326`.

## Results
- Critical journey: **1 passed** in 19.4s (`r0-critical-journey.spec.ts`)
  - `✓ [chromium] › tests/r0-critical-journey.spec.ts:22:7 › R0.12.3 critical journey › demo portfolio → VaR/ES → custom scenario → contributors → hedge compare (4.2s)`
- Existing dashboard smoke: **1 passed** in 8.1s (`dashboard.spec.ts`)
  - `✓ [chromium] › tests/dashboard.spec.ts:4:7 › Dashboard smoke › loads portfolio risk terminal from the live API (3.4s)`
- Frontend unit / backend pytest / QuantLib / native: not required for this slice; not re-run
- All tests pass (all applicable/affected suites required by the task): **yes**
- Unexplained failures or skips: none
- CI is green (all required checks): not started (this QA slice does not push)

## Known limitations / risks
- Hedge Compare is a **preset** SPY-flat what-if, not an interactive “add hedge trade” editor. That is the longest hedge prefix already in the UI.
- Spec does not assert that hedge **changes** a numeric VaR (would invent/compare client-side). It asserts API-backed before/after figures are visible after rerun.
- Test timeout is 120s on the spec only; `playwright.config.js` unchanged.
- Playwright artifacts: `e2e/test-results/` (local; not part of the task files).
- Dashboard `loadDashboard()` still waits on every listed POST; a single 500 would still fail the whole terminal. That is no longer the case for the current builtin demo snapshot.

## Follow-up / next owner
- Owner: none required for R0.12.3
- Requested action: none — journey and dashboard smoke are green
- Blocking?: **no**

## Handoff
Spec covers the full R0.12.3 prefix that exists in the UI (including Hedge Compare). Execution is green after `fd09326` threaded one root snapshot through stress / hierarchy / compare / reverse / drilldown.

```bash
cd /Users/user/src/riskforge-mvp/e2e && npm test -- tests/r0-critical-journey.spec.ts
# 1 passed (19.4s)

cd /Users/user/src/riskforge-mvp/e2e && npm test -- tests/dashboard.spec.ts
# 1 passed (8.1s)
```
