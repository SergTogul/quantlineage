# R0.12 Hierarchy benchmark (nightly only)

## Task
RF-016 leftover — hierarchy benchmark / identity on nightly only (R0.12.4)

## Owner
QA & Quant Validation / DevOps Platform

## Status
**DONE** — sibling nightly job `hierarchy-benchmark` (`nightly-hierarchy-benchmark`) runs `benchmarks/run_hierarchy_bench.py --json` on a deterministic 64-trade / 94-node book and asserts `impl`, `n_positions`, `n_nodes`, `market_value`, additive reconciliation, and a SHA-256 checksum of rounded node metrics. Wall time is recorded, not SLA-gated. RF-016 stays **IN PROGRESS** (labeled-runner SLA-K1/K2 remains).

## Summary
Added a real nightly-only hierarchy identity job. It is **not** on `pull_request` and is **not** in PR-FULL `needs:` (still the six PR jobs). The job does **not** run `check_m6_sla.py` and does **not** treat a positive throughput reading as an SLA. Builtin pricing is enough; QuantLib is optional. Pricing engines and risk-formula modules were not edited. No commit.

## Files changed
- `.github/workflows/nightly.yml` — new job `hierarchy-benchmark`
- `.github/workflows/ci.yml` — header comment only; PR-FULL `needs:` still the six PR jobs
- `benchmarks/run_hierarchy_bench.py` — new identity bench (checksum / nodes / MV)
- `backend/tests/test_nightly_ci.py` — hierarchy job ids + identity-not-SLA contracts
- `backend/tests/test_pr_full_ci.py` — `hierarchy-benchmark` forbidden in PR-FULL `needs:`
- `backend/tests/test_nightly_hierarchy_bench.py` — nightly-gated identity pytest
- `BUILD_NOTES.md` — nightly hierarchy identity bullet
- `reviews/r0.12-hierarchy-bench-report.md`

Not edited: `reviews/FINDINGS.md`, `reviews/REMEDIATION_MILESTONE.md`, `backend/app/pricing/*`, `backend/app/risk/hierarchy.py` / other risk-formula files, PR-FAST / QuantLib hard-gate bodies.

## Public/interface changes
- None. Nightly-only workflow + bench script. No API or pricing changes.

## Numerical conventions
- Units: equity MV / delta = quantity × snapshot spot (`10 × 100` per trade; book `64000`).
- Sign convention: parent additive metrics == sum(children) within abs `1e-9` (same as `test_hierarchy.py`).
- Day count/calendar if relevant: n/a
- Tolerances/reference: exact node counts (`64` positions, `94` nodes); exact MV `64000.0`; SHA-256 of rounded path/level/MV/delta/VaR99/ES99/stress lines = `2a51c37798de149329b37c58729c13302de6f902bcc26e3316542dab12537d1d`. Historical path uses `HistoricalRiskEngine(seed=1, observations=40, scenario_backend=python)`. Wall time is informational only.

## Tests added/updated
- `test_nightly_runs_hierarchy_benchmark` — job exists; runs `benchmarks/run_hierarchy_bench.py --json`; asserts `hierarchy_python` / checksum / node counts; `QUANTLINEAGE_NIGHTLY`; no `continue-on-error` / `echo-only` / `check_m6_sla.py`.
- `test_nightly_hierarchy_asserts_identity_not_sla` — pinned checksum and `64000`; no SLA step titles; no throughput-floor or `wall_ms >=` asserts.
- `test_hierarchy_bench_script_is_identity_not_sla` — bench script pins checksum / node / MV constants; no `check_m6_sla`.
- `test_pr_full_needs_does_not_include_nightly` — nightly job ids including `hierarchy-benchmark` are not in PR-FULL `needs:`.
- `test_pr_full_needs_production_relevant_jobs` — exact six PR jobs; `hierarchy-benchmark` absent.
- `test_nightly_hierarchy_bench.py` — gated by `QUANTLINEAGE_NIGHTLY=1`; reuses the bench identity (skipped in PR backend-pytest).

## Commands executed
```bash
cd /Users/user/src/quantlineage
PYTHONPATH=backend backend/.venv/bin/python benchmarks/run_hierarchy_bench.py --json

cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_nightly_ci.py tests/test_pr_full_ci.py
QUANTLINEAGE_NIGHTLY=1 PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_nightly_hierarchy_bench.py
.venv/bin/ruff check tests/test_nightly_ci.py tests/test_pr_full_ci.py tests/test_nightly_hierarchy_bench.py ../benchmarks/run_hierarchy_bench.py
```

## Results
- Hierarchy bench (local): `impl=hierarchy_python`, `n_positions=64`, `n_nodes=94`, `market_value=64000.0`, `additive_ok=true`, checksum `2a51c37798de149329b37c58729c13302de6f902bcc26e3316542dab12537d1d`, `wall_ms≈1034` (recorded, not gated).
- Backend contract tests: **18 passed** in 2.04s (`test_nightly_ci.py` + `test_pr_full_ci.py`).
- Nightly-gated identity pytest: **1 passed** in 3.45s (`QUANTLINEAGE_NIGHTLY=1`).
- Ruff: exit 0.
- Frontend: n/a
- QuantLib: not required. Hard-gate job left intact.
- C++: unchanged. `check_m6_sla.py` is not invoked on `ubuntu-latest`.
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none
- CI is green (all required checks): not started (commit/push out of scope)

## Proof PR-FULL is not blocked
PR-FULL `needs:` remains exactly:

```text
backend
backend-quantlib
frontend
lint
e2e
postgres-smoke
```

No `hierarchy-benchmark`. Nightly is a separate workflow file with no `pull_request` trigger.

## What nightly hierarchy actually runs
| Job | Real check |
|---|---|
| `nightly-hierarchy-benchmark` | `benchmarks/run_hierarchy_bench.py --json`; assert `impl`, 64 positions, 94 nodes, MV `64000`, additive ok, SHA-256 checksum |

## Known limitations / risks
- GitHub Actions itself was not executed from this host. First `workflow_dispatch` after push is the runner proof.
- This is an identity / reconciliation gate, not a throughput SLA. Labeled-runner SLA-K1/K2 (`check_m6_sla.py`) remains an RF-016 leftover.
- Hierarchy still recomputes risk at every node (RF-008). This job pins current semantics; it does not change the execution model.
- RF-016 is **not** CLOSED.

## Follow-up / next owner
- Owner: Lead Architect / DevOps — labeled-runner SLA-K1/K2.
- Requested action: do not add Nightly or `hierarchy-benchmark` to branch-protection required checks; do not add nightly jobs to PR-FULL `needs:`; do not close RF-016.
- Blocking?: no
