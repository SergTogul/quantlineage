# R0.10.3 loadDashboard RiskRun Fallback Independent Review

Slice: leftover RF-015 / R0.10.3 — Frontend `loadDashboard()` falls back to
`/risk/runs` with `run_type: "dashboard"` when HEAVY inline is refused.
Do not treat the implementer’s report as proof.
Workspace: uncommitted working tree vs HEAD on `/Users/user/src/quantlineage`
(`r0-core-remediation` @ `1a1f48e`).
Implementer report: `reviews/r0.10.3-loadDashboard-runs-report.md`
(claims, not proof).
Finding: `reviews/FINDINGS.md` RF-015 — Status **IN PROGRESS**. Do not close.

Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. The only write
is this file.

Other dirty-tree work (App.jsx BlockHelp, Analytics, styles, native tests,
README, docs/superpowers, etc.) was not scored.

## Verdict: APPROVE

## Checks performed

- Read the implementer report as claims only; read RF-015 acceptance
  (interactive UI must not fan out redundant synchronous risk) and the
  must-verify list below.
- Diffed vs HEAD. Scored paths only:
  - `frontend/src/api.js` (refuse detection; `getPortfolio`; dashboard →
    RiskRun fallback + poll; shared `json()` error body parse)
  - `frontend/src/api.test.js` (MSW live-path: 200, refuse+COMPLETED,
    refuse+FAILED, non-refuse 400/500)
  - `reviews/r0.10.3-loadDashboard-runs-report.md`
- Confirmed `reviews/FINDINGS.md` and `reviews/REMEDIATION_MILESTONE.md`
  have **no** working-tree diff. RF-015 Status remains **IN PROGRESS**.
- Index empty for scored production paths (`api.js` / `api.test.js`
  modified unstaged; report untracked). Branch still
  `r0-core-remediation`. This review did not commit.
- Compared refuse contract to `backend/app/api/backpressure.py`
  (`details.use == "/risk/runs"`, HTTP 400) and flat
  `{code, message, details}` JSON from `register_exception_handlers`.
- Confirmed dirty `App.jsx` is BlockHelp-only (+5/−1); still
  `loadDashboard().then(setData).catch((e) => setError(e.message))` —
  not part of this slice.
- Ran the required command (rerun, not the implementer’s log):

```bash
cd /Users/user/src/quantlineage/frontend
npm test -- --run src/api.test.js
# Test Files  1 passed (1)
# Tests  5 passed (5)
# ~2.43s
```

I did not rerun unrelated frontend suites or CI.

## Hunt results

| # | Hunt | Result |
|---|---|---|
| 1 | Gate-off / dashboard 200 still fans out risk POSTs? | **No.** Success path is one `POST /api/v1/risk/dashboard`. MSW spies eight fan-out paths; only the dashboard POST is recorded. |
| 2 | Refuse fallback on wrong status / wrong `details.use`? | **No.** `isHeavyInlineRefuse` requires HTTP **400** and `body.details.use === "/risk/runs"` (exact backpressure contract). Non-refuse 400 and 500 rethrow with no portfolio/runs. |
| 3 | Gate-on path invents risk numbers client-side? | **No.** GET `/portfolio` → `createRiskRun(..., { run_type: 'dashboard' })` → poll → return server `payload`. FAILED throws `error_message` (or id fallback). Missing payload throws; no client math. |
| 4 | Tests not live MSW / missing required cases? | **No.** Five MSW cases: 200 no-fan-out; refuse→COMPLETED (call order + 10 keys); refuse→FAILED throws; non-refuse 400; 500. |
| 5 | RF-015 closed in FINDINGS / milestone edited? | **No.** Status **IN PROGRESS**; FINDINGS / milestone working-tree diff size 0. |
| 6 | Slice edited dirty unrelated frontend files? | **No on disk for this slice.** Claimed files are `api.js`, `api.test.js`, report. App.jsx dirty is BlockHelp WIP, unscored. |

## Acceptance criteria

| # | Criterion | Result |
|---|---|---|
| 1 | Gate-off / dashboard 200: single `POST /risk/dashboard`, no risk fan-out | **Pass.** |
| 2 | Gate-on refuse: only HTTP 400 + `details.use === "/risk/runs"` → GET portfolio + `run_type: dashboard` + poll → COMPLETED payload with 10 App keys | **Pass.** Matches backpressure + ErrorBody shape. |
| 3 | Non-refuse 4xx/5xx do not fall back | **Pass.** MSW 400-other and 500. |
| 4 | FAILED risk run throws; no invented numbers | **Pass.** |
| 5 | No edits to dirty unrelated frontend files in this slice | **Pass.** |
| 6 | RF-015 not closed in FINDINGS | **Pass.** |
| 7 | MSW live-path coverage for success, refuse+COMPLETED, refuse+FAILED, non-refuse error | **Pass.** |

## Attack results

| # | Attack | Score |
|---|---|---|
| 1 | Success path still POSTs summary/stress/var/… | **No.** Fan-out handlers unused on 200 and refuse paths. |
| 2 | Any 400 (or 500) triggers RiskRun | **No.** Exact `details.use` gate; non-refuse cases prove no GET portfolio / POST runs. |
| 3 | Refuse path synthesizes VaR/stress client-side | **No.** Returns worker payload only; FAILED/missing payload throw. |
| 4 | `json()` error message format breaks App `e.message` | **No.** Still `` `${status} ${statusText}` ``; body attached on `err` for refuse detection only. |
| 5 | FastAPI wraps refuse under `detail` so client never matches | **No.** Central handlers emit flat `{code, message, details}`; backend backpressure tests assert `body["details"]["use"]`. |
| 6 | FINDINGS / milestone mark RF-015 CLOSED or invent R0.10 COMPLETE for this slice | **No on disk.** RF-015 **IN PROGRESS**. Milestone R0.10.3 already COMPLETE for refuse-gate earlier; text still says Compose loadDashboard POSTs dashboard only (stale after this slice; Lead updates; finding stays open). |
| 7 | Slice quietly ships BlockHelp / Analytics / styles | **No.** Those diffs are separate dirty tree; not in implementer file list; not scored. |

## Residual (not a fail)

| Item | Why it is not a leftover hole for this slice |
|---|---|
| Poll uses fixed `RISK_RUN_POLL_MS` with no max-wait | Documented. Transport-only; FAILED/COMPLETED still terminate. Timeout policy is a later hardening item. |
| No MSW case for COMPLETED without usable `dashboard` payload | Production throws `"Dashboard risk run completed without a usable payload"`. Test gap, not a silent invent path. |
| FINDINGS “Remaining” line still says loadDashboard POSTs dashboard only | Implementer correctly did not edit FINDINGS. Lead Architect refreshes wording after APPROVE. Status stays **IN PROGRESS**. |
| Other interactive HEAVY UI POSTs may still 400 under the gate | Explicit leftover; keeps RF-015 open. This slice only fixes dashboard load. |
| `dashboardPayloadFromRun` accepts a sole untyped result as fallback | Still server payload; does not invent numbers. Prefer typed `result_type === 'dashboard'` first. |

## Findings

### Critical

None. VALID count: **0**.

### Important

None that block APPROVE. VALID count: **0**.

### Minor

1. **No client-side poll deadline**
   - File: `frontend/src/api.js` (`pollRiskRunUntilTerminal`)
   - Hung worker → unbounded poll. Documented in implementer report.
     VALID count: **1**.

2. **Missing MSW case for COMPLETED empty/malformed payload**
   - File: `frontend/src/api.test.js`
   - Throw path exists in production; not covered. VALID count: **1**.

## Recommendations

- Keep RF-015 **IN PROGRESS**. Lead Architect may refresh FINDINGS
  “Remaining” to note Compose dashboard load now falls back to
  `run_type=dashboard`, and list any remaining HEAVY UI call sites.
  Do **not** close RF-015 until those are covered / accepted.
- Do **not** mark milestone work COMPLETE solely for this frontend
  leftover beyond noting the client path.
- Optional: add a max poll budget; add MSW for empty COMPLETED payload.

## Assessment

**Ready to merge: Yes** (R0.10.3 leftover Frontend `loadDashboard` →
RiskRun fallback slice only).

## Counts

| Severity | VALID |
|---|---|
| Critical | 0 |
| Important | 0 |
| Minor | 2 |

**Verdict: APPROVE** (zero VALID Important).

## Lead Architect adjudication (2026-09-09)

Accepted. Zero VALID Important. Minors deferred (poll deadline; empty COMPLETED MSW). RF-015 stays **IN PROGRESS** — Compose dashboard load now falls back to `run_type=dashboard`; remaining HEAVY interactive UI call sites still sync POST when the gate is on.
