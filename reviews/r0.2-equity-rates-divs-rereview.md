# R0.2 Equity Rates/Divs Re-review (Finding 1 only)

Slice: family A rates/dividends (RF-001), after the fixture pass.
Workspace: uncommitted work on `r0-core-remediation` vs HEAD.
Prior review: `reviews/r0.2-equity-rates-divs-independent-review.md`
(REQUEST CHANGES; Important #1 VALID). Orchestrator deferred Minors #2–3.
Implementer addendum: `reviews/r0.2-equity-rates-divs-report.md` (QA fixture
pass). Production code was not modified by this review.

This re-review attacks Finding 1 only. Minors were not reopened. Parallel
demo-snapshot edits (`sample.py` aggregate seed, PortfolioService/hierarchy
`market=` threading) were treated as sibling work, not this implementer’s
close-out.

## Verdict: REQUEST CHANGES

Acc 8 is green in the **combined** working tree. That is not the same as
Finding 1 being closed as a standalone pricing + fixture commit.

The fixture pass seeded hand-built option snapshots (`UNIT` / `SPY` / XYZ
already present). Those cases no longer raise. The DemoAggregate half of
Finding 1 still lives in `_DEMO_AGGREGATE_MARKETS` (`sample.py`), which is
not this slice. In-memory drop of those `dividend_yields` maps re-breaks
Acc 8: **20 failed / 50 passed**, all `MissingMarketDataError
dividend_yields[SPY]`. This slice is **not mergeable** without the
demo-snapshot files.

Remaining Critical/Important on Finding 1: **1 Important**.

## Checks performed

- Re-read `_required_settlement_rate` / `_required_dividend_yield` in
  `backend/app/pricing/builtin.py` and `quantlib.py`,
  `_DEMO_MARKETS` vs `_DEMO_AGGREGATE_MARKETS`, engine defaults
  (`DemoAggregateMarketDataProvider` on Stress / ScenarioAttribution /
  ReverseStress / MultiFactorReverseStress / AttributionEngine), and
  `PortfolioService` (`market_data=DemoPortfolioMarketDataProvider`,
  `attribution_engine=AttributionEngine(self.market_data)`, reverse-stress
  still calling `solve` without a threaded snapshot).
- Diffed this slice vs HEAD: pricers + authority/QL/surface tests +
  fixture seeds in `test_full_reval_golden.py`, `test_var_methodology.py`,
  `test_component_var.py`, `test_attribution.py` (XYZ). Confirmed
  `sample.py` only adds two aggregate `dividend_yields={NVDA:0, SPY:0}`
  lines (sibling).
- Runtime probes (no production-file writes; in-memory rebinds only):
  - `.get` restore on both engines’ rate/div helpers.
  - Strip `_DEMO_AGGREGATE_MARKETS["global-macro"|"equity-vol"].dividend_yields`
    to `{}` and re-invoke Acc 8 paths / the Acc 8 pytest command.
- Ran:

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=line \
  tests/test_attribution.py tests/test_full_reval_golden.py \
  tests/test_var_methodology.py tests/test_component_var.py \
  tests/test_scenario_attribution.py tests/test_next_phase.py \
  tests/test_reverse_stress.py tests/test_reverse_stress_multi.py
# 70 passed, 1 warning (Starlette TestClient deprecation)

PYTHONPATH=. .venv/bin/python -m pytest -q --tb=line \
  tests/test_market_authority.py tests/test_quantlib_pricing.py \
  tests/test_surface_vol_pricing.py
# 77 passed
```

## Attack results

### 1. Acc 8 / Finding 1 — combined tree vs standalone

| Tree | Acc 8 |
|---|---|
| Current workspace (pricing + fixtures + demo-snapshot seed) | **70 passed** |
| Same tree, aggregate `dividend_yields` stripped in-memory | **20 failed, 50 passed** |

Combined-tree greens are `MissingMarketDataError`-free. They are **not**
owned by this slice alone.

Hand-built fixture half (this pass) stays green without the aggregate seed:

- `test_attribution.py` XYZ option spot / vol / theta
- `test_full_reval_golden.py::test_historical_engine_full_reval_is_not_the_linear_path` (`UNIT`)
- `test_var_methodology.py` / `test_component_var.py` `_option_market` (`SPY`)

DemoAggregate half (Finding 1 remainder) goes red without the seed:

- `test_attribution.py::test_required_drivers_present` (`AttributionEngine()` default provider)
- `test_scenario_attribution.py`: five SAMPLE / `StressEngine.evaluate` cases
- `test_next_phase.py`: `test_reverse_stress_finds_equity_loss_threshold`,
  `test_new_api_endpoints` (`POST /risk/stress/reverse` unhandled 500),
  `test_scenario_comparison_measures_hedge_improvement`
- `test_reverse_stress.py` / `test_reverse_stress_multi.py`: SAMPLE solve +
  service/API cases

`_DEMO_MARKETS["global-macro"]` already had NVDA/SPY zeros before this
slice. After the sibling `market=` threading, `PortfolioService.demo_attribution`,
`stresses`, `hierarchy`, and `AttributionEngine(self.market_data)` price
SAMPLE from that production snapshot and **survive** the aggregate strip.
That is why `test_sample_demo_reconciles` and
`test_market_and_demo_attribution_api` stayed green in the drop probe.
Engine-default and reverse-stress paths do not: they still resolve
`demo-aggregate:global-macro`. Probed: SAMPLE `fut-es` / `opt-spy-put`
against the stripped aggregate raise `dividend_yields[SPY]`; the same
trades against `demo:global-macro` price (570678.34… / 9505.33…).

### 2. `.get` restore still fails the missing-key authority tests

Yes. Rebinding both engines’ helpers to
`rates.get(ccy, 0.04)` / `dividend_yields.get(symbol, 0.0)` priced every
empty-rate, empty-div, and SPY-only AUTH future/option and did **not**
raise (12/12). Builtin and QuantLib empty-div AUTH futures both returned
**51007.533…** (trade-local carry). The parametrized
`test_missing_equity_family_{rate,dividend_yield}_raises_*` cases would
fail `DID NOT RAISE MissingMarketDataError`. The “beats trade-local”
inequality cases were not used as the lock; they still would not catch a
restore when the USD/symbol key is present.

Fail-closed pricing is intact. Do not restore `.get`.

### 3. Can this slice ship without the demo-snapshot `sample.py` seed?

**No.** Dropping `_DEMO_AGGREGATE_MARKETS` `dividend_yields` re-breaks Acc
8 (20 failures above). Finding 1 is not closed as a standalone commit.

Sibling `market=` threading does not substitute for that seed: reverse
stress (service + API) and every engine constructed with the default
`DemoAggregateMarketDataProvider` still read the aggregate snapshot.

Do not ask this owner to dual-edit `sample.py` (orchestrator ban). Gate
the merge on landing the demo-snapshot aggregate seed in the same change
(or first).

### 4. Minors

Not reopened. Duplicated helpers and trade-id-less
`MissingMarketDataError` are unchanged and are not behavior bugs.

## Findings

### 1. STILL OPEN — Important — DemoAggregate caller migration is not in this slice

- Files: `backend/app/sample.py` (`_DEMO_AGGREGATE_MARKETS` equity-vol /
  global-macro), consumed by `StressEngine`, `ScenarioAttributionEngine`,
  `ReverseStressEngine`, `MultiFactorReverseStressEngine`,
  `AttributionEngine` default, `PortfolioService.reverse_stress` /
  `reverse_stress_multi` (no `market=` thread)
- What's wrong: After fail-closed r/q, a supplied snapshot must contain
  `dividend_yields[symbol]`. The fixture pass closed the hand-built
  family-A snapshots. The default aggregate snapshots that those engines
  still resolve for SAMPLE / `global-macro` are seeded only in the
  sibling demo-snapshot slice. Shipping pricing + fixtures alone leaves
  Acc 8 red.
- Why it matters: Brief §8 regressions are a handoff gate. Combined-tree
  70/70 is a workspace coincidence, not a standalone close-out. Live
  `POST /risk/stress/reverse` is an unhandled 500 without the seed.
- Suggested fix: Land the sibling `_DEMO_AGGREGATE_MARKETS` NVDA/SPY
  zeros with this slice (or first). Do not restore `.get`. Do not
  re-seed hand-built fixtures that are already green.

Fixture-half of original Finding 1: **closed** (`UNIT` / `SPY` / XYZ).
DemoAggregate-half: **open** until the sibling file is in the merge.

## Strengths

- Fail-closed helpers are still `market[key]` + `MissingMarketDataError`,
  not `.get`. QuantLib overlay still writes r/q onto a `model_copy`.
- Authority suite still locks the contract (77 passed). The `.get` restore
  still fails the missing-key cases, including QuantLib.
- Fixture pass is the right shape: trade-mark `0.0` on existing snapshots;
  goldens not rewritten; pricers not weakened.

## Assessment

**Ready to merge?** No — not as this slice alone.

**Reasoning:** Acc 8 / Finding 1 failures are gone only while
`sample.py` aggregate `dividend_yields` (and the sibling `market=`
threading that hides some PortfolioService paths) sit in the same tree.
Restoring `.get` would still fail the authority tests. Merge with the
demo-snapshot seed or keep REQUEST CHANGES.

**Verdict: REQUEST CHANGES**

---

## Orchestrator adjudication (2026-09-03)

Important (DemoAggregate half): **VALID**. Close it by landing `_DEMO_AGGREGATE_MARKETS` NVDA/SPY zeros in the same commit as the pricers and fixtures. Do not restore `.get`. Hierarchy / compare / reverse `market=` threading stays a separate slice (not required to close the aggregate-seed half).

| Severity | Remaining VALID |
|---|---|
| Critical | 0 |
| Important | 1 (Finding 1, DemoAggregate half) |
| Minor | not re-scored (deferred) |
