# RF-016 close gate (R0.12.7)

**Owner:** QA & Quant Validation Engineer  
**Date:** 2026-09-09  
**Branch:** `r0-core-remediation`  
**BASE:** `ce54761`  
**Finding:** RF-016  
**Recommendation:** **CLOSE**

This is an independent QA re-score. R0.12.1–R0.12.6 implementer reports and independent reviews were treated as claims until re-read against on-disk workflows, contract tests, and a fresh focused suite. No `check_m6_sla.py` job on `ubuntu-latest`. No host SLA floors invented. No pricing/risk algorithm change.

Controller must not treat CLOSED as final until Task 26 independent review **APPROVE**.

## Recommendation

**CLOSE** RF-016 for Milestone R0. Required PR-FAST / PR-FULL / NIGHTLY Postgres two-worker / larger full-reval / QuantLib E2E are **MET**. Labeled-runner SLA-K1/K2 and QA-024 demo-artifact range are **accepted residuals** and are **not** scored MET.

Would this gate defend CLOSE to a second reviewer? **Yes**, provided those named leftovers stay residuals — not relabeled MET — and QuantLib cannot skip-green PR-FULL.

KEEP OPEN trigger from the brief (QuantLib can still be skipped in PR-FULL) did **not** fire.

## Required direction — PR-FAST

| # | Required direction | Score | Evidence |
|---|---|---|---|
| 1 | Deterministic core unit/golden/property tests | **MET** | `.github/workflows/ci.yml` job `pr-fast` (`PR-FAST`). Runs `tests/test_var_es_golden.py`, `test_approx_pnl_units_golden.py`, `test_full_reval_golden.py`, `test_pricing.py`, `test_quant_properties.py`, `test_m9_risk_properties.py` with builtin pricing; then frontend `npm ci` / `npm test` / `npm run lint`. Contract: `backend/tests/test_pr_fast_ci.py`. Independent review APPROVE (`reviews/r0.12.1-independent-review.md`). |

## Required direction — PR-FULL

| # | Required direction | Score | Evidence |
|---|---|---|---|
| 1 | QuantLib import is mandatory | **MET** | Job `backend-quantlib` (`backend-quantlib-hard-gate`): `pip install -r requirements.txt` with no `requirements-no-ql` fallback; `python -c "import QuantLib"`; `RISKFORGE_REQUIRE_QUANTLIB=1`; `RISKFORGE_PRICING_ENGINE: quantlib`. Aggregator `pr-full` `needs:` includes `backend-quantlib`. No `continue-on-error`. Contract: `backend/tests/test_pr_full_ci.py::test_quantlib_hard_gate_still_requires_quantlib`. R0.1.6. |
| 2 | QuantLib golden suite | **MET** | Same hard-gate job runs `test_quantlib_golden.py`, `test_quantlib_pricing.py`, `test_quantlib_process_state.py`, `test_curve_pricing.py`, plus goldens and `test_quantlib_gate.py`. Skips fail when `RISKFORGE_REQUIRE_QUANTLIB` is set (`backend/tests/quantlib_gate.py`). |
| 3 | Native compile/parity | **MET** | `backend-pytest` step “Native C++ compile and smoke” compiles `kernel_test.cpp` + `risk_kernel_capi.cpp` (`-std=c++20`) and the shared lib; full pytest includes `tests/test_native_kernel.py` parity. Contract: `backend/tests/test_native_ci.py`. `pr-full` needs `backend`. |
| 4 | Frontend test/build | **MET** | Job `frontend` (`frontend-test-build`): `npm ci`, `npm test`, `npm run build`. In `pr-full` `needs:`. |
| 5 | Semantic API contract tests | **MET** | Job `backend` runs `pytest -q` (full backend suite), including `tests/test_api.py`, `test_risk_run_api.py`, `test_api_typed_models.py`, `test_scenario_wire_api.py`. In `pr-full` `needs:`. |
| 6 | Critical Playwright path | **MET** | Job `e2e` (`e2e-playwright`) runs `e2e/tests/r0-critical-journey.spec.ts` (builtin). R0.12.3 rereview APPROVE (`reviews/r0.12.3-rereview.md`). In `pr-full` `needs:`. |

`pr-full` display name is `PR-FULL`; `needs:` is exactly the six PR jobs (`backend`, `backend-quantlib`, `frontend`, `lint`, `e2e`, `postgres-smoke`). Independent review APPROVE (`reviews/r0.12.2-independent-review.md`). Nightly / SLA jobs are not in `needs:`.

**QuantLib skip-green:** `backend-pytest` may fall back to builtin if the QuantLib wheel fails. That does **not** green PR-FULL: the hard-gate job cannot skip, and the aggregator requires it. Acceptance evidence is **MET**.

## Required direction — NIGHTLY

| # | Required direction | Score | Evidence |
|---|---|---|---|
| 1 | PostgreSQL worker concurrency | **MET** | `.github/workflows/nightly.yml` job `postgres-two-worker`; `tests/test_postgres_two_worker.py`; `RISKFORGE_NIGHTLY`; unreachable DSN fails in CI/nightly (no skip-green). Contract: `backend/tests/test_nightly_ci.py`. |
| 2 | Larger full-revaluation samples | **MET** | Nightly `full-reval-sample` + `full-reval-n100` identity (not host SLA). Contract pins `check_m6_sla.py` unused. |
| 3 | Optional QuantLib E2E | **MET** | Nightly `quantlib-e2e`: mandatory QuantLib install + Playwright critical journey with `RISKFORGE_PRICING_ENGINE: quantlib`. Not in PR-FULL `needs:`. Independent review APPROVE (`reviews/r0.12-ql-e2e-independent-review.md`). |
| 4 | Benchmark/SLA on labeled runner | **PARTIAL** — **accepted residual** (not MET) | Harness `benchmarks/check_m6_sla.py` (SLA-K1 50× / SLA-K2 1.3× on `10k_x_1k`) + `docs/performance.md` + `benchmarks/RESULTS.md` exist. No self-hosted runner (`actions/runners total_count=0`, 2026-09-09). All workflow jobs `runs-on: ubuntu-latest`. CI must not run `check_m6_sla.py` on `ubuntu-latest`. Pin: `test_ubuntu_latest_jobs_do_not_run_check_m6_sla`, `test_labeled_sla_job_is_absent_honest_residual`. |
| 5 | Demo-artifact range check (QA-024) | **UNMET** — leftover residual | Nightly QuantLib E2E runs the existing journey (shape + `base_var_99 !== hedged_var_99`), not dollar bands vs `data/demo_risk_artifact.json`. Builtin demo check is byte-equality (`scripts/check_final_demo.py`), not a QuantLib range gate. Prose ranges in `docs/demo/final_demo.md` are not CI. Do not fake a range gate. |

## Why CLOSE (R0 rationale)

Milestone R0 required production-relevant CI tiers, not a dedicated hardware SLA farm. Same standard as RF-014 ACLs/TLS: required local/CI direction MET; shared/hardware leftovers accepted in writing.

| Exit word | Judgment |
|---|---|
| PR-FAST goldens + frontend unit/lint | **Yes.** |
| PR-FULL means QuantLib + native + frontend + API + Playwright actually ran | **Yes.** Broken QuantLib cannot green PR-FULL. |
| NIGHTLY heavier Postgres / full-reval / QL E2E | **Yes.** Sibling workflow; not a required PR check. |
| Labeled-runner SLA-K1/K2 | **No** — accepted residual, not MET. |
| QA-024 QL demo range | **No** — leftover residual, not MET. |

R0.12.6 correctly said KEEP OPEN **on that slice** because SLA-K1/K2 was unimplemented in CI. This close gate is the authorized moment to **accept** the missing labeled runner instead of inventing `ubuntu-latest` floors.

## Accepted residuals (not MET)

1. **No labeled / self-hosted runner** — `actions/runners total_count=0`. Registering a runner is out of scope.
2. **SLA-K1/K2 not CI-enforced** — `check_m6_sla.py` remains a reference-host harness. Nightly native/hierarchy jobs are checksum/identity, not SLA.
3. **QA-024 demo-artifact range** — no QuantLib CI range check against `data/demo_risk_artifact.json`.
4. **Staged lint strictness (QA-020)** — Ruff/mypy/ESLint run in PR-FULL `lint`; relaxations remain documented. Not a KEEP OPEN trigger.
5. **`backend-pytest` QuantLib optional fallback** — allowed; PR-FULL hard-gate is the mandatory path.

Do **not** add `check_m6_sla.py` on `ubuntu-latest` from this close.

## Commands executed

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_pr_full_ci.py tests/test_pr_fast_ci.py tests/test_nightly_ci.py \
  tests/test_rf016_close_gate.py
```

TDD: residual-marker tests failed first (2 failed, 30 passed) before FINDINGS / milestone text. After docs: **32 passed** in 3.51s. Ruff: all checks passed.

## Independent reviews cited (not restamped)

| Slice | Independent verdict |
|---|---|
| R0.12.1 PR-FAST | APPROVE (`reviews/r0.12.1-independent-review.md`) |
| R0.12.2 PR-FULL | APPROVE (`reviews/r0.12.2-independent-review.md`) |
| R0.12.3 critical E2E | APPROVE after rereview (`reviews/r0.12.3-rereview.md`) |
| Nightly QuantLib E2E | APPROVE; do not close finding on that slice (`reviews/r0.12-ql-e2e-independent-review.md`) |
| Hierarchy identity | APPROVE; do not close finding on that slice (`reviews/r0.12-hierarchy-bench-independent-review.md`) |
| R0.12.6 SLA honesty | APPROVE IN PROGRESS (`reviews/sdd-briefs/task-10-rf016-sla-review.md`) |

This CLOSE is not a restamp of R0.12.6 “keep IN PROGRESS.” It is a new required-tier vs accepted-residual disposition.
