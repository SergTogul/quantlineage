# R0.6.8 Reconstruction benches + isolated RSS + N=100 nightly

## Task
R0.6.8 / RF-007 remaining acceptance — reconstruction-honest QuantLib vs builtin (European options, not `qty*spot`); isolated RSS per impl; N=100 nightly (PR skip). No wall-time SLA floors.

## Owner
QA & Quant Validation Engineer

## Status
**DONE_WITH_CONCERNS** — reconstruction 10×50 European option book is recorded with per-engine checksums, isolated subprocess RSS, and a P&L gap within option-match `rel=2e-3`. N=100×50 is nightly-only. Close-matrix: builtin vs QuantLib **MET** on the option book; peak RSS still **PARTIAL**; N×S **PARTIAL** (N=100 nightly, N=1k **UNMET**). RF-007 stays **IN PROGRESS**. Do not close.

## Summary
Extended `benchmarks/run_full_reval_bench.py --json` so the payload still emits the R0.6.1 1×120 builtin identity (`impl`, `n_obs`, checksum `6602fa6906f2579f5c89af72a41ab274c07650234fff69387bc2202b5a40534f`) and R0.6.7 cash-equity `acceptance` at 10×50 (`a28cf4ee6199bf40da3f2598f4241fc85fa4adc4f86bccbbd97e4938047d7537`), and adds a nested `reconstruction` object:

| Field | Where | Close score |
|---|---|---|
| N×S | PR reconstruction 10×50 + cash 1×120 / 10×50; nightly N=100×50 | **PARTIAL** (N=100 **MET** in nightly; N=1k **UNMET**; default PR still 10×50) |
| wall time | `wall_ms` / `wall_ms_cold` / `wall_ms_warm` on reconstruction rows | **MET** at 10×50 (finite number, not a floor) |
| peak RSS | `peak_rss_kib` from a **subprocess per impl** (`--isolated-impl`) | **PARTIAL** (isolated so QuantLib does not include prior builtin; still process-lifetime `ru_maxrss` within that child) |
| scenarios/sec | `scenarios_per_sec` from cold `wall_ms` | **MET** at 10×50 (recorded only; no `throughput` key) |
| builtin vs QuantLib | reconstruction European option book; checksums **per engine**; `pnl_gap` at `rel=2e-3` | **MET** (not cash equity `quantity * spot`) |
| warm vs cold | two `wall_ms` on the same engine/fixture | **MET** at 10×50 |
| identity | cash-equity pins unchanged; reconstruction pins `3148a41a…` (builtin) / `0594ecd6…` (QuantLib) | **MET** |

This capture (Darwin, builtin + QuantLib 1.43, not an SLA):

- identity 1×120 builtin: checksum `6602fa69…` unchanged
- 10×50 cash-equity: checksum `a28cf4ee…` on both engines unchanged
- 10×50 reconstruction builtin: `wall_ms_cold≈135`, `peak_rss_kib≈39820`, `scenarios_per_sec≈370`, checksum `3148a41a0b3b4bb515c75d07991a96ecc186fb9d2294ccb20347df8f5322526e`
- 10×50 reconstruction QuantLib: `wall_ms_cold≈268`, `peak_rss_kib≈65596`, `scenarios_per_sec≈187`, checksum `0594ecd65f68e33a800dbf5c331ead44b1fd353dadb198591721cc445f745eef`, distinct `rss_pid`
- P&L gap: `max_abs≈2.5e-12`, `max_rel≈3.0e-13`, `within_option_match_rtol=true`

`check_m6_sla.py` is not imported or invoked. N=100 is a sibling nightly job (`full-reval-n100`), not PR-FULL `needs:`.

## Files changed
- `benchmarks/run_full_reval_bench.py` — reconstruction option book, isolated RSS subprocess, per-engine checksums, P&L gap
- `backend/tests/test_full_reval_bench.py` — reconstruction / isolated RSS / nightly-skip pins
- `backend/tests/test_nightly_full_reval_n100.py` — N=100×50 skip unless `RISKFORGE_NIGHTLY=1`
- `backend/tests/test_nightly_ci.py` — pin `full-reval-n100` sibling job
- `backend/tests/test_pr_full_ci.py` — `full-reval-n100` not in PR-FULL `needs:`
- `.github/workflows/nightly.yml` — sibling job
- `benchmarks/README.md` — R0.6.8 pointer
- `reviews/FINDINGS.md` — RF-007 residual (still IN PROGRESS)
- `reviews/REMEDIATION_MILESTONE.md` — § R0.6.8; close gate still KEEP OPEN
- `reviews/r0.6.8-reconstruction-benches-report.md`

Not edited: pricing engines, `var.py` / `historical.py` algorithms, C++ kernels, PR-FULL `needs:`.

## Public/interface changes
- None. Local bench script + pytest + nightly sibling. No API or pricing contract change.
- Operator flags `--reconstruction-n` / `--reconstruction-s` / `--isolated-impl` / `--reconstruction-only`. Checksum pin applies only at reconstruction 10×50.

## Numerical conventions
- Units: European ATM 1Y calls, `quantity=10`, spot `100`, vol `0.20`, USD rate `0.03`, dividend `0.01`; evaluation/`as_of` pinned to `2026-09-01`. Dataset: equity `linspace(-0.05, 0.05)`, vol `linspace(-0.10, 0.10)`, rate `linspace(-12, 12)` bp.
- Sign convention: down-move negative P&L.
- Day count/calendar: QuantLib evaluation date pinned; builtin uses `maturity_years`.
- Tolerances/reference: reconstruction P&L must **not** match cash-equity `N * qty * spot * equity_return`. Warm P&L matches cold at atol `1e-12`. Builtin vs QuantLib series `rtol=2e-3` (same as `test_european_option_matches_builtin_closely`). Per-engine SHA-256 of 12-decimal P&L lines. Cash-equity checksums unchanged. `wall_ms` / RSS / scenarios/sec are informational only.

## Tests added/updated
- `test_full_reval_bench_reconstruction_is_european_option_not_cash_equity`
- `test_full_reval_bench_reconstruction_rss_is_isolated_per_impl`
- `test_full_reval_bench_reconstruction_records_pnl_gap`
- `test_full_reval_n100_is_skipped_unless_nightly`
- `test_cash_equity_identity_checksums_unchanged`
- `test_nightly_runs_full_reval_n100` / `test_nightly_full_reval_n100_is_identity_not_sla`
- `test_reconstruction_n100_records_matrix_without_sla` (nightly-only)

## Commands executed
```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_full_reval_bench.py
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_full_reval_bench.py tests/test_nightly_ci.py tests/test_pr_full_ci.py
RISKFORGE_NIGHTLY=1 PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_nightly_full_reval_n100.py
```

## Results
See the Task 7 implementer report for exact command output. Focused bench tests green after reconstruction landing; N=100 nightly test 1 passed in ~15s with `RISKFORGE_NIGHTLY=1`.

## Known limitations / risks
- Peak RSS remains **PARTIAL**: isolated per impl, but still process-lifetime `ru_maxrss` in the child (interpreter + engine import + run).
- N=1k remains **UNMET** (optional same skip rule; not added to nightly.yml).
- QuantLib reconstruction checksum is pinned against QuantLib ≥1.43 from `requirements.txt`; a different QL patch could require a pin update.
- Cash-equity `acceptance.quantlib.peak_rss_kib` is still process-lifetime in the parent (includes prior identity + builtin). Reconstruction RSS is the isolated figure.
- Option A intra-run scenario-block multiprocessing remains PARTIAL.
- RF-007 is **IN PROGRESS**, not CLOSED.

## Follow-up / next owner
- Owner: Lead Architect / Task 8 close re-gate (only after independent APPROVE of this slice).
- Requested action: keep RF-007 IN PROGRESS; do not invent a host SLA; N=1k remains optional.
- Blocking?: yes — RF-007 stays open until Task 8.
