# R0.9.2 — Typed RiskEngine.calculate results

## Task
RF-010 / R0.9.2 — `RiskEngine.calculate` returns domain `RiskSummary`

## Owner
Portfolio Risk Engineer

## Summary
`RiskEngine.calculate` is annotated as `RiskSummary`. `HistoricalRiskEngine.calculate`
returns that model with `portfolio_id` from `portfolio.id` and the same numeric
fields as the previous dict (`market_value`, Greeks, `var_95` / `var_99` /
`expected_shortfall_99`, `methodology`). Callers use attributes, not
`dict[str, float]`. HTTP/JSON edges still serialize `RiskSummary` via FastAPI /
`.model_dump()`. Limit overlay keys (`stress_loss`, injected `key_rate_dv01`)
are an optional `extra` mapping on `LimitEngine`, not part of the engine
contract. VaR/ES formulas unchanged. No DI container; no FastAPI in `app.risk`
or `app.domain`.

**Disposition: IN PROGRESS** — do not CLOSE RF-010. R0.9.3 lifespan composition
remains. Dual-use engine/service inputs (`AttributionRequest`,
`RiskChangeAttributionRequest`, `WhatIfRequest`) stay in domain.

TDD: ABC return annotation failed first (`dict[str, float]`); live `calculate`
failed `isinstance(..., RiskSummary)` until the engine returned the model.

## Files changed
- `backend/app/interfaces/risk.py` — ABC returns `RiskSummary`
- `backend/app/risk/historical.py` — construct `RiskSummary`
- `backend/app/services/portfolio_service.py` — pass typed summary / limit extra
- `backend/app/risk/{hierarchy,limits,incremental_var,var_compare,stress,risk_attribution}.py`
- `backend/tests/test_typed_risk_results.py` — new pin
- caller tests updated from dict access to attributes
- `reviews/FINDINGS.md` — RF-010 **IN PROGRESS** with R0.9.2 residual
- `reviews/REMEDIATION_MILESTONE.md` — R0.9.2 COMPLETE; finding stays open

## Public/interface changes
- `RiskEngine.calculate` → `RiskSummary` (not `dict[str, float]`)
- `LimitEngine.evaluate` / `resolve_metrics` take `RiskSummary` plus optional `extra`
- Wire JSON field names for HTTP `RiskSummary` unchanged

## Numerical conventions
- Units: currency P&L / loss VaR and ES (unchanged)
- Sign convention: loss = -P&L; VaR/ES floored at 0 (unchanged)
- Day count/calendar if relevant: n/a
- Tolerances/reference: goldens in `test_var_es_golden.py` (abs 1e-12)

## Tests added/updated
- ABC `calculate` return annotation is `RiskSummary`
- `HistoricalRiskEngine.calculate` returns `RiskSummary`, not a dict, with `portfolio_id`
- `app.risk` / `app.domain` do not import FastAPI

## Commands executed
```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_typed_risk_results.py tests/test_omit_market.py \
  tests/test_var_es_golden.py tests/test_incremental_var.py \
  tests/test_var_compare.py tests/test_hierarchy_artifact_var.py
```

## Results
- Backend: required suite — **54 passed**, 1 warning (Starlette TestClient httpx deprecation, pre-existing)
- Frontend: not run (out of scope)
- QuantLib: not run (out of scope)
- C++: not run (out of scope)
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none
- CI is green (all required checks): not started

Exact required-suite result:

```text
54 passed, 1 warning in 5.85s
```

Covering caller suites: `test_risk.py` `test_limits.py` `test_limit_drilldown.py`
`test_hierarchy.py` `test_hierarchy_node_ids.py` `test_what_if.py`
`test_var_methodology.py` `test_full_reval_golden.py` `test_historical_data.py`
`test_demo_historical_dataset.py` `test_historical_scenario_kernel.py`
`test_factor_panel_historical.py` `test_quant_properties.py`
`test_m9_risk_properties.py` — **123 passed**, 1 warning.
`test_stress.py` `test_risk_attribution.py` `test_attribution.py` — **29 passed**.

## Known limitations / risks
- `app.risk.query` still imports HTTP `RiskQueryResponse` from `api.schemas` (R0.9.1 leftover)
- Dual-use `AttributionRequest` / `WhatIfRequest` remain in domain
- Process-global `PortfolioService` orchestration remains (R0.9.3)

## Follow-up / next owner
- Owner: Backend / Architecture
- Requested action: R0.9.3 lifespan composition. Do not CLOSE RF-010.
- Blocking?: no
