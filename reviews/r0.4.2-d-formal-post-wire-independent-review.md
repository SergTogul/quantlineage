# R0.4.2-D Formal POST Wire Independent Review

**APPROVE** — Critical 0 · Important 1 · Minor 2

Slice: R0.4.2-D — Formal `ScenarioWire` as primary stress POST + UI (RF-004 close candidate).
Workspace: `/Users/user/src/riskforge-mvp` branch `r0-core-remediation`
(scored work is uncommitted working-tree vs HEAD).
Implementer report: `reviews/r0.4.2-d-formal-post-wire-report.md` (claims only).
Reviewer did not implement this work. Production files were not edited. Index,
HEAD, and branch were not mutated. The only write is this file. This review does
**not** flip FINDINGS status. RF-009 was not reopened.

Scored only:

- `backend/app/api/stress.py`
- `backend/app/api/scenario_wire.py`
- `backend/app/api/execution_class.py`
- `backend/app/api/openapi_examples.py`
- `backend/tests/test_scenario_wire_api.py`
- `backend/tests/test_backpressure.py`
- `frontend/src/api.js`
- `frontend/src/lib/risk.mjs`
- `frontend/src/lib/risk.test.js`
- `frontend/src/components/ScenarioBuilder.jsx`
- `frontend/src/components/ScenarioBuilder.test.jsx`
- `frontend/src/test/mswServer.js`
- `reviews/r0.4.2-d-formal-post-wire-report.md`
- `reviews/FINDINGS.md` / `REMEDIATION_MILESTONE.md` /
  `r0.4-architecture-brief.md` — RF-004 CLOSED claim; RF-011 IN PROGRESS;
  RF-009 must stay CLOSED

## Verdict summary

Primary Scenario Builder / hedge-compare UI POSTs formal `ScenarioWire` to
`/api/v1/risk/stress/formal/evaluate/custom` and `/formal/compare`.
`scenarioPayload` emits `category` + `shocks[]` with display `%` → fraction and
rates bp → absolute decimal (`/ 10_000`). New formal compare route is registered,
HEAVY + methodology-bearing, OpenAPI critical-path listed, and backpressure-refused
when the external worker is on. Legacy `/stress/custom`, `/evaluate/custom`,
`/compare` remain functional with OpenAPI `deprecated=True`. Dual-wire is **not**
live on the primary UI path.

RF-004 acceptance evidence for this close candidate holds: R0.1.3 unit goldens
still pin wrong DV01/vega/bp/% scales; UI `20%` → `0.20` is unit-tested; formal
wire is the live builder/compare path; deprecated StressScenario POSTs are residual
under RF-011. **Lead may keep RF-004 CLOSED.** Do not reopen RF-009. Do not close
RF-011.

One Important: backend “UI-style” parity tests expand via `stress_to_wire` (market
keys), not the frontend `scenarioPayload` / `defaultHedgeScenarios` builders.
Independent probe shows canned demo GET `/portfolio` UI expansion matches market
wire and P&L exactly — so not a close blocker — but the automated proof does not
yet pin the real UI payload.

## Checks performed

- Read implementer report as claims only; diff working tree vs HEAD for scored
  paths + FINDINGS / milestone / brief.
- Grepped `frontend/src` for `equity_shock`, legacy `/evaluate/custom`,
  `/stress/compare` — live clients use formal URLs only (baseline
  `POST /risk/stress` on Heatmaps / dashboard remains DI scenario list, not
  builder StressScenario bodies).
- Confirmed `scenarioPayload` / `defaultHedgeScenarios` emit formal wire units
  and `risk.test.js` asserts `-20%→-0.2`, `50%→0.5`, `100 bp→0.01`, `-5%→-0.05`.
- Confirmed `POST /risk/stress/formal/compare` → `wires_to_scenarios` →
  `compare_scenarios`; HEAVY map + `METHODOLOGY_BEARING_ROUTES`; OpenAPI
  `CRITICAL_OPENAPI_PATHS` / typed `HedgeComparisonReport`; backpressure coverage.
- Confirmed FINDINGS RF-004 **CLOSED**, RF-011 **IN PROGRESS**, RF-009 **CLOSED**
  (RF-009 body untouched).
- Independent HTTP probe (below).
- Reran required suites (note: user checklist name `tests/test_execution_class.py`
  does not exist; used `tests/test_endpoint_execution_class.py`). Did not execute
  CI. Did not mutate index, HEAD, or branch.

### Required suites (independent rerun)

```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_scenario_wire_api.py \
  tests/test_stress.py \
  tests/test_stress_scenarios_di.py \
  tests/test_scenarios.py \
  tests/test_backpressure.py \
  tests/test_endpoint_execution_class.py
# 85 passed in 3.95s; 1 Starlette/httpx deprecation warning; no skips/xfails
```

```bash
cd /Users/user/src/riskforge-mvp/frontend && npm test -- --run
# 12 files / 89 passed in ~7s
```

Implementer “44 / 68 / 89” counts are not treated as proof; suites above were
rerun here.

### Independent probe (TestClient)

- Formal evaluate with shocks built like frontend `scenarioPayload` for GET
  `/api/v1/portfolio` keys (`NVDA`/`SPY` equity+vol, `EURUSD` FX+vol, `EUR`/`USD`
  rates) matches legacy scalar evaluate P&L to `1e-9`.
- Formal compare with UI-style equity-only Crash shocks on portfolio equities
  matches legacy compare `base_pnl` to `1e-9`.
- Shock-key set from UI-style expansion equals `stress_to_wire` on
  `demo_market_snapshot` for the canned demo book (8 shocks; empty symmetric diff).

## Attack checklist

| # | Attack | Result |
|---|---|---|
| 1 | Primary UI still POSTs legacy StressScenario | **Pass.** `evaluateCustomScenario` / `compareHedge` → formal paths; Scenario Builder + HedgeCompare call them with `scenarioPayload` / `defaultHedgeScenarios`. |
| 2 | `scenarioPayload` unit conversion wrong | **Pass.** `%/100`, rates `/10_000`; unit test pins amounts. |
| 3 | Formal vs legacy P&L parity broken | **Pass (demo).** Automated tests use `stress_to_wire`; independent UI-style probe matches. See Important #1. |
| 4 | Formal compare missing / miswired / not HEAVY | **Pass.** Route + HEAVY + methodology-bearing + OpenAPI + refuse path. |
| 5 | Frontend tests only URLs/labels | **Pass with Minor.** `risk.test.js` asserts shape/units; ScenarioBuilder tests do not assert request body. |
| 6 | RF-004 CLOSED without acceptance evidence | **Pass for Lead close.** Unit goldens (`test_approx_pnl_units_golden.py`), UI `20%→0.20`, formal primary path, residual dual POST → RF-011. |
| 7 | OpenAPI / execution_class / backpressure miss compare | **Pass.** |
| 8 | MSW only legacy paths | **Pass with Minor.** Evaluate MSW retargeted to formal; `formal/compare` absent from shared MSW (no HedgeCompare component test). |

## Findings

### Important

1. **VALID — “UI-style” parity tests do not POST `scenarioPayload`.**  
   `test_formal_ui_style_evaluate_matches_legacy_scalar_pnl` and
   `test_formal_compare_api_matches_legacy_pnl` build formal bodies with
   `stress_to_wire(...)` (snapshot factor expansion), not the frontend builders
   that expand onto portfolio-derived (or demo fallback) keys. Report wording
   overclaims automated UI-path proof. Deferred fix: add an API or contract test
   that serializes the same shock list `scenarioPayload(defaultScenarioForm(),
   demoPortfolio)` would emit (or a shared fixture). Independent demo probe
   already matches — not a RF-004 reopen.

### Minor

1. **VALID — Shared MSW has no `POST .../formal/compare` handler.** Evaluate is
   covered; hedge-compare would 404 under default MSW if a component test is
   added. Deferred.
2. **VALID — `ScenarioBuilder.test.jsx` does not capture/assert request JSON.**
   Relies on URL match + response fixture. Unit conversion coverage lives in
   `risk.test.js` instead. Deferred.

### Not findings / explicit non-reopens

- Legacy StressScenario POST routes still callable → expected RF-011 residual;
  OpenAPI deprecated.
- Reverse-stress still family-factor wire → out of slice (implementer explicit).
- Baseline `POST /risk/stress` (Heatmaps / dashboard) → DI scenarios, not builder
  dual-wire.
- RF-009 Status remains **CLOSED**; no reopen.
- `LISTED_HEAVY_ROUTES` / `_MILESTONE_HEAVY` omit both legacy and formal compare
  (same as pre-slice for legacy) — classification still HEAVY via
  `ENDPOINT_EXECUTION_CLASS`.

## RF-004 / RF-011 / RF-009 guidance

| Finding | Status in tree | Independent guidance |
|---|---|---|
| RF-004 | **CLOSED** (implementer) | **Lead may keep CLOSED.** Acceptance bullets met for primary UI formal wire + unit evidence; dual deprecated POST residual correctly parked on RF-011. |
| RF-011 | **IN PROGRESS** | Correct — do not close until typed nesting / dual domain StressScenario retirement. |
| RF-009 | **CLOSED** | Must stay CLOSED; this slice did not reopen it. |

## Must-verify matrix (FINDINGS RF-004 acceptance)

| Evidence | Met? |
|---|---|
| Wrong DV01 ×100 / vega ÷100 / bp-as-% fail | **Yes** — `tests/test_approx_pnl_units_golden.py` (prior R0.1.3; still present) |
| UI `20%` sent as `20` fails | **Yes** — `risk.test.js` asserts equity amount `-0.2` |
| Snapshot-aware approximate paths | **Yes** — prior R0.4 slices; not regressed by this UI/API wire move |
| Primary UI not dual-wire | **Yes** — formal evaluate/compare only on builder/hedge path |
| Dual POST residual | Tracked under RF-011 (deprecated back-compat) |

## Conclusion

Accepted. Zero Critical. One Important (parity tests pin `stress_to_wire`, not
frontend builders — deferred; demo probe green). Two Minors deferred. **Lead may
keep RF-004 CLOSED.** Keep RF-011 IN PROGRESS. Do not reopen RF-009.
