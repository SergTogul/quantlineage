# R0.10.1 Classify Endpoints Independent Review

Slice: leftover RF-015 / R0.10.1 — classify owned HTTP routes INTERACTIVE vs
HEAVY. Do not treat the implementer’s map as a contract until it matches
live OpenAPI.
Workspace: uncommitted working tree vs HEAD on `/Users/user/src/quantlineage`
(`r0-core-remediation` @ `c80ae3c`).
Implementer report: `reviews/r0.10-classify-endpoints-report.md` (claims,
not proof).
Plan: `reviews/REMEDIATION_MILESTONE.md` § R0.10.1.
Finding: `reviews/FINDINGS.md` RF-015 — no Status line; not CLOSED / COMPLETE.

Reviewer did not implement this work. Production files were not modified
by this review. Index, HEAD, and branch were not mutated. The only write
is this file.

R0.10.2 dashboard batch is out of scope. Sync handlers still computing on
the request thread is not a fail condition for this slice.

Other dirty-tree work (pricing, models, persistence, cache, full-reval
bench, R0.3.1 / R0.6.1 reports) was not scored.

## Verdict: APPROVE

## Lead Architect adjudication (2026-09-04)

Accepted. Zero VALID Important. Minors deferred (private-list pin is not the OpenAPI contract; callers must use `classify`, not the raw dict, for summary + FULL_REVALUATION). RF-015 stays **IN PROGRESS**; R0.10.2 / R0.10.3 remain later. This slice commits the classification map only.

The classification module matches live `app.main:app` OpenAPI. Every
R0.10.1 / RF-015 listed heavy example exists at the real `/risk/...` path
and classifies HEAVY (`is_interactive_only` is False).
`classify("POST", "/risk/summary", methodology="FULL_REVALUATION")` returns
HEAVY (string and `VaRMethodology` enum). Dual-mount `/api/v1` shares the
class. Tests import the real app and call `app.openapi()`. RF-015 and
milestone R0.10 were not marked COMPLETE.

No leftover listed-heavy route is classified INTERACTIVE-only. Unprefixed
strings (`/hierarchy`, `/what-if`, `/stress/evaluate`) are not OpenAPI
paths; they raise `KeyError` (fail-closed), which is correct.

Do **not** close RF-015. Do **not** mark R0.10 COMPLETE. Dashboard fan-out
and request-thread compute remain R0.10.2 / R0.10.3.

## Checks performed

- Read the implementer report as claims only; read § R0.10.1, RF-015, and
  PERF-004 / PERF-010 (heavy examples and “keep LINEAR/Δ-Γ summary sync”).
- Diffed vs HEAD: claimed production hunks are `backend/app/api/__init__.py`
  (+1 docstring line) and `BUILD_NOTES.md` (R0.10.1 section). New untracked
  files: `backend/app/api/execution_class.py`,
  `backend/tests/test_endpoint_execution_class.py`,
  `reviews/r0.10-classify-endpoints-report.md`.
  `reviews/FINDINGS.md` and `reviews/REMEDIATION_MILESTONE.md` have no
  working-tree diff. Index is empty. Routers (`risk.py`, `stress.py`,
  `limits.py`, `attribution.py`, `risk_runs.py`, `main.py`) have no diff vs
  HEAD.
- Loaded live OpenAPI via `from app.main import app; app.openapi()`
  (title QuantLineage API 0.3.0; 64 method+path rows, 32 owned canonical
  after `/api/v1` strip). Compared `ENDPOINT_EXECUTION_CLASS` to that set:
  missing `[]`, extra `[]`.
- Probed `classify` / `is_interactive_only` on the attack paths, listed
  heavy set, summary + FULL_REVALUATION (str and enum), and dual-mount
  prefixes. In-memory only; no production-file edits.
- Confirmed `classify` is imported only by the new test module (no router
  wiring). That matches “no router behavior change.”
- Ran:

```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_endpoint_execution_class.py
# 9 passed in 3.05s
```

I did not rerun the implementer’s broader API suite, ruff, mypy, or CI.

## Acceptance criteria

| # | Criterion | Result |
|---|---|---|
| 1 | Owned routes have an INTERACTIVE / HEAVY class | **Pass.** 32 owned canonical OpenAPI routes; map is a bijection. Unknown routes raise `KeyError("unclassified route: …")`. |
| 2 | Listed heavy examples exist in OpenAPI and are not interactive-only | **Pass.** All 11 `LISTED_HEAVY_ROUTES` are in live OpenAPI and classify HEAVY. See leftover table. |
| 3 | Cheap examples: completed-run GET, lightweight summary, small drilldown | **Pass.** `GET /risk/runs/{run_id}` INTERACTIVE-only. Default `POST /risk/summary` INTERACTIVE. `POST /risk/factors` and `POST /risk/limits/drilldown` INTERACTIVE. |
| 4 | `POST /risk/summary` + FULL_REVALUATION is HEAVY | **Pass.** Probed string and `VaRMethodology.FULL_REVALUATION`. `is_interactive_only("POST", "/risk/summary")` is False because the route is methodology-bearing. Static map entry stays INTERACTIVE (default LINEAR / Δ-Γ). |
| 5 | Dual-mount `/api/v1` shares the class | **Pass.** `normalize_path` strips `/api/v1`. Probed `/api/v1/risk/var`, `/hierarchy`, `/summary` (+ FULL_REVAL). |
| 6 | Tests load real app OpenAPI, not only a private list | **Pass.** `test_endpoint_execution_class.py` imports `app.main.app` and uses `app.openapi()["paths"]`. `test_listed_heavy_routes_exist_in_openapi` and `test_every_owned_route_is_classified` fail if map strings drift. The private `_MILESTONE_HEAVY == LISTED_HEAVY_ROUTES` pin is extra, not the only check. |
| 7 | No job platform / no router behavior change | **Pass.** No enqueue, reject, auth, or numerical-method change. Routers unchanged vs HEAD. `classify` has no production callers. |
| 8 | RF-015 / R0.10 not marked COMPLETE | **Pass.** RF-015 has no Status line and is still on the P1 backlog. Milestone status remains **IN PROGRESS**; § R0.10 has no COMPLETE row. FINDINGS / milestone files were not edited. Report Status **DONE** is the R0.10.1 slice only and states RF-015 / R0.10.2 remain. |
| 9 | R0.10.2 dashboard batch not claimed done | **Pass.** Report and BUILD_NOTES leave fan-out / batching to R0.10.2. `frontend/src/api.js` `loadDashboard()` still fans out nine POSTs (not scored as a fail). |

## Attack results

| # | Attack | Score |
|---|---|---|
| 1 | `/risk/var`, `/hierarchy`, `/stress/reverse/multi`, `/what-if`, `/stress/evaluate` classified INTERACTIVE or missing from the map | **No leftover on OpenAPI paths.** Live paths are `/risk/var`, `/risk/hierarchy`, `/risk/stress/reverse/multi`, `/risk/what-if`, `/risk/stress/evaluate`. Each classifies **heavy**, `interactive_only=False`. Unprefixed `/hierarchy`, `/what-if`, `/stress/evaluate` are **not** in OpenAPI and raise `KeyError` (fail-closed). They are not an INTERACTIVE misclass. |
| 2 | Map uses strings that do not match OpenAPI (tests green on a private list only) | **No.** Independent OpenAPI dump: map ↔ owned canonical is empty on both sides. Listed heavy set ⊆ OpenAPI. Tests call `app.openapi()`. |
| 3 | `POST /risk/summary` with FULL_REVALUATION stays INTERACTIVE | **No.** `classify(..., methodology="FULL_REVALUATION")` and enum form return HEAVY. Default / LINEAR stay INTERACTIVE. |
| 4 | Tests never load the real app OpenAPI | **No.** `from app.main import app` then `app.openapi()["paths"]`. |
| 5 | RF-015 / R0.10 marked COMPLETE as if work left the request thread | **No on disk.** RF-015 has no COMPLETE/CLOSED status. R0.10 is not COMPLETE. Report explicitly says classification does not move work off the request thread. |

## Leftover heavy-as-interactive holes

**None on the listed R0.10.1 / RF-015 heavy examples.**

| Route | OpenAPI? | Class | `is_interactive_only` | Hole? |
|---|---|---|---|---|
| `POST /risk/var` | yes | HEAVY | False | **No** |
| `POST /risk/es` | yes | HEAVY | False | **No** |
| `POST /risk/var/compare` | yes | HEAVY | False | **No** |
| `POST /risk/hierarchy` | yes | HEAVY | False | **No** |
| `POST /risk/stress/reverse/multi` | yes | HEAVY | False | **No** |
| `POST /risk/what-if` | yes | HEAVY | False | **No** |
| `POST /risk/stress/evaluate` | yes | HEAVY | False | **No** |
| `POST /risk/stress/evaluate/custom` | yes | HEAVY | False | **No** |
| `POST /risk/stress/formal/evaluate/custom` | yes | HEAVY | False | **No** |
| `POST /risk/contributors` | yes | HEAVY | False | **No** |
| `POST /risk/change-attribution` | yes | HEAVY | False | **No** |
| `POST /hierarchy` (no `/risk`) | **no** | unclassified | n/a | **No** (not an OpenAPI path) |
| `POST /what-if` (no `/risk`) | **no** | unclassified | n/a | **No** |
| `POST /stress/evaluate` (no `/risk`) | **no** | unclassified | n/a | **No** |
| `POST /stress/reverse/multi` (no `/risk`) | **no** | unclassified | n/a | **No** |

Other owned compute POSTs (`/risk/stress`, custom/formal stress, reverse
single, compare, attribution, attribution/demo, limits, query,
`POST /risk/runs`) are also HEAVY. Catalog / health / scenario-definition
GETs and `POST /market/snapshot` are INTERACTIVE.

### Residual (not a fail)

| Item | Why it is not a leftover hole for this slice |
|---|---|
| `POST /risk/limits/drilldown` is INTERACTIVE-only and can run Historical VaR + `contributors_for_metric` on the default root book | Plan cheap slot is “small factor / lightweight drilldown.” Implementer documented the large-book residual; R0.10.3 owns the workload bound. |
| `POST /risk/factors` is INTERACTIVE-only | One `value()` pass + Greek aggregation. Matches “small factor drilldown” and PERF-010 “keep LINEAR/Δ-Γ sync.” |
| Default `POST /risk/summary` is INTERACTIVE | Plan cheap example. FULL_REVALUATION is upgraded by `classify`. |
| `loadDashboard()` still fans out nine POSTs, seven of them HEAVY | R0.10.2. Not scored. |
| Sync `def` handlers still compute on the request thread; `classify` is not wired | Stated non-goal. Do not fail this slice for it. |

## Findings

### Critical

None.

### Important

None that block APPROVE.

### Minor

1. **Private list pin is not the OpenAPI contract**
   - File: `backend/tests/test_endpoint_execution_class.py:18-54`
   - `test_listed_heavy_routes_match_milestone_examples` only asserts
     `LISTED_HEAVY_ROUTES == _MILESTONE_HEAVY`. The real contract is the
     two OpenAPI tests. Fine as a milestone-example pin; do not treat it
     as proof by itself.
2. **Static map vs `classify()` for summary**
   - File: `backend/app/api/execution_class.py:69, 125-131`
   - `ENDPOINT_EXECUTION_CLASS[("POST", "/risk/summary")]` stays
     INTERACTIVE. Callers that read the dict and skip `classify(...,
     methodology=)` will miss the FULL_REVALUATION upgrade. The documented
     API is `classify`.
3. **RF-015 has no Status line**
   - File: `reviews/FINDINGS.md:771-812`
   - Pre-existing. Not marked COMPLETE. Other RFs have an explicit Status.
     Out of this slice’s edit set.

## Recommendations

- Keep RF-015 **open**. Next owner is R0.10.2 (dashboard batch / one
  risk-run artifact), then R0.10.3 (backpressure).
- When R0.10.2 consumes this map, call `classify` (with methodology when
  present). Do not treat the raw dict as complete for summary.
- Do not enqueue or reject from this module until a later slice owns
  router behavior.

## Assessment

**Ready to merge: Yes** (R0.10.1 classification slice only).

The map is a real OpenAPI bijection, not a private list. Listed heavy
examples are HEAVY. FULL_REVALUATION upgrades summary. The five attacks
do not land. RF-015 / R0.10 remain open; request-thread compute and
dashboard fan-out are explicitly leftover for later slices.
