# R0.12.4 Nightly / labeled runner

## Task
R0.12.4 Nightly / labeled runner (RF-016 remaining nightly tier)

## Owner
DevOps / Platform Engineer

## Summary
Added `.github/workflows/nightly.yml` with `schedule` (daily 06:00 UTC) and `workflow_dispatch` (the labeled-runner equivalent). It is **not** on `pull_request` and is **not** in PR-FULL `needs:`. Three jobs actually run heavier checks (not echo-only): native C++ benchmark binary at 10k×1k plus the scenario harness at 1k×1k; live Postgres two-worker `SKIP LOCKED` claim; larger FULL_REVALUATION sample (120 observations). PR-FAST, QuantLib hard-gate, and `postgres-persistence-smoke` are unchanged. RF-016 stays **IN PROGRESS** (not CLOSED).

Independent review APPROVE'd isolation and left two Important leftovers (Orchestrator: both VALID). This addendum closes those leftovers without committing and without closing RF-016.

## Independent-review leftovers (VALID)

### Important #1 — two-worker skip-green
`require_live_postgres()` replaces the module `skipif`. Gate:

- Local (`CI` and `RISKFORGE_NIGHTLY` unset): skip if DSN missing or unreachable.
- `RISKFORGE_NIGHTLY` set: **fail** if DSN missing or unreachable.
- `CI` set and a postgresql DSN is offered: **fail** if unreachable (the reproduced GHA hole).
- `CI` set and DSN unset: still **skip** so PR `backend-pytest` (`pytest -q`, `CI=true`, no DSN) is not broken.

Nightly two-worker pytest step now sets `RISKFORGE_NIGHTLY=1` (DSN already present). PR `postgres-persistence-smoke` is unchanged.

### Important #2 — native liveness sold as SLA
Dropped `wall_ms >= 0` / `throughput > 0` and the “SLA-sized” echo. Native 10k×1k JSON now asserts `impl == cpp_header` and `checksum == -9900`. Scenario harness still asserts `cpp_header` / `python` keys. `check_m6_sla.py` is **not** invoked on `ubuntu-latest` (host-specific SLA-K1/K2 floors flake). Labeled-runner SLA remains an **RF-016 leftover**.

Minor #3 (comment-blind YAML parser) is deferred.

## Files changed
- `.github/workflows/nightly.yml` (new; keep in the change set)
- `.github/workflows/ci.yml` (header comment only: nightly is a sibling workflow; `needs:` still the six PR-FULL jobs)
- `backend/tests/test_nightly_ci.py` (contract + fail-closed / local-skip unit tests)
- `backend/tests/test_postgres_two_worker.py` (claim tests + `require_live_postgres` gate)
- `backend/tests/test_nightly_full_reval_sample.py` (new; skipped unless `RISKFORGE_NIGHTLY=1`)
- `BUILD_NOTES.md` (nightly section; skip vs fail; checksum identity)
- `reviews/FINDINGS.md` (RF-016 status note only; not CLOSED)
- `reviews/r0.12.4-report.md`

## Public/interface changes
- None. New optional workflow + pytest files. No API, pricing, or e2e changes.

## Numerical conventions
- Units: n/a for CI wiring. FULL_REVALUATION sample reuses the R0.1.4 identity (shocked PV − base PV; unit equity PnL = `10 * 100 * r`, atol 1e-12).
- Sign convention: n/a
- Day count/calendar if relevant: n/a
- Tolerances/reference: native job asserts `impl` / `checksum` identity (`checksum == -9900` for the in-tree 10k×1k constant book). Not laptop SLA-K1/K2 floors (host-specific).

## Tests added/updated
- `tests/test_nightly_ci.py` — nightly.yml exists; `on:` is schedule + workflow_dispatch and not pull_request; PR-FULL `needs:` has no nightly id; nightly YAML names `benchmark.cpp` / `g++` / `--exposures` and `tests/test_postgres_two_worker.py` with a postgres service, `RISKFORGE_NIGHTLY`, and no `continue-on-error`; native job asserts checksum/impl and does not name SLA in step titles or run `check_m6_sla.py`.
- `test_two_worker_fails_when_ci_set_and_dsn_unreachable` — `CI=1` + unreachable postgresql DSN **fails** (monkeypatch; no Docker).
- `test_two_worker_skips_locally_when_dsn_unreachable` — same DSN skips when `CI` / `RISKFORGE_NIGHTLY` are unset.
- `tests/test_postgres_two_worker.py` — two concurrent claimers cannot double-claim one QUEUED row; two claimers take two distinct rows. Gate as above.
- `tests/test_nightly_full_reval_sample.py` — 120-observation FULL_REVALUATION identity; gated by `RISKFORGE_NIGHTLY=1` so PR backend-pytest stays fast.
- `tests/test_pr_full_ci.py` / `tests/test_pr_fast_ci.py` — not modified (Acc 5 left intact).

## Commands executed
```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_nightly_ci.py tests/test_pr_full_ci.py tests/test_pr_fast_ci.py
```

## Results
- Backend: **15 passed** in 1.39s (`test_nightly_ci.py` + `test_pr_full_ci.py` + `test_pr_fast_ci.py`). Local `tests/test_postgres_two_worker.py`: **2 skipped**, exit 0 (no Docker). Same file with `CI=1` + unreachable DSN: **2 failed**, exit 1 (not skip-green). Ruff on the touched test files: exit 0.
- Frontend: n/a
- QuantLib: not exercised. Hard-gate job and `RISKFORGE_REQUIRE_QUANTLIB` left intact. PR-FAST unchanged.
- C++: native job asserts `impl == cpp_header` and `checksum == -9900`. SLA-K1/K2 on a labeled runner remains RF-016.
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none (two-worker integration skip is the documented local-Docker-optional path)
- CI is green (all required checks): not started (commit/push out of scope; GHA cannot be run here)

## Proof PR-FULL is not blocked
PR-FULL `needs:` remains exactly:

```text
backend
backend-quantlib
frontend
lint
e2e
postgres-smoke
```

No nightly job id. Nightly is a separate workflow file with no `pull_request` trigger. Contract: `test_pr_full_needs_does_not_include_nightly` + `test_nightly_triggers_are_schedule_and_dispatch_not_pull_request`.

## What nightly actually runs
| Job | Real check |
|---|---|
| `nightly-native-benchmark` | Compile `backend/native/src/benchmark.cpp`; run 10k×1k `--json`; assert `impl` + `checksum`; run `benchmarks/run_scenario_bench.py --workload 1k_x_1k` (`cpp_header` / `python`) |
| `nightly-postgres-two-worker` | `postgres:16-alpine` + `scripts/smoke_postgres.sh` + `RISKFORGE_NIGHTLY=1 pytest tests/test_postgres_two_worker.py` |
| `nightly-full-reval-sample` | `RISKFORGE_NIGHTLY=1 pytest tests/test_nightly_full_reval_sample.py` (120 obs) |

Not in this slice: hierarchy benchmark, optional QuantLib E2E (must not edit e2e product journeys), labeled-runner SLA-K1/K2.

## Known limitations / risks
- GitHub Actions itself was not executed from this host. First `workflow_dispatch` after push is the runner proof.
- Two-worker integration tests skip on a laptop without Docker; GHA nightly sets `RISKFORGE_NIGHTLY=1` and fails if the DSN is missing or unreachable. PR `postgres-persistence-smoke` is unchanged.
- Native job is compile + checksum/impl identity. Labeled-runner SLA (`check_m6_sla.py` / SLA-K1/K2) remains an RF-016 leftover.
- Hierarchy benchmark and QuantLib E2E are still open RF-016 leftovers.
- RF-016 is **not** CLOSED.
- Minor comment-blind YAML parser rewrite is deferred.

## Follow-up / next owner
- Owner: Lead Architect / QA — labeled-runner SLA-K1/K2; decide whether hierarchy bench and optional QuantLib E2E belong in a later nightly job.
- Requested action: do not add Nightly to branch-protection required checks; do not add nightly jobs to PR-FULL `needs:`; do not close RF-016.
- Blocking?: no
