# R0.11.6 Container/dependency hardening

## Task
R0.11.6 Container/dependency hardening (RF-014 / RF-018)

## Owner
DevOps / Platform Engineer

## Summary
Pinned frontend runtime/build dependencies to the versions already resolved in `package-lock.json` (no `"latest"`). Frontend image now uses deterministic `npm ci` and serves `/usr/share/nginx/html` as the `nginx` user (writable pid/temp under `/tmp`, still listening on container port 80 so Compose `127.0.0.1:5173:80` is unchanged). Backend image creates system user `riskforge` (uid 10001) and drops to it before CMD; uvicorn still binds `0.0.0.0:8000` inside the container. Local Compose CPU/RAM expectations are documented in `BUILD_NOTES.md` as a laptop-demo note, not a product SLA. No authentication overlay and no `docker-compose.yml` edit.

## Files changed
- `frontend/package.json`
- `frontend/package-lock.json` (root pin strings only; no tree churn)
- `frontend/Dockerfile`
- `frontend/nginx.conf`
- `backend/Dockerfile`
- `backend/tests/test_container_hardening.py`
- `BUILD_NOTES.md`
- `reviews/r0.11.6-report.md`

## Public/interface changes
- None (image USER / install command / pinned versions only; host publishes and API listen address unchanged).

## Numerical conventions
- Units: n/a
- Sign convention: n/a
- Day count/calendar if relevant: n/a
- Tolerances/reference: n/a

## Tests added/updated
- `tests/test_container_hardening.py::test_frontend_package_json_dependencies_are_not_latest` — `dependencies` / `devDependencies` must not be `"latest"`.
- `tests/test_container_hardening.py::test_frontend_dockerfile_uses_npm_ci` — frontend Dockerfile contains `npm ci`.
- `tests/test_container_hardening.py::test_backend_dockerfile_drops_to_non_root_user` — final `USER` is not root.

## Commands executed
```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short tests/test_container_hardening.py tests/test_compose_loopback.py

cd /Users/user/src/riskforge-mvp/frontend
npm ci && npm test && npm run lint

docker info >/dev/null 2>&1 && echo "DOCKER_OK" || echo "DOCKER_MISSING"
```

RED (before production edits): `test_container_hardening.py` 3 failed — `"latest"` pins, `npm install`, missing backend `USER`.

## Results
- Backend: 4 passed in 1.47s (`test_container_hardening.py` ×3 + `test_compose_loopback.py` ×1)
- Frontend: `npm ci` added 393 packages, 0 vulnerabilities; vitest **8 files / 74 tests passed**; `npm run lint` exit 0 on retry (first combined `npm ci && npm test && npm run lint` hit `Segmentation fault: 11` in eslint after tests passed — rerun of `npm test && npm run lint` was clean)
- QuantLib: n/a
- C++: n/a
- Build: Docker image build not run — `docker info` reported **DOCKER_MISSING** (Compose CLI v5.3.0 is installed; daemon is not). Task does not fail solely on this.
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none (eslint segfault treated as host flake; confirmed green on immediate rerun)
- CI is green (all required checks): not started (commit/push out of scope)

## Known limitations / risks
- Non-root nginx still listens on container port 80 so R0.11.1 loopback mapping `5173:80` and `test_compose_loopback.py` stay valid. This relies on Docker's default `CAP_NET_BIND_SERVICE`; a future `cap_drop: [ALL]` overlay would need listen 8080 plus a Compose port change.
- Backend/worker processes share the same image `USER riskforge`. App files remain root-owned and world-readable; the process does not write into `/app`.
- Images are not read-only rootfs, have no Compose `mem_limit`/`pids_limit`, and are still not internet-ready (no auth — R0.11.5).
- Resource paragraph is a local-demo sizing hint (~2 CPU / 2–3 GiB), not an SLA.
- Docker daemon was unavailable here; runtime `USER` was not confirmed with `docker inspect`.

## Follow-up / next owner
- Owner: Lead Architect (R0.11 remaining slices / CI)
- Requested action: optional Docker build/`docker inspect` when a daemon is available to confirm non-root `USER` at runtime. Do not start R0.11.5 auth overlay from this handoff.
- Blocking?: no

---

# R0.11.6 review fix (independent review findings)

## Task
Fix VALID findings from `reviews/r0.11.6-independent-review.md` (orchestrator: all three VALID).

## Owner
DevOps / Platform Engineer

## Summary
Pinned frontend non-root and `npm ci` in tests that parse Dockerfile instructions (final stage after the last `FROM`; comments ignored). Asserted `frontend/nginx.conf` pid/temp paths under `/tmp` and no `user` directive. Added `frontend/.dockerignore` so `COPY . .` cannot overlay host `node_modules`/`dist`. Image tags, auth, and Compose ports unchanged. Docker daemon not required.

## Files changed
- `backend/tests/test_container_hardening.py`
- `frontend/.dockerignore`
- `reviews/r0.11.6-report.md`

## Public/interface changes
- None.

## Numerical conventions
- n/a

## Tests added/updated
- `test_frontend_dockerfile_uses_npm_ci` — requires a `RUN` whose install command is `npm ci`; rejects any `RUN` that invokes `npm install`.
- `test_comment_npm_ci_does_not_satisfy_install_requirement` — `# RUN npm ci` plus `RUN npm install` must not pass.
- `test_frontend_final_stage_drops_to_non_root_user` — last `USER` in the final stage is not root/`0`.
- `test_comment_or_build_stage_user_is_not_final_user` — `# USER nginx` or `USER` only in the build stage does not count.
- `test_frontend_nginx_conf_is_non_root_safe` — `pid` and five `*_temp_path` directives under `/tmp`; no `user` directive.
- `test_comment_user_directive_is_ignored_in_nginx_conf` — a comment `user` does not count.
- `test_frontend_dockerignore_excludes_host_build_artifacts` — file exists and lists `node_modules` and `dist`.

## Commands executed
```bash
cd /Users/user/src/riskforge-mvp/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_container_hardening.py tests/test_compose_loopback.py
```

RED (tests first, no `.dockerignore`): `1 failed, 9 passed in 2.57s` — `test_frontend_dockerignore_excludes_host_build_artifacts` (`frontend/.dockerignore must exist`).

GREEN (after `frontend/.dockerignore`):

```
..........                                                               [100%]
10 passed in 1.25s
```

In-memory mutations of the on-disk Dockerfile/nginx.conf (not committed): stripping `USER nginx`, commenting it, restoring `npm install`, or dropping `pid` / adding `user nginx` all fail the new parsers.

## Results
- Backend: 10 passed in 1.25s (`test_container_hardening.py` ×9 + `test_compose_loopback.py` ×1)
- Frontend: n/a (static Docker/ignore only)
- QuantLib: n/a
- C++: n/a
- Build: Docker daemon not used
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none
- CI is green (all required checks): not started (commit/push out of scope)

## Known limitations / risks
- Same as original slice: non-root nginx still listens on container port 80 (default `CAP_NET_BIND_SERVICE`). Runtime `USER` not confirmed with `docker inspect` (daemon missing).
- `.dockerignore` does not change image tags or Compose publishes.

## Follow-up / next owner
- Owner: Lead Architect (R0.11 remaining slices / CI)
- Requested action: none from this review fix; optional daemon `docker inspect` still stands.
- Blocking?: no
