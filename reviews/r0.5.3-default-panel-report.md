# Agent Handoff Template

## Task
R0.5.3 leftover / RF-005 — production default historical VaR → `HistoricalFactorPanel`

## Owner
Portfolio Risk Engineer

## Summary
Switched production `build_historical_risk_engine()` to wire a seeded synthetic
`HistoricalFactorPanel` (`create_synthetic_factor_panel`) with independent
per-name / per-tenor RNG streams over the documented demo universe (NVDA/SPY,
USD 0Y/2Y/5Y/10Y, EURUSD). Dataset identity remains via
`create_historical_dataset()` (still labeled `four_macro_demo` for run-spec
compat). Bare `HistoricalRiskEngine()` / `factor_panel=None` keeps the
four-macro broadcast path for goldens and fixtures.

**RF-005 is CLOSE-ready pending independent review — left IN PROGRESS; do not CLOSE.**

## Files changed
- `backend/app/risk/factor_panel.py` — `DEFAULT_PRODUCTION_PANEL_FACTORS`,
  `create_synthetic_factor_panel`; docstring update
- `backend/app/services/risk_factories.py` — production factory passes
  `factor_panel=`
- `backend/app/risk/historical.py` — docstring (panel is production-wired)
- `backend/app/risk/historical_data.py` — clarify `is_per_name_per_tenor_panel`
  docstring only
- `backend/tests/test_risk_factories.py` (new)
- `backend/tests/test_factor_panel.py` — synthetic independence + docstring
- `backend/tests/test_factor_panel_historical.py` — rename bare-ctor broadcast
  case; docstring
- `reviews/FINDINGS.md` — RF-005 CLOSE-ready / pending review (still IN PROGRESS)
- `reviews/REMEDIATION_MILESTONE.md` — § R0.5.3 production-default note
- `reviews/r0.5.3-default-panel-report.md` (this handoff)

No commit. Frontend/native WIP untouched.

## Public/interface changes
- `create_synthetic_factor_panel(*, seed=7, observations=750, factors=None, start=…)`
- `DEFAULT_PRODUCTION_PANEL_FACTORS` — documented demo column universe
- `build_historical_risk_engine()` now returns an engine with
  `factor_panel is not None` and `is_per_name_per_tenor_panel is True`
- Bare `HistoricalRiskEngine()` default `factor_panel=None` unchanged
- No API/DTO/endpoint schema change; run-spec dataset id still from dataset

## Numerical conventions
- Units: same as panel / `FactorObservationSeries`
  - equity / FX: relative return (`0.01` = +1%)
  - vol: relative vol change
  - rate: basis points (`1.0` = +1bp)
- Distribution knobs match `SyntheticHistoricalDataset` family means/stds
- Independent columns via `np.random.SeedSequence(seed).spawn(n_factors)` —
  not a broadcast of one series
- Sign convention: unchanged (loss = −P&L)
- Tolerances: exact / `1e-12` on constructed fixtures; VaR goldens use
  explicit `factor_panel=None` / array dataset path — **unchanged**

## Tests added/updated
- `test_build_historical_risk_engine_uses_per_factor_panel`
- `test_production_panel_equities_and_tenors_move_independently` — shocked
  NVDA≠SPY and USD 2Y≠10Y on factory panel
- `test_four_macro_path_still_available_without_panel`
- `test_build_portfolio_service_inherits_factory_panel`
- `test_synthetic_panel_columns_are_independent_not_broadcast`
- Renamed `test_bare_historical_engine_has_no_panel_and_broadcasts`

## Commands executed
```bash
cd /Users/user/src/quantlineage/backend
PYTHONPATH=. .venv/bin/python -m pytest -q --tb=short \
  tests/test_factor_panel.py \
  tests/test_factor_panel_historical.py \
  tests/test_historical_dataset_projection.py \
  tests/test_risk_factories.py \
  tests/test_var_es_golden.py \
  tests/test_risk.py \
  tests/test_risk_run_factories.py
```

Also: smoke `build_portfolio_service().summary` on all three demo books (ok).
Ruff clean on touched files.

## Results
- Backend required suite: **55 passed** in 2.12s. No skips.
- Breakdown: factor panel type (+ synthetic) 13, panel wiring 9, projection 4,
  risk factories 4, VaR/ES goldens 11, test_risk 8, risk_run_factories 6
  (included for factory regression).
- Frontend: not run (out of scope)
- QuantLib: not run
- C++: not run
- Build: n/a
- All tests pass (all applicable/affected suites required by the task): yes
- Unexplained failures or skips: none
- CI is green (all required checks): not started (no push / no commit)

## Known limitations / risks
- **RF-005 remains IN PROGRESS** (CLOSE-ready only after independent review).
- Default panel universe is a **documented synthetic demo set**, not a live
  vendor feed. Books needing symbols/tenors outside that set fail closed
  (`panel missing required factor(s)`).
- `VaRAnalytics` / ES contribution helpers still consume four-macro
  `dataset.factor_observations()` when constructed from PortfolioService
  hist_kwargs (no `factor_panel` passthrough). Portfolio `summary()` /
  `HistoricalRiskEngine.calculate` use the panel. R0.5.5 / follow-up may
  converge those paths.
- Native 4-shock scenario kernel is unused on the panel path.
- Cap/floor / swaption remain unmapped on the panel path (`TypeError`).
- Production VaR **numbers change** vs prior four-macro factory default
  (intentional). Seeded array / bare-ctor goldens were not weakened.

## Follow-up / next owner
- Owner: Lead Architect / independent reviewer
- Requested action: review this slice; if exit criteria hold on the default
  production path, CLOSE RF-005. Do not close from this implementer report.
  Consider wiring `factor_panel` into `VaRAnalytics` / ES contribution for
  R0.5.5 consistency.
- Blocking?: no
