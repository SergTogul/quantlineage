services:
  backend:
    build: ./backend
    environment:
      RISKFORGE_PRICING_ENGINE: quantlib
    ports: ["8000:8000"]
  frontend:
    build: ./frontend
    ports: ["5173:80"]
    depends_on: [backend]
